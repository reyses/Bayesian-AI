# Project Audit - 2026-03-17

## Previous Audit Implementation Status
Previous audits were examined (`AUDIT/OLD/DOC_STATUS_AUDIT (1).md` and `AUDIT/OLD/DOC_STATUS_AUDIT (2).md`).
Many suggestions from the previous audit seem to have been carried out, but several critical items remain incomplete:
1. `CLAUDE.md` and `AGENTS.md` instructions from the previous audit have not been fully addressed.
2. Generating a clean `ARCHITECTURE.md` and updating `SYSTEM_DESCRIPTION.md`.
3. Updating `ROADMAP.md` and `CHANGELOG.md`.

## Directory Structure and File Organization

1.  **Duplicate Active Directory**: There is both a `docs/Active` and `docs/active` directory. These should be merged.
2.  **Top-Level Markdown Files**: Files like `DEEP_RESEARCH_ENTRY_IMPROVEMENTS.md` and `DMI_ADX_IMPROVEMENT_SPEC.md` are sitting in the root directory. They should be moved to appropriate locations in `docs/` (e.g., `docs/specs/` or `docs/Active/`).
3.  **Generated Output Tracking**: Git is still tracking generated outputs despite `.gitignore` rules or instructions in previous audits to remove them from history. The root directory contains `reports/oos_report.txt`, `reports/oos_report_OLD.txt`, `reports/run_history.csv`, `reports/is_report.txt`, `reports/discovery_density.txt`.
4.  **Examples directory**: `examples/` directory contains various `.png` files, which are likely generated or one-off. It should be evaluated if these should be tracked.

## Cleanliness & Structure Issues

1.  **Tools Directory**: The `tools/` directory is cluttered with research scripts (`tools/research_*.py`), which should ideally be moved to `scripts/research/` or a dedicated `tools/research/` directory.
2.  **Audit Folder Location**: `AUDIT` was created in the root directory for this task.
3.  **Docs Directory Structure**: The `docs` directory has loose markdown files like `JULES_1M_ANCHOR_REFACTOR.md`, `JULES_MAGIC_NUMBER_REFACTOR.md`, `JULES_PARTIAL_BARS.md`, `JULES_SHAPE_QUALITY_FILTER.md`, `REGIME_TRADING_SPEC.md`. These should be categorized into `docs/specs/` or `docs/Active/`.
4.  **Debug Folder**: There are no debug files present. As per project guidelines, debug scripts should be organized in `scripts/debug/`. The directory does not exist.

## Prompt for Jules
Please execute the following improvements based on this audit:
1. Merge `docs/active/` into `docs/Active/` (or vice-versa, depending on desired casing) and ensure all active specs are placed there.
2. Move top-level `.md` files like `DEEP_RESEARCH_ENTRY_IMPROVEMENTS.md` and `DMI_ADX_IMPROVEMENT_SPEC.md` into `docs/specs/` or `docs/Active/` as appropriate.
3. Move the loose `JULES_*.md` files from `docs/` into `docs/specs/` or `docs/Active/`.
4. Check if `examples/` should be ignored by git or if its contents are essential.
5. Create a `tools/research/` or `scripts/research/` directory and move all `tools/research_*.py` and standalone analysis scripts there to clean up the `tools/` root.
6. Verify that `.gitignore` properly excludes `reports/*.txt`, `reports/*.csv`, `reports/*.png`, and `tools/plots/`, and run `git rm --cached` on any tracked generated artifacts if necessary, as requested in previous audits.
7. Carry over uncompleted tasks from the previous audit:
   - Create/Update `CLAUDE.md` and `AGENTS.md` at the repo root.
   - Surgically update `docs/memory/MEMORY.md` to fix navigation without deleting history.
   - Regenerate `ARCHITECTURE.md` from the actual codebase.
   - Update `SYSTEM_DESCRIPTION.md` to remove any remaining physics metaphors.
   - Update `ROADMAP.md` and `CHANGELOG.md` with recent changes.
