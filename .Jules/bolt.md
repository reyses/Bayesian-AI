YYYY-MM-DD - [Parallelizing Rolling R/S calculation]
Learning: Numba's `@njit(parallel=True)` combined with `prange` allows seamless parallelization of sliding window calculations without breaking numerical accuracy or introducing any dependencies. The original `_compute_rs_numba` iterates independently over the output array items, calculating rolling stats. Changing `range` to `numba.prange` takes full advantage of multiple cores without changing the algorithm logic.
Action: Add `@njit(parallel=True, cache=True)` and replace `range` with `prange` for completely independent tight rolling calculations on 1D arrays, as long as there is no data mutation inside the loop across iterations.
2024-06-25 - [Replacing numpy sliding_window_view with parallel Numba rolling agg]
Learning: Using `numpy.lib.stride_tricks.sliding_window_view` in combination with `.std()` inside a per-bar or batch computation path incurs significant overhead due to memory allocation and stride translation, particularly for large arrays. Replacing this pattern with a custom `@numba.njit(parallel=True, cache=True)` function that manually computes variance within a `numba.prange` loop avoids intermediate memory allocation, yielding ~60x speedups.
Action: Whenever a rolling aggregation over a 1D array is needed on the CPU, avoid `sliding_window_view`. Write a custom Numba JIT loop leveraging `prange` instead.
