YYYY-MM-DD - [Parallelizing Rolling R/S calculation]
Learning: Numba's `@njit(parallel=True)` combined with `prange` allows seamless parallelization of sliding window calculations without breaking numerical accuracy or introducing any dependencies. The original `_compute_rs_numba` iterates independently over the output array items, calculating rolling stats. Changing `range` to `numba.prange` takes full advantage of multiple cores without changing the algorithm logic.
Action: Add `@njit(parallel=True, cache=True)` and replace `range` with `prange` for completely independent tight rolling calculations on 1D arrays, as long as there is no data mutation inside the loop across iterations.
YYYY-MM-DD - [Parallelizing Rolling STD calculation and correctly handling single element Edge Case in Numba]
Learning: When using Numba `@njit(parallel=True)` for sliding window replacement of numpy operations, returning `np.nan` on zero-degree-of-freedom logic (such as when `window=1` causing division by zero) ensures output perfectly matches original NumPy `sliding_window_view(...).std(ddof=1)`, instead of clamping to zero which subtly drifts downstream variables. Also `np.allclose(actual, expected, equal_nan=True)` works best for asserting exact matching.
Action: Add `@njit(parallel=True, cache=True)` combined with `numba.prange` instead of native Python loops. Return exact `np.nan` instead of defaults if NumPy's edge cases handle operations like divisions by zero as floats.
