YYYY-MM-DD - [Parallelizing Rolling R/S calculation]
Learning: Numba's `@njit(parallel=True)` combined with `prange` allows seamless parallelization of sliding window calculations without breaking numerical accuracy or introducing any dependencies. The original `_compute_rs_numba` iterates independently over the output array items, calculating rolling stats. Changing `range` to `numba.prange` takes full advantage of multiple cores without changing the algorithm logic.
Action: Add `@njit(parallel=True, cache=True)` and replace `range` with `prange` for completely independent tight rolling calculations on 1D arrays, as long as there is no data mutation inside the loop across iterations.
YYYY-MM-DD - [Numba fastmath manual unrolling for hot loop computations]
Learning: Rolling window mathematical calculations like Scipy FFT (`extract_dominant_cycle`) or Numpy polyfit (`calculate_kinetic_damping`) have a tremendous overhead inside inner simulation loops (e.g. `simulate_trade_standalone`). By decomposing them into raw mathematical scalar components using Numba's `math` module (sin, cos, log) and writing manually unrolled arrays within `@njit(fastmath=True, cache=True)`, we can seamlessly map the computations to multiple parallel threads using `prange`.
Action: Manually unroll scalar computations for inner loop mathematical models with `@njit(parallel=True, fastmath=True)` combined with Numba's `prange` if no Numba supported functions exist (like `np.fft` or `np.polyfit`) for an order of magnitude performance leap (e.g. 11x faster).
