YYYY-MM-DD - [Parallelizing Rolling R/S calculation]
Learning: Numba's `@njit(parallel=True)` combined with `prange` allows seamless parallelization of sliding window calculations without breaking numerical accuracy or introducing any dependencies. The original `_compute_rs_numba` iterates independently over the output array items, calculating rolling stats. Changing `range` to `numba.prange` takes full advantage of multiple cores without changing the algorithm logic.
Action: Add `@njit(parallel=True, cache=True)` and replace `range` with `prange` for completely independent tight rolling calculations on 1D arrays, as long as there is no data mutation inside the loop across iterations.
2026-03-13 - [Parallelizing Rolling Max Drawdown/Drawup]
Learning: Rolling nested max/min calculations in Python loops combined with slice allocations and functions like `np.maximum.accumulate` or `.max()` are memory intensive and bottleneck performance. Replacing these loops with tightly coupled scalar operations running in parallel `prange` inside a Numba `@njit` kernel completely sidesteps memory allocations and drastically accelerates execution, achieving ~300x speedups for metric calculations like the dynamic giveback threshold (swing noise).
Action: Convert nested slice aggregations across windows into parallelized Numba prange loops that update scalar maximums/minimums internally per iteration to avoid Python overhead.
