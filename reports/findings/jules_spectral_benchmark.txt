Jules Spectral Vectorization Assessment — 2026-03-17
================================================================

BENCHMARK: Original scipy.fft + np.polyfit
  5,000 bars: 0.387s (0.08ms/bar)
  30,000 bars (monthly): ~2.3s
  Full year (Phase 3): ~28s across 12 months

WHERE IT RUNS: Phase 3 optimization only (orchestrator_worker.py)
  Not called during forward pass. Zero impact on IS/OOS runtime.

JULES APPROACH: Numba O(n^2) DFT replacing scipy O(n log n) FFT
  Window size: 32 elements
  For n=32: O(n^2) = 1024 ops, O(n log n) = 160 ops
  Jules's version is ~6x MORE work per call, not less.
  Numba JIT compilation overhead on first call ~2-5s.
  fastmath=True introduces floating-point non-determinism.

VERDICT: SKIP
  - 28 seconds total over the entire year is not a bottleneck
  - O(n^2) DFT is mathematically slower than scipy FFT for n=32
  - Touches core orchestrator_worker.py (risk)
  - 10 junk test files in the branch
  - Phase 3 only runs on --fresh (not on forward-pass reruns)
