# Composite Zigzag Pipeline — Python dependencies
# Tested on Python 3.11 / Windows. Other versions likely work.

numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
pyarrow>=12.0       # for parquet I/O
tqdm>=4.65
matplotlib>=3.7     # only used by visual diagnostics + chart tools

# Optional:
# numba>=0.57       # speeds up zigzag detection if installed
