# Documentation Status Audit — 2026-03-08

---

## IMPLEMENTED & COMPLETE ✅ (archive to `docs/archive/`)

These specs are fully implemented in the codebase. They should be moved to
`docs/archive/` to avoid confusion about what's still TODO.

| Doc | Status | Evidence |
|-----|--------|----------|
| `CLAUDE_CODE_UNIFIED_EXIT_ENGINE.md` | ✅ DONE | `ExitEngine` in `core/exit_engine.py`, unified training/live |
| `CLAUDE_CODE_BAND_CONTEXT.md` | ✅ DONE | `get_band_confluence()` + `BandContext` in TBN, 12 references |
| `CLAUDE_CODE_METAPHOR_PURGE.md` | ✅ 95% | File renames done, 15 remnants remain (Task 6 in REMAINING_WORK) |
| `CLAUDE_CODE_EXIT_IMPROVEMENTS.md` | ✅ DONE | Fix 3 (tiered giveback), Fix 4 (30m flip), Fix 1 (hurst script), Fix 2 (avg_mfe_bar) — all in code |
| `JULES_EXECUTION_ENGINE_INTEGRATION.md` | ✅ DONE | `ExecutionEngine` in `core/execution_engine.py`, live thin wrapper done |
| `CLAUDE_CODE_COMPRESSED_REPLAY.md` | ✅ DONE | `live/history_replay.py` (524 lines), `live/atlas_loader.py` (143 lines) |
| `JULES_COMPRESSED_REPLAY.md` | ✅ DONE | Duplicate of above (Jules-formatted version of same spec) |
| `improvement_plan.md` | ✅ DONE | All 4 fixes implemented (tiered giveback, 30m flip, hurst script, avg_mfe_bar) |

**Action:** `mkdir -p docs/archive && mv` these 8 files there.

---

## STALE / INACCURATE ⚠️ (archive old version, then update)

These docs reference deleted classes, old file names, or removed architecture
components. They actively mislead anyone reading them.

### `ARCHITECTURE.md` — SEVERELY STALE

Generated 2026-02-25 by Jules. References:

| Stale Reference | Current Reality |
|----------------|-----------------|
| `core/quantum_field_engine.py` | → `core/statistical_field_engine.py` |
| `QuantumFieldEngine` class | → `StatisticalFieldEngine` |
| `QuantumBayesianBrain` class | → `BayesianBrain` (subclass deleted) |
| `core/three_body_state.py` | → `core/market_state.py` |
| `ThreeBodyQuantumState` class | → `MarketState` |
| `core/dynamic_binner.py` | Deleted (no longer exists) |
| `training/orchestrator.py` | → `training/orchestrator_worker.py` |
| `training/fractal_dna_tree.py` | Deleted |
| `training/wave_rider.py` | Deleted (merged into `ExitEngine`) |
| `visualization/live_training_dashboard.py` | → `visualization/dashboard.py` |
| "three-body gravitational system" | Regression bands + z-scores |
| "quantum mechanics" | 3-class softmax + Shannon entropy |

**Action:** Archive to `docs/archive/ARCHITECTURE_v2.md` (preserves the Feb 25 snapshot),
then regenerate from current codebase. The header says "Auto-generated by Jules — do not
edit manually" so regeneration is the intended workflow:
```bash
# Walk core/ + live/ + training/, extract class names, docstrings, imports
# Build accurate file→class→role mapping
```

### `SYSTEM_DESCRIPTION.md` — STALE

Still says "three-body gravitational system," "quantum model," "particle
moving between three attractors." The physics metaphors are purged from code
but live in this doc.

**Action:** Archive current version to `docs/archive/SYSTEM_DESCRIPTION_v2.md`
(preserves the original framing as historical record), then update the active
copy with current statistical language:
- "OLS regression bands + z-score standardization"
- "3-class softmax probability distribution"
- "OU first-passage reversion probability"
- "Multi-TF belief network (weighted geometric mean of per-TF logistic regressors)"

### `CHANGELOG.md` — INCOMPLETE

Stops at V2.0.1. Missing all Phase 4+ work:
- Terminology refactor
- ExecutionEngine integration
- Unified ExitEngine
- Band context
- TBN 11-worker expansion (5s, 1s, 4h)
- history_replay.py + atlas_loader.py
- Feature extraction unification
- Exit improvements (Fixes 1-4)
- Metaphor purge (file renames)
- Compressed replay engine

**Action:** Add V3.0 changelog section covering Phase 4 through current state.

---

## ACTIVE / IN PROGRESS 🔧

These docs describe work that is partially done or actively being implemented.

| Doc | Status | What's Left |
|-----|--------|-------------|
| `CLAUDE_CODE_CONSOLIDATION.md` | 🔧 70% | Tasks 1-7 (LiveCandidate, make_position, trail, decomposition) — currently being worked |
| `CLAUDE_CODE_REMAINING_WORK.md` | 🔧 Active | Generated this session — the current TODO list |

**Action:** Keep both. `REMAINING_WORK.md` supersedes `CONSOLIDATION.md`
as the active task list. Once complete, archive both.

---

## FUTURE STATE 🔮 (keep, not yet started)

These describe future features that have not been started.

| Doc | Status | Depends On |
|-----|--------|-----------|
| `ROADMAP.md` | 🔮 Valid | Pattern Relevance branch needs spec |
| `LEVEL_DETECTOR_SPEC.md` | 🔮 Valid | Fibonacci level detection — deferred |
| `JULES_EXPECTED_PROFIT_PREDICTOR.md` | 🔮 Partial | `get_expected_pnl()` exists in brain; full predictor not built |
| `JULES_WAVEFORM_SEED_INTEGRATION.md` | 🔮 Not started | Zero waveform code found in codebase |

### `ROADMAP.md` — Valid but needs update

The roadmap items are still relevant:
- **Pattern Relevance & Living Brain** — not started, still the right next major branch
- **Rolling OOS Window** — not started
- **Partial Bar Aggregation** — not started
- **Counter-Trend Scalp Research** — research done, no code changes
- **Live Brain Persistence** — partially done (brain saves, but no `direction_memory.json` export)
- **Multi-Pass Convergence Detection** — `--passes N` exists, early-stop not done

**Action:** Keep. Add "Completed" section at top for items that are done.

### `JULES_WAVEFORM_SEED_INTEGRATION.md` — Not started

Zero waveform-related code exists. The `WAVEFORM_ANALYSIS_JOURNAL.txt` (83KB)
has research notes but no implementation.

**Action:** Keep for future reference. Low priority.

### `LEVEL_DETECTOR_SPEC.md` — Not started

Fibonacci level detection for the fractal hierarchy. Original spec from early
in the project. May be superseded by the current clustering approach.

**Action:** Keep. Evaluate relevance after pattern relevance branch.

---

## MEMORY.md — NEEDS SURGICAL UPDATE ⚠️

`docs/memory/MEMORY.md` is the single most important doc for AI agents
working on this codebase — it's loaded at the start of every session.
**It's feeding stale navigation instructions to every Jules/Claude Code session.**

However, MEMORY.md is also a **historical document**. Many entries record
WHEN and WHY decisions were made, what baselines looked like at specific
points in time, and what the system's evolution path has been. A "full
rewrite" would destroy that timeline. The correct approach is **surgical**:
fix broken navigation, preserve historical records.

### What's Broken (navigation — fix immediately)

These are instructions that currently point AI agents to wrong files/classes.
Updating them doesn't alter timeline — they're directions, not history.

**Architecture section — wrong file paths:**

| Stale Path / Class | Fix To |
|-----------|-----------------|
| `core/quantum_field_engine.py` | → `core/statistical_field_engine.py` |
| `ThreeBodyQuantumState` | → `MarketState` |
| `core/three_body_state.py` | → `core/market_state.py` |
| `QuantumBayesianBrain` | → `BayesianBrain` (subclass deleted) |
| `training/orchestrator.py` | → `training/orchestrator_worker.py` |
| `training/fractal_dna_tree.py` | **DELETE reference** — file does not exist |
| `core/dynamic_binner.py` | **DELETE reference** — file does not exist |
| `training/timeframe_belief_network.py` | → `core/timeframe_belief_network.py` |
| `make_position()` — "creates PositionState directly" | Being deleted (Task 2 in REMAINING_WORK). Update when done. |
| 10 TF workers | → 11 workers (5s and 1s added) |
| Pipeline phases 1-5 | → 1-6 (Session 2 on 2026-03-08 renumbered) |

**Tools section — ghost files (never created, or deleted long ago):**

Remove these entries entirely — they cause `FileNotFoundError` when agents try to use them:
```
tools/run_benchmark.py      — NEVER EXISTED
tools/compare_oos_runs.py   — NEVER EXISTED
tools/analyze_exits.py      — NEVER EXISTED
tools/make_atlas_1day.py    — NEVER EXISTED
tools/make_atlas_1week.py   — NEVER EXISTED
scripts/monthly_pnl_chart.py — wrong path, actually tools/monthly_pnl_chart.py
```

**Tools section — missing real tools (add these):**

```
tools/standalone_research.py  — research harness (A-R modules, delegates to tools/research/)
tools/research/               — subpackage: data.py, imr.py, screening.py, seeds.py, plots.py
live/history_replay.py        — compressed forward pass for live warmup
live/atlas_loader.py          — ATLAS parquet reader for date ranges
core/feature_extraction.py    — unified 16D feature vector (single source of truth)
core/execution_engine.py      — gate cascade + direction + sizing (used by live thin wrapper)
```

**Architecture section — missing modules (add these):**

```
core/execution_engine.py  — ExecutionEngine: gate cascade, direction, scoring, sizing
core/feature_extraction.py — extract_feature_vector(): canonical 16D feature computation
live/history_replay.py     — HistoryReplayEngine: compressed forward pass for live warmup
live/atlas_loader.py       — load_atlas_range(): ATLAS parquet reader
```

**Note: CUDA-only since Session 8 (2026-03-08).** CPU fallback path removed.
`_batch_compute_cpu()`, `_compute_hurst_numpy()`, `_compute_rs_numba()` all
deleted (~310 lines). Engine raises RuntimeError if CUDA unavailable.

### What's Historical (preserve — append, don't replace)

**Baseline numbers are dated snapshots:**
- "OOS: 536 trades, 88.4% WR, $10,804" — this was real at the time it was recorded
- Don't delete it. **APPEND** the new baseline below it with a date:

```markdown
## Current Baseline (IS+OOS, 2026-03-07, main branch)
- IS: 7,262 trades, 85.7% WR, $86,351 total, $11.89/trade, PF 3.54
- OOS: 536 trades, 88.4% WR, $10,804 total, $20.16/trade (~$5.4K/month)
...

## Updated Baseline (2026-03-08, oracle removed, pipeline restructured)
- IS: $86,685 PnL, 7704 trades, 78.8% WR, 52.8% correct direction
- OOS: $22,383 PnL, 1750 trades, 77.5% WR, 57.0% correct direction
- Max DD: $395, 0 consecutive losing days
- Key change: IS oracle direction override removed (was contaminating)
```

**Implemented Features is a changelog — append only:**

Add these entries (with date) below existing ones:
- (2026-03-08) ExecutionEngine integration: live delegates to EE thin wrapper
- (2026-03-08) Compressed history replay: `live/history_replay.py` + `live/atlas_loader.py`
- (2026-03-08) Feature extraction unified: `core/feature_extraction.py`
- (2026-03-08) CPU path removed (CUDA-only)
- (2026-03-08) Pipeline restructured to 6 phases (Strategy moved after OOS)
- (2026-03-08) IS oracle direction override removed
- (2026-03-08) Expected profit tracking: `get_expected_pnl()` in brain
- (2026-03-08) Run history CSV: `reports/run_history.csv`
- (2026-03-08) ExitEngine simplified: `open_position()` accepts pre-computed sizing
- (2026-03-08) PositionState trimmed: 7 dead fields deleted

**Design decisions are historical records:**
- "wave_rider.py DELETED (2026-03-07)" — correct, keep as-is
- "Belief flip research: DEAD END confirmed" — valuable historical note
- "No Hour/Session Filtering" design decision — preserve reasoning

### What Needs Clarification (label, don't delete)

**Clustering section:**
- Current text says "DMI pre-split → I-MR(DMI diff) → DBSCAN"
- This is the **experimental branch** design from waveform research
- Main branch uses recursive K-Means (`core/fractal_clustering.py`)
- **Fix:** Label both: "Main: recursive K-Means | Experimental: DMI→I-MR→DBSCAN"

**Spectral gates reference:**
- "Spectral gates: Fourier half-cycle + Laplace kinetic damping"
- Moved to `orchestrator_worker.py` — verify still active, update location

### HARD RULES Section — VALID ✅

The journal update rules are good and being followed (5 daily entries exist).
The workflow preferences are current. The training command is correct.
The NT8 bridge deploy path is still accurate.

### Summary: MEMORY.md Edit Rules

| Section | Action | Rationale |
|---------|--------|-----------|
| HARD RULES | Keep as-is | Current and followed |
| Workflow Preference | Keep as-is | Current |
| Architecture | UPDATE file paths/class names | Navigation instructions, not history |
| Architecture | ADD new modules | Navigation |
| Architecture | DELETE ghost file references | Prevent FileNotFoundError |
| Baseline | APPEND new baseline with date | Historical record — old numbers were real |
| Implemented Features | APPEND new entries with dates | Changelog — preserves timeline |
| CLI Flags | UPDATE to current | Instructions, not history |
| Data Pipeline | Keep as-is | Still accurate |
| Tools | DELETE ghost entries | Prevent errors |
| Tools | ADD real new tools | Navigation |
| Clustering & Design | LABEL as main vs experimental | Clarification, not deletion |
| Design Docs | APPEND status updates | Changelog |
| Jules Specs | APPEND current status | Changelog |
| Requirements | UPDATE if deps changed | Instructions |

---

## C&E METHODOLOGY — VALID ✅

`docs/memory/ce_methodology.md` is well-written and accurate.
The methodology steps match the actual code in `exit_engine.py`.
The data points reference specific OOS results (536 trades baseline)
which are now superseded (1,750 trades) but the method is evergreen.

**Action:** Keep as-is. The numbers are from a specific run — the methodology
doesn't change.

---

## DAILY JOURNALS — WELL-MAINTAINED ✅

5 entries from 2026-02-28 to 2026-03-08. The March 8 entry is **330 lines**
and is the most accurate description of the current system state — more accurate
than MEMORY.md or ARCHITECTURE.md.

| Date | Topic | Quality |
|------|-------|---------|
| 2026-02-28 | Waveform Analysis I, J, K | ✅ Complete |
| 2026-03-01 | Seed integration spec, standalone research | ✅ Complete |
| 2026-03-02 | NT8 setup, TCP connection verified | ✅ Complete |
| 2026-03-07 | Scalp/exit investigation, template bias | ✅ Complete |
| 2026-03-08 | 8 sessions: forward pass, pipeline restructure, bug fixes, consolidation | ✅ Excellent |

**March 8 key decisions documented:**
- Pipeline renumbered to 6 phases
- IS oracle direction override removed (was contaminating IS)
- Strategy selection moved to after OOS (was circular)
- CPU path removed (CUDA-only)
- Feature extraction unified
- `make_position()` simplified
- `PositionState` trimmed (7 fields deleted)
- Belief flip research: DEAD END confirmed
- Analysis U: 89.3% direction from k-NN at 270-day scale

**Missing journal entry:** Today's session (2026-03-08 late / 2026-03-09 early)
covering this audit, the 3 specs created, and the remaining work plan.

**Action:** Keep journals. They're the most reliable historical record.
Add entry for current session.

---

## REFERENCE DOCS 📚

| Doc | Purpose | Status |
|-----|---------|--------|
| `CAUSE_AND_EFFECT_MATRIX.md` | Six Sigma C&E methodology journal | ✅ Valid |
| `PIPELINE_ANALYSIS_JOURNAL.txt` | Historical analysis decisions | ✅ Valid (not updated recently) |
| `WAVEFORM_ANALYSIS_JOURNAL.txt` | Waveform research notes (83KB) | ✅ Valid |
| `NT8_BayesianBridge.cs` | NinjaTrader indicator C# source | ✅ Active (live bridge) |
| `checkpoint_reference/` | Schema docs, sample data, analytics | ✅ Valid |
| `nt8_logs/` | NT8 export logs | ✅ Valid data |

---

## PROPOSED DIRECTORY STRUCTURE

```
docs/
├── ROADMAP.md                          # Future work (updated)
├── CHANGELOG.md                        # Version history (updated with V3.0)
├── SYSTEM_DESCRIPTION.md               # System overview (REWRITTEN)
├── ARCHITECTURE.md                     # File/class reference (REGENERATED)
├── NT8_BayesianBridge.cs               # C# bridge source
│
├── active/                             # Currently being worked on
│   ├── CLAUDE_CODE_REMAINING_WORK.md   # Current TODO
│   └── CLAUDE_CODE_CONSOLIDATION.md    # Reference for remaining tasks
│
├── specs/                              # Future feature specs (not started)
│   ├── LEVEL_DETECTOR_SPEC.md
│   ├── JULES_EXPECTED_PROFIT_PREDICTOR.md
│   └── JULES_WAVEFORM_SEED_INTEGRATION.md
│
├── archive/                            # Completed specs (historical reference)
│   ├── CLAUDE_CODE_UNIFIED_EXIT_ENGINE.md
│   ├── CLAUDE_CODE_BAND_CONTEXT.md
│   ├── CLAUDE_CODE_METAPHOR_PURGE.md
│   ├── CLAUDE_CODE_EXIT_IMPROVEMENTS.md
│   ├── CLAUDE_CODE_COMPRESSED_REPLAY.md
│   ├── JULES_EXECUTION_ENGINE_INTEGRATION.md
│   ├── JULES_COMPRESSED_REPLAY.md
│   └── improvement_plan.md
│
├── reference/                          # Analysis journals & methodology
│   ├── CAUSE_AND_EFFECT_MATRIX.md
│   ├── PIPELINE_ANALYSIS_JOURNAL.txt
│   └── WAVEFORM_ANALYSIS_JOURNAL.txt
│
├── checkpoint_reference/               # Schema docs & sample data
├── daily/                              # Session journal
├── memory/                             # Persistent context for AI agents
└── nt8_logs/                           # NT8 data exports
```

---

## ACTION ITEMS (ordered)

### 1. Reorganize (5 min, zero risk)
```bash
cd docs/
mkdir -p archive active specs reference

# Archive completed specs
mv CLAUDE_CODE_UNIFIED_EXIT_ENGINE.md archive/
mv CLAUDE_CODE_BAND_CONTEXT.md archive/
mv CLAUDE_CODE_EXIT_IMPROVEMENTS.md archive/
mv CLAUDE_CODE_COMPRESSED_REPLAY.md archive/
mv CLAUDE_CODE_METAPHOR_PURGE.md archive/
mv JULES_EXECUTION_ENGINE_INTEGRATION.md archive/
mv JULES_COMPRESSED_REPLAY.md archive/
mv improvement_plan.md archive/

# Active work
mv CLAUDE_CODE_CONSOLIDATION.md active/
# REMAINING_WORK.md → active/ (generate from outputs)

# Future specs
mv LEVEL_DETECTOR_SPEC.md specs/
mv JULES_EXPECTED_PROFIT_PREDICTOR.md specs/
mv JULES_WAVEFORM_SEED_INTEGRATION.md specs/

# Reference journals
mv CAUSE_AND_EFFECT_MATRIX.md reference/
mv PIPELINE_ANALYSIS_JOURNAL.txt reference/
mv WAVEFORM_ANALYSIS_JOURNAL.txt reference/
```

### 2. Regenerate ARCHITECTURE.md (after remaining work is done)

Walk the codebase and produce accurate file→class→purpose mapping.
No physics metaphors. Include:
- Core pipeline: `StatisticalFieldEngine` → `FractalClusteringEngine` → `ExecutionEngine` → `ExitEngine`
- Live stack: `LiveEngine` → `HistoryReplayEngine` → `BarAggregator` → `NT8Client`
- Support: `BayesianBrain`, `TimeframeBeliefNetwork`, `OrderManager`
- File line counts and dependency graph

### 3. Rewrite SYSTEM_DESCRIPTION.md (after metaphor purge complete)

Replace physics framing with statistical language. Keep the structure
(Purpose, Architecture, Pipeline, etc.) but update content to match
actual code.

### 4. Update CHANGELOG.md

Add V3.0 section covering:
- Phase 4: Terminology refactor
- Phase 5: ExecutionEngine unification
- Phase 6: ExitEngine improvements (Fixes 1-4)
- Phase 7: Compressed replay + atlas loader
- Phase 8: Code consolidation + metaphor purge
- Phase 9: LiveEngine thin wrapper + decomposition (in progress)

### 5. Update ROADMAP.md

Add "Completed" section at top. Items done:
- Unified ExitEngine ✅
- Band confluence entry/exit ✅  
- Exit improvements (Fixes 1-4) ✅
- Compressed history replay ✅
- ExecutionEngine integration ✅

---

## TOOLS FOLDER AUDIT

### 14,307 lines across 19 Python files + 1 subpackage

#### Category A: Production Tools (keep, maintain)

| Tool | Lines | Purpose | Status |
|------|-------|---------|--------|
| `nt8_to_parquet.py` | 256 | Convert NT8 exports → ATLAS parquet | ✅ Active, CLI |
| `checkpoint_viewer.py` | 299 | Inspect pattern_library + brain | ✅ Active, CLI |
| `analyze_gates.py` | 386 | Gate threshold optimization → `gate_thresholds.json` | ✅ Active, CLI |
| `gate_interaction_matrix.py` | 510 | C&E Matrix validation (Six Sigma DOE) | ✅ Active, CLI |
| `run_analytics.py` | 286 | Re-run analytics on checkpoint data | ✅ Active, CLI |
| `golden_path.py` | 316 | Y10/Y11/Y12 golden path metrics | ✅ Active, CLI |
| `setup_oos_atlas.py` | — | Train/test split of ATLAS data | ✅ Active, CLI |

#### Category B: Research Tools (keep, document)

| Tool | Lines | Purpose | Status |
|------|-------|---------|--------|
| `standalone_research.py` | 6,967 | Full research harness (A-R modules) | ✅ Refactored into subpackage |
| `research/data.py` | 351 | ATLAS loading, physics extraction | ✅ Clean |
| `research/imr.py` | 248 | I-MR control charts, regime detection | ✅ Clean |
| `research/screening.py` | 215 | Factor screening, R² regression | ✅ Clean |
| `research/seeds.py` | 296 | Seed primitive library | ✅ Clean |
| `research/plots.py` | 831 | All research plotting functions | ✅ Clean |
| `research/tbn_trade_aware.py` | 1,102 | Forked TBN for research | ⚠️ Has metaphors (3 lines) |
| `research_belief_flip.py` | 490 | Side-by-side TBN comparison | ✅ Clean |
| `research/capture_comparison.py` | 392 | High-def trade charts | ✅ Clean |

#### Category C: One-Off Analysis Scripts (evaluate for deletion)

These are hardcoded analysis scripts with no CLI, reading from fixed paths
like `checkpoints/oos_trade_log.csv`. They were written for specific investigations
and are unlikely to be run again.

| Tool | Lines | Purpose | Delete? |
|------|-------|---------|---------|
| `analyze_scalp_timing.py` | 111 | When do counter-trend scalps happen? | ⚠️ One-off, hardcoded paths |
| `analyze_scalp_vs_early_exit.py` | 243 | Scalp vs early exit correlation | ⚠️ One-off, hardcoded paths |
| `analyze_scalps.py` | 100 | Brain vs trend on scalps | ⚠️ One-off, hardcoded paths |
| `analyze_wrong_dir.py` | 166 | Wrong-direction trade analysis | ⚠️ One-off, hardcoded paths |
| `monthly_pnl_chart.py` | 76 | PnL bar chart | ⚠️ One-off, hardcoded paths |

**Total one-off lines: 696**

**Recommendation:** Move to `tools/archive/` (not delete — they document past
research methodology). If any are needed again, they can be promoted back
with proper CLI and path arguments.

#### Category D: Visualization (keep)

| Tool | Lines | Purpose | Status |
|------|-------|---------|--------|
| `pattern_map.py` | 243 | Signal overlay on price chart | ✅ Active, CLI |
| `trade_visualizer.py` | 210 | Entry/exit markers on waveform | ✅ Active, CLI |

#### Category E: Generated Outputs (gitignore, not delete)

| Item | Size | What It Is |
|------|------|-----------|
| `tools/plots/` | 182 MB | 123 PNG files from research runs |
| `tools/standalone_report.txt` | 226 KB | Research report output |
| `tools/screening_gates.json` | 7 KB | Research screening output |

**Action on `tools/plots/`:** Add to `.gitignore`. These are regenerated on every
research run. 182MB of PNGs in git history is permanent bloat.

### Tools: Remaining Cleanup

1. **`research/tbn_trade_aware.py`** — 3 metaphor remnants (lines 349, 351, 504):
   - "quantum state" → "market state"
   - "Roche limit" → "2σ band extreme"
   - "quantum states" → "market states"

2. **`standalone_research.py`** — 6,967 lines but properly delegates to `research/`
   subpackage. The main file is now a thin orchestrator (~90 lines of actual logic,
   rest is the module runner). **No action needed.**

3. **`STANDALONE_RESEARCH_GUIDE.md`** — valid developer guide. Keep.

---

## REPO SIZE BLOAT — 533MB OF TRACKED GENERATED FILES

### Problem

```
tools/plots/     182 MB  (123 PNG files — research output, regenerable)
reports/is/      294 MB  (42 CSV files — IS training logs)
reports/oos/      55 MB  (10 CSV files — OOS training logs)
reports/live/    672 KB  (session reports — small, valuable)
─────────────────────────
Total tracked:   ~532 MB of regenerable artifacts
```

The `.gitignore` comment says `reports/ is intentionally tracked` but 349MB
of oracle/signal/trade CSVs in `reports/is/` and `reports/oos/` are training
outputs that can be regenerated by running the pipeline. They slow every
`git clone` and permanently bloat git history.

### Action

**Add to `.gitignore`:**
```gitignore
# Generated research plots (regenerated by standalone_research.py)
tools/plots/
tools/standalone_report.txt

# Training run outputs (regenerated by trainer.py)
# Keep text summaries, gitignore large CSVs
reports/is/*.csv
reports/is/shards/
reports/oos/*.csv
reports/training/*.csv

# Keep these tracked (small, valuable):
# reports/is_report.txt
# reports/oos_report.txt
# reports/live/
# reports/run_history.csv
```

**Then remove from tracking (but keep local files):**
```bash
git rm --cached -r tools/plots/
git rm --cached tools/standalone_report.txt
git rm --cached reports/is/*.csv
git rm --cached -r reports/is/shards/
git rm --cached reports/oos/*.csv
```

**Expected savings:** ~530MB removed from future clones. Git history still
has them (can't undo that without rewriting history), but new clones will
be ~45MB instead of ~575MB.

---

## UPDATED PROPOSED DIRECTORY STRUCTURE

```
docs/
├── ROADMAP.md                          # Future work (updated)
├── CHANGELOG.md                        # Version history (V3.0 added)
├── SYSTEM_DESCRIPTION.md               # System overview (REWRITE NEEDED)
├── ARCHITECTURE.md                     # File/class reference (REGENERATE)
├── NT8_BayesianBridge.cs               # C# bridge source
│
├── active/                             # Currently being worked on
│   └── CLAUDE_CODE_REMAINING_WORK.md   # Current TODO (7 tasks)
│
├── specs/                              # Future feature specs (not started)
│   ├── LEVEL_DETECTOR_SPEC.md
│   ├── JULES_EXPECTED_PROFIT_PREDICTOR.md
│   └── JULES_WAVEFORM_SEED_INTEGRATION.md
│
├── archive/                            # Completed specs
│   ├── CLAUDE_CODE_UNIFIED_EXIT_ENGINE.md
│   ├── CLAUDE_CODE_BAND_CONTEXT.md
│   ├── CLAUDE_CODE_METAPHOR_PURGE.md
│   ├── CLAUDE_CODE_EXIT_IMPROVEMENTS.md
│   ├── CLAUDE_CODE_COMPRESSED_REPLAY.md
│   ├── CLAUDE_CODE_CONSOLIDATION.md
│   ├── JULES_EXECUTION_ENGINE_INTEGRATION.md
│   ├── JULES_COMPRESSED_REPLAY.md
│   └── improvement_plan.md
│
├── reference/                          # Analysis journals & methodology
│   ├── CAUSE_AND_EFFECT_MATRIX.md
│   ├── PIPELINE_ANALYSIS_JOURNAL.txt
│   └── WAVEFORM_ANALYSIS_JOURNAL.txt
│
├── checkpoint_reference/               # Schema docs & sample data
├── daily/                              # Session journal
├── memory/                             # Persistent context for AI agents
└── nt8_logs/                           # NT8 data exports

tools/
├── nt8_to_parquet.py                   # Production: NT8 → ATLAS
├── checkpoint_viewer.py                # Production: inspect checkpoints
├── analyze_gates.py                    # Production: gate threshold optimizer
├── gate_interaction_matrix.py          # Production: C&E Matrix
├── golden_path.py                      # Production: golden path metrics
├── run_analytics.py                    # Production: analytics re-runner
├── setup_oos_atlas.py                  # Production: train/test split
├── pattern_map.py                      # Visualization: signal overlay
├── trade_visualizer.py                 # Visualization: trade charts
├── standalone_research.py              # Research: orchestrator (delegates to research/)
├── research_belief_flip.py             # Research: TBN comparison
├── STANDALONE_RESEARCH_GUIDE.md        # Research: developer guide
├── screening_gates.json                # Generated: research output
│
├── research/                           # Research subpackage (clean)
│   ├── __init__.py
│   ├── data.py
│   ├── imr.py
│   ├── screening.py
│   ├── seeds.py
│   ├── plots.py
│   ├── tbn_trade_aware.py
│   └── capture_comparison.py
│
├── archive/                            # One-off scripts (historical)
│   ├── analyze_scalp_timing.py
│   ├── analyze_scalp_vs_early_exit.py
│   ├── analyze_scalps.py
│   ├── analyze_wrong_dir.py
│   └── monthly_pnl_chart.py
│
└── plots/                              # Generated PNGs (.gitignored)
```

---

## FULL ACTION CHECKLIST

### CRITICAL (do first — stale MEMORY.md misdirects every AI session):

- [ ] **0. Surgical update `docs/memory/MEMORY.md`** — This is loaded at the start of
  every Jules/Claude Code session. It points agents to 6 deleted files, has wrong
  class names, and lists tools that don't exist. But it also contains historical
  records (baselines, design decisions, implementation timeline) that must NOT
  be overwritten. **Approach: fix navigation, preserve history.**

  **UPDATE** (navigation — these are instructions, not history):
  - Architecture section: fix all file paths/class names to current (`statistical_field_engine.py`, `market_state.py`, `BayesianBrain`, etc.)
  - Architecture section: add new modules (`execution_engine.py`, `feature_extraction.py`, `history_replay.py`, `atlas_loader.py`)
  - Architecture section: delete references to `fractal_dna_tree.py`, `dynamic_binner.py` (don't exist)
  - Tools section: delete 6 ghost tool entries, add real new tools
  - Pipeline: update to 6 phases (not 5)
  - CLI Flags: update to current
  - Clustering: label "Main: recursive K-Means | Experimental: DMI→I-MR→DBSCAN"
  - Add note: CUDA-only (CPU path removed Session 8, 2026-03-08)

  **APPEND** (historical records — add below existing, don't replace):
  - Baseline: add new dated entry below existing "2026-03-07" baseline
  - Implemented Features: append 10 new entries with (2026-03-08) dates
  - Design Docs: append status updates to existing entries

  **DO NOT** delete existing baseline numbers, implemented feature entries,
  or design decision records — they document the system's evolution timeline.

### Immediate (5 min each, zero risk):

- [ ] 1. Create `docs/archive/`, `docs/active/`, `docs/specs/`, `docs/reference/`
- [ ] 2. Move 8 completed specs to `docs/archive/`
- [ ] 3. Move `REMAINING_WORK.md` to `docs/active/`
- [ ] 4. Move 3 future specs to `docs/specs/`
- [ ] 5. Move 3 reference journals to `docs/reference/`
- [ ] 6. Create `tools/archive/`, move 5 one-off scripts there
- [ ] 7. Add `tools/plots/`, `reports/is/*.csv`, `reports/oos/*.csv`, `reports/is/shards/` to `.gitignore`
- [ ] 8. `git rm --cached` the 533MB of tracked generated files
- [ ] 9. Fix 3 metaphor remnants in `tools/research/tbn_trade_aware.py`
- [ ] 10. Add daily journal entry for this session (2026-03-09)

### After remaining 7 code tasks complete:

- [ ] 11. Regenerate `ARCHITECTURE.md` from actual codebase (archive v2 first)
- [ ] 12. Update `SYSTEM_DESCRIPTION.md` (archive v2 first, then update active copy with statistical language)
- [ ] 13. Add V3.0 section to `CHANGELOG.md`
- [ ] 14. Update `ROADMAP.md` with completed items section
