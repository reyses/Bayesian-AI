# 2026-07-19

## v0.3-RC first real backtest (Moises, 6/22-7/17, stop OFF) - BOTH FIXES VERIFIED
Entries now 6:36-6:45 AM PT = 8:36-8:45 CT (correct session; harness predicts
~8:40 first fire) on ALL 20 days. Exits: 19 X_RTriggerReversal (THE fix - was
0/44 in v0.2) + 18 X_SessionFlatten 15:55 CT (P2-11 works) + 1 NT8 session
close (7/03 half-day, legit). Economics (canonical): net +$220.80, PF 1.04
(Trade WR +0.04), Day WR 45%, $/day mode -$50 / mean +$11 CI[-$149,+$170]
NOT significant, N=20d/38 trades. Structure: R-trigger exits net -$2,936
(the loss-cutter role), 15:55 rides +$3,223. Right tail: +574/+555/+513/+461.
Cross-run same-window (all CIs overlap, NO significance claims): v0.2
wrong-session no-stop -300; v0.2 wrong-session stop50 +816; v0.3 designed
no-stop +221. Next: stop-ON A/B (Moises), then v0.3-vs-sim parity diff
(sessions now aligned - expect near 1:1).

## CUDA pivot + AG handoff (147)
Moises overturned the CPU verdict - correctly: the drone compared GPU
full-prompt evals vs CPU incremental frames (apples/oranges); like-for-like
GPU pp is 4-5x. Readout v2 (last-logits) also removed the 5GB buffer that
made WSL-GPU fragile. Blob copied to ext4 (~/models/qwen3-14b-q4.gguf, 87s)
killing the 9p cold-load tax. Runbook 147 hands execution to AG: CUDA smoke
(2 eps, acceptance criteria) -> full 156 in tmux -> ETA from elapsed fields
-> 142-format deliverable. Reviewer near usage limit; watcher stays up.

## v0.4-RC: catastrophic stop -> native resting order (Moises, P2-10 resolved)
Moises flagged stop rule-violations; diagnosis = the stop is only reached after
a bar-cadence wait. ROOT CAUSE confirmed in code: v0.3 stop was a POLLED check
on bar close + a MARKET flatten filling at the NEXT bar open -> slip scales with
bar size (10-230% over the 50pt setting). FIX: SetStopLoss(Ticks) attaches a
real resting stop per entry; NT8 fills intrabar at the level, bounded slip, any
bar size -> cadence-independent (pushed back on "anchor to 1m": a polled stop on
1m waits a full minute; a resting stop waits 0 -- TF is not the lever). Polled
block removed (no double-stop). Core region SHA identical to v0.2/v0.3 (PASS).
NOT deployed - awaiting compile. OPEN: if Moises runs on a 1m CHART, the "5s
primary" assumption is silently violated (would explain the P3 55%% direction
match) -- needs the DataLoaded resolution assertion + 5s-vs-1m substrate parity
call. Flagged, not yet done.
