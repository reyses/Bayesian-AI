# 2026-07-17 — Blind dojo verdict; wrong-dir dojo built; phold causal patch; phone bridge

> Note: the overnight portion (dojo pilot, Mamba anti-freeze smoke, reward
> dead-wire audit, speed gates) was journaled as appendices in
> `2026-07-16.md` ("night of 07-17" sections). This file covers the daytime.

## Corrected dossier — phold anchor patch (doc 097, commit 371e466b)
Audit found the P_hold τ-anchors missed the −period shift at the 5s layer
(≤5s feature-freshness peek). Patched (BAR_S), full rerun: FULL 0.6472→0.6381,
delta −0.0422→−0.0465 — **every doc-089 verdict UNCHANGED**, null got STRONGER
(the F-space benefited more from the peek and still lost). Higher TFs were
always clean (`_last_closed_idx`). Drone A patched+ran; reviewer wrote parity.

## Exit Dojo FULL RUN — 200 blind episodes (doc 098, commit b4b7cf35)
Stepwise-blind sandbox (gate + nonce audit PASS ×200). **Honest verdict: LLM
exit ≈ wash vs a dumb 5m-hold** (delta +3.9 CI[−1.0,+9.3] NS, beat-rate 40%);
only WINNERS significant (+19.5 [8.3,32.1], 63%, still just 0.29 of oracle);
wrong-side a significant LOSS (−9.6). The pilot's 7/10 was the leakage tax,
now measured. Grammar = real vocabulary (against-fires 88%, giveback 68%),
thin EV. Consequences: EXIT-GRAMMAR-01 → winner-ride only; Mamba promises
ride-length, not turn-timing. Security fix en route: fleet allowlist scoped to
the gate command (replaced --dangerously-skip-permissions from the build).

## Wrong-direction dojo (doc 099) — designed, built, re-cut on economics
Moises: "identify wrong direction… 50/50 good and bad… cut before damage, both
the dumb selector and the mamba." Built on the same sandbox; drone surfaced
the label-vs-economics mismatch (13% of label-wrong end UP; 50% of good
midflips end DOWN) → re-cut on TERMINAL economics (WRONG ≤−4pts / GOOD ≥+4pts,
dead-band dropped; goods split 50 dipped/50 clean), damage-avoided-in-time
metric, dumb-stop adverse-drawdown ROC as the explicit PASS/FAIL bar.
100/100 blind on 200 distinct days; fleet launched (verdict → doc 100).

## Telegram phone bridge (commits 82fb66da/a9c2c47a)
Headless claude -p bridge (no pyautogui/window focus — AG's pattern needed
remote-in); chat-id allowlist, project permissions, media both ways; SUDO
token takeover of AG's bot (409-race caveat documented). Cost: idle=0; per
message ≈ same task at desktop (cache absorbs the context reload).

## Ops/misc
Mamba output-routing hygiene (4abeb820 — no root litter); exit-decay +
ExNMP lineage recovery began this evening (continued into 07-18).
