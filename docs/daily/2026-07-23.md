# 2026-07-23 — Tiered run COMPLETE: gen-0 baseline measured (never-bail beats the teacher)

## The result (docs 148/149 arc concluded)
Tiered full-depth run finished: **156/156 episodes, 2,884 frames labeled, 7 tainted
(0.24%, random single frames 29-183 tok over budget — not structural)**. Labels rich:
p_exit 0.0006→0.97 with genuine late-ride EXIT signals (e.g. 0.970 @ minute 22 of an
L-episode — unreachable under the old telescope).

**Effectiveness vs truth (N=156, `tools/score_tiered_effectiveness.py`):**
| policy | mean pts/ep | capture vs oracle |
|---|---|---|
| Oracle | 87.4 | 1.00 |
| **Ride-to-end (never-bail)** | **38.9** | 0.445 |
| Teacher gen-0 (3 seed rules) | 26.3 | 0.301 |
| Fixed-5m | 24.7 | 0.283 |

Teacher − never-bail = **−12.56 pts/ep, 95% CI [−22.00, −3.39], SIGNIFICANT** (4,000
resamples). Teacher exits median minute 7 vs oracle minute 15 → **gen-0 exits too
early — replicating doc 107's law (no cut policy beats never-bail) at label level.**
NOT a harness failure: this is the pre-registered baseline evolution must beat.
Report: `research/dojo_forge/reports/tiered_effectiveness_2026-07-23.md`.

## Overnight rulings + artifacts (phone-driven, autonomous mode)
- Run stayed LOCAL (owner: same-env comparability + no double cloud spend).
- 7-taint forensics → **gen-1 text-definition spec** directive recorded in doc 149
  (fixed-width numerics, per-TF line templates with exact token budgets).
- **Test-base expansion** queued as a spec-conform comms proposal (Q0 power calc →
  target N, selection criteria, lockbox split per frozen RIDE_EDGE_GATE_SPEC).
- Anti-freeze dossier delivered (`research/mamba_zigzag_baseline/reports/
  ANTIFREEZE_FRAMING_2026-07-23.md`): freeze & overtrade are ONE gradient (cost/wiggle
  punish trade-count, capture pays ~0); 5 ranked hypotheses, 6 levers incl. teacher-KL
  anchor; PF-Trade-WR of tonight's epoch-0 = −0.931.
- Medical webpage debugged (build PASS 18 routes; 2 blockers: status union, Next-16
  async cookies; auth is demo-grade — report in project dir).
- WoW+memory scaffold template → `/media/moi/WindowsCode/WOW_MEMORY_SCAFFOLD_TEMPLATE.md`
  + sent to owner.

## Next session
1. Anti-freeze design session (dossier is the agenda).
2. Test-base expansion proposal (numbered doc) + gen-1 packet builder (text-definition
   spec + log-decay retention).
3. Gen-1 genome mutations targeting the measured too-early-exit failure.

---

# 2026-07-23 (cont) — 🚨 READOUT DISAGREEMENT: the baseline is in question

Owner asked "can't we ask it why it made that decision?" → built
`tools/ask_teacher_why.py` (replays exact tiered contexts with reasoning ENABLED).
**Result: 10/10 of the worst premature exits flip to HOLD when the model reasons**,
each correctly citing G0.3 (giveback/retrace + trend intact → HOLD) — at frames where
the think-bypass logit readout recorded EXIT at p=0.52–0.99.

**Implication:** the "gen-0 exits too early / never-bail beats teacher" baseline may be
substantially a MEASUREMENT ARTIFACT of the bypassed-reasoning readout, not the genome's
judgment. Caveats: explain-instruction prompt delta; worst-case selection (some flips =
regression). But 10/10 with consistent rule citation is loud.

**Next (awaiting owner go):** disagreement experiment — ~40 frames across the p_exit
spectrum, BOTH readouts, measure disagreement structure. If artifact confirmed: fix the
readout (reasoned generation + parse) and RE-BASELINE before any genome mutation.
Report: `research/dojo_forge/reports/teacher_why_2026-07-23.md`.
