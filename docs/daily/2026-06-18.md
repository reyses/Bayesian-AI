
## Forward-increment OOS test — the signal verdict (2024_02_20)
- Built `research/fspace_experiment/forward_increment_test.py`: predict close[t+h]-close[t] from causal F-space at t, 70/30 time-disjoint + embargo, RidgeCV, OOS R^2 vs real+Brownian+Fourier, both B2C(continuous) & B2T(tiled).
- RESULT: ALL cells/horizons NEGATIVE OOS R^2 (worse than mean); corr ~0; dir-acc 42-51%. Real does NOT beat null — real is WORSE (overfit/regime-shift anti-generalizes; nulls shrink to mean). Worsens with horizon.
- VERDICT: no tradeable forward signal in L1-L3 F-space at 1-60s; the in-sample tiled +16.7pp pristine edge forecasts NOTHING (pure in-sample regress-ability of an autocorrelated path).
- UNTESTED threads: L4 (NMP lambda = the actual thesis signal term, absent here), nonlinear models, multi-day, longer horizons. Next $ = add L4 then re-run this exact forward test.
- Files: reports/findings/forward_increment_2024_02_20.md + b2c_continuous_2024_02_20.md.

## Stage-1 R-curve-map enrichment — DONE + verified (2026-06-18)
- Per JULES_STAGE12_MAP_OVERHAUL.md: stage1_speed_pass.py now KEEPS the per-L R-curve (was discarded) + direction + flip descriptors.
- batched_ols_scan returns per-L r2/n_off/consec_off; main loop assembles seg['r_curve'] (incl. FLIP_TAIL_BARS=20 past break), slope_pts_per_s, net_move_pts, direction, break_reason, consec_off_at_break. Writes switched to periodic+final (r_curves bloat the JSON).
- SMOKE test (8h slice, 20701 valid rows, 401 segs): all keys present; r_curve traces r2 decay 0.979->0.795->0.658 through the flip = the distortion-through-flip map material. break_reason=consec_off, consec@break=5. WORKS.
- LEFT: (2) stage2 same-schema mirror, (3) build_map.py composite (explainability/hazard/direction/flip-sig + null bands), (4) full re-runs B2C/B2T x real/brown/four then build map. VWAP direction/flip primitive parked in spec TODO.

## C-family fully mapped — UPDATE CADENCE is the lever (2026-06-18)
- 1-day wide screen (2024_02_20), corrected rule + enriched R-curve map, survival(flip-timing) real vs Brownian/Fourier:
  - B2C (continuous, every-1s, TF-pinned windows): @45s 0.58|0.57|0.52 — WEAK (over-smooths, real≈null).
  - C-2 (continuous, free-N) @45s across w60/180/480/960: real≈null at EVERY window (0.33-0.49 vs nulls similar; real sometimes BELOW null). Window length is NOT the lever.
  - B2T (tiled, bar-close): @45s 0.31|0.10|0.04 — STRONG (real ~8x null survival).
  - Run C (bar-close-sampled sliding): @45s 0.20|0.09|0.01 — STRONG (~20x), behaves like B2T.
- VERDICT: the discriminator is UPDATE CADENCE (bar-close discretization), NOT window length or continuity. The original "continuous sliding window from 1s" thesis is the WEAKER rep for this signal; conventional TILED bar-close carries the flip-timing structure. C-2 = clean negative (correctly reviewed+built, doesn't work). Tiled was discarded too fast early (band-artifact 5%); map recovered it.
- SURVIVOR -> week: TILED bar-close, full schema. B2TF (L1-L5, +L4 lambda +L5 dist) built (242 cols) and stage1 running to test if L4/L5 sharpen the tiled edge.
- CAVEATS unchanged: n=1 null (need Fourier ensemble for p-value), in-sample survival, ONE day -> week + ensemble next.
- New/edited: build_run_c2.py, build_run_runc.py, build_run_b2t.py(+L4/L5 -> FEATURES_RUN_B2TF), build_map.py(+RunC), telemetry run_name-keyed. Findings: map_b2c_b2t_2024_02_20.md.
