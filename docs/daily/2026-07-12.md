# Daily Journal: 2026-07-12

## Session Report — Claude as executor (AG out of usage since ~07-11 evening)

### 1. What happened (arc since last exit report)
Claude executed Phase-5 directly. Full trail = numbered docs in
`research/nt8_catalog/comms/035–041` (one doc per turn, finalized-on-write).
- **CRITICAL BUG (voids all pre-035 Phase-5 numbers):** entry-feature extraction
  indexed FULL-session feature parquets (row 0 = 17:00 CT) with RTH-relative
  event_idx (row 0 = 08:30) → overnight features for daytime trades. Fixed in
  `tools/ag_phase5_doe.py` (per-dossier index-convention detection + RTH slice).
- DOE across 22 dossiers (entry-only, leakage-free, day-block bootstrap): 3
  forward-valid candidates; year-swap replication killed ATR-09 INVERT + RSI-06
  (one-year artifacts), kept only SEASON-12 ACT (skew-suspect). comms/036.
- Approach-ladder (lead-in lags) ATR-09: ride wins 94% forward by frequency, but
  4 catastrophic tails entry-invisible even with full 918-feat multi-TF ladder AND
  true forward-stepwise → tail is an EXIT problem. comms/037-038 (+ ceiling).
- Three-anchor conversion views (entry→exit→post-exit) all 20 dossiers:
  mean-reversion family shows real z_se conversion signature; trend family flat.
  `reports/AG_cat_00_CONVERSION.md` + plots.
- Visual galleries for Moises: `reports/assets/phase5_gallery/` (branches) +
  `/conversion/`. Moises inspected all; flagged PIVOT-16 + ROUND-05.
- **REALIZABLE-TRADE SCREEN, all 24 dossiers both directions** (from stored
  MFE/MAE, worst-case ordering, day-block CIs, envelope/censoring check):
  `reports/AG_cat_00_REALIZABLE.md`, verdict comms/041.

### 2. STANDING RESULTS (the state of the world)
- **Tier 1 tradable candidates (both-year sig, config-robust):**
  ROUND-05 breach-continuation ±20 (+5.43/+6.61 pts/event);
  PIVOT-16 FLIP fade-the-touch TP+11/stop20 (+8.07/+9.05);
  PIVOT-16 stated T20/S20 (+2.75/+2.31); VWMA-10 T20/S20 (+2.09/+3.00, 3/4).
- **PIERCE-THEN-BOUNCE anatomy** (comms/041 §3): watched levels pierce ~10-15pts
  then bounce 20+ — both PIVOT directions profitable at different geometries
  proves the two-phase path. Most structural finding of the catalog.
- Tier 2 sub-friction drift (sig, unpayable): OHLC/DOW/SAR/TUNNEL/ZONE/ORDERFLOW.
- Tier 3 envelope-suspect (re-measure wider windows): HNS-22, VP-01.
- Dead as trades: SEASON-12 gap-fade (unrealizable-peak artifact, comms/040),
  ATR-09 invert (tail), RSI-06 (lottery), rest null.

### 3. IN PROGRESS at time of writing — path-accurate backtest
Task: replace the worst-case MFE/MAE approximation with real bar-sequence
replay for the two clean Tier-1s.
- Tool being built: `research/nt8_catalog/tools/ag_phase5_backtest_path.py`.
- Spec: for each event, entry = close[event_idx] (signal at bar close, RTH-aligned
  5s bars from DATA/ATLAS/5s, event_idx is RTH-relative for these two dossiers);
  walk bars forward; stop/target by bar sequence; if BOTH inside one 5s bar,
  count STOP first (conservative); exit EOD close if neither. Configs:
  ROUND-05 dir=breach direction (mode column: bullish/bearish_continuation),
  T+20/S-20; PIVOT-16 dir=OPPOSITE of mode (flip: mode bullish_bounce → SHORT),
  TP+10/stop-20. Output: per-year N, WR, EV, day-block CI (use
  tools/ag_phase5_doe.py day_ci), distribution plot, comms doc 042.
- Gate: if path-accurate EV confirms within ~±30% of screen numbers → next =
  pierce-then-bounce anatomy study, then NT8-parity/SIM discussion with Moises.

### 4. How AG continues if Claude stops
1. Read `comms/CLAUDE_AG_REVIEW_PROTOCOL.md`, then comms/032 (standing rules),
   then comms/039-041 (findings). NO nulls (Moises directive, doc 013/§5 MVP);
   day-block CIs; worst-case ordering; raw points; one numbered comms doc per
   turn; commit+push every turn.
2. If `tools/ag_phase5_backtest_path.py` exists: finish/run it per spec above.
   If not: build it per spec. Report as next comms number.
3. Then: pierce-then-bounce anatomy (comms/041 §5 item 2), HNS/VP re-measure
   (item 3). Do NOT deploy anything; NT8 gate untouched.

### 5. Expected impact
If the backtest confirms, the project has its first two catalog-derived,
both-year-significant, realizable trade candidates (~$16-18 and ~$11-13 per
contract-day before friction) + a structural level-anatomy to build on.

## CORRECTION (same day, ~1h later) — Tier-1 RETRACTED
The path-accurate backtest (§3 task) was run: BOTH Tier-1 candidates FAIL.
ROUND-05 ±20: -0.78/+1.40 ns; PIVOT-16 flip: -1.45/+0.54 ns (EOD, 5m, 15m variants
all n.s.). Root cause: stored MFE/MAE semantics mismatch vs real entry (stops fire
5-10x more than stored MAE implies) + scratch-at-0 assumption. comms/042.
BINDING LESSON: excursion screens = hypothesis generators only; "tradable" requires
bar-sequence replay. Catalog stands at ZERO validated tradable strategies; pierce-
then-bounce demoted to hypothesis pending path-accurate anatomy study.

## NEXT PROGRAM (Moises' solution, same day): FPS audit -> optimize -> rerun all
Plan + continuation instructions: research/nt8_catalog/comms/043. One canonical
causal engine (core_v2 FPS) audited + optimized, then ALL 24 catalog strategies
re-run through it for accurate numbers. Kill/accept pre-registered (CI_lo > 2-tick
friction both years, cluster-shaped mode >= 2pts, N >= 100/yr).

## SESSION EXIT — FPS canonical run complete (Moises' solution executed)
FPS audited (core sound; skip-counter + use_5s_price added; joins vectorized,
PARITY SHA 675e744170679490 identical), throughput ~128k bars/s. ALL catalog
strategies re-run through the one engine: 521 days, 444,528 trades, ~9 min.
RESULT: **ONE candidate passes the pre-registered friction gate — ORB-02
Opening-Range Breakout AS STATED, T20/S20: 2024 +4.22 [1.86,6.55], 2025 +3.19
[0.71,5.66], monotone dose-response across configs, flip = mirror-negative.**
Everything else fails; ROUND-05/PIVOT-16 deaths confirmed by canonical engine.
NEXT (pre-registered in comms/044 §3, NOT yet run): ORB monthly stability,
T25/30 extension, slippage sensitivity, distribution plot — all BEFORE any
SIM/NT8 talk. Continuation: read comms/043+044; tools/fps_catalog_runner.py.

## FINAL CORRECTION — ORB-02 was lookahead; catalog verdict = ZERO (comms/045)
Moises removed mechanical stops from exploration level ("stops = management;
no risk in a backtest") -> horizon explorer (raw unstopped drift) -> ORB-02
showed %>0=1.00 at +30m both years = impossible -> audit: ORB alone slices
09:00 (event_idx 09:00-anchored) while mapper assumed 08:30 -> entries 30min
early with future-knowing direction. Offset-corrected ORB: dead (2025 negative
all horizons). FINAL: all 24 concepts = no edge, no drift, both years, canonical
engine. Corrective: dossiers must export entry_ts; %>0=1.00 = auto-fail tell.
The no-stops exploration view is what exposed it — barriers had masked the tell.

## SESSION END (Claude usage limit) — handoff to AG: comms/047
Moises' next plan: tag ALL catalog entries on one chart -> find co-firing
confluence zones -> overlay the golden auto-labeler labels to see how catalog
events match labeled opportunities. AG executes (protocol: next doc = 048,
evidence-coupled, commit each turn); Claude audits on return per comms/047 §4
checklist. ⚠ ORB entry_ts in fps_horizons.parquet still 30min early — correct
(+1800s) or drop. Day arc: catalog closed as directional null (045) but the
reversion family = real hour-matched amplitude markers +5..+14pt (046);
GARCH-matched ambient still the pending decisive drill.
