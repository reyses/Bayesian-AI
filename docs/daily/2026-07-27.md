
## Viz: dojo mode (tools/viz/plugins/dojo_replay.py) — NEW
- **What**: VizEngine plugin that overlays a dojo-forge EXIT-drill episode on the price chart — entry, per-frame HOLD/EXIT decisions from the gate transcript, committed EXIT + reason, and a favorable-pts subplot. `n/b` cycle episodes; `r` runs the qwen3 teacher LIVE (forge_harness, llama_cpp, GPU) streaming decisions as it commits.
- **Files**: `tools/viz/plugins/dojo_replay.py` (new). Reads `research/dojo_forge/reports/gen0/packets/*.json` + `gate_state/gen0/*.transcript.jsonl`; live writes to `gate_state/viz_live/`.
- **Run**: `python -m tools.viz.run --plugin dojo_replay --day 2025_01_21`
- **Status**: headless self-test passes (episode discovery, decision parse, price-snap, empty-day). GUI not launched (needs display). Live-run key not yet exercised end-to-end (loads 8.7GB model).
- **Next run — look for**: does `r` stream frames onto the chart as the model commits? Confirm DOJO_RUN_DIR/packets resolution + viz_live transcript path when run live.

## Tool: chat_teacher.py (research/dojo_forge/tools/) — NEW
- **What**: human<->qwen3 REPL. Two modes: (1) free chat (`--genome` to load genome), (2) BLIND episode gate (`--eid`) — serves one frame at a time using the EXACT forge-harness system prompt; no chart/future/outcome; `/next` reveal, `/ask` qwen commits, `/exit` commits+reveals fav-pts trajectory. Owner req: "blind for me too, so I can't see more than qwen."
- **Run**: `python research/dojo_forge/tools/chat_teacher.py --list | --eid <id> | [--genome]` (needs CUDA libs on LD_LIBRARY_PATH; loads 8.7GB qwen blob, n_gpu_layers=-1).
- **Verified**: `--selftest` OK; one-shot live gen works (qwen answered a trading Q correctly citing G1.8); blind internals (frame parse, fav trajectory, outcome reveal) tested headless. Interactive REPL not driven end-to-end (needs a live terminal).
- **Note**: viz dojo_replay plugin is NON-blind (hindsight reviewer). A blind-viz toggle (draw price only up to current frame) is offered, not built.

## Viz: dojo_replay BLIND mode — added (owner: "connect dojo to viz, see what it's seeing")
- `--blind` flag (or `x` toggle): chart reveals price ONLY up to current frame (future fogged with opaque axvspan); frame-text panel shows the EXACT packet frame text qwen reads; fav-pts subplot truncated to cutoff.
- Keys: `f`/`v` step frame reveal, `x` toggle blind, `n`/`b` episode, `r` run-live (auto-advances reveal to qwen's served/committed frame via transcript polling — watch it work blind).
- Run: `python -m tools.viz.run --plugin dojo_replay --day 2025_01_21 --blind`
- Verified headless at frames 5 & 9 (fog + frame-text box + truncated subplot render correctly). Live-sync tick reads viz_live transcript serve/commit events. GUI + live not exercised end-to-end (needs display).
- Cosmetic: long frame-text lines overflow right edge of the text box; acceptable, could wrap later.

## ANTICIPATE-THE-COMBINER pipeline (autonomous, owner via TG 2026-07-27)
Task (owner): give qwen the FULL combiner (22 streams + P + gov_dir) + curve regression + all features; judge ONLY anticipated LEG DIRECTION.
- **Go/no-go probe** (research/edge_probe/tools/anticipate_combiner.py): combiner IS anticipatable from streams+z_se alone (no P) — DIRECTION AUC 0.88@3bars / 0.82@8bars early; FIRE-timing AUC ~0.70. 200k bars, OOS. GO.
- **Curve regression = CUBIC endpoint** (owner correction: z_se_15 is the OLS *residual*, NOT the curve). Replicated docs/nt8/2-CubicRegressionEndpoint_v1.0-RC.cs: cubic y=a·t³+b·t²+c·t+d over 7.5min, endpoint value/slope(pts/min)/curvature. Impl: research/dojo_forge/tools/cubic_regression.py (precomputed weights on 5s, self-tested).
- **Packets**: research/dojo_forge/tools/anticipate_packets.py → reports/anticipate/packets/ (4149 built, last-30-days). Frames = run-up to each combiner fire; blocks = combiner(P/gov_dir/streams) + curve-reg(cubic 7.5m slope/curv) + reg-band(z15) + price. Label = leg_dir_true (dominant move pivot→next reversal).
- **qwen run**: research/dojo_forge/tools/anticipate_run.py (/no_think, DIR:LONG/SHORT). Background job scoring 500 episodes @ early frame; baselines = gov_dir + cubic-slope sign; ref = GBM 0.82-0.88, combiner 73%. Results → reports/anticipate/summary_early.md.
- Safety: 4.5h cron wake (job 9d2ed876). Watcher armed for TG. Autonomous.
- NEXT: when run lands, score qwen vs baselines; if qwen>gov_dir → the teacher adds anticipation lift worth distilling; then scale days / add 'fire' frame + earliness curve.

## ANTICIPATION RESULT — NEGATIVE (autonomous, 2026-07-27 late)
Ran qwen (500 ep) + GBM ceiling (4149 ep) on anticipating LEG direction at the early frame (~10 bars before the combiner fire):
- qwen (full combiner + cubic 7.5m + z_se): **58.0%** — BELOW gov_dir baseline
- GBM (same numeric ingredients): 62.4%, AUC 0.658
- gov_dir alone: 61.9% ; cubic-slope sign: 51.3% (coin flip)
Verdict: leg direction is NOT anticipatable early beyond the combiner's own lean (gov_dir ~62% is the ceiling). Cubic curve-regression, z_se, and qwen add ~nothing; qwen underperforms a trivial baseline.
CORRECTION to earlier probe: the 0.82-0.88 "anticipatable" (anticipate_combiner.py, target B) was anticipating the combiner's OWN gov_dir output (circular/autocorrelated), NOT the true leg. When the target is the actual leg, it's ~62% and gov_dir wins.
Reconciles with the exit-gate pattern: "edge looks real until you measure the right target; teacher can't beat a trivial baseline." Do NOT distill to mamba.
Tools (reusable): research/edge_probe/tools/{anticipate_combiner,anticipate_leg_gbm}.py; research/dojo_forge/tools/{cubic_regression,anticipate_packets,anticipate_run}.py. Results: reports/anticipate/*.
