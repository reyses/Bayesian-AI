# Daily Journal Index

| Date | Summary |
|------|---------|
| 2026-05-22 (trade-outcome diagnostic suite — 15 probability tables) | **Built `tools/trade_outcome_suite/` — a consolidated diagnostic suite over all hardened legs: shared `excursions.py` per-leg MAE/MFE dataset (reconstructed entry→exit from the 5s path), `questions.py` with 15 question functions, `run_all.py` light wrapper → one consolidated report.** Long iterative session — user wanted entry-to-close/MAE/MFE probability tables, then drilled into continuation, the giveback, "cut and bank", a breakeven safety limit, the adverse recovery sweep, "probability of getting worse", an n-iteration chain, MAE timing, and the bimodal split; consolidated into the suite on request. Findings: continuation `P(close≥$200|MFE≥x)` rises with the peak (a big close needs a big peak); **giveback is a fixed ~1R toll** (`close≈MFE−R`) — eats $50-100 peaks whole, minor on $300+; **cut-and-bank a winner LOSES** — hold−cut EV positive at every level (heavy right tail pays for the giveback); recovery `P(close>0|down −d)` ~18-28%, erodes gently with depth, mean close negative for every d≥$40; **cutting/bailing a loser LOSES at every drawdown level** — R-trigger recovers ~1R off the low (`drawdown_stop` re-confirmed); **equity loss is a low-MFE phenomenon** — by +$150 MFE `P(close<0)`≈1%, a "$250 safety limit" guards empty space (equity losses = trades that never worked → entry-quality problem, not exit-safety); "drawdown gains momentum" does NOT replicate (P(deepen) rises IS / falls OOS); **trade AGE beats depth** — recoverers run ~2× longer than non-recoverers (matches `leg_age_hazard`); bimodal MAE-timing = winners vs losers, NOT "peak then collapse" (losing-mode "peak" is ~$14 noise). **Through-line: the R-trigger reversal is an adaptive stop — no fixed-dollar cut/lock/bail overlay beats it; the only rewarded lever is the entry filter.** Standalone `trade_outcome_probability_table.py` removed (folded into the suite). Report: `reports/findings/trade_outcome_table/2026-05-22_trade_outcome_full_report.md`. Memory: `MEMORY.md`. Journal: `docs/daily/2026-05-22.md`. |
| 2026-05-21 (live-engine playback prep — engine_v2 --account) | **`engine_v2.py` now takes `--account` and prompts for the NT8 account when omitted — needed because NT8 Market Replay must trade `Playback101` but `engine_v2.main()` silently used the `Sim101` LiveConfig default.** Added `--account` + `_resolve_account()` (explicit flag wins; `--mock` uses the default; `--headless` requires the flag; otherwise prompts, listing Sim101/Sim102/Playback101 and forcing a y/N confirm on an unknown account) + a connect-time `ACCOUNT MISMATCH` warning when the NT8 bridge reports a different account than configured (re-instates a guard the legacy engine had and the v2 rewrite dropped). ~45 LOC, all in/above `main()`, engine internals untouched, `py_compile` clean. Indicator deploy audit: BayesianBridge / ZigZagATR / BayesianHistoryDumper already deployed byte-identical to repo (playback is ready as-is); HLCRegressionMeans / StatRegressionBands differ but are the old stat-bands system (not L5-relevant, left alone); BayesianCompanion (chart overlay of the L5 engine) NOT deployed — inert until the Python side writes `live/state/l5_overlay.txt`, which nothing does yet. Journal: `docs/daily/2026-05-21.md`. |
| 2026-05-21 (conditional probability table — diagnose-not-trade, entries 1-4) | **Built a 4-entry empirical "if-this-then-that" probability table over zigzag-leg events — a DIAGNOSTIC asset, not a trading model (user reframe: "I don't want to catch more — I want to diagnose that").** Entry 1 `conditional_probability_table.py` — chop begets chop: after 3 consecutive low-range legs P(next low)=65% IS vs 33% base, P(breakout)=8%, replicates OOS. Entry 2 `leg_chop_survival.py` — early chop forecasts a longer leg: v1 confounded (chop ratio co-grew with leg length), v2 fixed-early-window → early-choppy wide legs run ~2× longer (P(C≥K+5) clean 27%→choppy 48% IS @K=3, replicates OOS). Entry 3 `trend_continuation.py` — does the trend survive a chop/fakeout: v1 confounded (chop table alternated 65/37/64/36/65 by K-parity — a directional measure on leg-anchored windows is eaten by zigzag alternation), v2 leg-decoupled (regression-slope trend over a 90-min window) + no-K-parity gate PASSED → WEAK ~53-56% vs ~50-52% base (~+4pp). Entry 4 `leg_age_hazard.py` — is the move expended: conditional-survival curve, **HUMP-SHAPED hazard** — leg-death rate climbs 1%→9%/min, peaks at the ~5-min danger window, then DECLINES (a leg that clears ~5 min becomes MORE persistent — NOT monotone exhaustion; all four shape arms sig IS+OOS; min_bars=36 3-min-floor caveat on the early arm) → a pure "old leg = cut it" time-stop is unsupported, only a danger-window-aware check is backed. Plus `worst_trades_gallery.py` (worst trades = fast sharp adverse "entry disasters"; short-lean regresses worst-10 8/10 → worst-100 57/43), `atr_consensus_measure.py` (offline cross-ATR consensus +$57/leg INVERTS causally, corr +0.34→−0.13 = a weak leg-maturity signal), interactive `zigzag_atr_inspector.py` + `chop_inspection_chart.py`. **5 confounds caught this arc**; method lesson — never measure direction on leg-anchored windows (alternation parity), measure chop on a fixed window, read the full curve shape not endpoints, OOS-confirm. Pushed back on "B1-7 fixes direction": the directional edge (~+4pp) is too weak, B1-B6 augmentation already tested at −$76/day. Memory: `project_conditional_probability_table_2026_05_21.md`, `feedback_zigzag_conditional_table_confounds.md`. Journal: `docs/daily/2026-05-21.md`. |
| 2026-05-21 (late — LLM news-intensity scaffold for DRS) | **Built isolated dev scaffold for LLM-scored news-intensity feature; NOTHING RUN YET.** User asked about a local offline LLM to babysit live; pushed back on "babysit" (latency/non-determinism/CI-discipline break, and the live engine is already heavily babysat). Narrowed via AskUserQuestion to the actual high-value use: score US FOMC/CPI/NFP press-release text for short-term market intensity (continuous 0-10), drop the dead binary event flags (`is_fomc/cpi/nfp` ΔMAE = $0 each per 2026-05-18 DRS verdict), refit DRS, **gate strictly on OOS sealed Pearson lower CI > 0** (was -0.047, must flip positive). The LLM **never** touches the live decision path — pure feature engineering. **Isolation per user requirement**: self-contained module `tools/sourcing/llm_news/{fetch,score,join,cli}.py` + dev-only `_v2.py` augmenter + dev DRS trainer; outputs to `DATA/CROSS_DAY/dev/`; canonical paths byte-identical until promotion; backout = `rm -rf` the new dirs, zero production regression. Augmenter reads canonical `cross_day_features_with_target.parquet` (not a 297-line copy of the builder) — keeps promotion as a small surgical insert into Path A step 4. Llama-3.1-8B-Instruct Q4_K_M via llama-cpp-python with CUDA offload (live engine uses 0MB GPU per audit; ~4.7GB fits trivially); deterministic gen (temp=0, top_k=1, seed=42); anti-cheating synthetic test (hawkish vs dovish must order correctly or scoring aborts). DMAIC: `research/llm_news_intensity/project.md`; PDCA cycle_01 (Phase A): `cycle_01.md`. Plan: `~/.claude/plans/i-would-like-to-jaunty-spindle.md`. Status: nothing executed — user runs `fetch → test-synthetic → score → build_cross_day_features_v2.py → drs_canonical_gbm_v2.py` after downloading GGUF. Memory: `project_llm_news_intensity_2026_05_21.md`. Journal: `docs/daily/2026-05-21.md`. |
| 2026-05-21 (OOS bad-days ANALYZE — converged: no day-level fix) | **DMAIC ANALYZE/synthesis: the OOS bad days are not preventable by any day/session-level lever — the deployed B-stack is the validated mitigation.** Intraday cumulative-P&L session-stop (`tools/oos_intraday_stop_analysis.py`): FAILS like the per-trade drawdown stop — 39% IS / 81% OOS recovery; on OOS made negative days 14→20, worst day −$571→−$844, −$79/day CI [−154,−22] SIGNIFICANT loss. Bad-day clustering (`tools/oos_bad_day_autocorr.py`): daily P&L lag-1 autocorr IS +0.06 (p=0.24) / OOS −0.01 (p=0.96) — iid, bad days do NOT cluster, prior-day P&L is not a signal. (One real regularity: chop regime / leg amplitude IS autocorrelated day-to-day, IS +0.275 / OOS +0.485 — but doesn't propagate to bad-day clustering; B10 already exploits the vol regime.) RTH-only: cuts daily std ~15% + ~3 OOS bad days but $/day-neutral-ish (not significant) — a defensible pure risk control, not a fix. **Converged: 4 day/session-level levers ruled out (this run + DRS + per-trade stop); the B-stack already shaves bad days +$175/day significant; residual bad days = irreducible chop cost.** Next (user decision): causal ATR study OR strengthen B9. DMAIC: `research/oos_bad_days/project.md`. Memory: `project_oos_bad_days_2026_05_21.md`. |
| 2026-05-21 (OOS bad-days MEASURE: day-level + hourly) | **DMAIC MEASURE for the "lift OOS bad days" goal.** Day-level (`tools/oos_bad_day_characterize.py`): 14/51 OOS days negative (−$3,570; 8 worst = 85% of bad-day loss). **The L5 B-stack's entire edge is bad-day loss-shaving** — on the 14 bad days FLAT −$255→stack −$80, delta **+$175/day CI [+$98,+$269] SIGNIFICANT**; good-day delta −$9 (not sig); the non-sig headline +$42/day hid this asymmetric protection. Day-level ex-ante signals weak (n=14); macro/event features = noise. Hourly (`tools/oos_hourly_characterize.py`, user: "granularity hour by hour, like a manufacturing site"): decomposed FLAT $/day into ET hour-of-day shifts (sums exact to $690 IS / $454 OOS). **The strategy is a cash-session operation — ET hours 09–15 produce ≈100% of $/day (IS 09 +$196/day [CI +170,+225]); the overnight 16:00–08:00 block nets ≈$0 churn.** But hour-of-day is a weak LEVER: skipping negative hours does not lift $/day (IS-select/OOS-confirm not significant — negative hours are near-zero churn, and which are negative is not IS→OOS stable, e.g. hour 20 IS −$2 / OOS +$36). Bad days are RTH-chop failures (damage spread, hits the normally-good RTH hours), NOT an overnight problem; no shared clock hour. ANALYZE next: during-day "down by ET 11 → stays down?" test. Reports: `reports/findings/oos_bad_days/`. Journal: `docs/daily/2026-05-21.md`. |
| 2026-05-21 (ATR sweep = oracle artifact; re-aim to OOS bad days) | **The FLAT ATR-multiplier sweep is oracle-contaminated — it cannot select the multiplier.** Built `tools/atr_multiplier_sweep.py` (FLAT 1-contract zigzag forward pass, ATR 1–10, IS+OOS, reuses production Stage A+B; X=4 self-test reproduces $690.10 IS / $454.02 OOS exactly). Result: OOS $/day rises MONOTONICALLY as ATR shrinks, no interior peak over 19 points — ATR×1 = $3,480/day OOS, 100% winning days (51/51), PF 5.25. Not a causal edge: the FLAT pass trades the OFFLINE zigzag, a hindsight-clean SEQUENTIAL PARTITION (every leg a genuine pivot-to-pivot swing, zero whipsaw by construction); as r_price shrinks the offline pass finds ever more clean swings while a causal engine whipsaws — the metric scales with zigzag SUBDIVISION, not tradeability. X=4 trustworthy only because coarse-threshold offline≈streaming (~98% match). Valid only at fixed X (baseline + fixed-X B-stack deltas); cross-X needs a CAUSAL streaming pass. Stage-2 B9-retrain at the sweep's picks CANCELLED (would retrain at X=1). **Re-aim**: user granted full NN-training permission + unlimited research and set the real goal — *lift the OOS bad days*. Pivoted to a DMAIC run (`research/oos_bad_days/`): MEASURE bad days in the validated L5 stack → ANALYZE → one validated experiment. Memory: `feedback_flat_pipeline_cross_param.md`. Journal: `docs/daily/2026-05-21.md`. |
| 2026-05-21 (zigzag cold-start divergence) | **Mid-day cold-start zigzag vs beginning-of-time forward pass = BOUNDED TRANSIENT, not significant.** User reframed the per-day zigzag as a "beginning-of-time-dependent feature, the worst kind" and asked whether a mid-session cold-start diverges from a day-first-bar forward pass enough to matter. Built `tools/zigzag_coldstart_divergence.py`: per day, `detect_swings(closes[0:])` (beginning-of-time) vs `detect_swings(closes[K:])` for K ∈ {10,25,50,75}% into the day; measures resync lag (bars until the two pivot sequences become an identical tail — `detect_swings` is deterministic, so once they share a pivot + matching suffix they stay identical forever), transient mis-tiled legs, and leg-amplitude $ divergence. **54 days / 216 samples**: **0/216 never-resync** — every cold-start re-converges; resync lag median 27 min, mean 44 CI [38,50], p90 ~70–129 min, max 269 min; mis-tiled legs **median 0** (mean 0.7, max 4); mis-tiled leg-amplitude proxy mean $103 / median $80 CI [$91,$114], max $514 (a $ measure of divergence, NOT a realized loss). **Verdict**: the zigzag is NOT a beginning-of-time-dependent feature — it is a bounded-lookback feature with ~27 min median memory; the running extreme is memoryless beyond the last pivot, so two runs collapse to identical the instant they catch a common pivot; median 0 divergent legs means the transient is mostly warmup dead-air, not active disagreement. **Phase 2 reframed**: prior-day zigzag priming REJECTED (the forward pass runs `detect_swings` PER-DAY, so prior-day state would diverge from it); correct fix = same-day catch-up (at session start replay today's elapsed bars through the zigzag → byte-identical from bar 1), priority LOW (cheap, makes parity exact, does not gate SIM deploy). Docs corrected: `JULES_ZIGZAG_ONLY_REFACTOR.md` Phase 2, MEMORY.md, `project_parity_b9_horizon_2026_05_20.md`, `project_midleg_entry_2026_05_20.md`. Report: `reports/findings/regret_oracle/2026-05-21_zigzag_coldstart_divergence.txt`. Journal: `docs/daily/2026-05-21.md`. |
| 2026-05-20 (first SIM run + parity check + B9-horizon bug + zigzag-only refactor) | **First L5 SIM run (−$379) was an ENGINE BUG, not a strategy failure — the parity check caught it.** User ended the first live SIM run at ≈−$200 (full session −$379 / ~29 trades) and demanded a parity check ("same period in live sim and forward pass; no parity = drawing board"). **Drawdown-stop research** (`tools/drawdown_stop_analysis.py`, user idea "cut at −$100 unless evidence of recovery"): 76% of −$100-drawdown legs recover; B9 AUC at the −$100 checkpoint 0.465 (no signal); dumb −$100 stop ≈−$31/day [CI −55,−11] — rejected. **Price parity** (`tools/parity_live_vs_forward.py`): live −$379 vs zero-slip forward-pass −$476, net slippage +$96 favourable. **Decision parity**: ran the mock forward pass → found (a) the mock tool broken (B9 disabled by a wall-clock entry_ts bug), then (b) **the B9-HORIZON BUG** — `l5_decider` fired B9 at `pos.bars_held==5`, but `bars_held` (`core/ledger` `//60`) is MINUTES, so B9 fired 5 MIN after entry while the `b9_K5` model is trained for 25 SEC (K in 5s-bar units). 12× too late, every trade. **Both bugs fixed + verified**: l5_decider fires B9 off a 5s-bar counter; mock_bridge stamps fills with bar-time. **Parity verdict**: validated strategy on 2026-05-20 ≈ −$59 (flat day) — the −$379 was the bug. Residual divergence (24 vs 28 trades) = the live engine cold-starts with no zigzag state vs the warm forward pass → fix = zigzag-state priming. **Zigzag-only refactor** (spec `docs/JULES_ZIGZAG_ONLY_REFACTOR.md`): Phase 1 (blended-engine removal from `engine_v2.py`) DONE + verified byte-identical (mock SHA256 unchanged — pure dead-code deletion); Phase 2 (zigzag-state priming) pending. Lesson: always parity-check a live run vs the forward pass before trusting its P&L. Memory: `project_parity_b9_horizon_2026_05_20.md`. |
| 2026-05-20 (mid-leg entry research, autonomous) | **MID-LEG ENTRY / MISSED-SIGNAL LATE-JOIN — REJECTED, do not build.** User asked whether the engine can late-join an R-trigger it missed; specified Fork 1 (B9-gated) + Fork 2 (B1-B6 augmentation) + "other options"; went to sleep for autonomous run. 5 experiments, 51-day sealed OOS. **E1 Fork 1** (B9-gated, unconstrained, `forward_pass_midleg_entry.py`): +$303/day @ K=5 CI [+160,+457] SIG, +$232 @ K=10 SIG, decays to noise by K=30 — but unconstrained (treats every leg as a candidate); the +$303 is the value of B9-*gating* late entries, not new money (baseline already enters every leg at K=0). **E2 Fork 2** (`midleg_b1to6_augmented.py`): B1-B6 augmentation delta(B−A) = −$76/day @ K=5 CI [−158,−2] SIGNIFICANTLY HURTS, neutral-to-harmful at all K — the B9 GBM already extracts pivot-structure from raw V2 features, stacked B1-B6 predictions add only noise (lead-in-PCA failure pattern). Standalone structure gates lose money; pullback filter non-sig nudge. **E3 position-constrained 1c sim** (`midleg_constrained_sim.py`): the operational truth — baseline greedy catches **2,922 of 2,926 legs** (gross ≈$466 ≈ FLAT-1c $454, calibrated); late-join adds **incremental −$1/day, 4 late-joins in 51 days**. **E4 overlap** (`midleg_overlap_analysis.py`): **99.8% of consecutive-leg gaps = 0**, engine utilisation 99.9%, the 4 missable legs all losers. **Root cause**: hardened zigzag legs are a sequential partition (leg N exits where leg N+1 is born) — there is no "busy, missed a parallel leg" population, and this is structural (won't change with contract scaling). The "lost signals" the user sees = cold-start (~7 first-of-session legs/day lost to the ~20-min warmup) or B7 skips — NOT busy-missed legs. **Recommendation**: mid-leg entry SHELVED; build **zigzag-state priming** (replay detect_swings + pre-warm feature engine at startup) as the real lost-signal fix (awaiting approval); SIM deployment of L5 stack unaffected. Report: `reports/findings/regret_oracle/2026-05-20_midleg_entry_research.md`. Memory: `project_midleg_entry_2026_05_20.md`. |
| 2026-05-19 (late-morning, ENGINE=FORWARD-PASS VALIDATED) | **5-day mock May 11-15 vs OOS Phase-1 forecast: $+3,582 vs $+3,716 (gap $-27/day, 3.6%). Engine is decision-identical to forward_pass_full_stack on per-trade prices (20/20 EXACT match spot-check).** User pushed back on time estimates ("you always end up with extra time, improve assessments") -- saved as [feedback_time_assessment_calibration.md]. Then directed full rebuild. Fixed 7 issues in sequence: (1) pivot detection mismatch -- ported full `detect_swings` with min_bars=36, ATR-median-of-42-TRs to causal form (capture 28%->98% week-aggregate); (2) ATR pre-warming from 5-day prior 1m history; (3) PositionsView.is_flat attribute bug (was using non-existent `has_any`); (4) same-bar flip mechanics (exit+entry in single batch, engine_v2 bypasses `can_enter` for flip via new `allow_flip=True` kwarg); (5) `--pivot-source=replay` mode -- L5Context loads oos_hardened_legs_full.csv, fires R-triggers at entry_ts (NOT pivot_ts which is future knowledge); (6) zero-slip mock fills via engine-stamped `requested_price` (eliminated 1-bar-late phantom slippage from replay-loop race); (7) synced L5Decider thresholds to IS-calibrated (B7 1.90/2.10, B9 +5/+15 from `forward_pass_1contract.py`). **Validation result**: 20/20 EXACT entry+exit price match on May 11 spot check; 5-day aggregate within 3.6% of OOS Phase-1 forecast. Remaining gap from engine warmup (240 bars) missing first ~7 R-triggers each day, more pronounced on small days (Thu only 24 OOS legs). **Time calibration**: estimated 2:00, actual 3:04 (50% over but within autonomous tolerance; per-task variance <30% from estimate after first hour, suggesting calibration is improving). **Tomorrow's deploy options**: production-like `--engine-mode l5` (streaming pivots, real-time NT8 SIM), OR validation mode `--pivot-source replay` for regression-testing engine changes against known-good OOS. Files: live/l5_decider.py, live/engine_v2.py, live/mock_bridge.py, live/order_manager.py, live/config.py, tools/_diag_l5_zigzag.py. |
| 2026-05-19 (autonomous sleep-run, L5 SIM-deploy build) | **FULL L5 STACK BUILT + WIRED INTO engine_v2 BEHIND --engine-mode l5 FLAG. Phase-1 OOS GATING TEST: NOT SIGNIFICANT.** User went to sleep at midnight 2026-05-18 directing full autonomous build for SIM deploy. Constraint locked in: 1 contract/position max. Built (1) `training/live_feature_engine_v2.py` (subclass of LiveFeatureEngine, on-demand 185D V2 vector via `get_v2_vector(ts)`, includes 4h TF + bin_END normalization + stable-sort + dedup-before-sort matching V2 batch builder); (2) `tools/test_live_v2_parity.py` (replays day through streaming, compares column-by-column to batch parquets; **300/300 anchors PASS at 1e-6 tolerance on 2026_05_06**); (3) `live/l5_decider.py` (same `evaluate(state) -> DecisionBatch` interface as BlendedEngine; ATR(14) on 5s->1m aggregates -> r_price=ATR*4; running-extreme zigzag state machine -> R-trigger fires; per-position MAE/MFE tracking; B7 query at R-trigger fire as binary skip filter; B9 query at bars_held==5 as binary CUT; B10 day-mode set at session start from cross_day_features.parquet; cautious mode tightens B7+B9 thresholds on B10-cap days, NOT skip-day); (4) `tools/forward_pass_1contract.py` (IS-calibrated Phase-1 forward pass: sweeps thresholds on 12,120 IS normal-mode legs, locks, evaluates on OOS sealed); (5) `live/engine_v2.py` PATCHED (~30 LOC delta) with engine_mode='l5' branch swapping V1 LFE -> V2 LFE and BlendedEngine -> L5Decider, default remains blended; (6) `tools/preflight_check.py` (7 SIM-deploy preconditions); (7) `docs/Active/LIVE_L5_ARCHITECTURE.md` (thin-wrapper doc). **GATING RESULT** (OOS sealed 51d): FLAT 1c $+454/day CI [+$261, +$664]; Phase-1 stack $+496/day CI [+$311, +$693]; **delta $+42/day CI [-$39, +$118] NOT SIGNIFICANT**. IS-locked thresholds: B7 skip >= 1.90 (IS lift +$129/day), B9 cut < +5 (IS lift +$198/day), cautious adds +0.2/+10. IS-OOS generalization gap shrinks IS lift ~70%. **TWO PATHS for tomorrow**: (A) deploy L5 to SIM = zero financial risk, gives real-time data to refine; (B) deploy FLAT zigzag at 1c = same expected $/day. RECOMMEND A for SIM testing. Launch: `python -m live.engine_v2 --engine-mode l5 --mock` first then real NT8. **MUST run `tools/sourcing/build_cross_day_features.py` first** -- preflight FAILS on missing today's row. **Limitations to verify in SIM**: zigzag pivot timing (simpler running-extreme vs build_zigzag_pivot_dataset's min_bars=36 logic), B9 trajectory features from state['high']/low must match training. Files: `docs/Active/LIVE_L5_ARCHITECTURE.md`, `docs/daily/2026-05-19.md`, `reports/findings/regret_oracle/forward_pass_1contract_OOS.txt`, charts at `reports/findings/regret_oracle/charts/2026-05-19_*.png`. |
| 2026-05-18 (late-evening) | **OOS GRAPHICAL REPORT + LIVE INTEGRATION CORRECTION.** Built `tools/_viz/chart_forward_pass_stack.py` — 3 chart figures + per-day CSV under `reports/findings/regret_oracle/charts/` (OOS_summary 6-panel, OOS_equity zoomed, IS_vs_OOS comparison). Headline reconfirmed: OOS full stack +$1,126/day vs FLAT +$454/day = delta +$672/day CI [+$426, +$939] SIG. Layers additive with +$125/day interaction bonus; B9 is 96% no-op composing cleanly with B7/B10; B10 boost fires 28% of OOS days. **Live integration correction**: reviewed `live/engine_v2.py` (1,728 LOC) + `live/protocol.py` + OrderManager + Ledger + LiveFeatureEngine — found `live/L5_sidecar.py` (414 LOC) and `docs/nt8/ZigzagRunnerHybrid_v1.0.0-RC.cs` (497 LOC) DUPLICATE 80% of engine_v2's infrastructure (pending-order tracking, fill reconciliation, NT8 transport, position state, mock-bridge). Both marked DEPRECATED with header notes. **Correct path** (3 patches): (1) `training/live_feature_engine_v2.py` NEW subclass of LiveFeatureEngine adding `get_v2_vector(ts)` for ON-DEMAND V2 features (B10 session-start, B7 R-trigger, B9 K=5 — no streaming V2 engine needed); (2) `live/l5_decider.py` NEW with same `evaluate(state) -> Batch` interface as BlendedEngine; (3) engine_v2 surgical changes ~30 LOC: engine_mode flag, V2 engine swap, route B9 SIZE_ADJUST through new `OrderManager.build_scale_to_size_order(target_qty)`. **B9 timer clarification**: no threading needed — engine_v2's bar-driven main loop already wakes per-bar, per-bar check `if pos.bars_held == 5 and not pos.b9_fired` is sufficient. **Blocked on user approval** for the 3-patch plan; not building until parity tests + scale-to-size feasibility are confirmed. |
| 2026-05-18 (evening) | **DRS VERDICT UPGRADED: signal SIGNIFICANT on extended OOS, deploy-with-monitoring candidate.** User flagged "we have data up to May 15" — found 13 NT8 CSV exports in `DATA/ATLAS_NT8/{1m,1s}/MNQ_06-26/`. Built `tools/sourcing/convert_nt8_csv_to_parquet.py` with rebin (1s→5s, NT8 alignment `ts%5==4`), VERIFIED byte-identical to existing parquet on overlap day (max diff 0.0000 OHLCV). 17 new days converted (Apr 28-30 + May 1, 3-8, 10-15). Rebuilt cross_day_features → 43 NT8 days (up from 23). Ran inline pivot detection + hardened forward pass on new days (no V2/B7 dependency). Refit DRS on FLAT target across IS+OOS (apples-to-apples). **Extended OOS Pearson +0.259 CI [+0.054, +0.472] SIGNIFICANT** vs prior 23-day Pearson +0.139 CI [-0.047, +0.451] not sig. **Anti-predictive bias FIXED**: pred mean $+495 on 10 negative days vs $+643 on positive days (was flipped before). Rank sizing rules all point-POSITIVE: rank_0.7_1.3 +$29/day CI [-$4, +$66] (asymmetric risk: -$4 worst vs +$66 upper), rank_0.5_1.5 +$49 CI [-$7, +$110], rank_0.3_1.7 +$68 CI [-$10, +$154]. CIs technically cross zero on lower bound but only barely. Path forward: shadow-deploy 30 days → conservative-live 30 days. Lessons: (1) the 23-day verdict was sample-size-premature, (2) flat target more reliable than gbm_ev (gbm_ev sizing adds variance), (3) user's "fix the format" intuition correct — NT8 CSVs just needed parquet conversion. Verdict: `reports/findings/drs/2026-05-18_drs_extended_verdict.md`. |
| 2026-05-18 (morning, SUPERSEDED) | **DRS CANONICAL (Path A) — DO NOT DEPLOY. IS signal exists, OOS doesn't survive.** Morning: B/C rename complete (B10→B9 promotion, failed bad-trade→C11). Mid-day: C12 imminent-exit FAILED walk-forward (all 15 ops strictly negative); C13 leg-phase SIGNAL LEARNABLE (Pearson 0.32-0.48) but phase-gated pyramid action ZERO incremental over B9 — confirms 5-failure L5 paradigm boundary (R-trigger structurally optimal). Afternoon: completed Path A canonical pipeline (B-model inference + pivot cloud + hardened forward pass + per-day aggregation, ~15-30 min, 217 IS + 23 OOS days with honest gbm_ev hardened target). DRS GBM refit: **IS WF Pearson +0.191 CI [+0.098, +0.405]** strictly positive but lower bound below "strong" threshold; **OOS sealed Pearson +0.139 CI [-0.047, +0.451] CROSSES ZERO**. Naive `clamp(pred/IS_mean, 0.5, 1.5)` rule on OOS: **-$333/day CI [-$523, -$163]** strictly NEGATIVE — baseline drift (IS $1804 vs OOS $1086) kills it. Rank-based rules robust to drift: ALL 4 IS-WF significant (+$249 to +$831/day) but ALL 4 OOS non-sig (-$20 to -$67/day, CIs cross zero). Anti-predictive on 2 OOS negative days (DRS pred mean $+1466 for losers vs $+1069 for winners — flipped). Useful features: overnight_gap_pct, prior_day_range_pct, days_since_fomc; useless: dxy_*, vix_chg, dow, sparse binary event flags. Next: Phase 2 LLM news headline intensity, collect 60-90 more OOS days. Model `DATA/CROSS_DAY/drs_canonical_gbm.pkl` kept as research-only. Verdict: `reports/findings/drs/2026-05-18_drs_canonical_verdict.md`. |
| 2026-05-17 (sleep-run L5) | **AUTONOMOUS SLEEP-RUN: L5 during-trade paradigm validated via B9 (= former B10 remaining-amplitude), C11 (= former B9 bad-trade) KILLED.** User went to sleep at architectural-discussion phase, directed autonomous mode "do 9 and 10, bolt on everything for live". Built full pipeline: `build_is_hardened_legs.py` (17,767 IS legs / 275 days / $690/day flat, ZERO trade management → honest labels), `build_trade_trajectory_dataset.py` (74,976 rows × 5 K-horizons {5,10,30,60,120} in 5s units), per-K HGB trainers, sealed OOS test. **C11 binary cut (target exit_pnl<-$50): FAILED walk-forward** — 0/15 ops significant despite AUC 0.86-0.93 because Type 1 cost > Type 2 savings (mid-MAE trades recover). **C11 v2 cut-saves target: also FAILED** 0/30 ops significant. Confirms morning's trail-tightening structural failure with a different model: R-trigger is structurally optimal under binary cut/hold. **B9 (= former B10) continuous remaining-amplitude regressor PASSED** — IS walk-forward K=5 3/4 folds sig +$48/day CI [+$24, +$74]; **OOS sealed single-shot K=5: +$67/day CI [+$32, +$106], 14.1% lift over $475 baseline**. Anti-doom slippage stress: still +$38/day at $10/action friction. Why B9 works: continuous action (cut/half/full/pyramid) only acts on high-confidence predictions, marginal cases get size=1.0 (no Type 1 cost), pyramids capture upside. C9/C10 rename complete (failed LSTM candidates archived, B9 slot re-used). Production models saved: `b9_remaining_amplitude_K{5,10,30,60,120}.pkl`. Hybrid pipeline spec written: NT8 T+25s timer + SIZE_QUERY IPC + Python sidecar applying B9 K=5. Finding: `reports/findings/2026-05-17_b9_b10_l5_paradigm.md`. Spec: `docs/L5_HYBRID_PIPELINE_SPEC.md`. NOT done (pending wake): NT8 strategy update, sidecar code, sim parity check, deployment gates. |
| 2026-05-17 (drs-features) | **PHASE 1A: Day-Regime Sizer cross-day feature dataset built (293 days × 21 cols).** User picked "size down on bad days" as the lever to manage the composite zigzag pipeline's 5/31 negative OOS days (V2 features can't separate at session start, per HONEST_CAVEATS). User pushed back on my "$200-500/day not transformational" framing: against honest $600-700 floor that's +30-70% revenue, ~$50-125K/year, plus tail-risk reduction matters more than mean lift — recalibrated, saved as `feedback_dollar_lift_framing.md`. Built `tools/sourcing/{calendar_dates,fetch_vix_dxy,build_cross_day_features}.py`. Stooq now requires captcha API key (rejected); FRED endpoint timed out; installed yfinance 1.3.0 → clean fetch of VIX (^VIX) and DXY (DX-Y.NYB) daily for 2024-12 to 2026-05. Hand-curated 16 FOMC + 24 CPI + 24 NFP dates (flagged for user verification). **Features**: overnight_gap_pct, overnight_range_pct, prior_day_range_pct, prior_day_c2c_pct (computed from existing ATLAS 1m), VIX/DXY close+chg (yfinance), is_fomc/cpi/nfp/opex + days_since/to_next_fomc. **Bugs caught**: (1) Sunday futures-session files polluted day list — Monday's "yesterday" resolved to Sunday with 0 RTH bars, Mon systematically skipped (fix: weekday filter); (2) Sun-evening futures bars labelled date_et=Sun were missed by overnight_slice — fix: include intervening-date bars from today_df. Coverage: 293/316 Mon-Fri days included (23 skipped, mostly holidays). Spot-checked 5 days against known events — June FOMC, December FOMC+CPI double-event, etc. all correctly flagged. Event-day rates match expected frequencies (FOMC 3.4%, CPI 5.5%, NFP 4.8%, OpEx 3.8%). **Pending**: `target_day_pnl` column all NaN — need to run hardened forward pass on IS days to populate (Option A canonical, ~30-60min compute) OR use daily MNQ realized range as quick proxy first (Option B). Findings: `reports/findings/drs/2026-05-17_cross_day_features.md`. Output: `DATA/CROSS_DAY/cross_day_features.parquet`. |
| 2026-05-17 | **FULL DAY: composite trade-management validation. Exit modifications all FAIL, sizing WINS at +15.6%, entry suppression is a TRAP.** Morning: built `direction_signal_accuracy.py` + `live_zigzag_baseline.py` showing RAW trend3 +1.80pp vs indicator CI [-1.13,+4.40] NOT significant — V2-features-on-hindsight-direction doesn't beat 30-line indicator. Pivoted to forward-looking targets: B1 pivot-imminent (K=10 thr=0.85 → 78% prec / 4% cov / AUC 0.716), B2 fakeout (K=10 thr=0.70 → 66% prec / 19% cov / AUC 0.695). Evening: built full state-machine composite — B4 pivot-region (W=300 thr=0.85 → 79% prec at 10% cov, 2.5× B1 coverage), B5 leg-phase 3-class (weak standalone 38.6% acc, P(MID)>0.60 → 71% prec at 0.25% cov), B6 directional pivot 3-class (K=10 thr=0.70 → 53-57% prec at 5% cov per direction — first model that predicts WHICH direction). Composited into 8 action zones via `pivot_probability_cloud.py` (AT_PIVOT/NEAR_*/IMMINENT/WIDE_ZONE/WATCH/CLEAR + trajectory RISING/FLAT/DECAYING). **Three operational frames tested**: (A) trail tightening — naive -$44/leg, B6+hysteresis sweep 18 configs best -$0.29/leg, both CIs strictly negative → STRUCTURAL FAIL because R-trigger is optimal under V2 signal precision. (B) Target placement — 12 configs swept, all CIs strictly negative, best -$17.93/leg → fixed targets hit before peak. (C) **Sizing — aggressive scheme WINS: 2.0× on AT_PIVOT or B6≥0.70, 0× on CLEAR+B6<0.50 → $+81/leg vs $+70 flat = +15.6% per-leg = +$626/day on 32 OOS days (CIs overlap, N=32 underpowered)**. Counter-intuitive entry analyzer finding: AT_PIVOT entries produce $+121/leg (2.2× CLEAR) — confident-pivot zones predict STRONGER legs because they confirm genuine reversal. Entry suppression filter sweep: every binary skip filter REDUCES per-day total (skip CLEAR -$908/day, skip CLEAR+WATCH -$1,561/day) even when per-leg rises — kept-leg P&L lift can't compensate for dropping positive-expectation legs. Variable sizing dominates binary skipping. Inspector updated with composite zone color bar (z) + B6 directional triangles (d/D). Process rule logged: don't pivot mid-experiment (`memory/feedback_dont_pivot_on_suggestions.md`). 18 new tool files; 14 finding reports. Operational recommendation: variable sizing per entry composite, R-trigger stays, AT_PIVOT 1.5×/IMMINENT 1.5×/NEAR_* 1.2×/WATCH 1.0×/CLEAR 0.8×, hard skip only CLEAR+B6<0.50. Caveat: oracle-optimal entry timing inflates absolute numbers ~50%, relative improvements hold. Findings: `reports/findings/2026-05-17_composite_trade_management.md`. Journal: `docs/daily/2026-05-17.md`. |
| 2026-05-16 (golden-combined) | **GOLDEN-MOMENT + DIRECTION COMBINED OOS FAILS — direction classifier is anti-predictive at high-timing-confidence bars (41% TP rate vs 50% random).** User pivot: filter oracle to high-quality moves only. First by mfe>$200 (1.16% positive bars, OOS AUC 0.84 LR / 0.86 GBM, precision 14-20% at relevant thresholds). Then user pivot to VELOCITY filter (mfe_velocity > $5/min) — fast trades = real momentum = lower SL% in ceiling calc. Built GoldenCombinedStrategy (golden_combined.py) + pre-cache GBM predictions + KPI grid through tick-exact engine. **Critical bug found + fixed**: V2 layered data has sub-5s irregular timestamps, original dataset builder's `iloc[::12]` didn't align with ticker's `is_1m_close = (ts % 60 == 0)`, so 90% of cache lookups failed and only 0.5-2.5 trades/day actually fired (vs 25-30/day from classifier). Fix: filter `ts % 60 == 0`. After fix: fire rates jumped to 3-16/day matching classifier output. **OOS grid 60 combos with FIXED alignment**: all configs lose money (-$14 to -$71/day NET), all CIs strictly ≤ 0. Best: T_timing=0.90, T_dir=0.75, TP=$20/SL=$10 → -$15/day CI [-$23, -$6]. **Killer insight**: at high P_timing bars, direction TP/(TP+SL) = 41% — BELOW 50% baseline. Direction classifier (81% on average oracle bars) becomes anti-predictive at market extremes/inflections where timing fires high. The two classifiers are anti-correlated where it matters most. **Theoretical ceiling confirmed**: at 20 trades/day with perfect timing + 87% dir, $200-$1200/day depending on TP/SL (signal exists). **Path A is the next experiment**: direction classifier as FILTER on existing tier strategies (FADE_CALM has documented +$13.50/day OOS edge). Tiers fire at price-action moments (NOT extremes), so direction classifier should retain its 81% accuracy there. Findings: `reports/findings/regret_oracle/2026-05-16_golden_combined_failed.md`. Tools: `tools/{build_golden_entry_dataset,train_golden_entry_classifier,precompute_golden_timing,golden_ceiling_calc,golden_combined_kpi}.py`. Strategy: `training_iso_v2/strategies/golden_combined.py`. |
| 2026-05-16 (honest-clean-strategy) | **CLEAN LIVE STRATEGY OOS FAILS — direction classifier alone is not deployable; entry-timing is the real bottleneck.** User pivoted to KPI-driven autonomous mode (target $100/day NET, low MAE, high Day WR). Built proper pipeline through existing `training_iso_v2/` infrastructure: `tools/fit_direction_classifier.py` NEW (pickle scaler+LR on full IS daisy), `training_iso_v2/strategies/direction_classifier.py` NEW (strategy class with configurable threshold + cadence), `training_iso_v2/exits_tick_exact.py` NEW (TickExactTP/TickExactSL closing at exact TP/SL price using 5s OHLC, not bar close — critical fix), `training_iso_v2/engine.py` PATCH (honor `_force_exit_price`), `training_iso_v2/ledger.py` PATCH (track `trough_pnl` MAE), `tools/dir_clf_kpi_search.py` NEW (autonomous grid). **Critical bug discovered + fixed**: existing engine closes at bar-close after threshold cross → intrabar overshoot inflates $/day 20-30×. First grid (bar-close) showed +$787/day at best — pure artifact. **Honest grid with tick-exact exits + $4 cost/trade**: ALL 36 symmetric configs lose money (-$34 to -$234/day NET). Why: direction acc 87% does NOT mean TP-hit rate 87%; mid-move / end-of-move entries have small remaining favorable distance + noise reversals hit SL. **Asymmetric grid** (TP=$20/SL=$5 to TP=$40/SL=$10, T=0.85/0.90/0.95): only ONE config positive — T=0.95, 15m, TP=$20/SL=$5 → +$2.54/day NET, **CI [−$4.22, +$10.34] crosses zero, NOT significant** per CLAUDE.md. DayWR 47%, 2.3 trades/day. **All 3 KPIs fail**. **Architectural conclusion**: direction classifier alone cannot be a live strategy. Classifier was trained on daisy oracle entries (hindsight-selected); applying to every cadence trigger means firing mid-move / end-of-move. Three paths forward: (A) classifier as FILTER on existing tier strategies, (B) build separate entry-timing classifier, (C) price-action trigger + classifier routes direction. **What's validated**: infrastructure works, tick-exact bug fixed, MAE tracked, 56-day OOS bootstrapped. **NEXT (recommended)**: Path A — apply classifier as `cnn_filter` to existing tier strategies, measure $/day delta. Full findings: `reports/findings/regret_oracle/2026-05-16_clean_strategy_oos_failed.md`. |
| 2026-05-16 (forward-pass) | **FIRST FORWARD PASS — LSTM selector @ T=0.85, 16-tick R/R → $105/day OOS CI [$95, $115], 100% Day WR, +6.2 Trade WR PF.** Built `tools/scenario_forward_pass.py` NEW (direction-confidence selector + 2 outcome framings + bootstrap CI) and `tools/scenario_lstm_predict_proba.py` NEW (LSTM softmax extraction). Tested both LR (entry-only, AUC 0.864 IS) and LSTM (60-bar seq, AUC ~0.87 OOS) on 2026 OOS daisy (2085 trades / 56 sessions). **Framing A oracle-exit (upper bound)**: $1,691-3,222/day LR; $819-3,511/day LSTM. **Framing B 8-tick R/R (realistic)**: $28-$92/day LR; $19-$97/day LSTM. **LSTM beats LR by +$5-25/day in mid-range thresholds**; LSTM falls behind at T=0.90 due to coverage shrinkage. **LSTM's true win is calibration at high confidence** — at T=0.85 LSTM 87.7% acc vs LR 83.9% (+3.8pp on selective subset). Trade WR PF rises monotonically with threshold: LR +3.3 at T=0.50 to +4.4 at T=0.90; LSTM +4.2 to +10.1. Day WR ~100% across thresholds — every OOS day positive when stacking ~17 trades at 88% acc. **Honest caveats per anti-doom-cascade**: (1) daisy oracle gives entry timing FREE — real strategy needs an entry-bar detector; (2) 56 OOS days = thin sample; (3) mirror loss approximation may be optimistic OR pessimistic; (4) 8-tick R/R assumes no SL race vs TP; (5) ~17 trades/day unrealistic for single strategy (5-10/day more honest). **Headline number must be discounted by live-sim gap** (prior baselines ~$680/day Python-vs-NT8 gap; this simpler strategy might gap less but uncalibrated). **Conclusion**: selector layer is validated OOS; entry-timing detector is the next bottleneck (L4-L5 problem unsolved). Findings: `reports/findings/regret_oracle/2026-05-16_forward_pass.md`. |
| 2026-05-16 (autonomous-late) | **SCENARIO LSTM MULTI-HEAD with 2026 OOS — LSTM beats LR by ~1.7pp on direction OOS, ties/hurts on duration/speed.** User reframe: predict scenario buckets (direction × speed × duration × trajectory) from lead-in sequence via LSTM. Built full pipeline autonomous: (1) 2026 OOS daisy oracle (2,085 trades, 56 sessions, $289K total MFE) `tools/regret_daisy_chain_oracle.py`; (2) `tools/scenario_bucket_labeler.py` NEW — computes MAE from raw 5s OHLC, labels 4 buckets (dir binary, dur/spd quartiles, traj operational MAE/MFE cuts at 0.25/0.50/0.75); (3) OOS V2 join; (4) `tools/scenario_sequence_dataset.py` NEW — lead-in K=60 bars × top-30 V2 features, z-scored with IS stats applied to OOS; (5) `tools/scenario_lstm_train.py` NEW — LSTM(30, hidden=64, 1 layer, dropout 0.3) + 4 heads (25,486 params), inverse-frequency class weighted CE, Adam lr=1e-3, batch=128, early stop on val loss; (6) `tools/scenario_baseline_lr.py` NEW — LR with same balanced class weights on V2 entry features for apples-to-apples. **OOS comparison** (LR entry / LSTM seq, 2085 trades): dir 81.0% / **82.7%** (+1.7pp); dur 33.0% / 32.7% (tied); spd **44.3%** / 40.0% (LR wins, -4.3pp); traj 30.2% / 45.9% (both below 83.5% always-T0 baseline due to class-weighted loss forcing rare predictions). **Verdict**: LSTM's sequence input adds ~1.7pp on direction OOS, zero or negative elsewhere. **Information ceiling confirmed** — ~83% direction accuracy on V2 entry features is the LR=LSTM ceiling. Speed/duration signal lives at entry bar, not in lead-in trajectory. **Bucket finding**: MAE is mostly $0 for daisy trades (oracle picks BEST extreme → 84% MONOTONIC). 4-class trajectory is too imbalanced; collapse to binary CLEAN/PULLBACK or drop the head. **IS-OOS generalization** is good on dir/dur for both architectures (deltas ≤2pp). **Architectural conclusion**: drop lead-in trajectory as a model input; the ceiling is in V2 feature engineering, not model class. Next levers = richer entry features (new TF combos, intra-bar microstructure, calendar/event) or different target (regret-on-skip vs direction). Findings doc: `reports/findings/regret_oracle/2026-05-16_scenario_lstm.md`; comparison CSV: `reports/findings/regret_oracle/scenario_comparison_table.csv`. |
| 2026-05-16 (late-late) | **BINARY DIRECTION CLASSIFIER — AUC 0.864 on V2 entry features; LEAD-IN PCA EXPANSION REJECTED at all lookback lengths.** User reframe: "if we can deduce direction the rest we can do risk management → can we calculate the confidence of the direction upon entry?". Built `tools/regret_direction_classifier.py` — binary LogisticRegression on V2 entry features predicting LONG=1/SHORT=0, output = calibrated P(LONG); selector dial = \|P − 0.5\|. **Result (test 1585 / train 6340)**: AUC 0.864 / Brier 0.142, no train-test gap, well-calibrated at tails. At threshold 0.85: 40% coverage, 88% accuracy. At threshold 0.50: 100% cov, 83% acc. **Massively better than cluster-routed approaches (R²=−0.05)** — direct binary direction sidesteps cluster-misclass problem entirely. This is the L4 selector signal. **Lookback expansion** ("agree on expanding the lookback to see if it improves") — tested whether lead-in PCA signatures help. Three lookbacks: 60-bar (5min) → AUC 0.850, 240-bar (20min) → 0.842, 720-bar (60min) → 0.849. **ALL HURT** vs baseline 0.864. Train AUC always rises (model latches onto noise), test AUC always falls. Brier deteriorates at every lookback (0.142 → 0.152-0.156). No monotonic trend — failure is the approach, not the window. **Why**: V2 entry features (L1-L3 × 5s-1D) already encode the macro setup; PCA signatures are lossy 2-vector summaries (centroid + direction) of 240×184 matrix; unit-direction vectors into a linear model are geometrically incoherent. **Architectural implication**: drop lead-in PCA for direction. Next AUC lever is non-linear (GBM/CNN), not more features. L3 clusters may still help for magnitude/risk/exit but NOT direction routing. Files: `tools/regret_direction_classifier.py` NEW; `reports/findings/regret_oracle/direction_classifier_v2_lr*.npz`; `leadin_signatures_IS_full_{240,720}bar.npz` NEW. |
| 2026-05-16 (late) | **L3 LIVE-PREDICTABILITY TESTED — captures real structure (cluster R²=0.762) but NOT deployable at current design (live R² ≈ -0.05).** Built `regret_cluster_refit.py` (pool member trajectories, refit cluster PCA line — refined clusters have median 6.4σ perpendicular spread, not tight 1D lines), `regret_bayesian_r_scan.py` (r=1..15 sweep showing r=7 sweet spot: 152+146 archetypes, max_n 226-242, no absorbing pathology), `regret_leadin_trajectories.py` (per-trade PCA signature of past 60 bars BEFORE entry, using same global z-score as forward for parity). **Cluster variance decomposition**: 76.2% of signed_mfe variance explained by cluster membership alone (vs 33.7% for global V2 regression). Per-cluster σ median = 34% of global. **75% of 298 clusters have σ_within < 50% of global — user's "clustering generalizes within homogeneous groups" hypothesis empirically confirmed.** BUT only 47% of trades cluster; the other 53% have nearly 2× the global variance ($230 vs $120 std) — unassigned trades are unique non-archetypal. **Live-predictability tests**: entry features → cluster (LR 40% top-1, 72% dir match, R²=-0.05); lead-in trajectory → cluster (LR 28% top-1, 70% dir match, R²=-0.04); lead-in Ridge → signed_mfe direct (R²=0.146). All UNDER the L2 V2 direct regression ceiling R²=0.337. ORACLE (perfect cluster knowledge) R²=0.745. **Conclusion**: L3 cluster structure is real but emerges from forward-trajectory evolution, not from observable signals at entry. Entry/lead-in misclassifications dominate residual (wrong-cluster predictions have wrong-sign mean_signed). **Direction-only IS meaningful at 70-72%** — clusters capture direction reliably, just not magnitude. **Untested rescue paths**: longer lookback (240-bar / 720-bar), coarser clustering (20-50 k-means), direction-only hybrid (clusters for dir + L2 for magnitude), 2026 OOS validation (mandatory before more IS iteration). Findings: `reports/findings/regret_oracle/2026-05-16_l2_v2_redo_l3_smokes.md` (updated). **Earlier (night)**: L2 V2 k=2 R²=0.307 / k=3 R²=0.337 (top single feature `L2_15s_price_velocity_12` R²=0.248); L3 Bayesian table built (298 r=7 clusters covering 47%, top cluster mean_signed +$220 LONG / −$329 SHORT after refit). |
| 2026-05-16 (night) | **L2 redo on full V2 features + L3 Bayesian archetype build (autonomous family-stuff run).** L2 V2 results: k=2 R²=0.307 (vs prior 0.262); k=3 R²=0.337 (vs prior 0.307). Top single feature `L2_15s_price_velocity_12` lin_r²=0.248 (Spearman ρ=−0.62) — stronger than anything in prior ~19-feature set. **New direction pattern: multi-TF velocity divergence** — fast TF Q1 × slow TF Q5 → 100% LONG; mirror → 100% SHORT, n=34-74 per cell, mean_signed ±$306-$325. Built `regret_join_v2_features.py`, `regret_feature_prune.py` (184→132 reps at |corr|≥0.85). Extended `regret_kway.py` for parquet+features-file. **L3 BUILT END-TO-END** with 3 new CUDA-targeted tools: `regret_join_v2_trajectories.py` (12s for 7,925 trades), `regret_trade_signatures.py` (per-trade PCA via scipy gesvd after CUDA SVD failed on ill-conditioned matrices — 19.4ms/trade), `regret_bayesian_table.py` (hierarchical peel). **Full-IS Bayesian table**: 123 LONG seeds + 133 SHORT seeds, 7,673 of 7,925 trades assigned, 80 cluster nodes. **Top LONG cluster: n=6, mean_signed +$870** (rubber-band rally); **top SHORT cluster: n=8, mean_signed −$672** (crash archetype). L3 surfaces archetypes L2 cell-gating misses (L2 caps at ±$340; L3 reaches ±$500-$870 in small-n clusters). **Bug found + fixed**: 17.5% of daisy-chain trades have `exit_ts−oracle_ts` >> `time_to_mfe_min×60` due to <30min data gaps the session-detector missed; trajectory tool now uses `time_to_mfe_min` as algorithmic truth. **Open concerns**: only 3/80 nodes at L1 fine level (r-ladder didn't subdivide); small-n L3 clusters have multi-comparison overfit risk — 2026 OOS validation mandatory before any claims. Findings: `reports/findings/regret_oracle/2026-05-16_l2_v2_redo_l3_smokes.md`. **Next**: OOS validation + stratified-1D + r-ladder calibration + L3 trajectory-mode matching + L4 selector design. **Earlier**:|
| 2026-05-16 (eve) | **Bayesian Trade Archetypes via N-D Trajectory Clustering — protocol locked, build pending.** Long topic-by-topic discussion building on the morning's k-way findings. **Locked**: (1) peel-extremes-first clustering with `mfe_velocity` as seed; (2) full ~190 V2-feature pool joined per-bar over trade duration (user flagged my original ~19-feature listing as too narrow); (3) trade signature = **PCA line in 190-D space** (centroid + unit direction via SVD of trajectory matrix) — rejects per-feature 5%-of-seed matching because of zero-crossing degeneracy on signed features; (4) similarity = perpendicular distance from candidate trajectory points to seed's PCA line in z-scored units; (5) hierarchical r-ladder (coarse → fine, e.g., 0.5σ → 0.25σ → 0.125σ) — each peel produces a TREE of nested clusters; (6) **trade decay tracking** as free side-effect — `d(t)` time series of distance from trade's trajectory to its cluster's PCA line UNIFIES entry classifier + exit signal + duration prediction + Bayesian online posterior update for direction; (7) CUDA (PyTorch batched SVD). **Open questions deliberately deferred**: decay metric (raw vs normalized) and "decayed" threshold (hard-cross vs sustained-slope) — decide after first IS sanity check shows what decay curves actually look like. **Live applicability flagged**: clustering uses forward trajectories; live needs lead-in trajectory matching OR single-bar match with decay update. Phase 5 (live evaluator) resolves. **Build phases pending**: P1 V2-feature join, P2 per-trade signatures, P3 hierarchical Bayesian table builder, P4 IS sanity check, P5 live evaluator. Spec: `research/bayesian_archetypes/project.md`. **Earlier (morning)**:|
| 2026-05-16 (am) | **DIRECTION-AWARE k-way stratification + regression — R² ceiling 0.35; signed-target pivot was decisive.** Started morning: built per-feature 1D regression (`tools/regret_feature_regression.py`) — `bar_range` R²=0.136, `volume` 0.10, slope features near-zero linearly but U-shape quadratic. Built pair clustering + regression (`tools/regret_pair_clusters.py`, `regret_pair_regression.py`) — top pair `dist_15s_1m × slope_15s_10m` interaction R²=0.187, +$54/σ². Built triplet versions (`regret_triplet_clusters.py`, `regret_triplet_regression.py`) — 82,733 cells across 969 triplets, top cell `dist_15m_to_Ml × slope_15m_15m × bar_range` n=22 mean $396. **User reframe**: "the most important signal we need is direction." Critical methodological pivot — all prior analysis used `mfe_dollars` (magnitude only) which hid direction signal. Patched CSV with `signed_mfe = mfe_dollars × (+1 if LONG else −1)`. Built generalized `tools/regret_kway.py` (signed target, parameterized k & n_bins) and `tools/regret_stratified.py` (subset-first analysis per user's "shaft from seeds" fallback). **Sleep run** (autonomous): k=2 (R² 0.262), k=3 (0.307), k=4 with 3 bins (0.320), k=5 with 2 bins (0.348) — clear saturation around 0.35; 4-way and 5-way interaction terms add ~0. **Stratified by bar_range S3: R² 0.344; stratified by tod_minutes S5: R² 0.342 — stratified k=2 (2 features within a stratum) MATCHES unstratified k=5 (5 features + all interactions).** User's intuition empirically verified — heterogeneous data masks subgroup-specific direction signals. **Direction-callable cells (Wilson CI excludes 30%/70%) widespread**: 43% at k=2, 47% at k=4, 59% at k=5. Top: RTH-AM × `z_1m Q4 × slope_15m_15m Q1` n=43 **98% SHORT mean_signed −$340**; RTH-AM × `z_15s Q2 × bar_range Q5` n=55 **89% LONG mean_signed +$313**; HIGH-vol × `z_1h_high Q1 × slope_15s_3m Q5` n=31 **97% SHORT mean_signed −$338**. **The recipe**: high `bar_range` (Q4-Q5) + RTH `tod_minutes` (S4-S5) + `slope_15s_3m` sign as direction trigger + `z_1m` Q2/Q4 confirmation. `slope_15s_3m` is the single dominant direction predictor — invisible on `mfe_dollars` (R²=0.002) but R²~0.20 on `signed_mfe`. **Caveats**: IS-only (needs 2026 OOS per hard rule); multi-comparisons ~400k+ cells tested across all analyses (some top cells will be spurious); selector evaluator (Day WR + mode $/day per protocol) still pending. Findings doc: `reports/findings/regret_oracle/2026-05-16_direction_signal_kway.md`. **Next**: selector evaluator + 2026 OOS + causal logistic-regression direction predictor. |
| 2026-05-14 | **DAISY-CHAIN ORACLE + ENTRY/EXIT CLUSTERING — full IS 2025 sleep run.** New oracle variant matching the 1-hour-lapse premise: at each bar, look forward 60min, pick best favorable extreme, that's the exit + next entry; chain reset at session halts. Tool `tools/regret_daisy_chain_oracle.py`. **Full IS run: 7,925 sequential trades (LONG 4,005 / SHORT 3,920), MFE mode $59 / median $96 / mean $132 / total $1,046,546.** Sequential ceiling = $4,492/day if every link tradeable. **Earlier in day: Phase 2a EDA lookahead correction** — first attempt used `duration_bucket` + `regime_2d` (both lookahead); user caught it; re-ran with only entry-time-clean axes (tod_bucket, direction, liq_bucket, d_* state-vector categoricals). 45 of 209 joint cells tradeable (22%). Dominant pattern: **tod × d_rail_position** — RTH phases × rail-extended positions (`below_Ml`, `near_Ml`, `near_Mh`). Top cell `phase_5_rth_pm × below_Ml` n=740 mode $155 mean $222. Empirically validates the 3-body framework: rail position IS the discriminator. **Oscillation explorer** (`tools/regret_oscillation_explorer.py`): 256 chop-cluster candidates flagged (mfe<$50, spread≤20pts, n≥3, mixed-direction); 80% concentrate in overnight/pre-NY tod (matches the 2025-07-08 chart concern); 22% are one-sided (fusion would recover an underlying trade), 78% are true chop. **Clustering** (K=8, `tools/regret_cluster.py`): silhouette 0.161 entry / 0.178 exit. **Entry c7** (n=215, **mean $306**, 82% LONG) = rubber-band-up from dump (downward 15s slopes, ext_dn z_15m, below_Ml, extreme fan). **Exit c4** (n=418, **mean $323**, 96% SHORT) = the "dump exit"; **exit c1** (n=1,089, $155, 94% LONG) = the "rally exit." **Joint entry×exit** surfaces rubber-band archetypes: `c0→c4` n=133 mean **$361** 100% SHORT (entered mid → crashed); `c2→c4` n=36 mean **$423** 100% SHORT (counter-trend reversal); `c6→c4` n=21 mean $451 (extension reversal); `c3→c1` n=13 mean $327 LONG mirror. **50% of joint cells have 100% direction-skew — direction is structurally implied by (entry-cluster, exit-cluster) pair.** Caveats: K-means silhouette modest (false-merge risk per MEMORY); IS-only (needs 2026 OOS); noise-floor gate too strict for daisy-chain (entries at high-vol extreme bars → noise floor $289 vs $87 for local-extrema). Findings doc: `reports/findings/regret_oracle/2026-05-14_daisy_chain_clustering.md`. **Earlier (afternoon)**: Answered "mode at seed level" — oracle MFE mode is **$11** (median $38, mean $55, 5× skew); built `tools/regret_mode_analysis.py`. **Duration categorization** (user: "categorize trades on duration"): FLASH 0-3m (5%, $11 mode, $6.1/min) / FAST 3-10m (14%, $11, $3.7/min) / MEDIUM 10-25m (23%, $27, $2.0/min) / SLOW 25-60m (**57%**, $31, $1.1/min) — biggest-$ trades are slowest + lowest-velocity; the $-mode and risk ranking are opposite. **User corrections**: (1) oracle taking ALL opportunities is correct — it's the upper-bound label universe; the open problem is the *selector*, not the oracle. (2) selector must respect the **1-hour-lapse premise** → argmax-per-window, not threshold-crossing (v9c fires ~47/day; argmax-per-hour → ~6-7/day, and selects the most *distinctive* bar per hour vs the most *repetitive* — directly attacks calibration inversion). (3) regret analysis should run **continuously from IS day-1**, sessions = Globex maintenance halt-to-halt. **Empirical halt detection**: 61-min gap, DST-shifting (22:00-23:00 UTC winter / 21:00-22:00 UTC summer — confirmed Feb vs Jul) → never hardcode, detect from gaps. **IS/OOS locked**: IS = full 2025, OOS = 2026 forward (clean calendar cut). **`cusp_marker.py`**: `load_trades()` now parses oracle CSV schema (oracle_ts/oracle_utc, exit_ts derived from time_to_mfe_min, numeric-string epochs); added `P` key → screenshot to `examples/`. **Phase 1 done**: reworked `tools/regret_1m_oracle.py` — `build_session_info()` gap-based session segmentation; extrema detection + forward MFE are session-aware (no window crosses a halt); end-of-session extrema kept+tagged (`full_window`, `available_fwd_min`), not dropped; 8 new columns (session_id, session_date, tod_minutes, full_window, available_fwd_min, mfe_velocity, volume, bar_range). Smoke-tested 1 week: 5 sessions detected, 8 truncated-near-halt entries correctly tagged. **5s + raw-price follow-up**: user — "change TF to 5s... we will use raw price, indicators for analysis." Extrema now detected on RAW PRICE (peaks=bar high, troughs=bar low), entry_price=the raw extreme; CRMs/anchors are the analysis layer only — truest oracle definition, keeps the 5s-is-noise hard-rule intact (no 5s CRM as predictor). Added `load_tf_bars` (TF-generic loader), `--tf` (default 5s), minutes-based window CLI args, TF-aware `forward_mfe` + `extract_state_vector` (slope lookbacks normalized per-minute → TF-invariant; `tf_seconds=60` default keeps 1m importers unaffected). Re-smoke-tested 5s: 250 entries/wk, 0 negative MFE, sub-minute time_to_mfe, runtime 3s/wk. DMAIC frame at `research/regret_oracle/project.md`. **Next**: user runs full-IS pass, then Phase 2 classification (duration × tod × stack × rail) + Phase 3 window fine-tune + v9d argmax selector. |
| 2026-05-11 | **v6 3-BODY ARCHITECTURE — first positive un-marked IS result, FLAT_SMOOTH-only OOS edge found.** User clarified 3-timescale separation: 1h HL = POSITION context (slow, intentional), 15m CRM = TARGET (mean-reversion target), 15s CRM = TRIGGER (cusp signal). v6 fires SHORT when `z_15m_crm >= +1.5 AND slope_15s flipped + to -`; LONG when `z_15m_crm <= -1.5 AND slope_15s flipped - to +`. Target = revert to 15m mean (z_15m = 0). Stop = opposite extension (z_15m = ±2.7). Time stop 45min. **First genuinely positive un-marked IS result across all 6 simulator variants**: Jul 2025 IS = +$17.10/day at PF-WR +0.139, 47% win rate (n=250 trades). OOS Jan-Feb 2026 fails -$57.50/day, but breakdown by 2D regime reveals: **FLAT_SMOOTH days are the only positive regime** (6 days, +$163/day mean, +$980.5 total). FLAT_CHOPPY -$60/day, UP_SMOOTH -$144/day, etc. Regime gate concentrates the edge dramatically. ⚠️ Lookahead caveat: `regime_2d` is computed from end-of-day stats — needs real-time detector to deploy. User's 4 marked days are NOT FLAT_SMOOTH (UP_CHOPPY/UP_SMOOTH/FLAT_CHOPPY) — their visual picking catches trade types v6 misses. Two strategies emerge: (A) v6 regime-gated mean reversion = mechanical/deployable on FLAT_SMOOTH only, (B) user's manual cusp picking = visual pattern integration across regimes, needs template/clustering work to mechanize. Next: real-time FLAT_SMOOTH detector + sim v7 with forward-pass-honest gate + template clustering on user picks. Files: `tools/sim_v6_3body.py`, `reports/findings/decay_sim/v6_*.csv`. Journal: `docs/daily/2026-05-11.md`. |
| 2026-05-10 | **CRM-CUSP REJECTED OOS, but z_thr fix unblocks 3 FADE retunes → +$36.92/day OOS combined.** Forward pass on 2026-05-09 retune (`docs/RETUNE_RESULTS_2026-05-10.md`): 2 trend tiers validated OOS — COMPRESSION_BOUNCE_LONG +$3.78/day PF_WR 0.71 n=93, CAT_HARVEST +$1.10/day PF_WR 0.90 n=67. MA_ALIGN FAILED OOS overall (IS +$40/day → OOS −$45/day, 941 trades) but the OOS MODE is +$32.26 vs MEAN −$3.23 — typical trades profitable; tail losses kill mean (tail-event veto / regime filter could salvage). 3 FADE retunes silently broken — default seed `z_threshold=1.8` AND retune ceiling `z_band_hi=1.8` intersect at exactly 1.8 → 9 IS / 0 OOS trades each. Fixed with one-line `kwargs.setdefault('z_threshold', RETUNE_Z_LO=1.5)` in `fade_calm.py`, `fade_momentum.py`, `nmp_baseline.py`. **CRM-CUSP ENTRY** built per user direction: "enter at cusp of CRM" — detect |z| local maxima at 1m closes (|z[t-2]|<|z[t-1]|>|z[t]|, no lookahead). Research on 235 IS + 68 OOS days, z in [1.5, 1.8): cusp beats threshold-only at 15m/30m/60m horizons on BOTH splits, OOS sign-flips negatives to positives (h=15m: cusp +$0.28 vs thr −$0.39; h=60m: +$0.82 vs +$0.20). Cusp = ~55%-retention subset of NMP entries (the ones where reversion is already underway). Higher z (>1.8) buckets LOSE just like raw threshold; sweet spot at [1.5, 1.8). Built `training_iso_v2/strategies/crm_cusp.py` (CrmCuspFade) with 5-min cooldown + trend-velocity gate. **Forward pass verdict (277 IS / 68 OOS days)**: CRM_CUSP_FADE FAILED OOS (n=2163, mean −$0.45/t, mode −$0.59, $/day −$14.23, day-WR 40.9%) — research gross +$0.40/t research edge eaten by standard exit suite tuned for z≥1.8 NMP entries (TakeProfit / Giveback / OUReversionDecay / ZSeReversal / SwingNoiseSpike / TimeStop). DON'T DEPLOY. **z_thr fix WORKED**: FADE_CALM 9→18,582 IS / 0→3,794 OOS trades, NMP_FADE_RAW 9→16,758 IS / 0→3,535 OOS. **OOS edge**: FADE_CALM +$13.50/day PF-WR 0.07 day-WR 57.6%, NMP_FADE_RAW +$23.07/day PF-WR 0.12 day-WR 57.6%, FADE_MOMENTUM +$0.35/day (small-n n=168). **3-tier OOS sum +$36.92/day**. Combined deployable across both sessions: COMPRESSION_BOUNCE_LONG +$3.78 + CAT_HARVEST +$1.10 + FADE_CALM +$13.50 + NMP_FADE_RAW +$23.07 + FADE_MOMENTUM +$0.35 = **~+$41.80/day OOS** on 68 days (no tier-interaction modeling). Reports: `reports/findings/cusp_research/` (summary.md, summary_is.csv, summary_oos.csv, 4 PNGs). Next: (1) combined-tier interaction run, (2) cusp salvage attempt with stripped exits, (3) MA_ALIGN tail-risk veto. |
| 2026-05-09 | **REVERSION FRAMEWORK + 3-BODY ENVELOPE + MACRO-EVENT PROBLEM + V2 FEATURE INSPECTION.** Full V2 feature inspection sweep: direction-signal audit (`L2_1h_price_velocity_12` IS regression-mean velocity; correlation 1.00 with 1h_mean_slope; 5m/1m too noisy 0.16-0.55 sign-agreement; 4h_z_se 86% sign-agree as orthogonal macro confirmation; 1D bugged on test day at +180 constant). Regression-mean inspection (4h mean drifted ONLY -9.5 pts on a -408 pt day — useless intraday; 1h drifted -209 captures half the day; price-1h_mean q10=-375 q90=+53 heavily skewed below). I-MR/SPC analysis showed 4h_vwap 97.4% bar-to-bar MR=zero, 1h_vwap 90.5%, 1h_price_velocity 90.5% — TF features step-function at TF cadence; bar-to-bar MR wrong measurement. 14-panel V2 feature panorama confirms: USEFUL = price_mean_w (1m/5m/15m), price_sigma_w (5m/15m), price_velocity_w (1h), z_se_w (1m/5m/15m/4h), swing_noise_w (5m/15m), hurst_w (5m/15m stable around 0.6, 1m jittery), vol_mean/sigma_w (5m/15m); REDUNDANT = vwap_w (all TFs identical to price_mean_w), 4h price_mean_w line, 1h price_mean_w line itself; NOISE = L1 price_velocity_1b, 1m hurst, reversion_prob_w (saturated at 0.99 by NMP entry-gate selection bias). Volume = REGIME-CHANGE confirmation NOT direction (vol spike at rally peak and crash leg look identical). Z_high / Z_low corrected: bar-extreme z-scores not band positions; proper "regression+bands" is mean ± k·σ.  ---  Conceptual session: locked terminology (pivot ≡ inflection; reversion = leg following pivot; trend-follow = ride). Built the 3-body envelope visualization (M_close + M_high + M_low + their σ envelopes) — "always under tension, no force-free equilibrium." Conceptual session: locked terminology (pivot ≡ inflection; reversion = leg following pivot; trend-follow = ride). Built the 3-body envelope visualization (M_close + M_high + M_low + their σ envelopes) — "always under tension, no force-free equilibrium." Empirical validation across 30 IS days: crux state (near M_close + extreme HL) only 1.21% of bars; M_close±3σ_close encompasses M_high/M_low with median +2.51σ_high beyond M_high; bands COLLAPSE on chaotic days (2026_02_12) but NEST on calm days (2026_03_06) — band-collapse ratio is itself a regime indicator. **Mixed-regime envelope**: HL on slow TF (1h) for rare extremes + CRM on fast TF (5m) for responsive target — at 1h-HL ±2σ, P(continuation) ≈ 0; reversion is structurally certain. **2σ as Goldilocks trigger** (~5% of bars; ±3σ_close as outer-wall confirmation). **3σ extreme dwell stats (TF=1h, 50 IS days)**: 1.5-1.6% of bars, ~4.5/day events, median run 15-20s, q90 ~8min, max ~58min. **Compression-precedes-expansion validated** at long lookbacks (T-60min: σ-rank median 0.42 vs baseline 0.55-0.6; rises through 0.52 at T-30min, 0.63 at T-15min, 0.57 at T-5min before HIGH+ extremes). **9-layer probabilistic structure** mapped onto existing 9 ExNMP tiers as instances of a unified parameterized framework. **MACRO-EVENT PROBLEM**: 2-3 hour impulse phases break reversion framework; 2026_03_03 lost $1,324 due to reversion entries firing into a 3hr crash. OOS forward-pass-honest dropped from $172/day → $115 → $7 → −$40 as we removed lookahead — the regression is largely because the system can't distinguish "in macro-event" from "calm regime" in real-time. **CRM flatten-then-pivot detector** designed (state machine: NORMAL → DIRECTIONAL → FLATTENED → PIVOT → IMPULSE_CONFIRMED → STABILIZING → NORMAL); on 2026_02_12 caught the 14:30 macro pivot exactly but produced 17 false candidates needing 5-min monitor window for impulse confirmation. **STRUCTURAL REQUIREMENT (added late session)**: every probability table MUST condition on time-of-day, day-of-week, and calendar-event (FOMC/NFP/CPI). Reversion edge varies systematically across session phases (lunch peaks, opens trough). **VOLUME-DERIVED LEADING SIGNAL (latest add)**: at the 14:00-14:30 macro pivot on 2026_02_12, all four volume features (vol_mean, vol_sigma, vol_velocity, vol_accel) spike together BEFORE price breaks. Refined volume rule: vol_mean/σ remain direction-agnostic ("something is happening"), but vol_velocity/vol_accel are LEADING WHEN-pivot indicators. Multi-signal pre-pivot pattern: (1) σ-rank compression at T-30 to T-60min, (2) vol_velocity rising at T-N min, (3) vol_accel spiking at T-1 min, (4) CRM slope flattening at T-K to 0, (5) CRM slope flips at T=0, (6) vol_mean/σ explode at T+. Stack of 4-5 simultaneous confirmations beats any single signal. **CRM detector v2 BUILT + tested**: `tools/crm_pivot_detector_v2.py` with 5-min monitor + triple confirm (slope-hold, σ-rank≥0.65, volvel-rank≥0.65). Result: 7 confirmed / 11 suppressed on 2026_02_12, BUT the 14:30 macro pivot was OVER-SUPPRESSED ("no sigma-rise" inside 5-min window). σ rises slowly on macro events; single threshold can't separate wiggle from slow macro buildup. Two-path confirm needed (fast 5min threshold cross / slow 15-30min monotonic rise) or vol-only fast confirm (volvel was visible BEFORE σ on 02_12). **FADE_AT_BAND ISO REJECTED**: IS -$17.27/day net on 261 days, OOS +$28.88 was 6-FLAT_SMOOTH-day fluke (delivered +$1,650 of +$1,964 OOS total). 3 of 6 2D regimes flip sign IS↔OOS — textbook overfit per quantile-cell-overfit memo. The 4 robustness filters (hurst<0.60, max_counter_trend_vel=25, divergence-required, confirm_bars=6) DID gate macro-event blowups (2026_03_03 = +$51 vs v1 reversion -$1,324), so macro-gate is NOT critical path. The 15m ±2σ band-touch entry rule simply does not carry edge. Pickles kept at `training_iso_v2/output/{is,oos}_FADE_AT_BAND.pkl` as overfit research artifact. **FEATURE MARKER VISUALIZER BUILT**: `tools/feature_marker.py` — adapted from `trade_marker.py` to drop pins on price + toggle V2 features (price-space overlays + indicator panel). Captures feature snapshot at each pin. Saves to `chart/feature_markers/pins_<day>_<ts>.json`. **CONTEXT FILTER vs TIER LOCKED (eod 2026-05-09)**: the 4 robustness filters and the CRM macro-detector are CONTEXT FILTERS that condition when tiers fire — not tiers themselves. See `memory/project_context_filter_vs_tier.md`. Composition: `entry_signal AND f1(state) AND f2(state) AND ...` — multiplicative. Every new check now classified as entry-rule-component OR context-filter; conventions: tiers → `strategies/`, filters → `filters/` (new dir to add). The pending first-passage probability table will be organized as a filter grid (each axis is a context filter, the cell is the conditioned probability). **BAND-TOUCH POPULATION CENSUS BUILT** (`tools/band_touch_aggregation.py`): walks 277 IS + 68 OOS days, counts entries per (TF × anchor × side × k_sigma) at the close-vs-anchor z definition (z = (5s_close − M_anchor) / SE_anchor — DIFFERENT from V2's bar-vs-anchor z which never reaches |z|≥3 at any TF). Findings: 1h M_low below 3σ (crashes) 1,810 IS entries on 158 days; 1h M_high above 3σ (rallies) 1,621 on 153 days; combined 3,431 IS entries = sufficient for serious conditioning. Macro events at 1h HL ≥3σ ≥60s: 6,495 runs (5,340 IS + 1,155 OOS) on 288 of 345 days. 2026_02_12 14:55 caught at 186.5 min max_z=14.05; 2026_03_03 08:23 caught at 96.9 min max_z=4.92; worst-day 2025_11_25 had 909 min total macro time max_z=40. Outputs at `reports/findings/band_touch_aggregation/`. Substrate is now ready for V0 marginal-axis probability table (next session). **EVENT-BASED BUCKETING (replaces flawed regime-label-based)**: regime_labels_2d.csv is circular for this purpose (UP_SMOOTH days have 0 macro events because SMOOTH is defined by their absence). Built `tools/event_bucket_15m_crm.py` with 5 axes computed at event ENTRY bar (no lookahead): 15m CRM slope (5 bins), curvature (3), z_close (5), sigma_rank (5), and r2adj_5m (5 — R²-adjusted of 5min linear fit to 5s closes, single-scalar smoothness metric). 5,340 IS events labeled across all 5 axes. Per-axis charts at `chart/buckets/event_15m_crm_<axis>.png` with ±2hr window + 15m CRM overlay + ±2σ bands + event timestamps as primitives. Manifest at `reports/findings/buckets/event_manifest_15m_crm.md`. R²-adjusted axis is the cleanest single-scalar characterizer (high R² = local 5s prices fit a line = smooth/predictable; negative R² = chop or oscillation). Substrate ready for V0 probability table: per axis × bucket compute P(continuation), MFE, MAE outcomes, then OOS-validate sign stability per axis before any joints. Tools built: `three_body_state_analysis.py`, `extreme_dwell_analysis.py`, `compression_before_extreme.py`, `crm_pivot_detector.py`. Charts of record: `chart/2026_02_12_z_bands.png`, `chart/2026_03_06_z_bands.png`, `chart/2026_02_12_other_features.png` (14-panel V2 panorama), `chart/2026_02_12_crm_pivot.png`. Strategies: `FadeAtBand` with hurst/swing_noise/trend-vel/divergence filters wired into iso pipeline; default `k_sigma=2.0`. Pending: macro-event detector v2 with monitor window, empirical first-passage probability table, validation replay of 2026_02_12 + 2026_03_03. **EVENING — 5-LEVEL HIERARCHICAL SEGMENTATION SUBSTRATE**: extended segmenter to 5 levels (phrase 15m / motif 5m / sub_motif 1m / measure 15s / note 5s); same simple-shape primitive library at every level, only TF anchor + min span + slope lookback differ. Bulk-segmented all 345 days: 2,047 phrases / 6,276 motifs / 21,048 sub_motifs / 70,782 measures / 203,801 notes. Built parametric pipeline (4 stages, all reusable): `tools/segment_simple_bulk_v2.py` (bulk); `tools/segment_stepped_eda.py` (per-level shape/length/skew/Markov/parent-child distributions); `tools/segment_stepped_surface_regression.py` (forward-return marginals + 2D heatmaps shape×length, shape×sigma, shape×parent — the "hyper-volume" cross-sections — plus OLS coefs + MFE/MAE per shape; horizons 60min/30min/10min/2min/30s); `tools/segment_bayes_tables.py` (Beta(1,1) Jeffreys posteriors; priors per level + P(fwd>0 \| shape, parent_shape, sigma_q, skew) tables); `tools/segment_chart_multilevel.py` (5-panel hierarchy visualization). 60 strong cross-level findings saved to `STRONG_findings_n50_e10.csv`. **HEADLINES**: NOISE-after-STEEP_LINEAR_DOWN at note level → 35.5% UP (n=9,539, CI [0.345, 0.365]) — bearish continuation huge n; mirror NOISE-after-STEEP_LINEAR_UP → 62.2% UP (n=9,793). FLATLINE-after-rally at motif → 74.6% UP (pause-then-continue). STEEP_CONCAVE_UP within STEEP_LINEAR_UP at sub_motif → 68.4% UP (n=321; accel-during-rally). STEEP_CONVEX_UP within GENTLE_CONCAVE_UP → 24.5% UP (counter-trend exhaustion). NOISE share rises 21%→32% phrase→note; FLATLINE share drops 17%→7% phrase→note. Substrate is the lookup foundation for the meta-router's `P_cascade(t)`. Outputs: `reports/findings/segments/{simple_bulk_v2,stepped_eda,stepped_surface_reg,bayes_tables}/`. Charts: `chart/segments/simple/multilevel/multilevel_<day>.png` (10 examples). Findings doc: `reports/findings/2026-05-09_5level_substrate.md`. **PENDING** (next session): TOD axis + calendar-event axis on every Bayesian cell; OOS sign-stability per cell; per-tier oracle PnL per primitive chord (failure-mode identification); V0 meta-router using P_cascade. |
| 2026-05-04 | **MORNING BRIEFING — overnight V1→V2 migration audit (Phase 0+0b done while training ran)**. User kicked off `python training/run.py blended --fresh` before sleeping (~midnight PDT, 2-4h ETA). Claude built `core_v2/v1_compat.py` shim + `tools/v1_v2_parity_test.py` + `docs/JULES_v1_to_v2_migration.md` design doc. **Phase 0**: V2 is lookahead-clean (28/28 tests), covers all V1 dates, deliberately dropped 6 V1 concepts per spec Principle 7 ("L3+ compositions are the model's job"). **Phase 0b parity (4 days × 4 TFs)**: wick_ratio, vol_rel, variance_ratio (1m/5m/15m) reproduce at machine precision (Pearson 1.0000); 1h variance_ratio tight (Pearson 0.97). p_at_center and dir_vol drift because V1 cuda SFE and V2 numpy SFE produce slightly different z_se and price_velocity_w (V1 vs V2 z_se Pearson 0.87, median 0.47σ shift). **Critical migration finding**: tier-engine thresholds (ROCHE=2.0, VR_ENTRY=1.0) fire on slightly different bars between V1 and V2 caches — the −$164/day baseline WILL SHIFT during V1→V2 migration. Phase 1-5 plan awaits user approval. **3 open questions for user**: (1) threshold re-tune policy (match V1 bar fraction vs literal value), (2) CNN strategy (79D shim-fed vs 184D V2-native), (3) migration commit policy (end-to-end vs phase-by-phase). **Quick win available**: deploy RIDE_AGAINST regime gate on V1 NOW (+$21/day OOS, all 3 splits positive) as safety net before migration. |
| 2026-05-03 | **TRACK D feature×feature mirror (D1-D9, 7 layers) + 95-100% OOS survival.** User asked: did we ever measure feature×feature relationships independent of price? No, all prior layers conditioned on forward return. Built parallel track. **D1**: 23 features collapse into 6 redundancy families (`vwap_w == price_mean_w` r=1.000; dispersion / volume-kinetic / bar-direction / z-score families). **D2**: 577 regime sign-flip pairs. `price_velocity_w × price_sigma_w` 1h: DOWN_SMOOTH −0.84, UP_SMOOTH +0.89 (asymmetric volatility through v2 substrate). **D5**: 34.7%/32.7%/32.6% lead/contemp/lag — feature×feature is roughly symmetric (vs feature×price 89.7% lag); most "leads" are window-construction artifacts. **D6**: 43% pairs (3,255) have |tail_asymmetry|≥0.5. 887 cross-tail dominant pairs (extremes mutually exclusive — `price_mean_w × swing_noise_w` 1h UP_CHOPPY: cross 7.95×, joint 0.00). **D7**: of 254 D2 regime-flips, only 83 (32.7%) confirmed in both IS halves; FLAT_SMOOTH itself is non-stationary. **D8**: `z_se_w` is universal contextualizer (mean lift 0.28). 4,960/26,565 (18.7%) triplets where Z-bin literally flips sign of corr(X,Y). Top: `bar_range × body | z_se_w` lift 1.79 (corr ranges −0.90 → +0.90 across z-bins). **D9 OOS**: D7 pairs 79/83 (95.2%) survive, D8 triplets 100/100 (100%) survive. Dramatic contrast to feature×price track (C1 was 25.8% survival). Reason: feature×feature measured on same bars at same time, no time-asymmetric leakage. **Operational reframe**: don't predict price directly; use surviving D7+D8 rules as a regime DETECTOR (95-100% OOS-stable), then map regime→direction via regime baselines. Saved methodology preference: `feedback_blender_first_then_drill.md` ("blender first, then drill" — broad unrestricted run before any narrowing). Tools: `v2_features_within_tf_{interaction,regime,leadlag,tail_asymmetry,drift,contextualizer,oos_validate}_eda.py`.  ---  **Earlier:** **5-step single-feature drill-down + Layer C1 triplet (regime-stratified) + OOS-validation reality check.** Layer C1 (`tools/v2_features_triplet_regime_eda.py`): 270 leading-anchored triplets × 6 regimes × 27 cells → 11,106 cells with |lift| ≥ 10 ticks above regime baseline. Top IS cells claim +99 ticks lift (UP_SMOOTH `hurst_15m + sigma_5m + volvel_15m` (1,2,1) → +120 mean / 65% WR / n=96). **OOS validation (`v2_features_triplet_oos_validate.py`) — only 31 of 120 top IS cells (25.8%) survive.** 49 sign-flip, 34 magnitude-collapse, 6 no data. Mega +99 cells COLLAPSE to noise. True survivors share structural signature: 1h-window anchor (vol_mean_w / hurst_w / bar_range_1h) + 1m/5m amplitude (swing_noise_w_1m, bar_range_5m) + reversion-context companion (z_se_w_15m, reversion_prob_w_15m). Pure velocity/sigma combinations DO NOT survive. **Saved 31 surviving cells to `surviving_cells.csv` — actionable triplet rule set.** Methodology lesson saved to `memory/feedback_quantile_selection_overfit.md`: 27-cell partitions on 270 triplets create massive selection bias; always OOS-validate before quoting cell metrics. Going to 4-feature combinations would amplify this; stop at C1. ---   New 5-step sequence on regime-stratified Q4 cells: (1) **TOD** — all extreme regime signals concentrate in RTH (us_pre/us_open/us_morning/us_lunch); 45 same-cell sign flips across hours; the Layer-5 `price_sigma_w_5s UP_SMOOTH +48` was actually `us_pre +156 / pre_market ~0`. (2) **Persistence** — forward-return decay is mostly DISTRIBUTED (not front-loaded); 1h-window features mechanically sticky; long Q4 runs reappear with strong signal (regime-transition mechanic). (3) **Lead-lag** — **89.7% of cells LAG**, only 8.8% lead, 1.5% contemporaneous; `price_velocity_w_5m` peak corr +0.90 at s=−12 across ALL six regimes; the 12 genuine leaders include `bar_range_1h UP_CHOPPY` s=+12 corr +0.19 and `hurst_w` (4× in leaders — structure features lead, velocity lags). Re-frames every prior layer: most "regime signal" was lag artefact. (4) **Tail asymmetry** — **75% of cells SAME-SIGN BOTH TAILS** (Q0 and Q4 push same direction within a regime, just different magnitudes); only 10.7% are textbook symmetric_opposite; 20 one-sided trigger cells ALL live in FLAT regimes. **Interpretation**: regime carries direction, feature carries amplitude. (5) **Drift / calendar stability** — Q4 mean sign-flip rate 7.0% (23/328 cells, all in CHOPPY/FLAT); correlation sign-flip rate 14.9% (49/328); SMOOTH-regime Q4 signals robust across early/late IS halves (`bar_range_5m UP_SMOOTH` 44.78 → 40.95 stab 0.91); CHOPPY relationships drift. **Synthesis**: regime → direction; feature → amplitude × momentum confirmation. Real predictive content lives in 1h-window structure features (hurst, bar_range_1h, vol_mean_w_1h). Tools: `v2_features_tod_eda.py`, `v2_features_persistence_eda.py`, `v2_features_leadlag_eda.py`, `v2_features_tail_asymmetry_eda.py`, `v2_features_drift_eda.py`. Next: higher polynomial (triplet+) interactions with leading-feature anchors. ---  Earlier:  **V2 features × price descriptive EDA stack — 10 layers built, all on IS-only.** Added regime-stratified TF sweep (`tools/v2_features_regime_stratified_tf_sweep.py`) that walks each (concept × TF × regime_2d) cell. **Cross-regime inversions found**: every top-15 (concept, TF) by corr_fwd range INVERTS sign across regimes — bar_range × 5m goes from −0.157 to +0.235 (range 0.39); same feature, opposite directional implication depending on daily regime. Validates regime-conditional composite framework empirically. **Multiscale regularity** (saved as `feedback_chop_velocity_range_regularity.md`): chop at lower TF reliably WEAKENS signed velocity at higher TF for directional regimes (DOWN_SMOOTH → DOWN_CHOPPY at 1h: vel drops 41%). Range under chop is regime-asymmetric: chop expands range for non-directional regimes (FLAT +36%, UP +13%) but contracts range for clean-sell DOWN regime (−10%). Microstructure: sells produce wider bars when uninterrupted; chop only expands range when there's no clean opposing force. Earlier 9 layers Final two layers (TF sweep + contextualization) reframe the picture structurally. **TF sweep finding**: bar_range INVERTS sign across TFs (−0.24 at 5m → +0.18 at 1D — intraday wide range = sells, daily wide range = bull rally); vol_velocity_w only signals at 1D (−0.21, capitulation); price_accel_w 0 at short TFs to +0.55 at 1D; vol_mean_w inverts at the macro boundary. Universal directional carriers: price_velocity_w (peaks +1.25 at 1h), body, vol_sigma_w. **Contextualization finding**: features like vol_sigma_w, vol_velocity_w, hurst_w don't predict price directly but RESHAPE other features' price relationships. 1h vol_sigma_w modifies body's corr_fwd from −0.108 to +0.039 (sign flip across modifier quantiles, lift +0.15). 1h vol_velocity_w modifies price_accel_w lift +0.16. The 1h-TF is where contextualization lives. Volume features dominate as modifiers. **Operational implication**: composite signals CAN'T be additive sums of features — they need to be CONDITIONAL on modifier quantile (target sign FLIPS depending on modifier regime). Earlier 7 layers: A1 single-current, A2 pair-current, B1 single-lookback, B2 pair-lookback, chord 3-feature (cells 100% pure for FLAT_SMOOTH/CHOPPY at n=96), volume×variation (LOW_VOL_HIGH_VAR cell at 15m → +28.7 tick fwd, fakeout bounce; HIGH_VOL_LOW_VAR at 4h → 70% FLAT_CHOPPY purity), visual overlay (12 PNGs per regime). Substrate for regime-conditional strategy work. Open: OOS validation, state-fingerprint deployment, conditional rules from top contextualizers. User reframed from "predict" to "characterize how each feature behaves across regimes/phenomena". Six tools built: A1 single-current (`v2_features_regime_eda.py`), A2 pair-current (`v2_features_pairwise_eda.py`), B1 single-lookback (`v2_features_lookback_eda.py`), B2 pair-lookback (`v2_features_lookback_pair_eda.py`), chord 3-feature (`v2_features_chord_eda.py`), visual overlay (`v2_features_overlay_viz.py`), volume×variation (`v2_features_volume_variation_eda.py`). **Headlines:** L2_5m_velocity_w r=+0.86 with past N-bar return (it IS recent move); 5m_velocity_1b × 5m_body pair gives WR 42-75% via pure quantile binning; 6-bar mono velocity → 70% WR / +38 ticks fwd; 1m+5m REVERSAL_AGREE_DOWN coordinated bearish → 71% WR / +21 ticks (mean reversion); chord (4h_body, 4h_velocity_1b, 4h_z_low_w) has cells **100% FLAT_SMOOTH** vs **100% FLAT_CHOPPY** (n=96 each — state fingerprints, no fitting); volume × variation: LOW_VOL_HIGH_VAR cell at 15m gives +28.7 tick fwd (fakeout territory bounces), HIGH_VOL_LOW_VAR at 4h hits 70% FLAT_CHOPPY purity (compression); volume × variation doesn't carve new regimes but separates forward-return regimes within FLAT_CHOPPY. **Pattern**: 4h-TF features dominate state-fingerprinting; 5m-TF dominates short-horizon momentum; volume features describe activity-intensity not direction. All layers descriptive (no fitting), substrate for future regime-conditional strategy work. Open: OOS validation pass on all layers + regime-stratified versions. |
| 2026-05-01 | **Multi-TF v2 standalone + composite directional → MA alignment WINNER (deterministic rule, +17.6% lift) + 2D REGIME LABELS BUILT.** Ran full v2 `standalone_research` at 6 base TFs (1m-1D) on `DATA/ATLAS/FEATURES_5s_v2/`. Per-TF L: 62-71% own-cadence accuracy with gates (5m \|pred\|>20: 70.4% on 45%, +10.6%). 1D excluded (97 test × 185 feats, R²=−17.6). Built 5 composite tools: `v2_composite_directional` (DEPRECATED, wrong target), `v2_composite_quantile` (collapsed to FLAT under --quick GBM), `v2_composite_l_aggregator` (5-voter best 59.8% on 13%, +9.6%; 2-voter worst 58.8% on 1.9%; horizon mismatch tax), `v2_composite_single_horizon` (apples-to-apples but handicapped voters: 62.6% on 26%, +9.8%). **BREAKTHROUGH: `v2_composite_ma_alignment.py` — user's counter-proposal "use vwap_w/price_mean_w as smoothing signals not regression inputs". Result: 70.5% direction accuracy on 20% of 5m bars (+17.6% lift over 52.9% baseline) for 7-of-8 TF vwap alignment. Combined VWAP+PriceMean \|score\|>=15: 70.6% on 19% (+17.8%). Per-TF strongest: 15m_vwap (61.7% solo, +8.9%) and 1h_vwap (61.6%). Beats standalone L (70.4% on 45%, +10.6% over inflated 59.8% baseline) on lift by 67%. Deterministic rule, no fit, walk-forward stable.** Operational rule: LONG if 7+ of 8 TFs have price > vwap_w; SHORT if 7+ have price < vwap_w; FLAT else. Implementable in ~20 NT8 lines. Confirms user's distributional intuition — autoregressive features carry signal as direct comparators (not regression inputs). **REGIME CONNECTION**: MA alignment IS a trend-regime classifier — connects to prior chop-edge work (zigzag wins chop, loses trend, d_OOS=0.77). Right joint system = regime-conditional strategy selection. Built `tools/atlas_regime_labeler_2d.py` → `DATA/ATLAS/regime_labels_2d.csv` (348 days × {UP/DOWN/FLAT} × {SMOOTH/CHOPPY} + IS/VAL/OOS 60/20/20 split). Distribution: 25% UP / 18% DOWN / 57% FLAT; 64% SMOOTH / 36% CHOPPY. Substrate for all future regime-conditional analysis (per-regime MA-align perf, per-regime L-model perf, per-regime zigzag bleed_score perf, joint regime-router). **Bugs shipped:** imr.detect_regimes O(n²) → O(n+R log R) via np.bincount+LUT (was hanging 1m+1y on 24,973 raw regimes); Analysis F regime audit plot OOM → top-30 cap; L/M/N segment-overlay plots OOM → uniform-stride to 5000; --analysis-days 0 fix; plots dir suffixed with base_tf; Analysis L per-bar CSV export; Analysis H hangs on 5m-1D bases (route around via --start L). Next: parallel work = MA alignment exit rules (MFE/MAE sizing under the filter) + L-model refinement (drop autoregressive inputs, retain only as alignment voters); joint = filter+magnitude pair for entry sizing. |
| 2026-04-30 | **Headline reframed + validated + cleanup.** Surfaced the **+$165,141 / 14-month** tier-system + LinReg-slope-filter total (was buried under +$33k filter delta) and validated it three ways: per-tier table sums to $165,141.0 ($51.41/trade × 3,212 trades); 70/30 within-tier holdout shows 4 of 6 tiers generalize on test (RIDE_AGAINST/FADE_AGAINST/RIDE_CALM/BASE_NMP), FADE_CALM and KILL_SHOT no-op (don't degrade); source enriched-trades CSV row count consistent. Wrote `MORNING_BRIEFING_2026-04-30.md` + `HEADLINE_VALIDATION_2026-04-30.md` + `INDEX.md` (master nav for 125+ findings). **Critical flag: do NOT ship standalone BaseNmpRunner_v1.0-RC** — loses $30k-$49k on full data because it lacks the tier classifier gate that gave BASE_NMP its $24k edge in the blended pipeline; fires 10× more often than the gated tier (16,500 vs 1,195 trades). Recommendation: ship v1.0.8-RC zigzag (slope filter + daily gate) instead. **Cleanup**: 9 superseded RCs archived from NT8 Strategies/, 25 dead tools archived in `tools/archive/` (3 subdirs: 2026-04-29_overnight_research, superseded_v_x_runners, v15rc_chop_specialist). Compile fix: switched `LinReg(...).Slope[0]` to NT8's `LinRegSlope` indicator (BaseNmpRunner_v1.0-RC, ZigzagRunner_v1.0.8-RC). Next: user runs NT8 SA on v1.0.8-RC against MNQ JUN26 (MNQCONT load broken — data-series mismatch). |
| 2026-04-27 | **CHOP-EDGE DISCOVERED + 4 critical bug fixes shipped.** v1.0 baseline broken in 4 ways for 4 versions — NO mid-session exit logic (exits were a side effect of opposite-direction entries; always-long mode degenerated to 1 trade/day held until EOD). v1.3-RC DRM used stale `Position.AveragePrice` in multi-TF backtests → 37% of trades exited at exactly entry±5pt (Tier1 formula with maxPts≈0). Both fixed. Shipped v1.0.2 (RideWithTrend bool), v1.0.3 (PivotAction enum: Long/Short/Skip per pivot type), v1.0.4 (decoupled exit from entry), v1.3-RC (entryPrice+entryDir explicit), v1.4-RC (regime filter via `mean_range_5d` threshold + Day(1) data series). **Major analytical breakthrough: 95-day NT8 trade ledger (1/2-4/24/2026) shows clean two-regime split at 2026-02-26.** Working window 1/2-2/26 = +$4,096 (+$89/day, +$5.74/trade); bleed window 2/27-4/24 = -$4,648 (-$95/day, -$4.82/trade). Net -$552 over 4 months hides a real edge. Mean efficiency 0.437 (chop) in working vs 0.491 (more directional) in bleed. Stratification: range-expansion Q1 compressed = -$3,215, Q4 expanded = +$2,019; efficiency Q3 mid (0.51-0.64) = -$1,298 (worst). **HOUR-OF-DAY signal HUGE: 10 profitable hours = +$14,337 vs 12 losing = -$14,888 ($15k swing on same trades). Best hr 12-13 CT (post-lunch ET = +$7,496); worst hr 6 (+12pm trends = -$4,572). Thu/Fri = -$5,563.** Also: regime drift baseline = +$247/day buy-and-hold MNQ; perfect oracle = +$515/day. **Tested 11 simple per-day classifiers, NONE beat always-long.** Daily granularity too coarse — signal lives intra-day. NT8 dumps corrected by user (now have 1s/1m/1h/1D for MNQ_06-26). Built tools: `nt8_dump_parity_and_drift.py`, `nt8_trades_by_hour.py`, `chop_vs_trend_validate.py`, `regime_classifier_oracle.py`, `zigzag_param_interaction.py`, `zigzag_regime_finder.py`, `nt8_backtest_csv_quick.py`. Findings: `reports/findings/{nt8_dump_validation,zigzag_v13_interaction,zigzag_regime,regime_classifier,chop_vs_trend}/`. Archive trail in `docs/nt8/archive/`. **Next:** v1.5-RC chop-specialist with hour-of-day + range-expansion + dow filters layered on v1.4-RC base. |
| 2026-04-25 | **v1.4 hybrid disproved → Regime+Envelope filter found (+$80/day OOS).** Implemented `ZigzagRunnerHybrid` (1m extremes + Ns retracement) — Phase 2 backtest at R=30 swept N ∈ {1s..1h}. N=5 (intended setting) tripped both abort triggers: trade count +22% (limit ±5%) and theoretical OOS −74% (was supposed to be invariant). Root cause: 5s grid catches intra-minute spikes the 1m close averages out → spurious pivots. Surprising finding: longer N is monotonically BETTER (N=600 OOS realistic = −$48/d vs −$224 baseline at N=60), but user reframed: this isn't the original goal. **Original project = Resonance Cascade** — multi-TF z-score alignment to detect "energy buildup across all TFs"; existing `tools/resonance_cascade_research.py` (313 lines, never run). Built `tools/zigzag_multitf_overlay.py` with stacked zigzag polylines (macro on top, micro underneath); generated Feb 9 + Mar 20 visuals — confirmed pivots cluster around major reversals. Built `tools/cascade_pivot_quality.py` using pre-computed FEATURES_5s `<TF>_z_se` (no SFE recompute). 11K pivots R=30 / 43K R=10 analyzed. **Finding: cascade alignment is real but moderate.** Top-decile `dir_energy` shows WR +4-5pp and MFE +20-25% vs bottom decile; survives OOS at R=10. **Critical: MFE rises but ETD rises in lockstep.** Capture % stuck at −100% to −180% across ALL buckets — cascade does NOT fix the give-back problem; trail stop still required. Cascade and trail are orthogonal fixes for entry-quality vs exit-capture. User's reframe ("trade 1m in the context of other TFs") locked in: **v1.5 design = v1.3 trail + cascade tier sizing** (top-decile = full size + loose trail; bottom decile = smaller/skip). Estimated +$15-30/day on top of v1.3. v1.4 hybrid DROPPED from deploy ladder. **ADDENDUM**: User reframed concept — "1h pivot direction + 1h z-bands tell us expected noise; couple with 1m zigzag = catch fakeouts + estimate minimax." Built `tools/regime_envelope_quality.py` to test. 11K pivots in 2×5 (regime × band) buckets. Worst bucket OOS = AGAINST-regime + extreme (|1h_z|>2σ): WR 30%, **−$12.28/trade × 209 trades** = the fakeout-at-exhaustion trap. Best IS = WITH-regime + outside (1.5-2σ): 42% WR, +$4.54/trade. Noise fakeouts = WITH-regime + inside (|z|<0.5σ): −$3-4/trade. Filter test: skip 2 worst buckets only (~18% of trades). **OOS uplift +$79.88/day**, IS +$24.50/day — OOS gain 3.3× IS gain = NOT overfit. Mean MAE = $55-66 across all buckets → minimax = $70-80 SL placement. **v1.5-RC design = v1.3-RC trail + 1h regime+envelope filter** ← Yesterday's analytical model had filter+trail looking NET-NEGATIVE (−$440), but that was a 25× overstated trail model. **HONEST bar-by-bar simulator built today** (`tools/zigzag_trail_ticker.py` — adapted from existing live-system `nightmare_ticker.py` pattern with peak_pnl tracking). Real results: v1.0 OOS −$223/day, v1.3-RC −$122/day (+$101 from trail), **v1.5-RC −$80/day (+$42 more from filter, +$143 total)**. NT8 estimates with $680/day Python pessimism gap: v1.0=$455 (measured), v1.3-RC ~$556, v1.5-RC ~$598. Trail breakdown: 6,933 trail wins at +$33 (87% WR) + 3,508 pivot losses at −$73 (0.1% WR). Trail handles "good-entry-bad-exit", filter handles "bad-entry-period" — partial complements, not substitutes. Versioning policy adopted: only v1.0 RELEASED; everything else carries `-RC` suffix until promoted (`docs/VERSIONING.md`) (~150 LOC NT8 addition). Findings doc: `reports/findings/2026-04-25_cascade_pivot_quality.md`. |
| 2026-04-24 | **v2 CNN dead-end, zigzag NT8 strategy, LightGBM pivot.** CNN on v2 engine trades collapsed to AUC 0.51; diagnostic proved features carry signal (AUC 0.60 on v1 curated trades) — CNN is the wrong architecture for this tabular non-spatial problem. Built pure-rule zigzag backtest (`tools/zigzag_backtest.py`) — theoretical +$986/IS, +$1,059/OOS per day at R=30, but realistic model (1s-bar random fill + $2 commission) flips all R thresholds negative (R=30: -$132/day IS). Deployed `NT8_ZigzagRunner.cs` v1.2 with per-trade CSV log, trail-stop (10pt arm / 5pt trail), post-cutoff exit fix. Drafted `JULES_PIVOT_LGBM_V2.md` design doc for LightGBM replacement of CNN — 4-reason case against CNN (no locality, arbitrary TF order, data volume too small for DL), 7 open decisions flagged for counterbalance-LLM review before implementation. User to run NT8 Strategy Analyzer in parallel with LGBM review. |
| 2026-04-23 | **RM pivot NN + infrastructure.** Continued RM pivot research. Key findings today: **CNN filter works on RM pivots** (AUC 0.63, OOS post-hoc +$478/day) but **post-hoc FLIP uplift was an illusion** — engine-integrated showed only −$226/day because asymmetric TP/SL geometry can't be inverted by flipping PnL sign. **No-FLIP + no-TP/SL** variant with threshold 0.55 got to **−$10/day OOS at 43% DayWR** (break-even with NOISE_WIN mode). **Tier NN training failed** — AUC 0.50 on `iso_is.pkl` (only 5k samples, base rate already 58%). Need 88k `blended_is.pkl` for retrain. **CNN has LONG bias** (91-97% of filtered trades long despite balanced training) — fix pending. **Human-labeling tool identified**: `tools/trade_marker.py` — user will batch-label. **Autonomous infrastructure work**: built `research/TOOLS_INDEX.md` (all 106 `tools/*.py` cataloged), 16 category folders + READMEs in `tools/_*/`, top-level `tools/README.md` navigation. |
| 2026-04-22 | **DMAIC + PDCA adopted. RM pivot research: Cycle 1 PASS, Cycle 2 FAIL.** User called out rabbit-hole behavior; committed to DMAIC project frame + PDCA iterations + `research/<topic>/` layout. Metric definitions updated: Trade WR = (∑profit/\|∑loss\|)−1, $/trade as mode+mean, Day WR count-based. **Cycle 1**: Cohen d=−2.46 journal claim reproduces — \|d\|=1.96 IS/OOS at R=$4 with monthly std 0.16, 83.5% direction accuracy. **Key finding**: direction rule was INVERTED from naive mean-reversion — trade the pivot direction (LOW→LONG, HIGH→SHORT). **Cycle 2**: naive pivot-to-pivot forward pass FAILS all 7 gates at every R ($2-$20). Best R=$10: +$7/day, mean $1/tr (gate $10). Root cause: 2R retracement tax consumes leg amplitude. **Accuracy ≠ tradeable edge.** 0.24% RM cord capture (target 20–30%). Cycle 3 pending user pick of exit fix (mean-cross / asymmetric-R / fixed-TP / leg-filter). |
| 2026-04-21 | **CORD-CEILING REFRAME + DIRECTION-PREDICTION REALITY CHECK.** 20+ research tools built today. Key findings: (1) **Direction prediction at any bar = coin flip** confirmed 3 ways (91D Cohen d max=0.01, polynomial 0/27 stable, z-stratified max 3pp bias). (2) **Direction at zigzag pivots via 1m_z_se residual** = huge signal, Cohen d = -2.46 walk-forward, 86% oracle accuracy — but requires pivot lookahead. (3) **Price cord @ $15 = $8K/day IS, $10K/day OOS BUT regression cord (honest ceiling) = only $2.5-3K/day** (70% is intra-bar noise). (4) **1m-close sims overstated by ~3x** vs 1s intra-bar reality — TP$50/SL$3 forward pass showed +$779/day on 1m-close, −$248/day on 1s slippage (96% SL in 1-2 sec). (5) Wick×Volume×Velocity stratification at pivots shows 90% accuracy in killer cell, but 1s execution kills tight SLs regardless. **NMP captures only 2.3% of OOS regression cord** — 40x headroom. Next: build forward pass on **regression-line pivots** (not price pivots), target 20-30% of regression cord = $500-750/IS, $575-862/OOS. |
| 2026-04-18 | **TIER-FIXING DAY — $76/day → $149.86/day (+$74, 2×).** Three-question method applied to TREND_FOLLOWER (flip+peak rule+60m), RIDE_AGAINST (flip+phantom+relax, -$9,060 → +$2,944), KILL_SHOT (phantom, -$708 → -$345), MTF_BREAKOUT (flip+phantom, -$1,116 → -$96). **Defaults flipped (c0811e6d): iso now Phase 1 only, chains=1, no regret.** Isolation verified (single-tier run matches full-engine row). Playbook codified in `memory/tier_building_playbook.md`. **Baseline: 277 days, 14,983 trades, 57% win days.** Next target: KILL_SHOT loser-cut (61% WR + -$1.15/trade = broken exit asymmetry, losers 2× winner size). Path to $300/day target: +$150/day via loser-cut + RIDE_AGAINST tuning + NMP_RIDE relaxation + NMP_FADE micro-lift + chain multiplier. Findings: `reports/findings/2026-04-18_iso_baseline_149day.md`. |
| 2026-04-17 | **LOOKAHEAD BIAS IN build_dataset** — `searchsorted(right)-1` picked higher-TF bars at target_ts with future 5s data baked in. Fixed: subtract period before lookup. Regenerated features (moved inside ATLAS/). **Honest baseline: -$164/day** (was $740 with lookahead). Chains alone cost $157/day. **Per-tier isolated**: every tier sits at noise floor (-$16 to +$9/day, ~50% WR). **KILL_SHOT peak physics DISPROVED**: largest Cohen-d across peak = 0.19 (1m_wick), velocity flips only 3.3%, no detectable signal. Back-test: natural exit ($11.61/trade) beats every rule including 50% trail ($3.40), fixed $10 ($11.22). Peak is statistical maximum over noise, not physics event. **Regret on all 8 tiers**: ~49% counter-flip rate — every tier is a coin flip on direction. Counter-labeled trades have 38-45% WR (regret ranks right), but SAME/COUNTER boundary is near-random. Flip@exit oracle upper bound: +$2,183/day pooled. nn_v2 playbook may not transfer (NMP was 30-35% counter, here 50%). Caveat: `regret.py` LOOKAHEAD=360 bars on 1m = 6 hours, distorts optimal magnitudes. New tools: run_tier_isolated.py, killshot_peak_physics.py, regret_on_isolated.py. |
| 2026-04-16 | **FROZEN FEATURES BUG FIX** — LiveFeatureEngine SFE cache collided after 5000-bar trim. `valid_idx` was always 5000 for new bars → stale SFE state since Feb. Fixed cache key to `(valid_idx, latest_bar_ts)`. First honest live session on new architecture: **$900 peak** PnL (frozen z was hiding real edge). Phase 5 committed. Phase 6 live run confirmed architecture. **Winner MAE analysis**: 97% dip negative first, median -$8.50, 90% recover from ≤-$17. Hard stops kill winners: -$150 = 0.2% loss (sweet spot), -$50 = 25% loss. **Dashboard**: entry price line, z overlay on price chart, chain trades logged. **MockBridge** built for offline live engine replay. 1D aggregation added to converter (was stopping at 1h). Checkpoint v5 saves `preloaded_end_ts` + `day_ends` for parity metadata. |
| 2026-04-15 | **MORNING:** NT8 bridge v7.0.0 parity blocker fix. OrderManager handshake state machine. **AFTERNOON:** 79D rename. JULES_ENGINE_DECOUPLE_ORDERS Phases 1-5: `core/engine_signals.py` (types), `core/ledger.py` (shared ledger), `BlendedEngine.evaluate()` (stateless), `core/sim_executor.py` (sim driver), training loop switched (sim_executor default, -2% drift accepted), live engine_v2 rewritten (ledger+evaluate, PositionState deleted, silent-flip detection removed). 78 tests. Phase 6 next: live SIM session parity test. |
| 2026-04-14 | LIVE PARITY ACHIEVED. LiveFeatureEngine 7.5%→100% parity (130,943/130,949 cells exact). Engine v2 hardening: NT8 panic protection (3 layers), state sync, GPU/RAM cleanup, ACCOUNT_UPDATE dashboard. RIDE_AGAINST + FADE_CALM exit fixes. Negative exits via TIER_STRENGTH. Feature response surface: chop is #1 loser signal (15m_wick d=-0.27). MTF_BREAKOUT broken (43% WR). Gravity well concept implemented. ATLAS_NT8 clean rebuild (Mar 20+ NT8 + Mar 2-19 Databento context). |
| 2026-04-13 | First live SIM deployment. Dashboard wiring fixed (q100plus key, X button). Chains scale-in/out. Slippage tracking. Discovered feature path divergence between training and live (92% mismatch). Sunday session -$1,339 (thin market, expected). |
| 2026-04-12 | Sunday gate analysis: 11/13 IS losing days are Sundays. Skip-Sundays = 100% IS win rate. Built per-day baseline runner with DOW breakdown. Started live SIM deployment. |
| 2026-04-11 | MEGA SESSION: $3,011 IS, $3,319 OOS, $4,144 NT8. Chained lightning (chains=64% of PnL, higher WR). 12-tier waterfall EDA. Direction flips (MTF_EXHAUSTION 13%→77%). Engine v2 + Dashboard v2. NT8 60-day dump. Diagnostic pipeline. |
| 2026-04-10 | Parity mega-session: SFE review, training/live 5s cadence, timestamp bars_held, feed_bar() incremental API (validated 1e-5), NT8 vs Databento data mismatch found ($220 contract diff + 5s timestamp offset), dual OOS architecture (Databento OOS-1 + NT8 OOS-2). |
| 2026-04-09 | $740/day OOS (74 days). 10 ExNMP tiers (PEAK added). KILL_SHOT 1-bar, tiered RIDE exits, 5m exit patience. Entry research: exhaustion bar, absorption, delta divergence, regime flip, MTF exhaustion. |
| 2026-04-08 | Live hardening + pipeline + exit physics. SFE OHLCV mismatch fixed, dashboard cleaned, 8 live bugs. Oscillation discovery (100% long trades oscillate). FADE/RIDE exit modes. 3-bar confirmation. 4 ExNMP sub-tiers (fade/ride x calm/momentum). Full pipeline: $462/day IS, $340/day OOS (94% win days). |
| 2026-04-07 | CNN flip 70.6% → blended $367 IS, $397 OOS, 85% win. BASE_NMP $0.30→$7.90/trade. Entry-only CNN > path CNN. Stage 2 architecture: regret discovers new entry physics → CNN clusters → validate → expand ExNMP roster. |
| 2026-04-06 | Kill shot 96% WR. Blended engine (cascade/killshot/base). Pipeline $295 IS, $386 OOS. Tree exhausted: direction 55%, duration 64%, 5-point path 54% — can't predict BASE_NMP flips. Wick is the ONLY direction signal. Regime flip = poison (-$19/trade). Next: CNN for multi-feature patterns. |
| 2026-04-05 | Bayesian pipeline built. Root cause: counter-flip killed edge. Fix: corrected trades (regret → right dir + exit). IS $62/day (63%), OOS $51/day (52%). First real positive OOS. 17 commits. Next: DOE on 79D entry conditions. |
| 2026-04-04 | nn_v2 full pipeline. NMP→regret→tree→AI. Bulk OOS $711/day. Honest sequential built. Per-day iteration to $175/day IS. |
| 2026-04-03 | Clean data = -$2,427 (phantom spikes were fake edge). Template autopsy: 0 template matches, 100% peak reversal coin flip. Designed 79D unified feature vector (10 features x 6 TFs). NN architecture: direction + hold duration (signal half-life). 5s = atomic unit, TFs = aggregation windows. Cord length ceiling: 5% capture at 5s = $2,350/day. |
| 2026-03-29 | Levels + EDA (z_se=levels, 1m leads direction, 1h leads speed) + shapes (4 types distinct) + archive cleanup |
| 2026-03-28/29 | Oscillation research, calibration, level drawing tool, hand-drawn levels validated across month |
| 2026-03-27 | 29D pipeline built, 3-layer CNN ($602/day IS), pivoted to oscillation-aware proposal |
| 2026-03-26 | 18 live bug fixes, TradeCNN StatePredictor $1,609/day OOS, trailing stop |
| 2026-03-25 | Base measurements framework, CNN $736/day OOS, DMI flipper $208-400/day |
