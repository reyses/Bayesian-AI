# Daily Journal — 2026-06-12

## Session: memory + daily-journal condensation (resumed after session-limit failures)

### Context
The previous session launched two background condensation workflows (per user request:
"condense and synthesize docs\memory ... so it is easier for the LLM to remember" and
"do the same with daily journals so it's easier to go back to"). Both workflows DIED at
their write/synthesize stage on the session usage limit — twice — burning ~1.3M subagent
tokens combined and producing **zero output** (every write stage failed). On "resume the
condensation", I abandoned the fragile multi-agent approach and did it myself in one linear
pass on opus[1m]. Rationale: the actual content is ~135K tokens; a single agent with a 1M
window has no fragile synthesis-handoff to die on, and a third workflow run while the limit
was still active would just waste another million tokens.

### What changed
**1. `docs/memory/` condensed (the load-on-start knowledge base):**
- `MEMORY.md` rewritten **307 → 96 lines** as a 9-section knowledge base:
  0 START HERE (λ-completion program) · 1 HARD RULES · 2 METRIC DEFINITIONS (verbatim
  formulas) · 3 STATISTICAL TRAPS · 4 GRAVEYARD (7+ overlay families w/ costs) ·
  5 VALIDATED IMPROVEMENTS (with CIs + the lookahead caveat) · 6 ARCHITECTURE FACTS ·
  7 USER PROFILE & PROTOCOL · 8 POINTERS.
  Every metric formula, CI'd number, threshold, and lookahead scar preserved verbatim.
- `PROJECT_HISTORY.md` — **non-destructively** prepended an 11-era chronological arc header
  (≤60 lines) pointing to the detailed `## Source:` sections; the 262KB / 60-source archive
  body is **fully preserved** (5,411 → 5,428 lines). NOTE: this deviates from the original
  workflow design (which would have OVERWRITTEN the archive with a 60-line stub — that would
  have destroyed the only surviving copy of the deleted topic files). Caught and avoided.
- `README.md` — prepended a 2-line note: MEMORY.md is the condensed entry point; originals
  live in `archive/`.
- **All 8 originals archived** to `docs/memory/archive/*_pre-condense_2026-06-12.md` (archive-first,
  before any edit — the whole op is reversible).
- Left in place unchanged: `AGENT_FEEDBACK_RULES.md` (147KB), `USER_PERSONA_AND_PROTOCOL.md`
  (65KB — has known TRIPLICATED sections, flagged in MEMORY.md §8 as a dedupe candidate),
  `ce_methodology.md`, `ROADMAP.md` (obsolete pointer), `reference_fista_gpu_cv_step_bug.md`.

**2. `docs/daily/` navigation layer condensed:**
- `INDEX.md` rewritten **120KB → 24KB** (5× smaller): one TRUE ≤180-char line per session +
  a new `Tags` column for topic-grep, strict newest-first. Fixed two pre-existing defects:
  (a) the 2026-06-11 NMP-state row was stranded among the March rows (ordering bug);
  (b) **7 day files were never indexed at all** (2026-02-28, 2026-04-06-b, 2026-04-27_HANDOFF,
  2026-05-12, 2026-05-26, 2026-05-27, 2026-06-06, 2026-06-09) — read each and added accurate rows.
  Coverage now complete: 96 rows cover all 72 day files (multi-session days keep multiple rows).
- `TIMELINE.md` — NEW story-arc navigator (72 lines): 10 arcs (Foundations → blended-CNN →
  NT8 v1.0/zigzag → V2 EDA → regret-oracle → L5 build/SIM → THE LOOKAHEAD RECKONING →
  diagnostic suites → RL pivot → λ-completion), each with the pivotal days + key numbers,
  ending with a "Where decisions live" map.
- Old `INDEX.md` archived to `docs/daily/archive/INDEX_pre-condense_2026-06-12.md`.
- **The daily journal files themselves were NOT touched** — they remain the primary source.

### What files
- Wrote: `docs/memory/MEMORY.md`, `docs/daily/INDEX.md`, `docs/daily/TIMELINE.md`, this journal.
- Edited (prepend only): `docs/memory/PROJECT_HISTORY.md`, `docs/memory/README.md`.
- Created archives: `docs/memory/archive/` (8 files), `docs/daily/archive/` (1 file).
- No code touched. No trading path touched.

### What to look for next session
- Read `docs/memory/MEMORY.md` (now 96 lines) at session start — it should give the full
  picture fast. If anything load-bearing is missing, the verbatim original is in
  `docs/memory/archive/MEMORY_pre-condense_2026-06-12.md`.
- `docs/daily/TIMELINE.md` is the new way to orient on the project arc; `INDEX.md` to find a day.
- Open follow-up (optional, not done): dedupe the triplicated sections in
  `USER_PERSONA_AND_PROTOCOL.md` (would need a careful 3-way-identical confirmation first).

### Expected impact on metrics
None — this is a documentation/memory reorganization. Zero effect on any trading metric,
model, or live path. Pure findability/token-efficiency improvement for future sessions.

### Still pending (carried from prior sessions, unchanged)
- Stage-0 NMP-V2 forward pass (Jules build → review → user IS/OOS) per ROADMAP_LAMBDA_COMPLETION.md.
- KT1 oracle / KT2 nowcast kill-tests (need the segment corpus — VM in flight).


## Session Exit Report (14:41:01)

### 1. What Changed
Completed Phase 3 (Regime Extraction) and Phase 4 (Markov Transition Matrix). Mapped 80,717 MNQ geometric segments into 3,029 formal Regime Buckets using Degree Centrality clustering. Built a 3030x3030 Markov Transition Matrix to calculate the exact statistical probabilities of the MNQ jumping from one state to another.

### 2. Files Modified
- `research/phase3_extract_regimes.py`
- `research/phase3_plot_top_regimes.py`
- `research/phase4_transition_matrix.py`
- `artifacts/regime_buckets.json`
- `artifacts/transition_matrix.npy`

### 3. What to Look For Next Run
In the next run, the Bayesian state-space models can ingest `transition_matrix.npy` as the literal 'P' matrix for the Kalman filter, allowing the model to know what geometric shape is most likely to print before the first tick arrives.

### 4. Expected Impact on Metrics
Shatters Random Walk assumptions for the MNQ. Establishes mathematical proof of the 'Law of Inertia' (Regime 1 perpetuates 63% of the time) and the 'Chaos Resolution' (Unclassified Noise snaps into Regime 1 45% of the time).


## Session: USER_PERSONA_AND_PROTOCOL.md dedupe (follow-up to the condensation)

### What changed
Deduplicated `docs/memory/USER_PERSONA_AND_PROTOCOL.md`: **1,500 lines / 65KB → 454 lines / 20KB**.
The file held the same content ~3× in four blocks:
- Block A (1–467): "Consolidated" copy of all 7 topics — **mojibake-damaged throughout**
  (UTF-8 read as cp1252: `â€”`=—, `â†'`=→, `Ïƒ`=σ, `Î¼`=μ, `Â±`=±, `regresiÃ³n`).
- Block B (468–792): clean `## Source:` copies of 4 topics.
- Block C (793–1261): the file **embedded itself** (`## Source: USER_PERSONA_AND_PROTOCOL.md`)
  — a verbatim repeat of Block A, BOM and mojibake included.
- Block D (1262–1500): clean `## Source:` copies of the remaining 3 topics, plus a 4th copy of
  cognitive_style and an EMPTY collaboration_protocol stub (header only, no content).

### Method (lossless by construction, mechanically proven)
1. Read all 1,500 lines; mapped block boundaries from the full header map.
2. **Proof 1**: Python cp1252→utf-8 repair of Block A, then normalized equality vs every clean
   Source section → ALL 7 topics identical; C ≡ A exactly; D's cognitive ≡ B's; D's collab stub empty.
   (The single "unrepairable" character was the BOM on line 1.)
3. Rebuilt the file from the verbatim clean line ranges (B: cognitive/collab/headroom/level;
   D: schedule/specs/vp) in Block A's original order with A-style `## user_*.md` headers + a
   5-line dedupe note.
4. **Proof 2 (pre-write gate)**: assembled content ≡ repaired-Block-A under a second,
   independent normalization; 0 mojibake; all section headers accounted for. Write only on pass.
5. Post-write: 454 lines, 0 `## Source:` residue, 0 mojibake bytes, 7 topic headers exactly once.

### What files
- Rewrote: `docs/memory/USER_PERSONA_AND_PROTOCOL.md` (UTF-8, no BOM).
- Edited: `docs/memory/MEMORY.md` §8 pointer (removed "dedupe candidate" flag).
- Pre-dedupe original preserved verbatim at
  `docs/memory/archive/USER_PERSONA_AND_PROTOCOL_pre-condense_2026-06-12.md`.

### Expected impact on metrics
None — documentation only. ~45KB / ~11K tokens saved every time the persona file is loaded;
encoding now clean (σ/μ/→/— render correctly).


## Session: AUDIT of the segment-regime findings (phases 2–4)

### What was audited
The 14:41 exit-report claims: "80,717 segments → 3,029 Regime Buckets", "Law of Inertia
(Regime 1 perpetuates 63%)", "Chaos Resolution (Noise → Regime 1 45%)", "shatters Random
Walk", and the plan to feed `transition_matrix.npy` to a Kalman filter as a live prior.
Audited against the actual artifacts (stage2_year_segments.json, regime_buckets.json,
transition_matrix.npy, adjacency_matrix.pt 6.5GB). Tool: `research/audit_regime_findings.py`
(reusable). Report: `reports/findings/2026-06-12_segment_regime_audit.md`.

### Findings (full detail in the report)
1. **Pipeline is computationally FAITHFUL** — sweep diagonal == stage1's recorded
   max_residual/error_band to 2 segments of float jitter. The "seg 6" self-match concern
   was resolved. The CONCLUSIONS, however, are artifacts:
2. **Mega-bucket = error-band artifact.** Tier = max_res / TARGET's own band → loose-band
   targets match everything. Regime 1's root band = 11.075 = **99.9th percentile** (13.4×
   median); all top-6 roots ≥ 91.8th pctile; Spearman(degree, band) = +0.142, p=3.8e-15.
   "Regime 1" (56.4% of all segments) is a tolerance tube, not a market state. Bucket
   structure degenerate: median size 2, 97% of buckets <10 members.
3. **"Law of Inertia" ≈ base rate.** P(R1→R1)=62.9% vs base P(R1)=56.4% → lift +6.5pp
   (1.11×); +5.0pp on the honest contiguous timeline. Residual lift plausibly the KNOWN
   day-level volatility clustering (band ∝ vol). No random walk was shattered.
4. **"Chaos Resolution" is backwards.** P(NOISE→R1)=45.1% is BELOW base rate (−11.4pp,
   0.80×). And phase4's "NOISE" = 3,817 unbucketed-valid segments — the 31,572 PURE_CHAOS
   blocks aren't in the timeline at all.
5. **Timeline ≠ physical adjacency**: 51.8% of same-day consecutive valid pairs span a
   removed segment (median 15 bars); 341 cross-day pairs. (Recount barely moves numbers.)
6. **21.6% of the "valid" universe is tier-8 on its own data** — segmentation rule
   (>5 consecutive misses) and tier rule (max-residual ratio) are different metrics.
7. **FIREWALL**: the Kalman-prior plan as stated violates the SEGMENT FIREWALL (membership
   needs the completed segment + whole-day scaler). Label-side only until KT2 passes.

### Files
- NEW `research/audit_regime_findings.py`; NEW `reports/findings/2026-06-12_segment_regime_audit.md`.

### What to look for next run
If the bucketing is redone: symmetric band-independent similarity (or match within
volatility_tier), member×member coherence, PURE_CHAOS as an explicit Markov state,
transition probabilities reported as LIFT over destination base rate with day-block CIs.


## Session: F-space measurable-pattern diagnostic (user re-aim — "which indicators worth seeing")

### What was asked
Before fixing/rebuilding the buckets, answer the foundational question: does the F-space
follow a measurable selection pattern? Explicitly post-hoc/lookahead (whole-day scaler +
completed window) — the value is pruning the indicator set, not a live signal. Sidesteps the
broken similarity metric entirely (reads only the betas in stage2_year_segments.json).
Tool: `research/audit_fspace_pattern.py` (reusable). Report: `reports/findings/2026-06-12_fspace_pattern.md`.

### Finding — "yes, but weakly, and not the strong reading"
- **Stable VOL-INVARIANT vocabulary exists** (Jaccard top-10 across all 8 vol tiers = 0.82–1.0):
  the fast-TF reversion/extension family — `L3_5s/15s_{hurst,reversion_prob,z_high,z_low,z_se}`,
  `L2_{price_velocity,price_accel}`, `L1_{wick,body}`. **Converges on the NMP/λ feature family**
  (z_se, z_high/low, reversion_prob) — a post-hoc curve-fit reaches for the same quantities the
  a-priori NMP trigger uses. That convergence is the most valuable output.
- **Three caveats kill the strong reading:** (1) concentration barely beats null (Gini 0.602
  vs 0.545; most concentration is stage-1 `active_grid_cells` availability, not an elastic-net
  discovery — given availability, which survives is near-random); (2) 94.8% of surviving terms
  are QUADRATIC (15 terms from a ~350-term space on a 32-bar window = overfit signature);
  (3) beta signs ~coin-flip (ranks 1-2 sign-stability 0.51; best 0.67–0.76).
- **For "which indicators worth seeing":** WHICH = real/actionable (fast reversion/extension
  NMP family, vol-invariant; 15/177 never selected = prunable). HOW = NOT a simple
  per-indicator signal — interaction-heavy + sign-unstable → consistent with the 2026-05-03
  rule that the composite must be CONDITIONAL, not additive. A linear "watch indicator X" fails.
- Lookahead caveat stands: this prunes the feature set; it does NOT establish causal signal
  (needs causal nowcast + OOS = the KT2 gate).

### Files
- NEW `research/audit_fspace_pattern.py`, NEW `reports/findings/2026-06-12_fspace_pattern.md`.

### What to look for next run
The convergence on the NMP feature family is the bridge back to the main thread: the
Stage-0 NMP-V2 forward pass already targets z_se/z_high/z_low. The segment work independently
confirms those are the price-explaining features. Causal test (KT2) is the gate before any of
this changes a decision.


## Session: chaos precursor + DOE Pareto re-run (what to follow)

### Chaos precursor (research/analyze_chaos_precursors.py)
Contiguous-timeline, day-block-CI test of "what precedes CHAOS" (PURE_CHAOS = 28% un-fittable,
the regime the fit-vocabulary discards). Clean ORDERED result, base rate P(chaos)=28.1%:
PRISTINE prior -> 11.5% (lift 0.41x) / RECOVERED v3-5 -> 43% / v6-8 -> 51.5% / already-CHAOS
-> 72.2% (2.57x). Fittability DETERIORATION is a readable precursor; chaos clusters. Caveats:
(a) oracle-level (needs segment completion) not yet causal; (b) may re-express known vol
clustering / B10; (c) UNTESTED that chaos = where money is lost.
Range check: PURE_CHAOS is the BIGGEST-moving set (range med 14 vs PRISTINE 9.8), not chop;
net/range 0.54 like the rest. So chaos = big moves (opportunity), not whipsaw -- user correct.
Key precision: "un-fittable" = the segmenter's 15-beta polynomial, NOT the NMP rolling z_se
(always computable) -- so NMP keeps its z-reference in chaos.

### UNIFICATION (chaos <-> lambda-completion are the same problem)
Chaos = biggest moves. NMP takes a snap-back bet at the z-extreme. Whether it pays = does the
move revert (lambda<0, feast) or run (lambda>0, the equity burn). So: chaos = where the money
is (user) AND where fading a runaway burns (drawdown) -- reconciled by lambda. The chaos
precursor says WHEN you enter the lambda-critical regime; lambda-hat says WHICH bet. Decisive
test (pending leg-outcome source): split chaos-window P&L by lambda-hat sign.

### Gate is multi-axis (user insight): mean velocity + lambda-hat + vr
Regression-mean SLOPE (price_velocity) = trend NOW; mean FLATTENING = its derivative =
precursor of a turn. BOTH differ from lambda-hat (deviation expansion): price can ride a steep
mean at constant z (lambda-hat~0) yet be a strong trend -> only mean-velocity flags it. So the
snap-back/run gate is >=2D: mean-velocity (trend context) x lambda-hat/vr (deviation contracting?).
Mean-velocity flattening is CAUSAL (price_velocity_w is a live V2 feature) -> the deployable
chaos early-warning candidate.

### DOE standardized-effects Pareto re-run (research/plot_pareto_effects.py)
Prior minitab_pareto_effect.png was PARTIAL (globbed stage2_segments_*.json, missed the
consolidated stage2_year_segments.json). Re-ran on full 80,717 valid. AVG-|beta| ranking is
ARTIFACT-PRONE: top = L0_time_of_day (constant-within-a-2.5min-segment -> intercept absorber) +
rare high-coef terms (n<400/80,717 = high-lift-small-n trap). Robust ranking (by TOTAL
contribution, ex-time_of_day, count>=500) = the 2-axis gate, data-ranked:
  - snap-back trigger: 1m z_se_15 / z_high_15 / z_low_15 / hurst_15 + their interactions (NMP family)
  - run/trend context: 4h price_velocity / price_accel / vol_velocity, 1h vol_sigma (macro mean velocity)
THIRD independent method (after F-space concentration + chaos precursor) converging on
{1m z-family} x {macro velocity/vol}. Still post-hoc (lookahead) = strong feature-selection
PRIOR for the causal gate, not proven causal. Artifacts: pareto_effects_full.csv (4,866 terms).

### User rules added 2026-06-12
- Plain-language: avoid "fade"/"reversal" -> "snap-back bet"/"run bet"/"the turn" (MEMORY.md S7).
- Methodology: shape-bucketing (3,000 regimes) PARKED as a detour; the decision is 1-D (lambda
  sign), so two buckets (snap-back-worked vs run-burned) not three thousand.

### NMP forward pass + stratified-lambda KT1-lite test (spilled into 2026-06-13)
User: zigzag != NMP, run the NMP forward pass; and compute lambda "as a function of the
regressions" because the prior global lambda came back FLAT (= many feature clusters at
different intervals canceling; the segments were built to stratify that).
- NMP forward pass IS RUNNABLE NOW: `python -m training.run_strategy --strategy nmp_fade_raw
  --target {is,oos} [--analyze]`. Built/registered, loads L0-L4 (L4 has lambda_hat_{12,21,30}
  /lambda_t/vr_exact materialized), logs entry lambda per trade. GAP: nmp_fade_raw uses the OLD
  z-band [1.5,1.8], NOT the recalibrated Z*=1.8481/0.4752; the faithful port nmp_v2
  (JULES_NMP_V2_FORWARD_PASS.md) is SPEC-ONLY, not built.
- Stage-0 trades already on disk: reports/findings/strategy_runs/nmp_fade_raw_{is,oos}_atr4.csv
  (16,676 IS / 3,082 OOS), cols incl. extra_entry_lambda_t/hat (k=12), extra_z_se,
  extra_reversion_prob, pnl_usd. Base mean pnl NEGATIVE: -$4.95/trade IS, -$4.91 OOS.
- KT1-lite (research/analyze_nmp_lambda_stratified.py): does entry lambda separate winners
  from losers WITHIN strata (z_se, reversion_prob, dir), thesis lambda<0 (snap-back underway)
  out-earns lambda>=0 (runaway)? RESULT = WELL-POWERED NEGATIVE.
  - IS (16k, powered): FLAT everywhere. Global delta +$0.23 CI[-0.38,+0.83]; 0/strata sig;
    effect <= $1/trade = ~nothing. Stratifying did NOT de-flatten lambda.
  - OOS (3k): one cell |z_se|Q3 / rev_prob Q2 (same ~769 trades) +$4.47 CI[+0.34,+8.90] SIG,
    but = 1 hit / ~7 independent strata x 2 datasets, NOT replicated on IS -> multiple-comparisons
    false positive, not signal.
  - Only consistent thread: SIGN matches thesis (most-positive-lambda quintile worst on both
    IS -$5.52 / OOS -$7.11; lambda<0 consistently less-bad) but magnitude tiny, sub-sig.
  - Even best cohort still loses (base rate negative). lambda isn't rescuing nmp_fade_raw.
- CAVEAT (what is NOT killed): tested a PROXY stratifier (local z_se/reversion_prob), NOT the
  user's actual one (segment-cluster) or the DOE-ranked CAUSAL one (macro 4h/1h price_velocity).
  lambda logged k=12 only (noisiest); k=21/30 in L4 untested. Trigger = nmp_fade_raw not nmp_v2.
- DECISION (discipline flag: one step from fishing): take EXACTLY ONE more pre-committed,
  OOS-confirmed shot at the actual stratifier -- join trades->V2 features at entry, pull
  lambda_hat_21/30 + 4h/1h price_velocity + vol_sigma, stratify by macro-velocity regime. If
  flat too -> trade-level lambda-separation is DEAD; pivot (day-level lambda sizing / KT1
  segment-oracle / accept floor needs a different lever). Tool: analyze_nmp_lambda_stratified.py.

### DATA: 2024 dataset acquired (2026-06-13)
User obtained the 2024 dataset. Expands the data landscape beyond the current IS=2025
(DATA/ATLAS, Databento) / OOS=2026 (DATA/ATLAS_NT8, NT8). Potential uses: more IS history, an
additional sealed validation period, or larger segment/NMP corpora. TODO (not yet done):
confirm format/source + location, convert/build V2 features (core_v2/build_dataset.py) if
needed, decide its role in the IS/OOS split (keep the 2026 NT8 OOS sealed; 2024 likely extends
IS or becomes a second validation block). Flagged for integration; no code touched yet.
