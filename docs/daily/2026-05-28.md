# Session Exit Report: 2026-05-28

Reviewed the large pending changeset, committed + pushed the safe subset, and
audited the resulting system state for documentation. Headline: the working
tree had been carrying a **major architecture pivot** (supervised CNN → RL) plus
a **live Telegram bot token in plaintext** and **11 GB of binary training
artifacts**. Committed the source/docs safely; flagged a real regression
(dangling imports of deleted modules).

## 1. What Changed
- **Committed + pushed `4b658e2a`** to `origin/main` (164 source/doc files;
  `0c001c1f..4b658e2a`). Contains the RL-engine pivot, the `training/`
  reorganization, the `core_v2/build_dataset.py` move, the trade_outcome_suite
  analysis scripts, `AGENTS.ini`, `rl_whitepaper.md`, and the 05-26/05-27
  journals.
- **Scrubbed a leaked credential**: the Telegram bot token + chat ID were
  hardcoded in `telegram_mcp.py`, `telegram_bridge.py`, `telegram_listener.py`,
  `push_alert.py`. Refactored all four to read `TELEGRAM_BOT_TOKEN` /
  `TELEGRAM_CHAT_ID` from environment variables. The token was **never
  committed** (confirmed clean history) — nothing leaked to the remote.
  **ACTION FOR USER: rotate the bot token via @BotFather** — it sat in plaintext
  on a OneDrive-synced folder, so treat it as exposed.
- **Gitignored regenerable / large artifacts**: `training/rl_engine/*.h5`
  (8.7 GB + 2.3 GB experience buffers), `*.pth` epoch checkpoints, `logs_*.txt`,
  `scratch/`, `telegram_inbox.txt`. The `.h5` buffers alone exceed GitHub's
  100 MB hard limit — committing them would have failed the push.

## 2. What Files
- `.gitignore` — added RL-artifact / scratch / inbox rules.
- `telegram_{mcp,bridge,listener}.py`, `push_alert.py` — token → env var.
- Commit `4b658e2a` — full file list in `git show --stat 4b658e2a`.

## 3. Audit Findings (for doc updates)
- **Architecture pivot**: the supervised CNN/blended/nightmare stack was
  DELETED (`cnn_*`, `nightmare*`, `physics_labels`, `compute_features`, `ai`,
  `model`, `memory`, `feature_processor`) and replaced by an RL engine —
  "Parallel Worlds Curriculum RL" (CNN+LSTM DQN, V-trace, hindsight-regret
  shadow queue, curriculum `EXIT_NMP→ENTRY_NMP→YOLO`, 8-agent DOE). New:
  `training/rl_engine/`, `training/trainers/`, `training/utils/` (relocated
  shared helpers), `training/regret/` (now a package), `training/strategies/
  zigzag.py`. Doc: `rl_whitepaper.md`.
- **REGRESSION — dangling imports of deleted modules.** The deletions broke
  ~15 still-active importers. The `regret` references are FINE (package
  re-exports resolve). Broken:
  - `live/live_engine.py` → `nightmare_blended`, `compute_features` (NT8
    blended orchestrator won't import)
  - `live/maintenance.py`, `live/diagnostic_run.py` → `compute_features`
  - `training/run.py` → `nightmare`, `nightmare_blended`, `ai`, `physics_labels`
    (nmp/ai/blended/label subcommands broken)
  - `tools/util/{hypothesis_test,blended_test}.py`,
    `tools/archive/lookahead_impact.py`, `tools/exits/giveback_analysis.py`,
    `tools/eda/sunday_hourly_eda.py`, `tools/data/validate_sfe_parity.py`
  - `tests/test_engine_evaluate.py`, `tests/test_sim_executor.py`
- **Doc drift**: `CLAUDE.md` (Key Files lists deleted `cnn_trade_manager.py`,
  `cnn_entry_direction.py`; Active Work describes the blended/CNN pipeline),
  the in-repo `docs/memory/MEMORY.md`, and `AGENTS.ini` (`status = production
  blended pipeline`) all predate the RL pivot.

## 4. What to Look For / Open Question
- **Is the blended/nightmare layer intentionally retired** (so the dangling
  imports are acceptable transitional debt and `live_engine.py` is superseded
  by `engine_v2.py`), **or is the live path supposed to still work** (in which
  case the deleted modules need to be restored or the importers rewired)? This
  gates the CLAUDE.md / AGENTS.ini / MEMORY.md rewrites — pending user decision.
- The RL engine is mid-training (curriculum segment 10, overfit interventions
  per 05-27); it is NOT yet a deployed production path.

## 5. Expected Impact on Metrics
- None directly — this session was repo hygiene + audit, no strategy/feature
  logic changed. The dangling imports do not affect any already-running process
  but will ImportError on the listed entry points until rewired.
