# 2026-07-18 — ExNMP archaeology → NMP9 in the dossier; wrong-dir dojo; Mamba spec draft

## Lineage recovery (Moises-directed archaeology)
- **Exit-decay lineage** (`research/exit_lineage_recovered/`, 1de05a56): genesis
  2026-01-31 `bayesian_brain.py` (HashMap StateVector→WinRate — the earliest
  training model) + `wave_rider.py` (high-water-mark trailing exit) → regret
  2026-02 → decay_sim 2026-05 (two-rule rail strategy, v4 capture 4% = the
  physics-era near-wash exit verdict). The idea the whole program still chases.
- **ExNMP lineage** (`research/exnmp_lineage_recovered/`, 2ad09f1d): term born
  04-05; earliest code 04-06 = the Trio (killshot/wick-overshoot/cascade) →
  BlendedEngine same day → 9-tier ladder 04-08.
- **9-tier extraction** (5493acc1 + corrections 7dfe117d/5375baf6): full
  waterfall + constants + per-tier exit physics. CORRECTED history: ladder ran
  CNN-FREE from 04-10 (7 effective physics tiers); ran LIVE — the 2026-04-16
  **$900-peak-full-giveback trade** (journal receipts) = founding trauma of the
  exit/capture program; native NT8 port recovered (BaseNmpRunner_v1.0-RC.cs).
  Derivation = the **Shainin Red X process** (Moises): good-vs-bad contrasts →
  tree fracture → wick (.83/.77) → 1h amplification → velocity 50/100 split;
  04-09 verdict table (FADEAGAINST poison / RIDEAGAINST surprise winner)
  independently confirmed by the 2026-07 league family split.

## NMP9 — the original ladder, ported plain + retuned (docs 101/102)
- **Verbatim port** (800fb5cd): 9 NMP9-* streams, λ̂ in the CNN's head seat
  (labeled completion). FINDINGS: **V1 FADEAGN was direction-INVERTED vs the
  original** (original follows the 1h z, aligned 0.758; NMPT kept fade, 0.412);
  FADEMOM≠FADECALM (V1 absorption was a real loss); λ̂ selective value +0.070
  calm / +0.016 momentum; combiner +0.004 (0.672→0.676 same-pool; 0.689 was a
  stale 38-stream anchor). Ride family aligned 0.76-0.85, pure fade 0.17-0.29.
- **Quantile retune** (0f748e83): era-anchored 2024-only match (wick pair was
  3× too stringent — the CASCADE starvation driver). VERDICT: **structure
  threshold-robust, retune immaterial** — fires re-centered, every verdict CI-
  stable, combiner +0.0006. No Shainin re-derivation needed.
- **Folded into the canonical dossier** (03b50181): league_merge auto-discovers
  all rows parquets; dossier_signal_league.md = 49 streams incl. 8 NMP9.

## Wrong-direction dojo (doc 099; verdict 100 pending)
Economic re-cut (WRONG=ends≤−4pts, GOOD=ends≥+4pts, 50 dipped/50 clean goods),
100/100 blind on 200 distinct days; gate+nonce sandbox; dumb-stop ROC = the
bar. Fleet interrupted twice by Claude Code host restarts (lesson: background
children DIE on restart — relaunch fleet/bridge/drones after any restart; the
2nd restart killed the launcher mid-run); resume-safe relaunches; scoring after
the final 42 land.

## Mamba production spec DRAFT (a95b815a)
`research/mamba_zigzag_baseline/PRODUCTION_RUN_SPEC.md`: ride-winner objective
(per 098 — no turn-timing promises), FIXED2 wiring, ticks ruling (policy reads
price; noise = separate channels; σ-divisions out), state additions (leg
geometry, dojo grammar channels, NMP9 tier one-hot, c_t), curriculum gates
(gap watch epoch ~5), §6 wrong-dir cut pending doc 100.

## Ops
Telegram bridge live (dedup after restart chaos — 3 pollers were fighting);
phone watcher pattern established; everything through 03b50181 pushed.

## Wrong-Direction Dojo VERDICT (doc 100) — dumb stop wins net; veto is the skill
198 scored (nonce PASS all). PRE-REGISTERED BAR: FAIL — agents catch 95% of
losers, +7.5 t/ep net, but the dumb 24-tick stop nets +17.7. The knife on
dipped goods (54% false-bail; single misses -454/-630/-795t) erases the catch
edge; clean goods held at 10% false-bail = the discrimination EXISTS, the
pricing was wrong. Lane "LLM bail beats a stop" CLOSED (consistent w/ 098 +
stop graveyard). Survives: bail grammar (adverse 88%, against-fires 63%,
giveback 61%, loss-ACCEL 38%) = hypothesis for the sealed harness. Mamba spec
S6 RESOLVED: cut-head baseline = dumb stop @24t; the net learns ONE thing —
veto the stop on recoverable dips. Both dojos now closed with honest nulls +
one narrow learnable margin each (ride winners longer; veto dips).

## Night program: cut-losers mechanisms (docs 103/105/106)
Charter (Moises): cut losers fast / ride winners, WITHOUT Mamba (GPU cost).
- **103 stop+re-entry (his counter-proposal): FAIL unconditional** (-6 t/ep vs
  plain stop) BUT mechanism confirmed: repairs the dipped-good knife +48.4/ep
  exactly as designed; gives -35/ep back re-buying bouncing losers. Needs a
  discriminator gate.
- **105 distilled veto logistic: FAIL with a GOLD diagnostic** — path-only
  features have ZERO test discrimination at the trigger (precision 30% = base
  rate 30%; frozen p* collapsed to always-veto on the 2024->2025 regime flip).
  The 10-vs-54pct discrimination the blind agents showed must live in NON-path
  channels (fires/z/tier). AND: even the plain stop's +17.7 is CI[-12.4,+46.7]
  NOT SIG at N=198 — the whole cut frontier was under-powered.
- **106 POWERED frontier (in flight): the fix** — mechanical policies cost
  nothing to evaluate; rerun the full frozen frontier on ALL ~20k+ test
  engagements at NATURAL class mix, day-block CIs ~7x tighter. Answers: does
  ANY cut beat never-bail at scale?
- Memory FTS augmentation live (104, segments-only; top recurring-correction
  surfaced: the Order Flow day-over-day flip).

## DAWN SYNTHESIS (doc 107) — cut side CLOSED; ride is the program
Powered frontier (106): N=23,378 natural mix, 282 days — NO cut policy beats
never-bail (all stops negative; doc-100 +17.7 was a 1:1 balance artifact; the
dipped-good bleed -212..-278 outweighs wrong-pay on the real tape, dipped =
25pct of tape). Law: losers cut themselves at this entry quality. Spec S6
re-resolved: cut-head NONE, exit head = ride-length only -> smaller cheaper
Mamba curriculum. Standing bar for future cut ideas: beat never-bail on
powered_cut_frontier.py. Night ledger: re-entry FAIL(-6)/veto FAIL(zero
discrim)/powered frontier decisive. Docs 103/105/106/107.

## Dojo Forge Harness F1 (doc 108)
- **Execution:** Built esearch/dojo_forge/pipeline/forge_harness.py\. Configured \InProcessGate\ for nonce tracking. llama-cpp-python CUDA compilation failed in WSL. Activated the Ollama HTTP API fallback on Windows python3.11 for \gemma4:e2b\.
- **Verdict:** F1 gate is complete. Transcripts and state logged. Prefix caching unmeasurable via the fallback API. Awaiting Claude review. Report 109 generated.

## Distillation swarm + archive pass (docs 119/120)
27 research topics carded by a 25-drone swarm (Sonnets + scoped Opus); all
DISTILLED.md cards committed + indexed in shared FTS (context 27->54).
19 dead topics moved to research/archive/ after orphan greps. KEEP-LIVE: the
spine + ai_auto_labeler (ground-truth regeneration path, 39 dependents) +
4 open lines + lineage folders. Salvage flag: Footprint Imbalance filter
(+$17.23/day CI[12.27,22.99], sole positive-CI survivor) -> spin-out
candidate. Root strays run_sweeps*.sh archived with leg_clock.

## Footprint Imbalance spin-out: REJECTED at extraction (doc 121)
The archive sweep's one money candidate died honestly: the June +$17.23/day
was a missing-data dilution artifact (delta data on only 114/536 sweep days;
79pct structural zeros compressed the CI), reads the graveyard-rejected
order-flow feed, and cannot be sealed (no 2024 data). Card updated. The
"don't enter fails" thesis test still awaits a candidate on AVAILABLE data.

## Entry-fail Red X verdict (docs 122/125) — the lane closes honestly
THE finding: entry P is ANTI-predictive on terminal economics (AUC 0.4961) —
Moises diagnosis fully vindicated. But no cure at the snapshot: full model
0.5135, +2.1pp good-rate at -65pct volume; ~50pct fail regime not escapable
at entry. lambda-hat FLAT (0.500) on terminal outcomes; thin signal = nmp9
tier + det identity (CASCADE goods 0.247, small-N caveat). Third appearance
of the law: terminal outcomes are not knowable at entry -> fail problem
routes to the ride/path side. Graveyard entry added.


## Session Exit Report (14:46:21)

### 1. What Changed
Prepared Phase 0 of the NT8 Native Port B. I wrote a python script (find_top_k.py) to extract the top 22 streams from the dossier combiner logistic model that comprise >80% of the absolute sum of coefficients. These 2024-sealed model parameters were saved to reports/top_k_streams.txt to serve as the fixed parameters for generating the golden parity vectors and eventual C# entry port. Then, I wrote an interim handoff comm to Claude so another agent can continue the execution of the golden vectors.

### 2. Files Modified
- `research/nt8_port/tools/find_top_k.py`
- `research/nt8_port/reports/top_k_streams.txt`
- `research/nt8_catalog/comms/130_2026-07-18_AG_HANDOFF_TO_CLAUDE.md`

### 3. What to Look For Next Run
In the next run, Claude or the subsequent agent needs to write `generate_golden_vectors.py` utilizing the top-K stream names and coefficients to build the 1m-bar golden parity vectors for 20 days. Ensure that exactly the 1m boundaries are sampled.

### 4. Expected Impact on Metrics
No expected impact on statistical metrics, as this is purely preparation for building parity vectors.

## NT8 Native Port (B, Moises-confirmed): P0+P1 in one day (docs 129/131)
Branch cleanup done (main only; 4 archive tags). P0 golden vectors: 20 days,
byte-deterministic, top-K=22 double-derived (drone+AG independent agreement);
combiner proven DIFFUSE (top stream 11pct; no shortcut). P1 C# port: ALL 3
pre-registered bars PASS - fires 99.962pct, P at machine epsilon, entries
100.000pct exact; 21/22 streams bit-identical; TMPL0 residual (0.038pct) =
undefined same-ts tie rule in golden itself (pandas unstable sort), P2 pins
it. dotnet 10 harness, platform-neutral. P2 = NinjaScript packaging +
native zigzag + tie-rule pin.

## P2 COMPLETE — perfect parity + the first strategy draft (doc 133)
Tie rule pinned both sides -> TMPL0 100.000pct and the WHOLE ensemble now
178,640/178,640 exact (P1s residual was the golden generators own ambiguity,
as diagnosed). Native zigzag/R-trigger: bit-exact (legs, confirms, pivot
prices 0.0 diff); scalar-R note settled (shared open-anchored knob).
docs/nt8/7-EnsembleRunner_v0.1-RC.cs drafted (ride-only, cat-stop param,
15:55 flatten, native z_se confirmed portable; 14 numbered TODOs for the NT8
compile loop - chief: stream-port the 22 generators FROM the parity-proven
harness). NOTHING deployed; deploy gate intact. Next: Moises compile loop
(P2 TODOs) -> P3 SIM parity.

## NT8 Strategies cleanup (docs 132/134/135)
AG cleaned the TOP level (5 @Sample files, SHA-verified archive, good evidence
format) but missed that NT8 compiles Strategies\ RECURSIVELY: the archive\
subdir still held 13 real strategies in the compile path. Reviewer completed:
all 13 -> docs/archive/NT8/machine_archive_2026-07-18/ (SHA 13/13 OK; needed
git add -f, docs/archive is gitignored), then removed from the machine.
MATERIAL FIND: ZigzagRunner_v1.0.8-RC.cs (66KB) existed NOWHERE in the repo -
the machine copy was the only copy; now captured. bin\Custom\Strategies is
now truly empty (0 items recursive). EnsembleRunner deploy still gated on
per-revision approval. Verdict 135 filed (134 = PARTIAL).
