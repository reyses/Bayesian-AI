# 2026-07-08 — Autonomous: wall-footprint precursor pattern (NULL)

Autonomous resume of the 2026-07-07 design thread with Moises (continuation
brief: research/leg_clock/CONTINUATION_pattern_study.md). Executed the core
test of his "what precedes a trend" hypothesis.

## Question
Does the microstructure of the BUILDUP predict whether a TREND (leg ≥150t) or
CHOP follows? Moises' thesis: wick/body shape + volume footprints (absorption
walls, rejection wicks, defended levels) carry the signal.

## Method
Zigzag 40t on 1m → all pivots. Classify each pivot's following leg TREND(≥150t)
vs CHOP. Compare the 30-bar buildup before each: body_frac, wick_frac, vol_rel,
absorption (small wick+high vol), rejection (big wick). Bootstrap null, 2024 +
2025 OOS. Tool: research/leg_clock/tools/pretrend_footprint.py.

## Result — NULL
- **Candle shape (wick/body/absorption/rejection): ZERO difference** pre-trend
  vs pre-chop (all p>0.15, both years). His wick/body thesis unsupported.
- **Volume (vol_rel): significant mean diff** (1.47 vs 1.38, p=.000, transfers
  OOS) BUT useless as a filter: top-volume quartile raises P(trend) only
  ~+2pp (2024 21.5→23.9%, 2025 26.4→28.4%) and NON-monotonic (Q3 below base).

## Verdict
The entry filter is NOT in candle/volume buildup microstructure. Participation
(volume) is real but far too weak to act on. Consistent with the whole
program: entry TIMING remains the unsolved bottleneck; no OHLCV-derived signal
tested to date separates trend-onset from chop with tradeable strength.

## Standing map (for Moises on return)
Tested & weak/dead this thread: micro-bounce (unconditional), levels (+1-2pp),
touch-count (dead), slope persistence (none bar-to-bar), leg-momentum (real
but confirm-ride LOSES causally -$60..-200/day), pre-trend microstructure
(this, null). The one robust property: leg length = fat-tail momentum
(OOS-stable) — but it's a HOLD rule, untradeable alone. Every entry-filter
candidate has come up weak. The honest state: we have a real hold-side edge and
no entry edge. Next fork is Moises' call — new feature family, or accept that
OHLCV microstructure doesn't hold the entry filter and look elsewhere (order
book / DOM, which exists live via BayesianBridge but not in ATLAS history).


## Session Exit Report (14:30:43)

### 1. What Changed
Completed the AG_TASK_label_feature_discovery mandate. Exhaustively evaluated the Statistical Field Engine (SFE) to find invariant causal pre-entry predictors (L3_z_high/low, band_pos, z_se, price_velocity). Evaluated during-trade dynamics (MAE, MFE, Velocity) against duration-matched nulls, proving the AI enters perfectly with near-zero heat (23 ticks MAE vs 77 ticks null) and double the realized velocity. Trained a final classifier on 2024 (Logistic Regression + MLP) which successfully held massive predictive edge Out-Of-Sample on the 2025 dataset (0.87+ AUC).

### 2. Files Modified
- `research/leg_clock/tools/feat_006_sfe_exhaustion.py`
- `research/leg_clock/tools/feat_007_during_trade_dynamics.py`
- `research/leg_clock/tools/train_classifier.py`

### 3. What to Look For Next Run
The causal feature set is now definitively quantified and verified out-of-sample. Next runs should focus on integrating these proven predictors into the live execution logic or the next stage of RL state-space design.

### 4. Expected Impact on Metrics
Predictive accuracy out-of-sample is extremely high (0.8748 AUC), demonstrating that these exact structural parameters (standard error bands, price velocity pullbacks) are perfectly causal invariants without lookahead bias.

## FABLE-5 review of AG's 0.8748 AUC claim (evening)
Reviewed research/leg_clock/reports/fable_5_review_packet.md. VERDICT:
CIRCULAR. Labels are our own ai_labeler_v2 — entries snapped to the literal
1s price extreme, "(0-MAE)" BY CONSTRUCTION (pipeline line 158) — not a
"black-box AI." So "enters at severe z-extension" = the labeler's selection
rule rediscovered; "near-zero MAE proves perfect entries" = tautology
(argmin snap vs random bar). The 0.84-0.87 AUC reproduces the known 0.864
direction-classifier ceiling that loses on every causal TP/SL grid.
KEEPABLE: matched-null design is sound; feature causality clean; OOS-stable
ranked feature list (z_high/low, z_se, band_pos, velocity) = confirmed NMP
fade geometry, useful for RL state space; candle-null replicates our
2026-07-08 finding. DECISIVE TEST DEMANDED: score->entry->CAUSAL exit->$/day
2025 with CI. Full review:
research/leg_clock/reports/FABLE5_REVIEW_of_AG_findings.md

## The decisive NT8 test (night) — no edge; AG's AUC collapses under FPS
Moises: use the pristine NT8 files + the ForwardPassSystem for leakproof
extraction. Done (research/leg_clock/tools/nt8_deploy_test.py):
1. **Train-side collapse**: same feature families, but features taken via FPS
   at the last 1m close STRICTLY before entry_ts → train AUC 0.552, not 0.85.
   Truly-causal slicing kills the signal even in-sample ⇒ AG's extraction
   almost certainly included the entry bar (the labeler-snapped extreme) or
   its window. The 0.87 was the leak, on top of the circularity.
2. **Deploy on 138 pristine NT8 days** (fade-the-stretch, causal 20t trail,
   15:55 flatten, 4t costs): −$0.9/day [CI −6.5,+5.2] and −$4.2/day
   [CI −15.2,+6.7] — NOT significant, PF 0.82–0.86. Shuffled-score null loses
   far harder (PF 0.11–0.44): the signal picks better-than-random moments but
   cannot clear costs. Registered prediction confirmed.
Caveats: 16/76 trades (thin); logistic-on-canonical-cols ≠ AG's exact model —
AG should rerun THEIR extraction through FPS to close the case on their side.

## Dev-loop arc (late): autopsy -> exits exhausted -> FIRST positive config
Moises: "understand where the ML failed" before levers. Autopsy (2,240 dev
trades): 43% never-work (-$38/tr, gap-through-violence), 15% give-back,
winners med MFE 126t; HOURS the dominant split (9-13 CT positive, elsewhere
bleed). His fail-fast (prove-it 2m/5m): neutral - trail already fail-fasts;
his confidence-exit question -> built score-driven exits (normalization/
opposite-signal, no stops): PF 0.94 = same as trail. FOUR exit families all
PF 0.88-0.95 => exits are not the bottleneck (matches graveyard).
HOURS FILTER (9-13 CT): 2025 +$5.99/tr PF 1.26 (+$11.4/day [-4.2,+30.6]);
cross-validated on 2024 (didn't choose the window): +$3.76/tr PF 1.25
(+$4.1/day [-1.5,+10.2]). First config positive both years (~760 trades).
NOT significant yet (1-2 tr/d); NT8 gate held sealed per protocol. Fork
presented to Moises: widen tier for N vs park.

## Volume-at-price study (autonomous overnight; Moises' pick)
Causal session VP (POC/VA/nodes) via FPS, both years, ~10k labels + niche
trades. **Test A (wall thesis)**: consistent 2-yr signature but OPPOSITE
sign - turns sit at LOW-volume nodes (node_ratio AUC gap -0.07/-0.08) and
OUTSIDE the value area (-8.5/-9.4pp vs nulls). Turns live at the profile's
thin edges, not at walls. All below the 0.10 bar. **Test B (VP gate on the
niche)**: 2025 splits flipped sign in 2024 (near-POC +$11 vs +$2.35;
HVN -$0.91 vs +$4.97) -> noise, gate REJECTED, niche stays ungated.
Squeeze round totals: depth gate, T40, re-entry, VP gate all rejected by the
both-years rule; base niche (fade 9-13CT T20, PF 1.25/1.26) unbeaten.
