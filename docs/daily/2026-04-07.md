# 2026-04-07 — CNN Flip Integrated + Stage 2 Architecture

## CNN Results
- Entry-only CNN: **70.6% accuracy** (tree was 55.5%, path CNN was 67.3%)
- Path data ADDS NOISE — entry-only is better
- The 6×13 TF grid captures cross-TF patterns at entry that the tree can't see

## Blended + CNN Results
| | IS | OOS |
|---|---|---|
| $/day | **$367** | **$397** |
| $/trade | $8.2 | $9.1 |
| Win days | 92% | **85%** |
| Losing days | 22 (avg -$58) | 4 (avg -$18) |

BASE_NMP: $0.30/trade → **$7.90/trade** with CNN flip. 26x improvement.

## CNN Flip Math (verified per-trade)
```
RAW (no filter):  $9,770   ($0.8/trade)
SKIP counter:     $61,521  
FLIP counter:     $113,272 ($9.1/trade) — 11.4x better than raw
```

## Dead Ends
- Tree flip predictor: 55.5% — tree exhausted for direction
- Tree duration predictor: 64.4% — time_of_day dominates, 79D adds nothing
- 3-point path + tree: 56.0% — path features don't help tree
- 5-point curve + tree: 53.8% — MORE features = MORE noise for tree
- Regime flip exit (vr>1.0): 14% WR, -$19.9/trade — poison
- Regime shift early (DMI+VR): 23% WR — still bad
- Flip on regime shift: 38% WR — breakeven, not worth complexity
- Energy exit at 5s: too noisy (1 bar = noise)
- Energy exit at 1m: too aggressive (exits everything)

## Key Discovery: CNN > Tree for Multi-Feature Patterns
The tree splits on ONE feature at a time. The CNN convolves across the full 6×13 grid.
"z extreme + wick + 1h aligned" is ONE spatial pattern to the CNN but THREE independent
splits for the tree. This is why CNN gets 70.6% where tree gets 55.5%.

## Stage 2 Architecture (next session)
Not a TRADE/SKIP classifier. A physics discovery engine:

1. Run blended+CNN → trades
2. Regret on those trades → corrected trades (optimal entry/exit)
3. Extract 79D at optimal entry points (earlier entries, different physics)
4. CNN Stage 2 learns: what 79D patterns at these optimal entries predict profit?
5. Cluster new entries → find unique physics that generalize
6. Test each cluster on full IS → only generalizing physics survive
7. Surviving clusters → new ExNMP strategies → expand blended engine
8. Repeat

This is the real epoch learning — discovering new physics, not tweaking exit bars.

## Risk Assessment
- Worst IS day: -$341 (less than 1 avg winning day of $403)
- Worst OOS day: -$38
- At $500 equity: survivable. No wipeout risk from single day.
- 22 losing IS days avg -$58 — tiny compared to 255 winning days avg $403

## CNN Distillation — Readable Rules
Distilled CNN into decision tree (58.8% agreement). What the CNN learned:
1. **15m_wick_ratio** is the primary split (same as kill shot discovery)
2. **Multi-TF wick alignment** (15m + 5m + 1h) — resonance cascade
3. **1D_velocity** — daily momentum context (from v2 builder fix!)
4. **1h_dmi_gap** — hourly trend strength
The 41% the tree CAN'T express = non-linear cross-TF combinations. That's the CNN's unique value.

## Process Rules Added
- Update journals continuously as discoveries/dead ends emerge (not just session end)
- Read time/date before suggesting session end

## CNN Hold Predictor — Real-Time Regret for Exits
- **94.8% accuracy** (5-fold CV, consistent 94.4-95.0%)
- HOLD accuracy: 98.9% — almost never exits too early
- EXIT accuracy: 69.6% — correctly identifies 70% of optimal exit bars
- Trained on 124,245 samples from trade paths (10 bars sampled per trade)
- Current p_center exit: 4 min hold, $2.50/trade
- CNN hold says "keep holding" through the p_center trigger → captures more
- Dataset imbalanced (86% HOLD) but EXIT accuracy is the real signal

## Architecture Update
Two CNNs working together:
1. **CNN Flip** (70.6%): at entry → which direction?
2. **CNN Hold** (94.8%): during trade → keep holding or exit now?
Both trained on regret's answers, predict from current 79D physics only.

## Key Insight
Regret is the perfect teacher. CNN is the real-time student. Skip trees/rules/books —
train the CNN to mimic regret directly. Would have saved 3 days if done from start.

## Next Session
1. Run regret on CNN-flipped blended trades
2. Extract 79D at optimal entry points from corrected trades
3. Train Stage 2: discover new entry physics from regret corrections
4. Cluster → validate → expand ExNMP roster
5. Epoch strengthening of CNN

## Files
- `nn_v2/cnn_flip.py` — CNN flip predictor (entry-only, 70.6%)
- `nn_v2/nightmare_blended.py` — integrated CNN flip for BASE_NMP
- `nn_v2/tree_flip.py` — tree flip predictor (55.5%, exhausted)
