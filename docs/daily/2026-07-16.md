# 2026-07-16

## Disk cleanup (post 0-bytes-free incident, Moises-approved)
C: hit 0 bytes free during the 07-15 autonomous night (crashed the signal pipeline
mid-run). Full project sweep found repo = 58.6 GB. Deleted (all regenerable,
OneDrive 30-day recycle):
- training/rl_engine/experiences_FULL_TRAIN_{EXIT,ENTRY}_NMP.h5 (10.9 GB, May 27
  pre-Composite-Brain buffers; regenerate via experience collection)
- artifacts/{adjacency_matrix,sweep_cache_flat,sweep_cache_part1,part2}.pt (9.7 GB,
  Jun 12 regression-segments caches)
- ATLAS regenerated builds (2.8 GB): 14x FEATURES_RUN_* (fspace_cadence),
  ML_CHECKPOINTS (misnamed fspace_ml DOE outputs; regen via
  tools/fspace_ml/ml_extraction_pipeline.py), FEATURES_1s_{GS,TRUE},
  baseline_features_416D.parquet
- npm/pip caches (3.6 GB, night of 07-15)
ATLAS policy set by Moises: keep ONLY OHLCV price parquets (1s->1D) + SFE feature
stores (FEATURES_1s_v2, FEATURES_5s_v2); everything else regenerate-on-need.
Kept order_flow_delta_5s.parquet (40 MB, 3 active research consumers).
Result: 0 -> 65.2 GB free (incl. ~40 GB OneDrive dehydration).
OPEN: .venv_wsl 6.7 GB inside OneDrive (move to WSL fs = rebuild), archive/DATA
2.1 GB (cold storage), .git 3.2 GB (history rewrite someday), DATA/RAW duplicate
.dbn vs .zst (0.76 GB).

## Round 2 (Moises-approved): venv -> WSL ext4, raw data -> D:, artifacts/checkpoints purged
- **.venv_wsl (6.7 GB) rebuilt at `/home/reyses/venvs/bayesian-ai` (WSL ext4)** — perf win
  (venv on /mnt/c was a known Mamba drag) + stops OneDrive syncing CUDA libs. Method:
  fresh `python3 -m venv --copies --without-pip` + byte-copy of site-packages (preserves
  the fragile mamba_ssm 2.2.4 / causal_conv1d 1.5.0.post8 cp312 wheels — no source
  rebuild). VERIFIED: torch 2.6.0+cu124 CUDA True on RTX 3060; bare `import mamba_ssm`
  fails identically in old and new venv (known transformers-5.x issue; training code
  stubs it). Freeze snapshot: docs/reference/venv_freeze_2026-07-16.txt (148 pkgs).
- **Raw data -> D:\Bayesian-AI-data\** (all byte-verified before source delete):
  DATA/RAW (1.86 GB, 1324 files), DATA/RAW_NT8 (0.69 GB), archive/DATA (2.10 GB),
  and Desktop\RAW (3.01 GB, 1333 files — the databento ingest source found OUTSIDE
  the repo at Desktop/RAW).
- **artifacts/ (0.66 GB) + checkpoints/ (0.33 GB) emptied** (Moises: delete everything
  incl. research_A segment .pth; folders recreated empty).
- **Path repoints**: config/settings.py RAW_DATA_PATH; DATA/pipeline/databento_to_atlas.py
  RAW_ROOT; order_flow_ablation {cumulative_delta_builder,extract_baseline_features}.py;
  tools/sourcing/convert_nt8_csv_to_parquet.py RAW_ROOT; 11 launcher .sh + perf docstring
  + train_mamba_rl{,_seq}.py WSL respawn -> /home/reyses/venvs/bayesian-ai/bin/python.
  Launcher-style invocation verified (cuda True).
- **C: free: 65 -> 72.7 GB** (this round +15.3 GB local; total incident recovery 0 -> 72.7).

## Full-roster dossier expansion (doc 083): 13 new streams -> 25-stream league + combiner
Moises: "do 4 and 5, easiest to hardest" -> ALL skip-list dossiers ported (11 faithful,
line-cited; ORDERFLOW-14 honest skip = no 2024 delta data) + CURVE (causal endpoint
variant of the labeler cubic N=20/1m). New league entries: VP-01 AUC 0.732; SAR-23
0.618 (37k); RENKO-24 0.611 (198k!); CURVE 0.606 (26k, terciles 0.45->0.66); ZONE-21
0.584. INVERTER family hardened: RSI 0.04 / MACD 0.05 / SCALP 0.02 / PIVOT 0.05 /
ATR 0.01 / FIB+VA 0.33 — every fade/divergence/pullback premise fires mid-move
against the label. 25-stream combiner (N=447k): OOS AUC 0.675 base 0.510,
calibration near-perfect (0.16->0.17 ... 0.75->0.74, CIs +-0.01), TAILS: bottom
decile 0.17 => invert=83% right, top 0.74; ~52k OOS fires in actionable tails.
Consensus INVERTED in full pool (6+ co-fires = 0.48, crowding = chop warning).
PERF: day-block bootstrap + consensus vectorized (day_block_ci) — combiner 55min+ -> 3min
(algorithmic, CUDA unnecessary). OPS: hermes venv python hung (2 runs lost) -> WSL venv;
legacy mailbox watcher killed (approved). WoW protocol ratified: delegation ladder
Fable->Opus->Sonnet->AG(hardened specs)->Haiku + ALT (Opus reasoner if no Fable);
memory feedback-worker-delegation-ladder. Next: Opus worker = economic conversion;
Sonnet worker = overfit-decay sweep.

## NMP + NMP-EXT in the league — LAMBDA TERM VALIDATED ON LABELS (doc 084)
Moises: "add the NMP and extended NMP to the dossier." Canonical L3_1m_z_se_15
(FEATURES_1s_v2 — 5s store is a _30 build, thresholds don't transfer), verified
Z*=1.8481/0.4752, lambda_hat k=21 on closed 1m bars (derive.py estimator).
**V1 NMP (pure fade): agreement 0.26 = ANTI-ALIGNED (inverted low tercile 87%
right). NMP-EXT (lambda ride/fade): flips 59.6% of fires -> 0.54 (+28pp), AUC
0.574.** First direct OOS label-level measurement of the lambda-completion
thesis. Roadmap log appended. 27-stream combiner: N=469k, AUC 0.678, calibration
diagonal, tails 84%/74% on ~54k OOS fires. Caveat: P(label-right) != P($).

## Correction 085 + the REAL extended NMP: V1 tier ladder ported
Moises: "the extended is a bunch of augmented NMP, check the journals" -> found the
V1 tier ladder (blended_engine_2026_04_18._classify_full_tier). TWO corrections to
084: (1) my NMP lacked the vr<1.0 gate (trap #6) — fixed, result unchanged
(0.26->0.27; vr adds nothing the labels care about); (2) "NMP-EXT" renamed
NMP-LAMBDA (it's the lambda-complete trigger, not extended NMP). Ported 8 tiers
verbatim (exact V1 quantities recomputed on clock-aligned TF buckets; thresholds
transfer; edge-triggered at 1m boundaries; parity tell: FADECALM 37.5/day ~ legacy
40/day). **RESULT: the ladder splits EXACTLY along ride/fade lines — RIDE aligned
(MTFBRK 0.80, MTFEXH 0.79, FREIGHT 0.75, RIDEAGN 0.61), FADE anti-aligned (NMP
0.27, KILLSHOT 0.40, FADEAGN 0.41, FADECALM 0.42).** FADECALM = surprise workhorse
(AUC 0.676, low tercile inverted = 75% on 3.7k). Legacy's own WR ranking (FREIGHT
86%, MTFEXH 76%) reproduced from an independent measurement. League = 37 streams;
combiner N=522k AUC 0.677, diagonal calibration, tails 83/75 on 30k/decile.

## Template engine identified + pattern layer in the league (doc 086)
Moises' "template thing on a branch" = the fractal K-means pattern-template engine
(Feb-Mar 2026): candlestick/geometric events (DOJI/HAMMER/ENGULF/COMPRESSION/WEDGE/
BREAKDOWN) -> 16-D vectors -> recursive K-means -> PatternTemplates (EV/WR/bias +
Markov transitions) + Bayesian brain per template x direction. Branch deleted; code
intact at 09cd30d8; removed from main 2026-04-07. Ported the pattern layer verbatim:
PTRN-ENGULF AUC 0.616 base 0.62 (aligned continuation — ride>fade law again);
PTRN-HAMMER 0.615 base 0.55. DOJI skipped (directionless). League = 39 streams;
combiner N=557k AUC 0.677, calibration 0.18->0.18 ... 0.76->0.75. Resurrection
proposal (2024-frozen template stream, Opus-worker trial) specced in doc 086 §3.

## TMPL0 VERIFIED + first Opus-worker trial PASS (doc 087)
Opus worker resurrected the template engine per spec (18 min, zero false claims,
kill-points respected): 203k events -> 1,020 frozen 2024 templates -> TMPL0 stream
N=157k, OOS AUC 0.631, base 0.68, terciles 0.56/0.68/0.79 (reviewer reproduction
BYTE-IDENTICAL). Honest ceiling: +0.015 AUC over raw PTRN-ENGULF (below signal
bar) — the clusters mostly repackage the named streams' signal; the black-box
instinct was right. BUT in the MIX it's the second-strongest coef (+0.295) and
lifts the 40-stream combiner to **OOS AUC 0.689** (N=714k; bottom decile 0.20 ->
invert=80%; consensus flipped positive +0.385 with TMPL0's dense fires).
Template question CLOSED (identified 086 §1, pattern layer ported, engine
resurrected+verified 087). League = 40 streams.

## ECON CONVERSION + SHELF-LIFE verified — MAMBA GATE OPEN (doc 088)
Ladder trials #2 (Opus, econ) + #3 (Sonnet, shelf-life): both verified exactly
(headline cells byte-identical; 15/15 raw drift spot-checks incl. the 15:15 cap;
shelf-life window recomputed exact). ECON: P(right) converts to points — monotone
sign flip at calibration midpoint; **top decile @5m: mode +1.0 / median +3.25 /
mean +3.86 pts [+2.46,+5.12] (net +3.26 after friction; N=40k; a real
distributional shift)**; bottom-decile-inverted +1.33@5m sig; ALL 15m+ cells NS
= harvest in 1-5m = the Mamba's job. SHELF-LIFE: median 37-41 wks, half the
windows never decay (censored); monthly retune amply safe; combiner not fragile
(initial edge 0.14-0.24 every window). GATE OPEN: next = Mamba state-vector
handoff spec (design doc first). NOT a $/day claim (exploration-level, no stops).

## P_hold during-trade logistic — HONEST DOUBLE NULL (doc 089)
Moises' design: run the entry logistic DURING the trade on the full F-space,
open-ended exit ("confidence should decay as the buy turns into a sell").
Theory SHAPE confirmed (decay curves separate: held 0.70-0.76 vs flipped
0.61->0.42) but BOTH kill-points fired: (A) FULL 409-dim F-space AUC 0.647 <
4-feature context baseline 0.689 (baseline wins by 0.10+ in first 10 min);
(B) exit-on-decay captures median -2.75/-3.00 vs fixed-5m +1.75 (0.5-crossing
lags the flip by median +3 min). LOAD-BEARING: oracle exit = +27.5 median /
0.23 capture ratio vs fixed-5m +1.75/0.014 = **16x exit gap**; trivial 4-feature
P_hold is a free strong hold-state; the flip is a SEQUENTIAL event -> the exit
is the Mamba's job, proven by null. Verified exact from rows parquet. Ladder
trial #4: discipline pass; process defect (workers backgrounding their run)
noted twice -> future specs say run synchronously.

## Turn Catalog + old-school brackets (docs 090/091)
TURN AUDIT: no stream detects turns above chance at +-2m (all prec 0.10-0.25 vs
0.43 null); entries land at median phase 0.64 -> perfect-exit ceiling 23% vs
Moises' 50-80% capture target -> binding constraint = a +-1-2min TURN detector
(chained labels make exit=entry=one instrument). TURN_CATALOG_DRAFT: 16 concepts
mined from 463 articles (quotes spot-verified); port queue TURN-10 HeikinAshi
flip / TURN-07 Sweep-and-Reclaim / TURN-06 climax exhaustion; delta divergence
DATA-BLOCKED 2024. BRACKETS (sealed 2024-select): top-decile SL20/TP30 = outlier
trap (mode -21, mean +2.06 < plain 5m hold +2.87); bottom-inv SL20/TP12 =
consistency reshape (mode/med +12) but EV < hold; both cells at SL grid boundary
(wider stop always better = stops are pure cost, R-trigger lesson re-confirmed);
brackets lane CLOSED. Dedup finding: 60s re-fire bursts carry extra drift
(freshness = real state var). Worker caught reviewer's own spec error (5m vs
15m ref) — ladder discipline working both directions. Ladder trials #5/#6 pass.

## Exit corpus + EXIT_CATALOG (Moises' scraping candidates)
Moises supplied exitis.md (98 URLs, 2 rounds). Scraped 2 passes: 59 OK / 20
thin / 19 blocked (Cloudflare+Reddit egress; 4 Wayback rescues; WPI thesis
20.8k words = the prize). EXIT_CATALOG_DRAFT: 12 concepts, quotes verified;
top: EXIT-05 Keltner momentum-decel reversal (fully parameterized NinjaScript
from the thesis), EXIT-06 efficiency-ratio chop filter, EXIT-04 MFE-duration
time stop. EXTERNAL REPLICATION of doc-091: buildalpha ("without a stop
produced 2x the net profit... more common in mean reverting"), QuantInsti EPAT
("optimized exit conditions underperform control"), WPI Kaufman (tested
stopless) — three independent sources match our measured verdict. Ladder
trials #7 (mining) + scrapes pass. Port queue next: TURN-10/07/06 +
EXIT-05/06/04 on the turn-audit scorecard.
