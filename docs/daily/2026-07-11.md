# Daily Journal: 2026-07-11


## Session Exit Report (03:05:59)

### 1. What Changed
Cleaned up the NT8 catalog folder structure to comply with the research-discipline standard (moved synthesis and task docs to reports/, moved scraper states to tools/ingest_state/). Edited the MASTER_VALIDATION_PROTOCOL.md to officially authorize the event-driven (t(e)) exception for discarding base rates/null controls, and explicitly documented why Median is used over Arithmetic Mean.

### 2. Files Modified
- `research/nt8_catalog/MASTER_VALIDATION_PROTOCOL.md`
- `tools/ingest_batch.py`
- Various moved markdown files into `research/nt8_catalog/reports/`

### 3. What to Look For Next Run
In the next run, ensure the ingest_batch.py script correctly reads and writes from `tools/ingest_state/`.

### 4. Expected Impact on Metrics
No expected impact on statistical metrics. This was purely a structural cleanup and documentation update to align the MVP with actual practices.

## Session Exit Report — Article-Accuracy Audit (Claude, later same day)

### 1. What Changed
Audited the entire `research/nt8_catalog/` corpus (4 pillar synthesis files + 18 test dossiers) for accuracy AGAINST THE RAW ARTICLES. Report: `research/nt8_catalog/AUDIT_ARTICLE_ACCURACY.md` (catalog root, AUDIT-ACC-01). Audit only — no scripts or dossiers modified.

Headline findings:
- **Fabricated/imported numbers in the synthesis**: VA-rotation "80%" (not in any article), footprint imbalance "3:1" (article says customizable), ATR "90% rule" (article says **72.44%**, YM, 6-month sample), squeeze "21 EMA" (article: 20 SMA), Elliott "34-EMA" tunnels (article names no periods).
- **Tests that don't test the article's claim**: SEASON-12 (Mon/Tue-long / Thu/Fri-short is INVENTED — the article's weekday stat is gap-fill rates; "Seasonality Survivor" from 2026-07-09 downgraded to untested-as-written); ROUND-05 (tested a bounce; article claims breach→cascade CONTINUATION — opposite direction); ADX-08 (ADX proxy = range/ATR×100, scale ~100s, so the >25 gate is vacuous — fires exactly 1 event/direction/day); CROSS-11 (article's golden cross is 50/200-DAY; test is minute-scale — the "Death Cross Inversion" is a different object + a single barely-sig cell); VWAP-03 (article mandates z-turn confirmation before entry; test enters on raw ±2σ touch); SCALP-18 (drops 9-EMA + delta from the article stack, N=4–20/yr); ORDERFLOW-14 (trapped-traders condition diluted; 2024 has no delta data yet header says dual-year).
- **Honest sweep summary**: per the dossiers' own CIs, NOTHING is significant-positive in both years; the repeatable results are negative-EV (FIB-17 bearish, VA-13 bullish rotation, ORDERFLOW-14 delta divergence) = INVERSION-CANDIDATE flags per protocol §6. "2 Survivors + 2 Inversions" (2026-07-09) is not supported by the dossier numbers.
- Null-removal edit (user, protocol §5) is consistent with exploration; stale null references remain in `AG_cat_00_INDEX.md` Execution Rules.

### 2. Files Modified
- `research/nt8_catalog/AUDIT_ARTICLE_ACCURACY.md` (new, catalog root)
- `research/nt8_catalog/README.md` (audit pointer)
- this journal + `docs/daily/INDEX.md`

### 3. What to Look For Next Run
Five article-faithful re-runs are specified in AUDIT-ACC-01 §7 (weekday gap-fill, round-number breach-continuation, real DMI ADX, z-turn-confirmed VWAP entry, true 14-day-ATR range-fill). One change each vs the existing scripts.

ADDENDUM 2 (augmentation layer, user-requested): AUDIT-ACC-01 §6 — the MVP does NOT document the per-dossier `augmentation/` stage (4-phase protocol, DOE logistic, F-space reports have no MVP section/gates). Worse: `ag_logistic_model.py` (identical in all 18 dossiers) fits its logistic regression to `X = np.random.randn(len(df), 5)` — every `fspace_doe_report.md` tier table is noise (AUC ≈0.48). The appended real F-space stepwise blocks are duplicated 0-4× per dossier and their McFadden R² 0.0467 is below the 0.05 signal bar. OHLC-01/ORB-02/ORDERFLOW-14 lack events.parquet (augmentation can't run).

ADDENDUM (same session): reviewed the second-level joint logistic regression (`ag_joint_bayes_model.py`) on request — AUDIT-ACC-01 §5. The "+26.3pp top-tier lift" is INVALID: the pooled label mixes ~50%-base directional outcomes with a ≈always-true either-direction volatility label for squeeze rows (first-level index already showed Squeeze P(resp)=1.00), so sqz_state (coef 2.57, odds 13×) just separates label type; also fully in-sample, 2024-only, per-trigger row duplication (82k rows ≠ 82k events), no day-block CI, and σ = 5s-diff std instead of the §7 sigma standard. The 2026-07-10 audit's "significant lift" note is superseded.

### 4. Expected Impact on Metrics
The Phase-3B/4 conditioning sweep should treat the 2026-07-09 survivor/inversion labels as unverified until the re-runs land; joint-model inputs built on SEASON-12/CROSS-11 events inherit the invented definitions.
