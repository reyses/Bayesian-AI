# 2026-04-30 — Morning briefing, headline validation, codebase cleanup

## TL;DR

Took the overnight (04-29) tier work and made it digestible: surfaced the **+$165,141 / 14 months** headline (was buried under the +$33k filter delta), wrote a 1-page morning briefing, validated the headline against three independent sources, and aggressively cleaned up `tools/` and `NT8 Strategies/` while the user reviewed the briefing. Shipped a flag warning that the standalone `BaseNmpRunner_v1.0-RC` should NOT go live — it loses money on full data because it lacks the tier classifier gate that gave BASE_NMP its $24k edge in the blended pipeline.

## Headline reframed

The morning briefing now leads with the validated total: **$165,141 = $390/day NT8 ≈ 7.8× v1.0.4's current $50/day live**. Per-tier breakdown:

| Tier | Filtered PnL (14mo) | Trades kept |
|---|---:|---:|
| RIDE_AGAINST | $50,807 | 1,077 / 1,423 |
| FADE_CALM | $42,881 | 357 / 365 |
| FADE_AGAINST | $27,832 | 292 / 352 |
| BASE_NMP | $24,159 | 929 / 1,195 |
| RIDE_CALM | $16,891 | 451 / 637 |
| KILL_SHOT | $2,571 | 106 / 106 |
| **TOTAL** | **$165,141** | **3,212 / 4,078** |

## Validation done

`reports/findings/HEADLINE_VALIDATION_2026-04-30.md` — three-way cross-check:
1. Per-tier slope-filter table sums to $165,141.0 exactly. $/trade avg $51.41 ✓
2. 70/30 within-tier holdout: 4 of 6 tiers generalize on test (RIDE_CALM, RIDE_AGAINST, FADE_AGAINST, BASE_NMP). FADE_CALM and KILL_SHOT are no-ops at chosen T (don't degrade either).
3. Source enriched-trades CSV has 4,124 rows, consistent with 4,078 baseline-eligible + ~46 minor-tier trades.

**Verdict: headline is internally consistent and survives basic out-of-sample sanity check.**

## Cleanup completed

### NT8 Strategies/ (Documents/NinjaTrader 8/bin/Custom/Strategies/)
9 superseded RCs moved to `archive/`:
ZigzagRunner_v1.0.6-RC.cs, v1.0.7-RC, v1.1, v1.3-RC, v1.4-RC, v1.5-RC, VWAP_MTF_Rev12.cs, MyCustomStrategy.cs, MyCustomStrategy1.cs.

Strategies remaining live: `ZigzagRunner.cs` (v1.0.4 baseline), `ZigzagRunner_v1.0.8-RC.cs` (slope filter + daily gate, awaiting NT8 SA), `BaseNmpRunner_v1.0-RC.cs` (DO NOT SHIP — see below), `ZigzagRunner_v1.2.cs` (alt baseline option).

### tools/ — 25 files archived total today
- `tools/archive/2026-04-29_overnight_research/` — 7 one-shot overnight EDA scripts (outputs preserved in `reports/findings/`)
- `tools/archive/superseded_v_x_runners/` — 6 v1.0.4/v1.0.6/v1.2-RC backtest+parity scripts
- `tools/archive/v15rc_chop_specialist/` — 12 v1.5-RC chop-specialist family (v15_*, nt8_bleed_harvest, tier9_bleed, tier_day_*, sweep_phantom, zigzag_5s_perfect_fill)

Index updated: `tools/INDEX.md` now lists archive subdirs.

### reports/findings/ — navigation aid built
Created `reports/findings/INDEX.md` cataloguing 125+ markdown reports + 21 subdirectories. Most-recent overnight (04-29) and morning (04-30) work indexed first.

## Critical: do NOT ship standalone BaseNmpRunner

The standalone NT8 BaseNmpRunner_v1.0-RC compiles fine, but on full 14-month data it loses **-$30k (canonical params)** to **-$49k (NT8 SA "best")**. Trade counts are 10× the original BASE_NMP tier (16,500 vs 1,195), meaning the standalone fires on bars the blended pipeline would have routed to other tiers. **The standalone strategy lacks the tier classifier that gated BASE_NMP's edge.**

**Recommendation**: ship v1.0.8-RC zigzag with slope filter. Drop standalone BaseNmpRunner until either (a) tier classifier is ported, or (b) Python bridge deploys the actual blended pipeline.

Detail: `reports/findings/base_nmp_param_comparison/2026-04-30_results.md`.

## Compile fix landed

LinReg.Slope[0] is a method-group access NT8 doesn't allow on its `LinReg` indicator. Switched to NT8's separate `LinRegSlope` indicator type. Affected:
- `BaseNmpRunner_v1.0-RC.cs:336`
- `ZigzagRunner_v1.0.8-RC.cs:833, 1120`

Both compile cleanly now (per overnight smoke test). MD5s:
- v1.0.8-RC: `71bab6f4fd44c88c323f26376af0300e`
- BaseNmpRunner: `ee5c26aaaa8e0efacd88747514076e35`

## What's next (ranked by impact)

1. **User runs NT8 SA** on `ZigzagRunner_v1.0.8-RC` against MNQ JUN26 (MNQCONT loading is broken → diagnosed as data-series mismatch). Compares to v1.0.4 baseline.
2. If SA validates: ship v1.0.8-RC live. Otherwise diagnose tier-style filter underperformance (likely slippage/timing diff).
3. Continue tools/ cleanup pass on remaining ~200 root-level scripts (low priority, low risk).
4. Long-pending: investigate the SafetyPanicLong 2-contract event.

## Files touched today

| File | What |
|---|---|
| `reports/findings/MORNING_BRIEFING_2026-04-30.md` | NEW — 1-page TL;DR with $165k headline |
| `reports/findings/HEADLINE_VALIDATION_2026-04-30.md` | NEW — 3-way sanity check |
| `reports/findings/INDEX.md` | NEW — master nav for 125+ reports |
| `reports/findings/OVERNIGHT_SUMMARY_2026-04-29.md` | EDIT — headline reframed to lead with $165k |
| `tools/INDEX.md` | EDIT — appended archive section |
| `tools/archive/superseded_v_x_runners/` (6 files) | NEW — moved orphans here |
| `tools/archive/v15rc_chop_specialist/` (12 files) | NEW — moved v1.5-RC family here |
| `<NT8>/Strategies/archive/` (9 files) | EDIT — moved superseded RCs |

## What to look for next session

- NT8 SA results from v1.0.8-RC. Either ship-live or diagnose-divergence.
- Any cleanup lint from imports broken by the archival (none found in spot-check, but worth a `python -c "import tools.*"` smoke test if anything strange shows up).

---

## Afternoon session — reframe + new ask + commits + visualization

### Context shift

User reframed the project narrative:
- Original thesis (chart `chart_reg_z_2025_06_09.png`): mean-regression CNN entry/exit on price vs LinReg ±2 SE bands.
- The +$165k headline IS the CNN-blended pipeline's edge (verified: enriched-trades CSV has `cnn_flipped` and `exit_reason=cnn_exit` columns) — but the trade CSVs at `sandbox/training/output/...` are dated **Apr 10**, while `training/nightmare_blended.py` was modified **Apr 20**. Headline reflects stale model state.
- User running the blended pipeline themselves now to refresh CSVs.

### What was missed overnight

User reminded: before bed last night they asked for the **other features visualized at peak points of interest**. Overnight produced statistical aggregates (Cohen's d, distribution histograms) but NOT the time-axis visual overlays they wanted — i.e., a chart in the spirit of the regression-z reference, with peaks marked on the price panel and the most-separating physics features stacked underneath as time-aligned subplots.

### Hard rule re-stated by user

*"so every turn write to your goddamn journal."* Plan now bakes journal hygiene as Step 0, runs every turn, not just at session end.

### Plan approved (Auto mode active)

Plan file: `C:\Users\reyse\.claude\plans\parallel-squishing-ladybug.md`

Two threads:

1. **Commit tidy-up** — 3 logical commits scoped to today's work only (242 pending changes in repo most of which are NOT today's; stage by name to avoid backlog pollution). Code → reports → journals.
2. **Peak feature overlay visualization** — build `tools/peak_feature_overlay_chart.py`. 7-panel time-aligned chart: price + peaks marked on top, then 6 physics-feature subplots (z_se, dmi_diff at 2 TF tiers, variance_ratio, reversion_prob+hurst, velocity). Sample mode first (6 representative days), validate format, then per-day batch for full 14-month range.

### Execution log (this session)

- [done] Step 0 journal append (this entry)
- [done] Commit 1: cleanup + NT8 LinRegSlope fixes — `3d765e62` (28 files)
- [done] Commit 2: reports/findings — `c71fedf6` (4 files, +526 lines)
- [done] Commit 3: journals — `768e9f97` (3 files, +192 lines)
- [done] Built `tools/peak_feature_overlay_chart.py` (~280 lines)
- [done] Reference day validation (2025-06-09) — clean output
- [done] Iterated layout: HH:MM x-axis, panel ylabels short, full titles
  inline as faded gray text, vertical dotted guides at every peak
- [done] 6-day sample run: 4 charts produced (`2025_06_09.png`,
  `2025_11_04.png`, `2026_01_08.png`, `2026_02_19.png`), 2 skipped
  (`2025-03-15` was Saturday, `2025-09-22` had no FEATURES_5s parquet)
- [done] Per-day batch — **328 charts, 0 skipped, 16.3 min, 118 MB**
- [done] Summary markdown updated with batch stats
- [done] .gitignore updated to keep only 4 sample PNGs tracked (the other
  324 are reproducible via `--mode per_day`)
- [done] NT8 indicator built: `docs/nt8/StatRegressionBands.cs` —
  regression mean + ±1/±2/±3/±4 SE bands. OLS+SE math validated to
  machine precision against scipy.linregress. Deployed to NT8 Indicators
  folder. Commit `eee9a4f6`.
- [next] Final commit: .gitignore update + summary markdown update

### Observations from sample charts (visual EDA)

**2025-06-09** (reference day, the chart user showed me):
- Multiple H peaks (red ▼) cluster around 21880 between ~12:30-19:00 UTC
- Multiple L peaks (green ▲) at ~21720 in early session 02:00-09:00 UTC
- 5m_z_se panel oscillates cleanly between ±2 throughout the day
- variance_ratio mostly < 1 (mean-reverting regime) consistent with chop
- DMI slow (15m+1h) shows extended period below zero — bearish drift
  underlying the chop

**2026-02-19** (DOWN-trend day):
- H peaks early (24960 at session open), then descending L peaks at
  24800 → 24750 through the day
- variance_ratio > 1 in the decline window (continuation, not revert)
- DMI slow panel deeply negative throughout — strong bearish trend
- Clear contrast vs 2025-06-09: visible regime difference in the panels

The visual EDA confirms what the overnight Cohen's d table said
quantitatively (e.g., 5m_dmi_diff distinguishes H vs L peaks at d=1.33),
but adds the time dimension: you can SEE that DMI doesn't just have
a different mean at peaks — it CONFIRMS the regime context (DMI
direction matches the trend on trend days, oscillates on chop days).

### New task this turn — NT8 indicator: regression mean + z-bands

User asked for an NT8 indicator that plots the regression mean and z-bands at ±1, ±2, ±3, ±4 SE, with a configurable period — matching the visual reference in `charts/chart_reg_z_2025_06_09.png` so it can be applied directly to live NT8 charts.

Building `docs/nt8/StatRegressionBands.cs`:
- Single `Period` parameter (default 20)
- 9 plots: regression mean + 4 upper bands + 4 lower bands
- Computes OLS slope/intercept manually (so SE is from residuals, not channel-MAE)
- Color scheme: mean blue, ±1σ gray, ±2σ orange (fade trigger), ±3σ red, ±4σ dark red
- IsOverlay = true (lives on price chart)

Will commit standalone after building and self-validating the math (compare against scipy linregress in a small Python parity test).

### Re-frame note for next session

This commit cluster captures the **state of analysis as of pre-fresh-pipeline-rerun**. The +$165k headline is mathematically defensible against the Apr 10 trade CSVs. When the user finishes their fresh blended pipeline run, the next session must:

1. Re-run `tools/tier_linreg_slope_filter.py` against the fresh CSVs
2. Diff the new per-tier breakdown against the Apr 30 numbers committed today
3. If headline holds: the deployment question is just "Python live bridge" (timeline)
4. If headline drifts materially: re-validate the slope filter on fresh data before any deploy

The peak-overlay visualization (this session, in progress) is **independent of trade CSV freshness** — it analyzes the FEATURES_5s × peak intersection, which is unchanged by pipeline reruns.
