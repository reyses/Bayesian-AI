# 2026-04-30 — Morning briefing, headline validation, codebase cleanup

## TL;DR

Took the overnight (04-29) tier work and made it digestible: surfaced the **+$165,141 / 14 months** headline (was buried under the +$33k filter delta), wrote a 1-page morning briefing, validated the headline against three independent sources, and aggressively cleaned up `tools/` and `NT8 Strategies/` while the user reviewed the briefing. Shipped a flag warning that the standalone `BaseNmpRunner_v1.0-RC` should NOT go live — it loses money on full data because it lacks the tier classifier gate that gave BASE_NMP its $24k edge in the blended pipeline.

## Headline reframed

The morning briefing now leads with the validated total: **$165,141 = $390/day NT8 ≈ 7.8× v1.0.4's current $50/day live**. Per-tier breakdown:

| Tier | Filtered PnL (14mo) | Trades kept |
|---|---:|---:|
| RIDE_AGAINST | $50,807 | 1,077 / 1,423 |
| FADE_CALM | $42,881 | 357 / 365 |
| FADE_AGAINST | $27,832 | 292 / 352 |
| BASE_NMP | $24,159 | 929 / 1,195 |
| RIDE_CALM | $16,891 | 451 / 637 |
| KILL_SHOT | $2,571 | 106 / 106 |
| **TOTAL** | **$165,141** | **3,212 / 4,078** |

## Validation done

`reports/findings/HEADLINE_VALIDATION_2026-04-30.md` — three-way cross-check:
1. Per-tier slope-filter table sums to $165,141.0 exactly. $/trade avg $51.41 ✓
2. 70/30 within-tier holdout: 4 of 6 tiers generalize on test (RIDE_CALM, RIDE_AGAINST, FADE_AGAINST, BASE_NMP). FADE_CALM and KILL_SHOT are no-ops at chosen T (don't degrade either).
3. Source enriched-trades CSV has 4,124 rows, consistent with 4,078 baseline-eligible + ~46 minor-tier trades.

**Verdict: headline is internally consistent and survives basic out-of-sample sanity check.**

## Cleanup completed

### NT8 Strategies/ (Documents/NinjaTrader 8/bin/Custom/Strategies/)
9 superseded RCs moved to `archive/`:
ZigzagRunner_v1.0.6-RC.cs, v1.0.7-RC, v1.1, v1.3-RC, v1.4-RC, v1.5-RC, VWAP_MTF_Rev12.cs, MyCustomStrategy.cs, MyCustomStrategy1.cs.

Strategies remaining live: `ZigzagRunner.cs` (v1.0.4 baseline), `ZigzagRunner_v1.0.8-RC.cs` (slope filter + daily gate, awaiting NT8 SA), `BaseNmpRunner_v1.0-RC.cs` (DO NOT SHIP — see below), `ZigzagRunner_v1.2.cs` (alt baseline option).

### tools/ — 25 files archived total today
- `tools/archive/2026-04-29_overnight_research/` — 7 one-shot overnight EDA scripts (outputs preserved in `reports/findings/`)
- `tools/archive/superseded_v_x_runners/` — 6 v1.0.4/v1.0.6/v1.2-RC backtest+parity scripts
- `tools/archive/v15rc_chop_specialist/` — 12 v1.5-RC chop-specialist family (v15_*, nt8_bleed_harvest, tier9_bleed, tier_day_*, sweep_phantom, zigzag_5s_perfect_fill)

Index updated: `tools/INDEX.md` now lists archive subdirs.

### reports/findings/ — navigation aid built
Created `reports/findings/INDEX.md` cataloguing 125+ markdown reports + 21 subdirectories. Most-recent overnight (04-29) and morning (04-30) work indexed first.

## Critical: do NOT ship standalone BaseNmpRunner

The standalone NT8 BaseNmpRunner_v1.0-RC compiles fine, but on full 14-month data it loses **-$30k (canonical params)** to **-$49k (NT8 SA "best")**. Trade counts are 10× the original BASE_NMP tier (16,500 vs 1,195), meaning the standalone fires on bars the blended pipeline would have routed to other tiers. **The standalone strategy lacks the tier classifier that gated BASE_NMP's edge.**

**Recommendation**: ship v1.0.8-RC zigzag with slope filter. Drop standalone BaseNmpRunner until either (a) tier classifier is ported, or (b) Python bridge deploys the actual blended pipeline.

Detail: `reports/findings/base_nmp_param_comparison/2026-04-30_results.md`.

## Compile fix landed

LinReg.Slope[0] is a method-group access NT8 doesn't allow on its `LinReg` indicator. Switched to NT8's separate `LinRegSlope` indicator type. Affected:
- `BaseNmpRunner_v1.0-RC.cs:336`
- `ZigzagRunner_v1.0.8-RC.cs:833, 1120`

Both compile cleanly now (per overnight smoke test). MD5s:
- v1.0.8-RC: `71bab6f4fd44c88c323f26376af0300e`
- BaseNmpRunner: `ee5c26aaaa8e0efacd88747514076e35`

## What's next (ranked by impact)

1. **User runs NT8 SA** on `ZigzagRunner_v1.0.8-RC` against MNQ JUN26 (MNQCONT loading is broken → diagnosed as data-series mismatch). Compares to v1.0.4 baseline.
2. If SA validates: ship v1.0.8-RC live. Otherwise diagnose tier-style filter underperformance (likely slippage/timing diff).
3. Continue tools/ cleanup pass on remaining ~200 root-level scripts (low priority, low risk).
4. Long-pending: investigate the SafetyPanicLong 2-contract event.

## Files touched today

| File | What |
|---|---|
| `reports/findings/MORNING_BRIEFING_2026-04-30.md` | NEW — 1-page TL;DR with $165k headline |
| `reports/findings/HEADLINE_VALIDATION_2026-04-30.md` | NEW — 3-way sanity check |
| `reports/findings/INDEX.md` | NEW — master nav for 125+ reports |
| `reports/findings/OVERNIGHT_SUMMARY_2026-04-29.md` | EDIT — headline reframed to lead with $165k |
| `tools/INDEX.md` | EDIT — appended archive section |
| `tools/archive/superseded_v_x_runners/` (6 files) | NEW — moved orphans here |
| `tools/archive/v15rc_chop_specialist/` (12 files) | NEW — moved v1.5-RC family here |
| `<NT8>/Strategies/archive/` (9 files) | EDIT — moved superseded RCs |

## What to look for next session

- NT8 SA results from v1.0.8-RC. Either ship-live or diagnose-divergence.
- Any cleanup lint from imports broken by the archival (none found in spot-check, but worth a `python -c "import tools.*"` smoke test if anything strange shows up).

---

## Afternoon session — reframe + new ask + commits + visualization

### Context shift

User reframed the project narrative:
- Original thesis (chart `chart_reg_z_2025_06_09.png`): mean-regression CNN entry/exit on price vs LinReg ±2 SE bands.
- The +$165k headline IS the CNN-blended pipeline's edge (verified: enriched-trades CSV has `cnn_flipped` and `exit_reason=cnn_exit` columns) — but the trade CSVs at `sandbox/training/output/...` are dated **Apr 10**, while `training/nightmare_blended.py` was modified **Apr 20**. Headline reflects stale model state.
- User running the blended pipeline themselves now to refresh CSVs.

### What was missed overnight

User reminded: before bed last night they asked for the **other features visualized at peak points of interest**. Overnight produced statistical aggregates (Cohen's d, distribution histograms) but NOT the time-axis visual overlays they wanted — i.e., a chart in the spirit of the regression-z reference, with peaks marked on the price panel and the most-separating physics features stacked underneath as time-aligned subplots.

### Hard rule re-stated by user

*"so every turn write to your goddamn journal."* Plan now bakes journal hygiene as Step 0, runs every turn, not just at session end.

### Plan approved (Auto mode active)

Plan file: `C:\Users\reyse\.claude\plans\parallel-squishing-ladybug.md`

Two threads:

1. **Commit tidy-up** — 3 logical commits scoped to today's work only (242 pending changes in repo most of which are NOT today's; stage by name to avoid backlog pollution). Code → reports → journals.
2. **Peak feature overlay visualization** — build `tools/peak_feature_overlay_chart.py`. 7-panel time-aligned chart: price + peaks marked on top, then 6 physics-feature subplots (z_se, dmi_diff at 2 TF tiers, variance_ratio, reversion_prob+hurst, velocity). Sample mode first (6 representative days), validate format, then per-day batch for full 14-month range.

### Execution log (this session)

- [done] Step 0 journal append (this entry)
- [done] Commit 1: cleanup + NT8 LinRegSlope fixes — `3d765e62` (28 files)
- [done] Commit 2: reports/findings — `c71fedf6` (4 files, +526 lines)
- [done] Commit 3: journals — `768e9f97` (3 files, +192 lines)
- [done] Built `tools/peak_feature_overlay_chart.py` (~280 lines)
- [done] Reference day validation (2025-06-09) — clean output
- [done] Iterated layout: HH:MM x-axis, panel ylabels short, full titles
  inline as faded gray text, vertical dotted guides at every peak
- [done] 6-day sample run: 4 charts produced (`2025_06_09.png`,
  `2025_11_04.png`, `2026_01_08.png`, `2026_02_19.png`), 2 skipped
  (`2025-03-15` was Saturday, `2025-09-22` had no FEATURES_5s parquet)
- [done] Per-day batch — **328 charts, 0 skipped, 16.3 min, 118 MB**
- [done] Summary markdown updated with batch stats
- [done] .gitignore updated to keep only 4 sample PNGs tracked (the other
  324 are reproducible via `--mode per_day`)
- [done] NT8 indicator built: `docs/nt8/StatRegressionBands.cs` —
  regression mean + ±1/±2/±3/±4 SE bands. OLS+SE math validated to
  machine precision against scipy.linregress. Deployed to NT8 Indicators
  folder. Commit `eee9a4f6`.
- [done] Final commit: .gitignore + summary update + journal — `8bf3c68e`

### Autonomous loop turn — entry-outcome overlay (next analysis cycle)

User invoked `<<autonomous-loop-dynamic>>` (no specific prompt). Picking
the highest-value next step that aligns with the project reframe:

**The peak overlay showed physics at price extremes (peaks).** It does NOT
tell us whether trading at those moments would have been profitable.

**The entry-outcome overlay** is the parallel analysis the CNN-thesis
reframe pointed at: take the actual fade entries from
`sandbox/training/output/trades/blended_is.csv` (the blended pipeline's
labeled trades — same CSV that produced the +$165k headline), plot the
physics around each entry, **color-coded by outcome (green = winner,
red = loser)**.

That tells us, visually:
- Do winning fades have a different physics signature than losing fades?
- Is the loser-physics state visible BEFORE the trade fires (so a CNN
  could filter)?
- Where in the regression-z chart does each fade actually land — at +/-2
  SE? past +/-3? — and does the band level predict outcome?

Building `tools/entry_outcome_overlay_chart.py`, mirroring the peak
overlay tool's structure (peak loading → entry trade loading;
H/L markers → win/loss markers; same 7-panel layout).

Caveat re-stated: trades CSV is Apr 10 vintage, blended pipeline code is
Apr 20 vintage — the user's pipeline rerun will refresh. The methodology
is reusable; we re-render after fresh CSVs land.

### Findings — quantitative (aggregate Cohen's d)

Across 1,912 fade-tier trades (BASE_NMP + FADE_CALM + FADE_AGAINST):

| Feature | Cohen's d (winner-loser) | Direction |
|---|---:|---|
| 1m_reversion_prob | **+0.472** | winners HIGHER |
| 1m_hurst | -0.255 | winners LOWER |
| 1m_variance_ratio | -0.219 | winners LOWER |
| 1D_dmi_diff | -0.141 | winners on DOWN-day |
| (rest near zero) | | |

z_se features have d ≈ 0 — they ARE the trigger, so the trigger doesn't
distinguish winners from losers within the triggered set. That's the
whole point: physics-CONDITIONAL-on-trigger is what matters.

### Findings — heuristic threshold filter (NEGATIVE result, important)

Hypothesis: skip fades with low revprob and/or high hurst → expect +PnL
delta similar to the LinReg slope filter (+$33k overnight).

Reality: every threshold combo REDUCES total PnL. The filter raises
per-trade quality (WR ↑, $/trade ↑) but drops trade count faster than
it raises win density. Skipped trades have lower per-trade PnL but
still net positive in aggregate.

Best heuristic combo (revprob>=0.85): only +$240 vs baseline. Not
exploitable as a threshold rule.

**Implication**: physics-at-entry contains real info but extracting it
needs a NONLINEAR decision (CNN). The 70% cnn_flip accuracy is the
right tool; thresholds aren't. This is a sharper validation of the
CNN thesis: not "the rule says skip these," but "you need a model that
can do nonlinear feature combinations."

**Counter-intuitive corollary**: the +$33k LinReg slope filter
discriminates on a DIFFERENT axis than these per-bar physics features
(none of them encode local slope structure directly). Two filters,
two different discrimination axes, both real.

### Outputs

- `tools/entry_outcome_overlay_chart.py` — visualization tool
- `reports/findings/entry_outcome_overlay/2026-04-30_entry_outcome_summary.md`
- `reports/findings/entry_outcome_overlay/2026-04-30_cohen_d_table.csv`
- `reports/findings/entry_outcome_overlay/per_day/*.png` — 284 charts on
  disk (99 MB), 4 representative samples committed (2025-06-09, 2026-01-07,
  2026-01-08, 2026-02-05). Rest gitignored, reproducible via tool.
- Commit `966612bd`: tool + summary + Cohen's d CSV + 4 samples + .gitignore

### Closing analysis — orthogonality of slope vs per-bar physics

Quick test on the enriched CSV to see if the +$33k LinReg slope filter
(overnight) and the per-bar physics (today) discriminate on the same axis
or different axes:

| Pair | Pearson r |
|---|---:|
| 1m_reversion_prob × seg_slope_pts_per_day | +0.022 |
| 1m_hurst × seg_slope_pts_per_day | (≈ 0) |
| seg_slope × pnl | +0.029 (weak) |
| 1m_reversion_prob × pnl | +0.135 |

The segmenter's daily-regime slope (what the macro_slope_segmenter.py
labels) has near-zero correlation with the per-bar revprob/hurst features.
**Different discrimination axes** — they could combine.

Caveat: the +$33k filter used **bar-level 1m LinReg slope at 30-bar
lookback**, which is NOT in the enriched CSV. The seg_slope I tested is
the multi-day regime label, not the bar-level slope. Definitive test
would require re-computing bar-level LinReg slope at every entry
timestamp from FEATURES_5s. Deferred — orthogonality is plausible but
not yet proven at the right granularity.

### Session totals (2026-04-30)

7 commits in clean topical order, no backlog noise:

| Commit | Topic | Net |
|---|---|---|
| `3d765e62` | Cleanup: 25 dead tools archived + NT8 LinRegSlope fix | 28 files |
| `c71fedf6` | Reports: $165k headline reframe + validation + INDEX | +526 lines |
| `768e9f97` | Journal: daily + INDEX + research journal | +192 lines |
| `68560520` | Peak overlay viz tool + 4 sample charts | +526 lines |
| `eee9a4f6` | NT8 indicator: StatRegressionBands | +225 lines |
| `8bf3c68e` | Peak overlay batch (328 charts) + gitignore | +86 lines |
| `966612bd` | Entry-outcome overlay: physics at fade entries × outcome | +633 lines |

Standing pending (user's queue):
- Blended pipeline rerun (in user's hands)
- NT8 SA on `ZigzagRunner_v1.0.8-RC` against MNQ JUN26
- Compile + try `StatRegressionBands` on a chart
- Review the 4 + 4 sample charts to validate the visual EDA pattern
- Review the entry-outcome summary's NEGATIVE heuristic finding (this
  is the most actionable insight of the day — it sharpens the
  CNN-is-required argument)

Pausing autonomous loop. No further self-scheduled wakeups — user has
context to direct the next move and a full pipeline run in flight.

### Resume — NT8 compile error on StatRegressionBands.cs

User dropped `examples/errors.csv` showing CS0246 / CS0103 errors:
- `Stroke` not found (8 hits, lines 73-81)
- `DashStyleHelper` not in current context (lines 74-75)

Root cause: missing `using NinjaTrader.Gui;` (the `Stroke` and
`DashStyleHelper` types live there, not in `NinjaTrader.Gui.Tools`).
Fix: add the using directive, redeploy, recommit.

### Observations from sample charts (visual EDA)

**2025-06-09** (reference day, the chart user showed me):
- Multiple H peaks (red ▼) cluster around 21880 between ~12:30-19:00 UTC
- Multiple L peaks (green ▲) at ~21720 in early session 02:00-09:00 UTC
- 5m_z_se panel oscillates cleanly between ±2 throughout the day
- variance_ratio mostly < 1 (mean-reverting regime) consistent with chop
- DMI slow (15m+1h) shows extended period below zero — bearish drift
  underlying the chop

**2026-02-19** (DOWN-trend day):
- H peaks early (24960 at session open), then descending L peaks at
  24800 → 24750 through the day
- variance_ratio > 1 in the decline window (continuation, not revert)
- DMI slow panel deeply negative throughout — strong bearish trend
- Clear contrast vs 2025-06-09: visible regime difference in the panels

The visual EDA confirms what the overnight Cohen's d table said
quantitatively (e.g., 5m_dmi_diff distinguishes H vs L peaks at d=1.33),
but adds the time dimension: you can SEE that DMI doesn't just have
a different mean at peaks — it CONFIRMS the regime context (DMI
direction matches the trend on trend days, oscillates on chop days).

### New task this turn — NT8 indicator: regression mean + z-bands

User asked for an NT8 indicator that plots the regression mean and z-bands at ±1, ±2, ±3, ±4 SE, with a configurable period — matching the visual reference in `charts/chart_reg_z_2025_06_09.png` so it can be applied directly to live NT8 charts.

Building `docs/nt8/StatRegressionBands.cs`:
- Single `Period` parameter (default 20)
- 9 plots: regression mean + 4 upper bands + 4 lower bands
- Computes OLS slope/intercept manually (so SE is from residuals, not channel-MAE)
- Color scheme: mean blue, ±1σ gray, ±2σ orange (fade trigger), ±3σ red, ±4σ dark red
- IsOverlay = true (lives on price chart)

Will commit standalone after building and self-validating the math (compare against scipy linregress in a small Python parity test).

### Re-frame note for next session

This commit cluster captures the **state of analysis as of pre-fresh-pipeline-rerun**. The +$165k headline is mathematically defensible against the Apr 10 trade CSVs. When the user finishes their fresh blended pipeline run, the next session must:

1. Re-run `tools/tier_linreg_slope_filter.py` against the fresh CSVs
2. Diff the new per-tier breakdown against the Apr 30 numbers committed today
3. If headline holds: the deployment question is just "Python live bridge" (timeline)
4. If headline drifts materially: re-validate the slope filter on fresh data before any deploy

The peak-overlay visualization (this session, in progress) is **independent of trade CSV freshness** — it analyzes the FEATURES_5s × peak intersection, which is unchanged by pipeline reruns.

## Standalone Research v2 features integration (evening session)

Switched `tools/standalone_research.py` to consume v2 precomputed features from `DATA/ATLAS/FEATURES_5s_v2/` instead of recomputing v1 SFE physics every run. The `--cache` flag is now polymorphic: pass an `.npz` for the existing assembled-matrix cache, or pass the v2 features directory to use the new path.

### What changed
- New module `tools/research/features_v2.py`: `detect_v2_cache`, `load_v2_features_for_day` (reads + joins all 25 L0/L1_*/L2_*/L3_* dirs), `compute_v2_features_live_for_day` (fallback via `core_v2.StatisticalFieldEngine.compute_L0/L1/L2/L3`), `align_v2_to_base_tf` (5s → base-TF reindex via `searchsorted(side='right')-1`), `reshape_v2_to_stack` ((N, 8, 23) per-bar matrix builder)
- `tools/standalone_research.py`:
  - `--cache` polymorphic detection at top of `main()`
  - `--base-tf` defaults to `1m` in v2 mode (was `15m`)
  - New v2 branch in Step 7-8: skip SFE compute, load v2 parquets, reindex to 1m, build (N, 184) flat stack + 2 extras (current_MR, L0_time_of_day) → X shape `(N, 186)`
  - `_set_active_spec_v2()` rebinds module-level `TF_HIERARCHY`/`TF_LABELS`/`FEATURE_NAMES` so existing analyses pick up v2 names automatically
  - PID drill-down (`FEATURE_NAMES.index('self_pid')`) now resolves via `_ACTIVE_PID_NAME`: `self_pid` in v1, `reversion_prob` in v2
  - `--full` pipeline gracefully exits in v2 mode (`build_stacked_matrices()` is v1-only)
  - `_X_193d` shape check now accepts both 193 (v1) and 186 (v2)
  - Hardcoded `(12, 16)`, `range(192)`, `importances[192]` in Analysis K replaced with active-spec sizes
  - `--cache` save only writes `.npz` when target is `.npz` (skips when target is v2 dir)
- Spec doc: `docs/JULES_standalone_research_v2.md`

### Validation
End-to-end smoke test: `python tools/standalone_research.py --data DATA/ATLAS --cache DATA/ATLAS/FEATURES_5s_v2 --context-days 7 --analysis-days 1`
- 9 days loaded from precomputed parquets in 2.5s (vs ~30s+ for v1 SFE compute)
- 112,107 5s feature rows × 186 cols joined and reindexed onto 930 1m bars
- All 19 analyses A-S completed without traceback (R still skips on missing `tools.cnn_pattern_model`, unrelated)
- Output: `tools/plots/standalone/1d/research_report.txt` + 33 PNG plots

### Known caveat
With a 1-day analysis window, 1D TF features are constant within the window so their correlations come back as 0 (or NaN→0). Analyses surface them at the top of correlation tables until something with non-zero correlation breaks the tie. This is expected — not a bug. For meaningful macro-TF feature correlations, run with a multi-month window.

### Files touched
- `tools/research/features_v2.py` (new)
- `tools/standalone_research.py` (modified)
- `docs/JULES_standalone_research_v2.md` (new spec doc)

## Standalone Research v2 — full year run results

Ran `python tools/standalone_research.py --data DATA/ATLAS --cache DATA/ATLAS/FEATURES_5s_v2 --context-days 21 --analysis-days 0` against the full 345-day v2 feature span. Three bug fixes ran first:
1. `imr.detect_regimes` had an O(n²) merge loop that hung on 24,973 raw regimes (1m base + 1 year). Vectorized with `np.bincount` and per-pass batch merging.
2. Analysis F's per-regime audit plot OOM'd at ~10 GB RSS trying to render 5,701 active regimes (12 in × 17,103 in figure). Capped to top-30 by sample size.
3. Analyses L/M/N segment-overlay plots OOM'd creating one Line2D per of ~371k samples. Uniform-stride subsampled to 5,000 segments before plotting.

After these, the run completed with 371,503 1m analysis bars (~371,503 oracle MFE/MAE pairs).

### Real edge — Analysis L (Signed MFE OLS)

Predicting **signed MFE magnitude** (continuous), not binary direction. Test R² = 0.060 on the regression. Direction = sign(prediction). Confidence gates:

| `\|pred\|` threshold | N | Accuracy | Lift | % of data |
|---:|---:|---:|---:|---:|
| 0 | 111,217 | 61.9% | +3.6% | 100% |
| 5 | 81,193 | 65.4% | +7.1% | 73% |
| 10 | 54,936 | **68.4%** | **+10.1%** | 49% |
| 20 | 22,829 | **73.1%** | **+14.8%** | 20% |

This is the headline. The 73.1% / 20%-coverage tier is what to chase as a tradable filter.

### Analysis K (Gradient Boosting on direction)
Test 58.9%, +8.1% lift on the global pool. Top features cluster around **5m and 15m TFs**:
- 5m: `price_velocity_w`, `swing_noise`, `hurst`, `SE_low`, `reversion_prob`, `z_se`
- 15m: `vol_accel_w/_1b`, `z_low/high`, `price_accel_w`, `vol_sigma_w`, `hurst`, `reversion_prob`
- 1h: `price_accel_w`, `vol_mean_w`, `vol_accel_1b`
- 1m: 1 feature in top-20 (`vol_mean_w`). 5s/15s: zero.

Conclusion: **5m–15m are the right base TFs for direction work; 1m is too noisy.**

### LEAK FOUND — Analyses A, M, N, P all suffer autoregressive identity issues
- **A (Price R² = 1.0000):** top features are every TF's `vwap_w` / `price_mean_w` at r ≥ 0.99. VWAP IS price — definitional identity, not signal.
- **M (Next-price R² = 0.9968):** same vwap≈price collapse, but direction accuracy = 50.5% (+0.4%).
- **N (Delta-direct R² = −0.0068):** model can't fit delta. "Top coefficients" of 382, −254 on `vwap_w` / `price_mean_w` are spurious multicollinearity cancellation, not signal.
- **P (Direction 100% / +49.9% lift):** ⚠️ **LABEL LEAK.** Model selects `B_current_MR` at step 1, achieves 100% test accuracy. `current_MR` is defined at [tools/research/imr.py:27](tools/research/imr.py#L27) as `np.diff(close)` — it IS the bar-to-bar signed delta. So `B_current_MR` = `close[t+1] − close[t]` = the direction target itself. The 100% is an identity, not a finding. **Pre-existed v2; running it at 371k samples just made it obvious.**

### Analyses B, C, D, O — direction is genuinely unpredictable at 1m bar-to-bar
- B: Top direction factor `current_MR` at r=−0.0122. R² = 0.0003.
- C/D: delta features add +0.0001 (still 0.0%) to direction prediction.
- O (stepwise on delta direction): 50.9% test, 8 features, +0.8% lift. No useful confidence gating.

This is consistent: **the v2 features cannot predict 1m bar-to-bar direction without leak.** They CAN predict signed MFE magnitude (L), which is the right framing.

### Analysis Q — macro vol-accel as magnitude differentiator (worth follow-up)
- 361,259 non-flat moves, 9 magnitude bins (4 UP, 5 DN).
- For ALL bins (UP and DN), the dominant transition signal is **`1h_vol_accel_1b` / `4h_vol_accel_1b` / `1D_vol_accel_1b`** — macro volume acceleration differentiates the magnitude tiers.
- This pairs naturally with L's confidence gating: large predicted MFE + large macro vol-accel = the candidate "set up" condition to test.

### Analysis S — Exit trend guard (1m, full year)
- 6.93M tighten events with no guard → 4.99M with guard (28% suppression).
- Aggregate MFE delta: **−19.1** ("guard neutral/hurts").
- High-suppression trades (>50% blocked): n=113,186, avg MFE=37.5 ticks. Low-suppression: n=257,197, avg MFE=56.7.
- Conclusion: at 1m, the trend guard suppresses too many tightens for the average MFE retained.

### Operational lessons
1. **Discard the autoregressive-identity findings (A, M, N, P).** Strip `vwap_w` / `price_mean_w` and `current_MR` from any future direction model — they leak.
2. **Analysis L is the only direction headline that survives scrutiny.** 73.1% / 20% / +14.8% is the tradable tier.
3. **Move base-TF for direction research to 5m or 15m.** 1m base produces 6,519 regimes (most <100 bars), wastes signal in noise.
4. **Investigate macro vol-accel × signed-MFE confidence as a stacked filter.** Q's bin signal + L's prediction-magnitude gate are independent dimensions.

### Pipeline performance
- Full v2 load (345 days × 25 layer dirs): ~1.5 min
- Reindex 5M 5s rows → 371k 1m bars: ~5 sec
- Total run: ~25 min including all analyses
- Compare: v1 SFE compute on the same span would be ~hours just for the per-TF physics

### Files touched (v2 hardening commit chain)
- `tools/research/imr.py` — vectorized merge loop in `detect_regimes()`
- `tools/standalone_research.py` — Analysis F regime cap (top-30); Analyses L/M/N segment caps (5000)
- `docs/daily/2026-04-30.md` — this section
