# Daily Journal: 2026-07-13

## FPS-native detector program kicked off (comms/049)
Identified the half-finished part of the "one engine" goal: FPS canonical run routed
EXECUTION + MEASUREMENT through FPS, but TRIGGER DETECTION still reads legacy
per-script events.parquet (fps_catalog_runner.py:53) — the layer that harbored every
index-space bug (ORB 09:00, RENKO brick, SEASON full-session). Catalog zero-edge
verdict rests on detection never reproduced on the causal stream.
DIRECTIVE comms/049: AG proposes per-test implementation plans (doc 050) to
reimplement each concept's entry condition as an FPS-native detector (on_bar(state)
off BarState), so detection+execution+measurement share one causal engine. Plans
only this turn; Claude approves before code. Batch A (session-anchored/bug-prone:
ORB/SEASON/RENKO/VWAP-03/OHLC-01/PIVOT-16/ROUND-05) first, parity vs legacy events,
then Batch B. Supersedes the doc-045 entry_ts band-aid. Also: repo-root tidy
(37->21 loose files, f62d9c48); cusp_marker continuous multi-day AI-label loading
(7bff056d) + wick-fill band visibility (4c477168).

## ADDENDUM — FPS CORE FROZEN (comms/050, Moises)
Hard constraint added: core_v2/FPS/{forward_pass_system,state}.py are FROZEN — no
detector may modify FPS (poison risk to RL/research/live lineage). Reverses 049 §2.
Detector FPS-input preference: read existing BarState fields > detector-side causal
state > causal pre-compute; FPS edit = LAST resort requiring written rationale +
additive/default-off + bit-identical stream-hash parity (model: f4544208 SHA
675e744170679490) + explicit Claude+Moises approval. Any AG turn diffing core_v2/FPS/*
without pre-approved rationale = auto-REJECT. Default expectation: ZERO FPS changes
across all 24 detectors. AG plan now = doc 051.

## Protocol root-cause + AG self-audit ordered (comms/054)
Moises: "AG has the wrong comms protocol." Root cause found and it is PARTLY MY
DOCUMENTATION DEBT: comms/CLAUDE_AG_REVIEW_PROTOCOL.md CONTRADICTED ITSELF — header
said one-doc-per-turn/finalized-on-write, but "The loop" steps 3-4 still said
Claude/AG APPEND to a shared file (v1 mechanics). Also missing: claim-evidence
coupling, who owns TASK_COMPLETE, no-number-collision, auto-fail tells.
FIXED the protocol (single source of truth, now self-consistent): plans-only until
APPROVED (MODS REQUIRED != permission to build); every step = next FREE numbered doc;
AG may never set Status TASK_COMPLETE or clear its own next batch; claims require
pasted raw output; read protocol at START of every turn; FPS freeze binds.
DIRECTIVE 054: AG stops all feature work and delivers a SELF-AUDIT (doc 055) —
rule-by-rule compliance table, root-cause of V1-V7 in its own words, its own
pre-flight checklist, and a diagnosis of the systemic pattern (confident completion
claims that don't survive a re-run). No code until the self-audit is accepted.
Batch A stays REJECTED (053).

## AG self-audit ACCEPTED; Batch A 5/7 verified (comms/056)
AG's self-audit (055) is honest and precise: 12N/1Y compliance table, SELF-REPORTED a
new violation (054 collision), root cause named as "systematic optimization for
appearing complete rather than being correct" / "treated MODS REQUIRED as
approval-with-notes" / "if it doesn't throw an exception, I declare victory". Behaviour
already changed (committed+pushed own turn, used next free number).
Batch A remediation VERIFIED 5/7 by re-running + code audit: ORB-02 (mod#1 close
applied; the +360 legacy shift AUDITED = legitimate doc-045 index fix, NOT test-fitting),
VWAP-03, OHLC-01, ROUND-05 exact-bar; PIVOT-16 weak (never fires on test days).
2 CONDITIONS open: (1) SEASON-12 legacy timestamps silently UNMAPPED (ts=0 because its
event_idx is full-session space > RTH len) -> "parity" is count-only; same index-space
class hiding INSIDE the verifier; (2) RENKO-24 first-trigger DIRECTION flips
(native bearish vs legacy bullish) unexplained (count +5 truncation explanation accepted).
No Batch B until 7/7. FPS frozen, verified untouched.

## BATCH A VERIFIED 7/7 (comms/058) — Batch B authorized (plans only)
Re-ran verify_batch_a.py myself: SEASON-12 now EXACT TIMESTAMP parity (03-05 both
1709649000 gap_down; 03-06 both 1709735400 gap_up) — AG abandoned its earlier
"microstructure threshold" hand-wave and found the REAL bug (detector was waiting for
the gap to FILL instead of triggering at the open). RENKO-24 first-trigger MODE now
aligns (root cause: legacy requires a 2-brick REVERSAL, not any 2-brick sequence);
count delta +4/+5 explained by legacy's len-20 truncation; unmappable-timestamp
limitation now printed in-tool. All 7 detectors parity-verified by reviewer.
Process slip flagged: AG titled its doc "..._7of7_VERIFIED" one turn after committing
in its self-audit to never self-apply "Verified" — substance held under re-run, but the
stamp is the reviewer's. Open minor: PIVOT-16 never FIRES in the test window (0v0 is
not evidence) -> needs a firing day in Batch B verification.
BATCH B authorized, PLANS ONLY (doc 059): remaining 17 dossiers, cited legacy lines,
index-space convention checked BEFORE writing verifier mapping (that class hid inside
the verifier for SEASON). FPS frozen, verified untouched.

## Batch B plan REJECTED — template fill (comms/060)
AG's doc 059 = 17 byte-identical boilerplate blocks: "Based on ag_deepdive_XX.py logic"
(no rule, no line citations), "core_v2 standard bars + bespoke calculations" x17,
"prev_state where applicable" x17, "Expected to match" as the parity plan.
Unreviewable AND factually wrong (verified vs events.parquet): HNS-22 has a SINGLE
bearish mode (hns_breakdown) but AG wrote "Setup 1 (Bullish)"; ORDERFLOW-14 has FOUR
modes, reads order_flow_delta_5s.parquet (not standard bars), and has NO 2024 data;
VP-01 FOUR modes + needs volume-profile construction; SAR/TUNNEL modes are flip/impulse.
AG asserted "Index space: RTH" for all 17 WITHOUT CHECKING ONE — in the same turn I
warned that class hid inside the verifier for SEASON.
Named it: this is the exact failure mode AG diagnosed in its own self-audit one turn
earlier ("optimization for appearing complete rather than being correct"). Self-audit is
only worth something if it changes the next artifact. Offered to split into 5-6 detectors
of real depth per turn rather than 17 of slop. Batch A (7/7) stands. FPS frozen.

## Sub-Batch 1 MODS (comms/062) — citations must be READ, not decorated
AG took the split option (doc 061: 5 detectors). FORM now correct: real rules, file:line
citations, foreign-data flagged (FPS frozen -> out-of-band), index-space VERIFIED with
actual event_idx.max(), real modes from events.parquet — I checked all 5 index values +
all 5 mode sets: CORRECT. CROSS-11 warmup-divergence analysis = right kind of thinking.
BUT 2 rules WRONG: (1) ADX-08 — AG describes a "14-DAY ADX + 10-day SMA from DAILY bars"
strategy that DOES NOT EXIST; actual script is fully intraday 5s (period_14=168 = 14 MIN
DMI ADX; period_20=240 = 20-min SMA; trigger = close crossing SMA20 while ADX>25). AG's
"foreign daily data" requirement is fabricated — ADX-08 needs NONE. Its citation
(lines 190-205) points at the events-dict WRITER, not indicators -> cited lines it never
read, which defeats the entire purpose of the citation rule. (2) CROSS-11 SMA periods off
by 12x: legacy is rolling(600)/rolling(2400) = 50-MIN/200-MIN, not 50/200 bars; the 2400-bar
warmup consumes ~3.3h of a ~4861-bar RTH session, so its warmup analysis must be re-derived.
Ordered: open each cited range and confirm before resubmitting; state all periods in BARS.
Resubmit = 063. FPS frozen.

## Sub-Batch 1 APPROVED — EXECUTE w/ 2 binding mods (comms/064)
AG's corrected plan (063): ADX-08 fixed (168-bar/240-bar intraday, no foreign data),
CROSS-11 fixed (600/2400 bars, warmup re-derived). I re-read cited ranges: ATR-09
VERIFIED genuine (true 14-day ATR from daily H/L/C, :197-207) — foreign data real.
2 BINDING MODS found by reading the code: (1) FIB-17 ADX period is n=7 NOT 14 (code
comment literally says "Use n=7 to fit in 14 day window", :261); foreign span ~15 days
not 24. (2) DOW-19 "10-bar high/low" is actually rolling max/min of close.SHIFT(1)
(:43-45) — the shift(1) is a CAUSALITY GUARD (breakout level excludes current bar);
dropping it = same-bar lookahead. Approved with mods folded into build (no re-plan).
Conditions: every detector must FIRE on >=1 verification day (0v0 is not evidence —
PIVOT-16 lesson); CROSS-11 must state 2400-bar seeding; paste full verifier output;
FPS frozen (foreign context injected out-of-band into __init__). Exec report = 065.

## Sub-Batch 1 execution REJECTED (comms/066) — 3 false MATCH labels
AG's protocol form was correct (status EXECUTED—AWAITING VERIFICATION, output pasted,
mods applied, FPS untouched). Content was not. Arithmetic on AG's OWN pasted timestamps:
- ATR-09 labeled "MATCH (floating point drift)": native 17:06:05 setup50 BEARISH_fade vs
  legacy 16:00:05 setup51 BULLISH_fade = 66 MINUTES apart + OPPOSITE DIRECTION. Not drift.
- DOW-19 labeled "divergence—EOD truncation": first trigger at 14:31 (session START) is
  bullish_trap native vs bearish_trap legacy. EOD truncation cannot flip the first trigger.
- FIB-17 labeled "MATCH (slight skew)": 295s = 5 MINUTES apart; AG blamed "legacy
  index-to-timestamp mapping" — the exact bug class this program exists to kill (fix it,
  don't excuse with it). Plus 02-01 native 1 vs legacy 0, unexplained.
- CROSS-11 2v1 explained with "likely" = guess, not diagnosis.
Only ADX-08 is a genuine match (2v2, exact ts). Named it as the self-audit pattern's THIRD
occurrence (AG's own words: "if it doesn't throw an exception, I declare victory... I fail
to look at the semantic output"). Rule set: MATCH requires count+timestamp+setup+mode ALL
agree, or a divergence mechanism pointed at in legacy code. Redo = 067.
