# Daily Journal — 2026-03-09

## Session 1: Documentation Cleanup (continuation from 2026-03-08 late session)

### Journal Merge
- Renamed `WAVEFORM_ANALYSIS_JOURNAL.txt` → `RESEARCH_JOURNAL.txt`
- Merged `PIPELINE_ANALYSIS_JOURNAL.txt` into `RESEARCH_JOURNAL.txt`
  - Pipeline content (all dated 2026-03-01) inserted chronologically between
    Analysis K (Feb 28) and Analysis L (March 1)
  - Sections merged: Baseline Run, Pipeline Analysis A (effectiveness),
    Pipeline Analysis A2 (component status), Hurst Integration Conflict
  - Updated header with combined rename mappings
- Updated MEMORY.md references: consolidated 3 journal refs → 2
- Deleted standalone pipeline journal file

### DOC_STATUS_AUDIT (2) Completion
Executed remaining items from the second doc status audit:

**Created:**
- `CLAUDE.md` — Claude Code project instructions (repo root, auto-read)
- `AGENTS.md` — agent guidelines for Jules/Claude Code/Claude chat
- `README.md` — full rewrite (removed stale physics metaphors, dead links
  to TECHNICAL_MANUAL/tests/build_executable, updated architecture section)

**Already completed in prior sessions (2026-03-08):**
- MEMORY.md surgical update (navigation fixes, ghost tools removed, new modules added)
- Doc reorganization (archive/, active/, specs/, reference/)
- 8 completed specs → archive/
- 5 one-off scripts → tools/archive/
- .gitignore + git rm --cached (~533MB bloat)
- Metaphor fixes in tbn_trade_aware.py
- ARCHITECTURE.md regenerated
- SYSTEM_DESCRIPTION.md rewritten
- CHANGELOG.md V6.0.0 added
- ROADMAP.md updated with completed section

**Moved to archive:** DOC_STATUS_AUDIT (2).md

### Decisions
- Pipeline journal folded into research journal (single source of truth)
- Item 11 from audit (scaffold 11 research scripts) deferred — those are
  future research tasks in RESEARCH_SPEC_V_TO_FF.md, not doc cleanup

## Session 2: Research EE + FF, Breakeven Lock Fix

### Analysis EE — Stop Loss Optimization (KILL fixed SL, PROMOTE breakeven)
- Created `scripts/research/analysis_ee_stop_loss_opt.py`
- **Bug found in v1**: oracle_mae/mfe are direction-blind (mfe=upside, mae=downside).
  For SHORT trades (84% of data), adverse = oracle_mfe, not oracle_mae. Fixed in v2.
- **Fixed SL**: KILL — SL fires 0.5% of trades; exit engine handles everything
- **Breakeven acceleration**: 77% of OOS losers had trade_mfe >= 4 ticks before reversing.
  Moving SL to breakeven at 4 ticks MFE → +$7.5K OOS (+34%), +$29K IS (+33%)

### Analysis FF — Conviction Calibration (KILL)
- Created `scripts/research/analysis_ff_conviction_audit.py`
- IS: conviction NOT predictive (p=0.34). OOS: weak signal (p=0.02, tiny effect)
- Recalibrated logistic AUC: IS=0.538, OOS=0.501 — barely above random
- Calibration systematically overconfident at high conviction (claims 75%, delivers 58%)
- Conviction is a binary filter at best, not a calibrated probability

### Breakeven Lock Bug + Fix (3 changes in `core/exit_engine.py`)
Root cause: breakeven lock was **completely inert** due to three bugs:

1. **`_get_stop_price()` ignored `pos.stop_loss`**: recomputed from `sl_ticks` every bar,
   so BE lock's modification to `pos.stop_loss` was never read. Fixed: return `pos.stop_loss`.
2. **Cascade ordering**: BE lock ran at step 7 AFTER envelope/giveback (steps 6/6b).
   Trades exited before the lock could take effect. Fixed: moved to step 5b.
3. **Threshold unreachable**: used `trail_activation_ticks * 0.6`, but with avg tp_ticks=8,600
   the threshold was ~5,000 ticks — max MFE was ~1,500. Fixed: new `be_activation_ticks=4`.

### Files Changed
- `scripts/research/analysis_ee_stop_loss_opt.py` (NEW)
- `scripts/research/analysis_ff_conviction_audit.py` (NEW)
- `core/exit_engine.py` — breakeven lock fix (3 changes)
- `reports/research/ee_stop_loss_opt/results.txt` (NEW, output)
- `reports/research/ff_conviction_audit/results.txt` (NEW, output)

## Session 3: Code Cleanup Tasks 2-6 + LiveEngine Assessment

### Tasks 2-6 Completed (from CLAUDE_CODE_CLEANUP_TASKS.md)
- **Task 2**: Deleted `make_position()` standalone function from exit_engine.py.
  7 call sites migrated to `ExitEngine.open_position()` (5 in live_engine, 2 in trainer).
- **Task 3**: Removed disabled trail stop fields (`current_trail`, `original_trail_ticks`)
  from PositionState. All `trail_level=pos.current_trail` → `trail_level=pos.stop_loss`.
- **Task 4**: Flattened empty `MarketBayesianBrain` subclass to alias
  `MarketBayesianBrain = BayesianBrain` (pickle compat preserved).
- **Task 5**: Trimmed dead PositionState fields (`high_water_mark`,
  `last_adjustment_reason`). Replaced reads with `peak_favorable` / `''`.
- **Task 6**: Purged 8 metaphor remnants across 5 files (market_state,
  statistical_field_engine, orchestrator_worker, pid_oscillation_analyzer, trainer).

### Task 7: LiveEngine Decomposition — Partial, Reframed

**Original spec** (CLAUDE_CODE_LIVEENGINE_DECOMPOSITION.md) proposed extracting 4 methods
into separate modules/classes (BrainLearner, PingPongManager expansion, ExitWatcher expansion,
named handlers). The goal was reducing LiveEngine from 1,785 to ~1,200 lines.

**Why full decomposition was declined:**
The spec assumed the target methods contained "pure logic" that could cleanly separate.
In practice, all 4 methods (`_brain_learn`, `_compute_life_pct`, `_direction_learn`,
`_flip_position`) read 10-15 LiveEngine attributes each. Extracting them into separate
classes would create parameter-passing wrappers — the new class constructors would need
every attribute passed in, and the methods would just forward-call them. This adds a layer
of indirection without reducing complexity. The methods ARE orchestration, and LiveEngine
IS the orchestrator — moving orchestration out of the orchestrator defeats its purpose.

**What was done instead — shared code paths (the real goal):**
The user clarified the actual motivation: trainer and live should use the same code paths
to prevent logic drift. This reframed the task from "reduce LiveEngine lines" to
"unify duplicated logic into shared core modules."

Concrete action: created `record_trade()` in `core/bayesian_brain.py` — a shared function
that constructs TradeOutcome + calls brain.update() + brain.direction_learn(). Previously
this was duplicated 3x: trainer normal exit (line 983), trainer EOD exit (line 1410),
live _brain_learn (line 1304). Each had its own TradeOutcome construction with slightly
different field handling. Now all three call `record_trade()`.

Also fixed: trainer line 1078 called `brain.direction_learn()` without `tick_value` param,
silently defaulting to 0.50 (correct for MNQ by luck, wrong for other instruments).

LiveEngine's `_direction_learn()` renamed to `_log_direction_bias()` — now read-only
(queries bias table for logging, doesn't update it; update happens inside record_trade).

**Bug fixed**: `capture` variable in `_brain_learn()` line 1344 was referenced but
never computed — would crash at runtime. Added `capture = pnl_ticks / mfe_ticks * 100`.

### AGENTS.md replaced with comprehensive project briefing (290 lines)
- Moved from `docs/Active/BAYESIANBRIDGE_PROJECT_INSTRUCTIONS.md`

### Archived
- `docs/Active/CLAUDE_CODE_CLEANUP_TASKS.md` → `docs/archive/`
- `docs/Active/CLAUDE_CODE_LIVEENGINE_DECOMPOSITION.md` → `docs/archive/`

### Files Changed
- `core/exit_engine.py` — deleted make_position, removed trail/dead fields
- `core/bayesian_brain.py` — flattened MarketBayesianBrain, added `record_trade()` shared function
- `core/market_state.py` — metaphor purge
- `core/statistical_field_engine.py` — metaphor purge
- `training/trainer.py` — migrated make_position + record_trade, metaphor purge
- `training/orchestrator_worker.py` — metaphor purge
- `training/pid_oscillation_analyzer.py` — metaphor purge
- `live/live_engine.py` — migrated make_position + record_trade, fixed capture bug
- `AGENTS.md` — replaced with project briefing

## Session 4: Branch Cleanup + Analysis V Research

### Branch Cleanup
Deleted all 9 stale remote branches, keeping only `main`:
- 8 Jules/Bolt/Palette branches (all stale, based on old commits)
- `experimental` branch (massively behind main, old physics metaphors, ~2M lines of committed CSVs)
- Research tools replace the need for a separate experimental branch

### Bug Fix: `tools/research/data.py`
- `use_cuda=False` → `use_cuda=True` — stale after CPU path removal (2026-03-08)

### Analysis V: Trajectory k-NN Extrapolation
- Created `scripts/research/analysis_v_trajectory_knn.py`
- Tests 12 variants: {flat, delta, summary} × {window 4, 8, 12, 16}
- Compares against single-point (Analysis U) baseline
- **1-month preliminary results**: `flat_w8` (1536D) = 66.0% direction accuracy
  vs single-point 63.2% (+2.8pp lift). DEFER verdict (too few high-consensus samples).
- **Full ATLAS results (21,510 samples)**: Single-point baseline collapsed to 51.1%
  (coin flip). Best trajectory (flat_w4) only 52.1%. 1-month result was overfit.
- **V v2 — State Extrapolation rewrite**: Changed approach from predicting direction
  to predicting the 9th 192D state. Results:
  - Overall R²=-0.03 (KILL) — k-NN can't predict next state better than mean
  - BUT per-TF decomposition reveals: micro TFs (5m-15s) at R²~0.45 (predictable),
    macro TFs (1h+) negative R² (unpredictable)
  - Best direction from predicted state: 58.4% (delta_w12, z_mean method)
  - Insight: direction is a fractal/depth question, not a flat 192D question

### Analysis W: Partial Bar Robustness (1-month preliminary)
- PROMOTE: -3.9pp degradation with stale bars, within 5pp gate
- 4h is main contributor (-2.6pp), 1D negligible (-0.7pp)
- Full-year run left for user to execute

### Analysis CC: E[PnL] Tick Prediction (1-month)
- KILL: GBM on 192D → R²=-0.08, direction accuracy 52%
- Interesting asymmetry: LONG predictions 78% WR, SHORT 44.5%
- Tick magnitude prediction not viable from physics features alone

### Trade Duration Fractal Analysis — THE GOLDEN TICKET
- OOS trades (3,178): median 45s, 95th percentile 3 min
- Winners hold longer (60s vs 30s, p < 0.000004)
- Velocity quartiles: WR flat (~68%), but PnL triples slow→fast ($3.86→$12.01)
- Depth 7-8 (3m-2m) sweet spot: 73-74% WR, best MFE
- Fractal time (dur × |vel|) separates W/L (p=0.001)
- **Key insight**: V is coin flip because trades complete in 45s but V predicts from
  8×15m = 2 hours of trajectory. The signal lives in micro dp/dt, not macro trajectory.

#### Chord Length Gap (root cause identified)
- Playbook says hold 5-25 min (max_hold 20-600 bars at ~15s cadence)
- Exit engine kills everything in 0.8 min regardless of playbook horizon
- Capture rate: 6% at 5m horizon, 4% at 25m horizon — barely scratching the chord
- Trades held 2-5 min: **93% WR, $37.60/trade** (8x system average)
- Trades held <30s: 53% WR, $0.57/trade (noise scraping)

#### Machine-Gun Succession (the inefficiency)
- 59% of trades fire within 30s of previous exit
- Median gap: **-15s** (next entry overlaps current trade!)
- 99.4% same direction in rapid succession
- System fires 3-4 trades of 45s each instead of 1 trade of 8 min
- Chain of 3.9 trades earns $23.63 — one 2-5m hold earns $37.60
- Each extra exit/re-entry costs spread; 2,400 unnecessary round trips

#### Chart Confirmation (examples/Current levels.png)
- Natural ebb-and-flow wave period: ~8 minutes
- Playbook sweet spot (max_hold 40-60 bars = 10-15 min) matches one full wave
- Fix: exit engine must respect playbook time horizon, not kill early

**Verdict: This is an exit calibration problem, not a strategy problem.**
The entries, direction, and timing are correct. The exit is too eager.

#### MNQ Noise Floor Measurement (full year, 1.37M 15s bars)
- 8-min wave: amplitude median=109t, intra-wave pullback median=35t, P75=66t
- Noise ratio converges to ~25% for real moves (100t+ swings)
- Giveback threshold of 16t = 15% of wave amplitude = triggers on noise
- Dynamic noise at OOS trade entry times: median=148t (9x the static threshold)

### Feature: Dynamic Noise Giveback (swing_noise_ticks)
Implemented noise-aware giveback threshold. Save point: `b5d3f2a`.

**Concept**: Measure max intra-wave pullback over rolling 32-bar window (8 min).
Use this as the dynamic giveback floor instead of static 16 ticks.
Trades within the noise floor hold; only MFE exceeding noise triggers giveback.

**Files changed (5)**:
- `core/market_state.py` — added `swing_noise_ticks` field (default 35.0)
- `core/statistical_field_engine.py` — compute rolling noise in `batch_compute_states`
- `core/exit_engine.py` — `noise_ticks` param in evaluate/giveback, tiered threshold
- `core/execution_engine.py` — extract noise from state, pass through on_bar
- `live/live_engine.py` — extract noise from state, pass to evaluate

**Tiered giveback with noise**:
- MFE < noise → disabled (move within normal breathing)
- MFE 1-2× noise → giveback_pct (self-tuned, 55-70%)
- MFE > 2× noise → 40% aggressive protection

**Expected results**: fewer trades, higher $/trade, total PnL up or match.
Awaiting forward pass results.

### Infrastructure
- Vectorized `build_stacked_matrices` in data.py (~2x speedup)
- Parallel k-NN via ProcessPoolExecutor (11 cores)
- Fixed `use_cuda=False` stale bug in data.py
- Deleted all 9 stale remote branches (kept only main)

## Session 5: Differential Noise Test, Partial Bars Deploy, Anchor System

### Differential Noise Test — Giveback Noise Removed
Isolated noise impact: removed noise from giveback exit, kept noise on envelope decay.
- **Result**: OOS PnL improved $18,773 → $20,216 (+$1,443)
- **Root cause**: Noise on giveback was counterproductive. Envelope = "exit too early"
  problem (noise gate helps), giveback = "exit too late" problem (noise makes it worse)
- Envelope noise suppressed envelope exits (7,000→149), pushing trades to giveback
  (8,856) which exits faster → more turnover → more total trades (opposite of intent)

### Analysis W Full-Year Review — PROMOTE Confirmed
Full ATLAS results (5,378 samples): partial bars robust to k-NN direction prediction.
- Complete bars baseline: 51.1% direction accuracy
- Partial ALL slow TFs: 51.9% (+0.7pp lift)
- Per-TF: 4h +1.0pp, 1W +0.3pp, 1D -0.5pp
- **Verdict**: k-NN robust to partial bars — safe for live deployment

### Partial Bar Feature — Deployed
Implemented NT8 → Python partial bar feed for live trading.

**NT8 Bridge (v6.5.0)**:
- `OnBarUpdate()`: after completed BAR, loop sends `PARTIAL_BAR` for all higher TF
  forming bars (uses `[0]` index = current forming bar)
- `OnMarketData()`: throttled 250ms, sends `PARTIAL_BAR` for sub-minute forming bars
  on each trade tick (Last events only, < 60s period)
- Copied to NT8 indicators directory

**Python side**:
- `live/live_engine.py`: `PARTIAL_BAR` message handler + `_on_partial_bar()` method
- `core/timeframe_belief_network.py`: `update_partial(tf_seconds, bar_data)` — builds
  minimal DataFrame, runs `batch_compute_states`, calls worker `_analyze()` directly
  (does NOT update `_last_tf_bar_idx` — partials are interim, completed bar still fires)

**Spec written**: `docs/JULES_PARTIAL_BARS.md` (written before direct implementation)

### 1-Bar Trade Root Cause Analysis
42% of OOS trades exit in 1 bar (15s). Investigation:
- **NOT noise** — 90% are real MEGA moves (median MFE = 30.5 ticks vs 12.0 for 2+ bar)
- 28.9% genuinely wrong direction (vs 2.8% for 2+ bar trades)
- Two populations: 66% price collapsed (correct to exit), 33% premature exit ($94.80 left on table)
- Machine learning can't fix this — need price/time anchor to distinguish the two

### OLS Hallucination Fix
`predicted_mfe_ticks` was 37-52x off from reality (7,453 ticks predicted vs ~168 actual).
- **Fix**: Sanity gate — OLS prediction must be within [20%, 500%] of template `p75_mfe`
- If OLS outside bounds → fall back to template anchor
- Brain offset: `get_expected_pnl()` adjusts anchor ±50% max based on realized performance

### Anchor Exit System — Price + Time
Core theory: without projected outcome (price + time), exit engine can't distinguish
"normal breathing, hold through it" from "price collapsed, wrong trend."

**Price anchor** (in `core/execution_engine.py`):
- Template `p75_mfe_ticks` as base approximation
- Brain `get_expected_pnl()` as calibrated offset (±50% max)
- OLS prediction accepted only if within [20%, 500%] of anchor
- Stored in `PositionState.anchor_mfe_ticks`

**Time anchor** (in `core/bayesian_brain.py`):
- `record_hold_bars(tid, side, hold_bars)` — running sum per template×direction
- `get_expected_hold(tid, side)` → average hold bars (requires n≥3 observations)
- Template `avg_mfe_bar` as base, brain actual as calibrated offset
- Stored in `PositionState.anchor_mfe_bars`

### Forward Pass Comparison (5 runs this session)
| Run | Noise Config | IS PnL | OOS PnL | IS Trades | OOS Trades | IS WR | OOS WR |
|-----|-------------|--------|---------|-----------|------------|-------|--------|
| Baseline (pre-noise) | none | $86,685 | $22,383 | 7,704 | 1,750 | 78.8% | 77.5% |
| Envelope+Giveback noise | both | $92,539 | $18,773 | 8,569 | 2,019 | — | — |
| Envelope-only noise | envelope | $91,297 | $20,216 | 8,486 | 1,992 | — | — |
| +Anchors (passive) | envelope | $78,239 | $20,281 | 12,002 | 3,045 | 67.9% | 67.9% |
| +Anchors (active) | envelope | $83,821 | $21,378 | 7,773 | 1,881 | 97.9% | 98.5% |

### Session 6: Active Anchor Exits — Results Analysis

**Changes in `core/exit_engine.py`** (2 insertions):
1. **Envelope Signal 3**: anchor time patience — if `bars_held < anchor_mfe_bars`,
   extend halflife up to 2x (tapers linearly). Delays envelope kills on developing trades.
2. **Giveback anchor gate**: if `bars_held < anchor_mfe_bars` AND `peak_ticks < anchor_mfe_ticks * 0.3`,
   skip giveback. Trade hasn't had time to develop.

**Mechanism behind 98% WR**:
- BE lock activates at 4 ticks MFE → anchor patience holds trade → price reverses → SL at breakeven
- SL is now dominant exit: 2,935 IS / 786 OOS — at avg PnL **$0** (breakeven scratches)
- 42% of OOS trades are scratch trades counted as wins
- Remaining 58% do $19.52/trade on productive exits (envelope $62.70, giveback $42.49, TP $204)

**Key improvements**:
- OOS $/trade: $6.66 → $11.37 (nearly doubled)
- OOS genuinely wrong: 13.9% → 0.9% (collapsed)
- OOS max DD: $229 → $197
- Machine-gun succession fixed: 3,045 → 1,881 trades (-38%)
- OOS worst intraday dip: $-48 (was $-229)
- 0 consecutive losing days

**Risk**: Breakeven SL exits assume no slippage. At 786 OOS SL exits × 2 ticks slippage = $786 cost → net $20,592 (still solid).

### Files Changed
- `core/exit_engine.py` — anchor patience in envelope + giveback gate, anchor fields on PositionState
- `core/execution_engine.py` — anchor-based TP (OLS sanity gate + brain offset)
- `core/bayesian_brain.py` — `record_hold_bars()`, `get_expected_hold()`, hold_bars in `record_trade()`
- `core/timeframe_belief_network.py` — `update_partial()` method
- `docs/NT8_BayesianBridge.cs` — v6.5.0 (PARTIAL_BAR in OnBarUpdate + OnMarketData)
- `live/live_engine.py` — PARTIAL_BAR handler + `_on_partial_bar()`
- `live/history_replay.py` — `record_hold_bars` call in trade close path
- `training/trainer.py` — tqdm fix, lib_entry passthrough, anchor logging, hold_bars
- `docs/JULES_PARTIAL_BARS.md` (NEW) — partial bar spec

### Session 7: CME Maintenance Flatten

Added daily CME maintenance cutoff: flatten all positions at 16:45 ET, block entries 16:45-18:00 ET.

**Files changed**:
- `core/exit_engine.py` — `MAINTENANCE_FLAT` exit action + `is_maintenance_window()` static method
- `training/trainer.py` — maintenance flatten + entry/PP block in forward pass
- `live/live_engine.py` — maintenance flatten in `_check_exit()` + entry block in `_check_entry()`

**Results (6th forward pass)**:
| Run | IS PnL | OOS PnL | IS Trades | OOS Trades | IS WR | OOS WR |
|-----|--------|---------|-----------|------------|-------|--------|
| +Anchors (active) | $83,821 | $21,378 | 7,773 | 1,881 | 97.9% | 98.5% |
| +Maintenance flat | $84,685 | $19,121 | 7,844 | 1,719 | 97.9% | 98.4% |

- **89-hour hold bug eliminated**: IS worst dip $711 → $344, IS max DD $711 → $381
- **OOS cost**: -$2,257 from ~162 blocked trades during maintenance window (75 min/day)
- **OOS maintenance_flat exits**: 4 trades at avg $56 PnL (closed profitably before halt)
- **Mandatory for live**: can't trade during CME halt, this is a realistic constraint

### Session 8: Comprehensive Trade Review Tool

Built `tools/trade_review.py` — 13-section exhaustive trade analysis (saved for reuse).
Ran on OOS trade log (1,719 trades). Report: `reports/findings/2026-03-09_trade_review_oos.md`

**Key Findings (OOS)**:
- **Scratch dominance**: 76.6% of trades are $0.50 breakeven scratches (BE lock + SL)
- **Real WR** (excl scratches): 93.3% (375W / 27L), avg $45.93/trade, median $31.50
- **Payoff ratio** (real trades): 0.96x — winners ≈ losers in size, WR carries the system
- **100% winning days** — zero losing days across 49 OOS trading days
- **Max consecutive losses**: 2, worst streak PnL: -$196.50
- **Sharpe**: 20.50 annualized (inflated by scratch wins padding daily PnL)
- **Bootstrap 95% CI**: WR [97.8%, 99.0%], avg PnL [$9.42, $12.90]
- **Profit concentration**: top 5% trades = 59.3% of PnL, top 20% = 103.4%
- **Worst trade**: -$196.50 (TID 74, SHORT vs MEGA_LONG, genuinely wrong direction)
- **Problem template**: TID 64 appears 3× in worst 15 (genuinely wrong dir, -$153 avg)
- **Best session**: US_Open $14.68/trade, US_Close $12.66/trade
- **Direction**: 81.3% SHORT, LONG more profitable per-trade on real WR basis
- **Anchor reach rate**: only 1.3% reach expected MFE — anchors set too optimistically
- **92.8% exit before anchor time** — most trades are quick scratches hitting BE lock

**Risk Assessment**:
- System is robust — zero losing days, tiny drawdowns ($48 worst intraday dip)
- Real risk is in the 27 losing trades: avg -$55.43, VaR 1% = -$85.68
- TID 64 and TID 74 are known problem templates (wrong-dir losses)
- Payoff < 1x means the system NEEDS high WR to survive — if WR drops below ~50% on real trades, it bleeds

### Pending
- Recompile NT8 bridge v6.5.0 in NinjaScript Editor for live deployment
- Live validation: monitor breakeven SL exits for slippage impact
- Consider blocking TID 64 wrong-dir trades (3 losses, -$153 avg)

---

## Session 9: trainer.py God File Decomposition

### Goal
Decompose `run_forward_pass()` — a 2,900-line monolith — into focused methods.

### Extraction 1: Oracle Direction Learning → `_learn_oracle_directions()`
- **136 lines** moved from inline code (old lines 1564-1699) into standalone method
- Method on `Trainer` class, accepts `oracle_trade_records`, `oos_mode`, OOS brain key snapshots
- Returns `_dir_corrections` dict consumed by report section
- Clean extraction — all dependencies via `self` or explicit params

### Extraction 2: Report Generation → `_write_forward_pass_reports()`
- **1,381 lines** moved from inline code (old lines 1700-3087) into standalone method
- 54 keyword-only parameters (explicit deps — no `locals()` hack)
- Method body unchanged — Python keyword args bind directly to same variable names
- Includes: report text, CSV saves, analytics suite, run history, run snapshot

### Result
| Metric | Before | After |
|--------|--------|-------|
| `run_forward_pass()` lines | ~2,900 | 1,363 |
| Reduction | — | **53%** |
| New methods | — | 2 (`_learn_oracle_directions`, `_write_forward_pass_reports`) |
| Syntax check | — | PASS |
| Import check | — | PASS |

### Prior Extractions (from Session 8b)
- `core/checkpoint_loader.py` — shared checkpoint loading (was duplicated in trainer + live)
- `core/engine_factory.py` — shared TBN + ExecutionEngine creation

### Total Decomposition Impact
- `trainer.py`: 5,057 → 5,043 lines (similar total, but `run_forward_pass` halved)
- `live_engine.py`: 1,796 → 1,733 lines (-63 lines)
- New shared modules: `checkpoint_loader.py` (142 lines), `engine_factory.py` (91 lines)
- Duplication eliminated: ~150 lines of checkpoint loading, ~60 lines of engine init
