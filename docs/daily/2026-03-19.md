# 2026-03-19 — Peak Override, ADX Filter, Live Sim, NT8 Bridge

## Session Summary
Major session: sensor-enriched exits, peak override (tried and reverted), ADX chop filter, daily drawdown stop, live sim debugging, NT8 bridge enrichment.

## What Changed

### Code Changes
1. **Full codebase cleanup** — -1,700 lines dead code (dead imports 33 files, dead methods, unicode em-dashes/arrows, duplicate assignments)
2. **Hard quality filter** — templates must have >=10 members, >=55% WR, <=10 ticks sigma. 436 -> 176 templates.
3. **Sensor-enriched exits** — belief_flip, tidal_wave require sensor confirmation. New peak_state_exit (inverted entry). Dropped belief_flip from ~631 to 19 fires.
4. **Peak entry gate** — `_1m_confirms_peak()` 3-layer gate: sensor opposition, fake peak (vol+fm), ADX chop (<15 = blocked).
5. **Buildup filter** — 10-bar buffer tracks P_center/F_momentum deltas. Requires exhaustion, buildup, or regime shift pattern. Blocks single-bar noise spikes.
6. **Sensor-adaptive trailing stop** — 40t MFE activation, trail % adapts to sensor state (50%/65%/80%).
7. **Peak override on exits** — TRIED AND REVERTED. Peak trades could override exit cascade when sensors agreed. Result: 5.8% WR, PF 1.01, equity trending down. Held losers too long.
8. **Daily drawdown stop** — $1K buffer below day-start PnL. Caps crash days. Feb 9: capped at -$1,048 (was -$8,869).
9. **IS warmup** — first 20 trading days context-only (no trades). Prevents cold-start contamination.
10. **CUDA cache clear** — `torch.cuda.empty_cache()` at startup + between phases. Shows free VRAM.
11. **Duplicate order fix** — `is_flat` now checks pending orders, not just filled positions.
12. **Peak skip logging** — INFO-level logs showing why peak entries were blocked (sensor/fake/ADX/buildup).
13. **NT8 Bridge 6.6.0** — DMI value + volume in every BAR/PARTIAL_BAR message. DMI = normalized (DI+ - DI-) / (DI+ + DI-).

### Files Modified
- `core/exit_engine.py` — peak override (added then reverted), PositionState fields
- `core/bar_processor.py` — ADX chop filter, buildup filter, peak skip logging
- `core/exits/breakeven.py` — sensor-adaptive trailing stop
- `core/exits/peak_state_exit.py` — NEW: inverted entry exit
- `core/exits/belief_flip.py` — sensor enrichment
- `core/exits/tidal_wave.py` — sensor enrichment
- `core/timeframe_belief_network.py` — unicode fix (arrow, sigma), sensor signals in exit_signal
- `training/trainer.py` — daily DD stop, IS warmup, CUDA cache clear
- `live/order_manager.py` — pending order check in is_flat
- `live/config.py` — account name
- `docs/NT8_BayesianBridge.cs` — DMI + volume enrichment

### Research (10 new tools)
- Peak template research: 174K peaks, 69% reversal rate, top features: range/volume/momentum
- Feb 9 deep dive: -$8,869 from 183 LONGs into selloff, 16:00-21:00 worst
- Inverted entry exit analysis: sensor fusion (1s vel + 1m vol) for exit timing
- Cluster analysis: 145 clusters / 524 trades (before), ~0 after peak override
- Fractal dimension research (Z): KILL verdict at both 1m and 1s resolution

## Results

### OOS Baseline (best run, $500 DD buffer)
- $14,336 PnL, 3,362 trades, 66.6% WR, $4.26/trade
- PF 1.97 (2.21 without Feb 9)
- Feb 9: -$534 (capped by $500 DD buffer)
- Max DD from peak: $1,174
- 2-5m holds: $7.08/trade, 74% WR

### IS
- $1,943 PnL, 2,010 trades, 56.8% WR, $0.97/trade
- Max DD: $334
- OOS outperforming IS — likely due to few HDBSCAN templates (3) + 20-day warmup skip

## What to Look for Next Run
1. ADX chop filter impact on IS — does it improve IS $/trade?
2. Feb 9 with $1K DD buffer — capped at ~$1,048
3. Peak skip logs — are we blocking too many or too few?
4. NT8 bridge DMI/volume — are values reaching Python correctly?

## Decisions Made
- Peak override REVERTED — sensors lag, trades held through full round-trips
- ADX < 15 = chop, block peak entries — validated: Feb 9 went from -$8,869 to -$24 (1 trade)
- Daily DD: $1K buffer below day start (not zero tolerance — 78% of days dip below $0)
- Direction: peak trades should bypass cascade, use peak-implied direction + 1m/1s confirmation
- Templates: move toward auto-seed library (31,605 seeds) instead of HDBSCAN clustering

## Next Session Priorities
1. Augment auto seeds with peak detection + sensor data + 192D context
2. Wire seed-matching entry system (replace HDBSCAN templates)
3. Quantum reconnect as feature enrichment on seed context
4. Re-run EE (BE optimization) and FF (conviction audit) on new trade population
5. FibonacciPivots as support/resistance context for peak detection
