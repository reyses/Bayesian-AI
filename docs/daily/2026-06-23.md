# Daily Journal: 2026-06-23


## Session Exit Report (01:45:11)

### 1. What Changed
Isolated the dynamic ATR zigzag Mamba pipeline into a dedicated research folder (`research/mamba_zigzag_baseline`). Fixed an input dimensionality mismatch crash on holiday data by enforcing exact 385 expected columns via Pandas reindexing in `dataset.py`. Fixed a file locking `RuntimeError` during checkpoint saves using atomic temporary file replacements. Autonomously executed a full 604-day dataset baseline batch run.

### 2. Files Modified
- `research/mamba_zigzag_baseline/pipeline/dataset.py`
- `research/mamba_zigzag_baseline/pipeline/train_mamba.py`
- `research/mamba_zigzag_baseline/reports/EDA_Baseline_Run.md`

### 3. What to Look For Next Run
The model generates stable loss (~0.19) but spikes occasionally (e.g. up to 1.65). Next runs should integrate the LLM Cortex inference loop specifically triggering on these loss spikes.

### 4. Expected Impact on Metrics
The baseline loss profile is established. The pipeline is now completely resilient to missing data feeds/holidays and file lock exceptions.


## Session Exit Report (21:27:03)

### 1. What Changed
Migrated the Mamba RL agent from a fully generalized 5-day curriculum with strict rule-based safety nets to a raw, 'naked' 1-day overfit curriculum. Removed all structural stop losses, trailing stops, and target-reached logic from the V2 exit suite within `mamba_env.py` to force the neural network to absorb the raw causal consequences of the price structure. Additionally enforced a strict 1-minute `DURATION_THRESHOLD` lockout on both the SCRATCH and REVERSE actions, forcing the agent to physically ride its entry choice for 12 bars before being allowed to exit or flip. Fixed a minor duration calculation bug in the RL reporter (Ledger was returning minutes, Env now correctly tracks exactly 5s bars elapsed).

### 2. Files Modified
- `research/mamba_zigzag_baseline/pipeline/train_mamba_rl.py`
- `research/mamba_zigzag_baseline/pipeline/mamba_env.py`
- `core_v2/telemetry/reporter.py` (added OSError exception catch for Windows file locking)

### 3. What to Look For Next Run
In the next run, verify that the agent's `Trades` count drops drastically and `AvgDur` skyrockets as it learns that the only way to minimize the massive -$5 entry penalties is to hold a position for an extended duration. Since all safety nets are off, the initial rewards will likely plunge much deeper than before until the structural weights finally align with the Kalman indicator within the `v2_vector`.

### 4. Expected Impact on Metrics
Drastically faster epoch turnaround time (reduced from 80,000 steps to 16,000 steps). Short-term reward metrics will plummet due to the removal of safety net exits, but long-term `AvgDur` and hold-times should stabilize much closer to the true structural trends.

## Direction pre-screen (funnel gate before Mamba) — NEGATIVE
- research/fspace_cadence/tools/direction_screen.py: causal F-space (B2T L1-L3) at pristine-regime START -> sign of forward move; HistGradientBoosting; train days 20/21/22 -> test 23/26 (disjoint); REAL vs Fourier null; horizons 30/60/120/300s.
- RESULT: REAL AUC 0.493/0.511/0.498/0.505 (~chance), acc ~50%, does NOT beat null (null often higher). Macro horizons (120/300s, the psychohistory bet) also fail.
- VERDICT: direction NOT forecastable at regime start with L1-L3 -> DO NOT build Mamba. Funnel did its job (killed the heavy build cheaply). Flip-TIMING/persistence edge stands; DIRECTION is a coin flip even conditionally.
- REMAINING cheap variants before closing direction: (1) wait-N anchor (start+N, the user's confirmation-window idea — N=0 tested here), (2) L4(lambda)/L5 features (B2TF, untested). If both ~0.5/<=null -> direction dead.
- Report: research/fspace_cadence/reports/direction_screen_2024_02.md.

## Direction screen — FULL variant sweep, CONCLUSIVE NEGATIVE (Mamba killed by funnel)
- Variants (B2T tiled, day-disjoint OOS, REAL vs Fourier null, HistGBC): L1-L3 {N=0, wait10, wait20} + L4/L5(B2TF) {wait0, wait10}, horizons 30/60/120/300s.
- BEST real-vs-null AUC gap across ALL variants = +0.008 (L1-L3 N=0 h30). L4/L5 best +0.006. All others <=null.
- USER THRESHOLD (Telegram 2026-06-23): edge >=0.10 real, 0.05 conditional, <~0.05 (e.g. .00079) = NOT signal. -> ALL variants firmly NOT signal.
- CONCLUSION: direction NOT forecastable from this F-space (any anchor/horizon/feature incl lambda). Mamba OFF the table (funnel saved the build). lambda(L4) does NOT predict direction (negative for 'complete the equation' DIRECTION thesis in this setting). Flip-TIMING/persistence structure survives (real>null, sig) but is not directional -> not tradeable alone. fspace_cadence DIRECTION thread CLOSED.
- Fixed direction_screen.py ragged-schema bug (align by COMMON_COLS across days; B2TF days had 231 vs 246 cols). New memory: signal-threshold-magnitudes.
- Reports: research/fspace_cadence/reports/direction_screen_*.md.
