# 2026-07-28 (autonomous, owner on TG)

## MAMBA/PATH probe — 85% was LEAKAGE; direction is trivial, pivot detection is the wall
Owner: "use mamba to improve it, there's got to be something" (path not snapshot).
- GRU over run-up path: acc 85% / AUC 0.92 vs snapshot GBM 62%. LOOKED like a win.
- LEAK CHECK (research/edge_probe/tools/path_sequence_probe.py --end-lead): sign(px-pivot)@cutoff alone = **97%** at end_lead 0/3/5. leg_dir_true is ~97% determined by which way price already left the pivot.
- => Direction is NOT anticipation — it's ~97% committed within ~3 bars of the pivot. GRU (85%) is WORSE than raw price-sign (97%); streams/cubic/z_se add nothing for direction; gov_dir 58-65%.
- CONCLUSION: same wall — the bottleneck is PIVOT/TURN DETECTION, not direction. Once you have the pivot, direction is free; the combiner burns 8 min getting there. Point the mamba at pivot detection, not direction. Cubic curvature-flip (leads turn ~20s) is the right pivot-detector thread to revive.

## Revived probability suite + cubic — CONVERGENCE on one unsolved core
- **Probability suite = research/recovery_dynamics/ + archive/tools/suites/trade_outcome_suite/.** Owner's "it recovers, hold" = the recovery_2024_2025 artifact. His second-leg suspicion CONFIRMED by its own definition ("recovery = ONE FULL OSCILLATION around entry" = the 2nd leg). DISTILLED verdict already quantified: 2nd-leg favorable only 52% (A2/A1 1.13), 9% death; reduces to unsolved oscillator-vs-runaway discrimination (death-pred AUC 0.572). "Hold it recovers" is NOT an edge.
- **Cubic (orange line):** orange_line_eda found curvature-flip leads slope-flip (the turn) ~20s causally — never made into a rule. BUT recovery_dynamics graveyard: cubic inflection as runaway/trend predictor ALREADY tested + FAILED (|d3| 0.61 return-vs 0.69, weaker). Skeptical.
- **CONVERGENCE of all 2026-07-27/28 threads:** direction trivial (97% price-sign), recovery 52% coin flip, cubic-inflection null, mamba-on-direction was leakage. ALL reduce to the SAME core: early OSCILLATOR-vs-RUNAWAY discrimination, stuck ~0.57 AUC across every attempt.
- **Proposed next (awaiting owner):** sequence/mamba over the run-up PATH predicting RUNAWAY-vs-OSCILLATOR (non-leaky, the right target) vs the 0.572 static baseline. Only angle not yet tried at the right target.

## CORRECTION — the real probability suite (owner pointed me to the records)
My "probability suite = recovery_dynamics" above was WRONG. The actual suite the owner meant:
- **tools/trade_outcome_suite/ (2026-05-22)** — trade-outcome probability tables over hardened NMP legs (continuation + recovery, MFE/MAE conditional, 95% CI).
- **2026-05-10** — the "it recovers, hold" cell (P_revert=0.71) DEBUNKED as SELECTION BIAS: predicted 71%, delivered 46-48% WR, losing (-$4..14/day). Table averaged mid-crash bars; mechanical firing lands mid-crash, not the cusp.
- **2026-05-22 verdict**: P(close>0|MAE>=d) 42.7%->25.6%; MEAN close NEGATIVE for every drawdown >=0.5R; deep-d rebound = SURVIVORSHIP not a hold signal; 99-100% of recovered legs dipped-then-rallied (mechanical = the owner's "second leg"). "Hold the loser, it comes back" NOT supported. Equity loss = ENTRY-QUALITY problem, not exit-safety. Giveback = fixed ~1R; only lever = entry filter.
- (recovery_dynamics is a SEPARATE later random-entry oscillation-clock study; related but not "the suite".)
- Reinforces today's convergence: only lever anywhere = entry/pivot quality, not exits/holds. [[project-oscillator-runaway-core]]

## SECOND-LEG CONFIRMED (probability suite repurposed) — owner suspicion proven
Repurposed the recovery/probability machinery into a second-leg decomposition (research/recovery_dynamics/tools/second_leg_confirm.py, full ATLAS, combiner entries + zz_confirm R-trigger pivots):
| thr | P(recover) | UNCOND | %recov via confirmed 2nd-leg pivot | med bars |
| 10pt | 79.3% | 78.1% | 50% | 18 |
| 20pt | 71.0% | 67.7% | 68% | 32 |
| 30pt | 63.5% | 58.9% | 80% | 44 |
- **DECISIVE: P(recover) ≈ UNCONDITIONAL at every depth** → entry adds ~zero recovery edge; recovery is the market's oscillation (any level returns at the same rate), NOT the trade being right.
- Depth-dependent second-leg: 50%@10pt → 80%@30pt of recoveries cross a CONFIRMED reversal pivot. The deeper underwater (where "hold it recovers" matters), the more it's literally a second leg. Shallow 10pt recoveries are half intra-leg wiggle (honest: not 100%).
- Confirms owner's suspicion + the 2026-05-10/05-22 verdicts: "hold the loser, it recovers" = watching the oscillation's 2nd leg, no edge. [[project-oscillator-runaway-core]]

## FALSE-PROFIT / CATASTROPHIC-TAIL of holding wrong trades (owner insight quantified)
Owner: holding wrong trades harvests small FALSE profits (2nd-leg oscillation past entry), paid for by a rare catastrophic non-recovery = negative skew, "catastrophic."
Tool: research/recovery_dynamics/tools/false_profit_skew.py. 49,637 combiner entries that went >=20pt adverse, hold-to-EOD:
- 72.2% recover (false-profit peak past entry median +67pt — NOT capturable, realistically small); 27.8% don't: mean terminal -116pt, worst -2055pt (-$4110).
- Never-bail-to-EOD distribution: mean -26pt (WORSE than cut@20pt=-20pt), median -25.5, p5 -262, p1 -469, worst -$4110. Catastrophe: 8.7% lose >200pt, 22.7% >100pt.
- CONFIRMS owner: negative skew, false profits don't pay for the runaway tail.
- **TWO CAVEATS (flagged, not yet resolved):** (1) hold-to-EOD overstates vs the REAL never-bail exit at R-trigger (which caps tail except on runaways where R-trigger never fires = the catastrophe). (2) Apparent TENSION with never-bail doc-107 (N=23k "no cut beats never-bail"): this slice conditions on already-deep-wrong trades; doc-107 measured full-population cut policies. Simpson-like — needs proper reconciliation before overturning never-bail.
- NEXT (offered to owner): redo with R-trigger exit + reconcile vs doc-107. [[project-oscillator-runaway-core]]

## DOC-107 REDO (leg-decomposition) — never-bail's edge IS the second leg (owner thesis proven)
Tool: research/exit_dojo/tools/doc107_second_leg.py (reuses select_wrongdir + stop_reenter_sim; N=23,378, validated NB advantage +3.39 = doc-107 exactly).
Decomposition of never-bail's +3.39 t/ep advantage over the best stop (X=48):
| class (leg) | share | NB mean term | %of NB profit | stop net | NB adv from class |
| good_clean (1st-leg win) | 17.9% | +294t | 48% | +0.0 | -0.00 |
| good_dipped (2ND-LEG recovery) | 25.3% | +224t | 52% | -212t | +53.72 |
| wrong (rode to loss) | 49.7% | -196t | — | +107t | -53.30 |
- **NET edge = +53.7 (2nd-leg harvest) − 53.3 (catastrophe-ride) = +3.39.** Razor residual of two huge opposing forces.
- **52% of never-bail's profit is the SECOND LEG** (good_dipped false-profit recoveries). 48% is genuine 1st-leg (good_clean, stops don't touch it = the REAL edge).
- Wrong-class tail never-bail eats: mean -196t, p1 -933t, worst -4501t (-$2250). A stop caps these; never-bail rides them fully to keep the good_dipped 2nd-leg wins.
- **THE PRIZE**: discriminate good_dipped (2nd leg → HOLD) from wrong (runaway → CUT) at the dip = the oscillator-vs-runaway core (~0.57 AUC). never-bail punts holding-all (+3.39); a stop punts cutting-all (-3.39); the discriminator beats both.
- Refines (not overturns) doc-107: never-bail net +11.76 t/ep still profitable, but its edge-over-cutting is fragile 2nd-leg harvesting + catastrophic tail exposure. [[project-oscillator-runaway-core]]

## FLIP-WHEN-WRONG — mechanical loses, but ORACLE-flip = 15.7× (the quantified prize)
Tool: research/exit_dojo/tools/flip_when_wrong.py (doc-107 population, absolute pnl, friction 2.4t/RT).
- Mechanical flip@X vs never-bail (+9.4 t/ep): Δ -8 to -16 t/ep, sig at early X. Works as theorized:
  wrong (50%): -198→+78t (rides runaway = catastrophe becomes PROFIT); good_dipped (25%): +221→-328t (whipsawed out of 2nd-leg recovery). good_dipped whipsaw eats the wrong capture → net negative.
- Owner's "earlier is better" caveat is INVERTED for MECHANICAL flip (earlier flips 84% indiscriminately → more whipsaw). Only holds WITH a discriminator.
- **ORACLE-flip (flip ONLY true wrong, hold rest) = +147 t/ep ($73.5/ep) = 15.7× never-bail.** Wrong-class swing +277t on 50% of trades.
- **KEY: flip > cut for harvesting — a cut only caps the loss, flip converts the catastrophe to PROFIT.** The 3 mechanical punts (hold-all=never-bail +9.4 / cut-all / flip-all) all near-degenerate; the discriminator is worth 15.7×.
- **PROGRAM #1 TARGET (quantified): the oscillator-vs-runaway discriminator (~0.57 AUC) → then FLIP.** Everything 2026-07-27/28 converges here. [[project-oscillator-runaway-core]]

## GOOD-DIP ENVELOPE + gated flips — gates reach never-bail PARITY, not better
Tools: research/exit_dojo/tools/{good_dip_horizon,time_gated_flip}.py (doc-107 pop).
- **Envelope**: good_dipped MAE median -17pt (p95 -67, deepest -221); wrong MAE median -48 (p95 -157). HUGE depth overlap → depth is a soft gate. good_dipped RECOVERY TIME: median 2m, p95 7m (fast).
- **Separation P(wrong | dip reached -D)**: 66%@2pt → 88%@40pt — climbs but never ~100% (survivorship rebound; 12% good_dipped survive -40pt). Soft.
- **Depth-gated flip**: best D=40pt = +7.85 vs never-bail +9.36 → still LOSES (-1.5). The 12% deep good_dipped survivors whipsaw.
- **Time-gated flip** (let dip work T min, flip if not recovered): BEST T=7min (=good_dipped p95!) = +8.91 → never-bail PARITY (-0.45, a wash). wrong-flip purity 87%. Validates the envelope but doesn't beat never-bail.
- **THE TRADEOFF (owner caveat quantified)**: flip EARLY = capture runaway (wrong -198→+78) but whipsaw good_dipped; flip LATE (time-gate) = spare good_dipped but eat the runaway damage first (wrong only -198→-160). Wait-and-see gates can't escape it.
- **CONCLUSION**: gates → never-bail parity; the +147 oracle needs flipping the true-wrong EARLY at the dip → requires a FEATURE discriminator (cubic curvature/velocity/regime), NOT a gate. good_dipped's 2-7min recovery envelope = the LABEL to train it. [[project-oscillator-runaway-core]]

## LEG-BODY ENTRY — "just be in a leg" FAILS (a detectable leg is a spent leg)
Tool: research/edge_probe/tools/cubic_body_entry.py (ATLAS 1m + cubic 7.5m slope, 150 days, friction 0.89pt/RT).
Owner: "enter randomly but make sure you're on the body of a leg." Cubic slope = leg-body detector.
- Enter sign(slope) when |slope|>=THR, ride till slope flips. ALL modes LOSE after friction.
- KEY: BODY (with slope) is NOT better than ANTI (against) or RAND — often WORSE at steep slopes: thr=40 body -2.57pt/trade, anti -0.41, rand -1.14.
- Why: a DETECTABLE leg body (steep cubic slope) = MATURE leg near EXHAUSTION. Steep slope = price already moved (97%-committed) = LATE = buying the top. Fading (anti) least-bad but still unprofitable.
- Same wall from the other side: value is at the PIVOT; by the time you can confirm "in a leg," the leg is spent. No free lunch in "be in a leg." Cubic slope carries no causal continuation OR reversion edge after cost.
- Reconfirms: catch the TURN early (oscillator-vs-runaway/pivot detector) is the ONE hard problem where value lives. [[project-oscillator-runaway-core]]

## Leg duration (reference)
R-trigger legs (pivot->pivot, 7,907 over full ATLAS): mode 4-6min, MEDIAN 13min, mean 23min (fat tail p99=135). p25/p75=7/28. Amplitude median 68pt/mean 86pt. Bigger legs longer (10-25pt=6min, 50pt+=17min). Full oscillation (2 legs) ~26min = recovery_dynamics median 27min. Combiner pivot+8min = ~60% of median leg gone.

## Leg-to-leg autocorrelation (owner: can previous leg locate us on the current?)
Consecutive R-trigger legs, within-day, N=7,388 pairs:
- AMPLITUDE lag-1 r=0.42 (log 0.45) — moderate; DURATION r=0.20 — weak.
- current/previous amplitude ratio: median 0.99 (current ~= previous size), middle-50% 0.66-1.5x, p10-90 0.44-2.2x. R^2(cur~prev)=0.18.
- ANSWER: YES moderately — previous leg predicts current SIZE in expectation (~1x), direction is free (opposite). Phase = displacement/prev-amplitude, but ±factor-2 per trade (rough compass not ruler).
- PAYOFF proposed: prev-leg-normalized ADAPTIVE runaway envelope (dip>~2x prev-amp=runaway/flip; <~1x=oscillator/hold) vs the fixed depth gate that capped at 88% purity. Regime-aware. [[project-oscillator-runaway-core]]

## Band as cusp region (owner: cusp = region in space = z_high/z_low)
- TEST 1 (band=cusp): |z_se| at confirmed pivots 1.16 vs 0.85 all bars — turns DO cluster at band edge. z_high/low ARE the oscillator cusp region. ✓
- TEST 2 (band-break as runaway discriminator, N=48,410, recover 71%): raw DEPTH AUC 0.818 for runaway; band-break z AUC **0.461** (worse than coin); depth+z 0.820 (z adds nothing).
- KEY: band z is a MEAN-REVERSION signal (high band-break → stretched → snaps back → RECOVERS = oscillator), the OPPOSITE of runaway. A RUNAWAY trends WITH the regression → band FOLLOWS → z stays modest. So the band is blind to runaways by construction; runaway = absolute-points (depth), not band-relative.
- Caveat: depth 0.818 partly circular (runaway=deep move); early prediction still the hard part, band doesn't help. [[project-oscillator-runaway-core]]

## New leg-region feature (owner: track the high/low region better) — does NOT beat depth
Runaway = no recovery in 240 bars (7.5% death rate), N=44,360. AUC for runaway:
- raw DEPTH 0.642 | depth/prev_leg_amp (proposed feature) 0.641 (identical) | depth/prev3-median 0.651 (+0.01) | prev_amp alone 0.484.
- Leg-amplitude normalization doesn't help: runaway is absolute-points; prev_amp (r=0.42) too noisy to add regime value; ratio median 0.95 (adverse ~= 1x prev leg) confirms autocorr but doesn't sharpen the call.
- PATTERN: every region/magnitude framing plateaus ~0.64 (band 0.46, depth 0.64, leg-norm 0.64); pre-depth ~0.57. Runaway/oscillator resists MAGNITUDE features.
- Constructive: the untried signal-TYPE is a DERIVATIVE (decelerating=revert vs accelerating=runaway). Cubic curvature candidate (leads turns 20s) though inflection-as-runaway-predictor is in the graveyard. [[project-oscillator-runaway-core]]

## LEG REVERSAL IS ~MEMORYLESS — the root of the wall (owner: plateau-region test)
Measured reversal hazard (leg ends within 3 bars) by leg_pos=disp/prev_amp, N=165,933 bar-obs:
- leg_pos 0-0.25: 34% (chop/false starts) → 0.5-0.75: 12% → 1.0-1.25: 9.5% (MIN, the "expected plateau") → 3.0+: 11%. Baseline 13%.
- The hazard does NOT peak at the plateau (leg_pos~1) — it's MONOTONE-DECREASING then flat-low. An established leg is the LEAST likely to reverse; reaching expected amplitude does not raise turn prob.
- **STRUCTURAL: once established (>~0.5x), a leg is ~MEMORYLESS — constant ~10% reversal hazard/3bars regardless of distance.** No plateau region exists.
- This EXPLAINS: (a) fat-tailed exponential leg duration (mean 23 > median 13); (b) WHY every position/region/phase/magnitude feature fails to anticipate the turn — memoryless ⇒ turn equally likely at any point ⇒ "where you are" carries no turn info.
- Likely the ROOT of the whole 2026-07-27/28 wall. The one real signal is EARLY (first bars off pivot high-hazard 34%; survive=established=persists). [[project-oscillator-runaway-core]]

## Hills-on-a-hill (multi-scale) discriminator — NEGATIVE (memoryless again)
Owner concept: dip = small hill on intact big leg (recovers) vs big leg turning (runaway); skew axis = detrend by big leg. Test: 30min big cubic trend at the dip vs runaway. N=14,266, runaway 7.4%.
- AUC runaway: depth 0.669 | big-trend dir 0.470 (worse than coin) | small-trend 0.495 | depth+big+small 0.676 (+0.007 = nothing).
- DECISIVE: recover-rate is IDENTICAL whether big trend favors (92.3%) or opposes (92.7%) the trade. Big-leg context carries ZERO info about the small-dip outcome.
- Consistent with the memoryless finding: reversal is memoryless → bigger context can't predict it.
- META: ~10 framings tested (direction/path/cubic/band/depth/leg-region/prev-norm/multi-scale), ALL ~0.6 AUC. Likely a genuine ceiling — turn/runaway not causally predictable above coin because the reversal is memoryless. [[project-oscillator-runaway-core]]

## ⭐ DETRENDED FRAME — the oscillation is NOT memoryless (owner "skew the axis" = KEY)
Reversing the memoryless finding. Detrend price by the 30min cubic (drift/skew), re-run zigzag (R=10) on the residual, N~26k detrended legs, 150 days:
- Leg amplitude autocorr: RAW 0.11 → **DETRENDED 0.53** (detrended sizes regular).
- Reversal hazard by leg_pos: RAW ~memoryless (26→39% flat) → **DETRENDED 27%→47%→64%→77%(@leg_pos 1.0-1.25)→83%** — a STRONG PLATEAU. At ~1x expected detrended amplitude, reversal hazard ~77-83%.
- Cusps ALIGN: detrended troughs -15pt / peaks +15.7pt = symmetric ±15pt band (owner's "cusps align once you remove the skew").
- => The oscillation is MEAN-REVERTING with a characteristic amplitude; the DRIFT hid it on the raw axis. First break of the ~coin ceiling all session.
- CAVEAT: detrending by a trailing curve CAN manufacture mean-reversion; decisive test = causal tradeability after R-lag+friction (running next). Cubic is causal (trailing endpoint) so not the classic look-ahead trap, but must verify P&L.

## Detrended fade — STRUCTURE real, TRADEABILITY fails (reversion toward moving drift)
Causal fade in detrended frame (research inline): short the extended detrended move at plateau (leg_pos>=THR), exit on detrended reversal, raw-price P&L, friction 0.89pt/RT, 150 days.
- LOSES: -0.86(THR.5)/-1.91(.75)/-3.69(1.0)/-3.25(1.25) pt/trade. Small N (90-153).
- Mechanism (hypothesis): detrended reverts 77% but toward the MOVING drift; raw price = detrended+drift, so the fade wins on paper (detrended) and loses in cash (raw). Can't monetize "price reverts to its own trailing trend."
- Net: owner's skew insight is STRUCTURALLY correct (reverses memoryless) but the revealed mean-reversion is around a moving drift → no raw edge from a simple fade. Next: confirm via detrended-P&L (+ vs raw -); ask if an exit/scale harvests it or if you must trade the DRIFT not the wiggle. [[project-oscillator-runaway-core]]

## ⚠️ RETRACTION — the detrended "breakthrough" was a DETRENDING ARTIFACT; MEMORYLESS STANDS
The 30-min cubic used as "drift" HUGS the price (endpoint tracks price tightly) → detrending leaves high-freq noise that mean-reverts trivially (bounded residual reverts to its tracking curve). That manufactured the autocorr 0.53 + 77% hazard plateau. The example plot exposed it (drift ≈ price line).
Re-tested with a PROPER SLOW drift (60/120/240-min LINEAR trend):
- amp autocorr: 0.19 / 0.12 / 0.10 (≈ raw 0.11) — NOT 0.53.
- hazard 0.25x→1.0x: 27→39% / 29→36% / 33→35% — FLAT (memoryless), NOT 27→77%.
=> Structure vanishes with a real drift. **MEMORYLESS FINDING STANDS; the fade losing was correct; no real edge.** Lesson: detrend SCALE matters — a fast/price-hugging curve manufactures fake mean-reversion. The owner's demand for plots is what caught it.

## PIVOT: capture the HUMAN process (owner: "run the dojo and explain what I do")
After 2 days of mechanical features all at the ~coin ceiling, pivot to capturing the owner's discretionary process (his manual picks work where mechanical rules fail — doc 2026-05-10).
- Built + tested: research/dojo_forge/tools/human_dojo.py — blind episode recorder. Serves gen0 packet frames one at a time (no chart/future), owner types read+decision each frame (HOLD/EXIT/LONG/SHORT/TURN logged), /commit reveals outcome. Logs reports/human_dojo/<eid>.<who>.jsonl.
- Alt: dojo_replay --blind (visual blind) for narrating while seeing causal price.
- CRUX experiment: is the owner better than coin BLIND (causal) or is the edge HINDSIGHT? Then mine his notes for the missing feature.

## CHART replay recorder (owner: "no chart?" — chart trader needs a chart)
- Built + tested: tools/viz/plugins/chart_replay_recorder.py. Causal bar-replay in the VizEngine: day price with future fogged; f/F advance cursor, v back; t/l/s/e set click type (TURN/LONG/SHORT/EXIT); CLICK marks a call at the revealed price (can't click the fog); u undo. Logs bar+ts+price+type+bars_early to reports/human_dojo/replay_<day>.<who>.jsonl.
- Fixed epoch bug (pandas 2.x datetime64[s] → astype('datetime64[s]').astype('int64')).
- Run: python -m tools.viz.run --plugin chart_replay_recorder --day <YYYY_MM_DD> --who moises
- NEXT: owner marks turns on several days → score human turn-calls vs true pivots (hit-rate, timing, earliness) = the crux (causal edge vs hindsight).

## Owner process captured + signal #c tested (flat+alternating -> leg)
- Captured owner's discretionary method (reports/human_dojo/OWNER_PROCESS.md): 1m last-3-cusp levels + 1h confluence + flat/alternating closes = cusp tell -> leg coming.
- Tested #c naive (leg>=M within 10 bars from flat bar): OPPOSITE — consolidation predicts FEWER/SMALLER legs (P(leg)=7.8% vs 39.6%, moves 9.8 vs 22.3pt) = chop begets chop.
- Mis-tests the real signal: leg comes on RESOLUTION (breakout) not during flat; flat-alone ignores cusp-LEVEL + 1h confluence. Right tests proposed: compression->expansion breakout; flat AT a cusp level.

## chart_replay_recorder — added owner's TOOLS + zoom-fit
- Added: cubic/orange line (7.5min, causal to cursor) + last-3-cusp horizontal levels (causal zigzag R=25) — so the owner records with his actual toolkit visible.
- Added zoom-fit: view auto-fits to revealed ~3h window on cursor step (f/F/v) + 'z' key (was unusable full-24h compressed). ylim fits revealed price+levels.
- OWNER_PROCESS.md now has all 9 elements (levels, location, 1h, compression tell, NMP energy, alternation, cubic state).

## chart_replay_recorder — CANDLESTICKS added (owner: "add the candle")
- OHLC candlesticks (green/red, vlines wicks + ax.bar bodies) up to cursor, causal; overlaid with cubic + cusp levels + zoom-fit + fog. Loads OHLC from DATA/ATLAS/1m aligned to engine bars.
- Recorder now = faithful trading chart: candles + cubic state + last-3-cusp levels, revealed bar-by-bar, click to log turns. Ready for owner's desk session.

## chart_replay_recorder — sigma bands added (owner: "add the sigma bands")
- Sigma bands = cubic ± 1σ/2σ where σ = rolling std(close−cubic, W=20), causal; shaded ±1/±2 + dashed ±2 lines, behind candles. self._sigma precomputed in _ensure_day.
- Recorder toolkit now COMPLETE per owner: candles + cubic + sigma bands + last-3-cusp levels + zoom-fit + causal fog + click-to-log-turns. Ready.

## chart_replay_recorder — scoped to last 45min + windowing fix (owner)
- VIEW_BACK=45/VIEW_AHEAD=5. All overlays (candles/cubic/sigma/levels) now drawn ONLY over the visible [cur-45..cur] window; fog limited to +5 bars (was end-of-day → autoscale blowout); cusp labels at view-left; _fit_view() called at end of draw to lock x+y (holds even without engine's xlim-restore). Verified headless (50min-wide, tight y).
- Recorder is now a faithful, readable 45-min trading chart with the full toolkit. Ready for owner's calls.

## chart_replay_recorder — current-bar sigma horizontal lines (owner "the fun part")
- Added horizontal axhlines at the CURRENT bar's cubic ± {1,2}σ (blue) + µ=cubic (dotted orange), labelled on the right; included in y-fit. Update per bar as cursor steps. Full toolkit: candles + sloped sigma envelope + current-bar band lines + cubic + last-3-cusp levels.

## chart_replay_recorder — TIMEFRAME TOGGLE 1m<->5s (owner: enter on 5s)
- Full tf-aware rewrite: loads both 1m+5s OHLC; cubic computed on 5s + mapped per tf; sigma per tf; cursor as TIMESTAMP (preserved across toggle); engine base-line covered by white rect so plugin owns the chart at any tf. Keys 1/5 toggle; f/F/v move by tf bars; click logs {tf,ts,price,type,bars_early}. VIEW_BARS 1m=45/5s=150.
- Verified headless: 1m+5s render, toggle preserves 02:50 cursor, 5s SHORT click logged. Live keys/clicks need owner verify.
- Recorder now supports owner's real multi-TF entry: read 1m structure -> 5 -> place entry on 5s -> 1.

## chart_replay_recorder — TF-toggle scaling fix (owner: candles didn't scale)
- Bug: engine.draw restores the PRE-draw xlim, so on 1m->5s toggle the 5s candles were drawn into the old 1m 45min window (squished). Fix: _redraw() = _fit_view() BEFORE engine.draw(), so the engine captures+restores the new TF's window. Verified via engine-restore simulation: 1m=50min, 5s=~17min, back=50min. All interaction handlers route through _redraw.

## chart_replay_recorder — zoom + major levels + proximity (owner: exit-long = level proximity)
- Zoom: self.zoom (- out ×1.7, = in, z reset); scales VIEW_BARS in _fit_view + draw. MAJOR levels from bigger-R zigzag (1m=70/5s=40), thick, filtered to visible range. Proximity readout: current price → nearest major, Δpt; turns red "CLOSE (exit zone)" within PROX=6pt = owner's exit-long trigger.
- Owner's exit-long = zoom out, check if current top is in close proximity to a major level → exit; else hold. Now explicit + causal.
- OWNER_PROCESS complete: theme + cusp-levels + cubic-side + alignment + sigma + NMP energy + zoom/major-level proximity exits.

## Level coordinate system + σ-telescope (owner capture, PM)
**What changed:**
1. NEW `research/dojo_forge/tools/level_coordinate_system.py` — codifies owner's hand-drawn levels (Figs 1-8) as a deterministic TF TELESCOPE (day/4h/1h). Greedy density-peak + NMS per scale (owner rule: "touch as many highs/lows as possible, a region expressed as a line"). Emits per-bar coordinate features (norm_pos, dist_up, dist_dn). Validated 2026_07_14..16: day 29458(9t) wall, 4h 29223 support-turned-resistance, 1h 29160(6t) live pivot — matches owner's eye. Render: reports/human_dojo/levels_<day>.png.
   - Key finding: single flat R over 3 days = 25% coverage (FAILS). Telescope (per-scale R/window + dedup) is mandatory — confirmed owner's "you got to do a TF telescope."
2. EDIT `tools/viz/plugins/chart_replay_recorder.py` — LIVE σ sample-size knob ([ / ] keys) sweeping the horizontal σ levels, centered on cubic. Stores residual (c−cubic) per TF, recomputes σ at self.sw[tf] on redraw. Title shows σW.
   - Empirical confirm of owner's "σ bands reveal levels automatically": swept W telescopes ±nσ through the level hierarchy (W=20→1h 29160/29185; W=60→−2σ=29160 exact; W=120→−2σ=29121,+2σ=29223 4h; W=240→+σ=29276). CAVEAT: must center on cubic (detrend), else large-W raw-mean drifts on trending windows (+2σ off by 160pt at W=480). Recorder already centers on cubic → built right.

**Files:** level_coordinate_system.py (new), chart_replay_recorder.py (σ knob), OWNER_PROCESS.md (2 entries).
**Next run:** open recorder, sweep [ / ] to see σ telescope; decide whether to replace recency CUSP_R/MAJOR_R with the density-telescope lines; optional N→N+1 stability test on the coordinate frame.
**Impact:** codifies the owner's coordinate frame (the load-bearing "where am I in space" judgment) into a computable feature — ties off the old z_high/z_low / "we need a new feature" thread. Not a predictor; the frame the real signals are read against.

## HIGH GEAR: owner process v1 codified + causally backtested (PM, full run)
**Build sequence (all in research/dojo_forge/tools/):**
1. level_coordinate_system.py upgraded: wick+body candidate touches (dedup by pivot event), sep=2τ anti-clutter, Agg guard for plugin import.
2. level_frame_stability.py — N→N+1 gate: **overnight-STATIC frames FAIL** (capture 0.31 vs null 0.41; trend days leave the old frame, e.g. 06_15 cap 0.00). Confirms owner's own repositioning practice → frame must be ROLLING/causal. Report: reports/human_dojo/frame_stability.md.
3. chart_replay_recorder.py — causal TF-telescope frame wired in (refit at cursor, cached per 1m bar; replaces recency CUSP/MAJOR; legacy fallback if day-scale empty).
4. owner_process_v1.py — full causal backtest of the captured process: THEME (cascade ≥2 agree/none oppose) + ENTRY (frame-band touch + cubic agrees + MIN_TARGET_PT room + cooldown) + EXIT (other-cusp target / harsh-turn scratch / theme death / EOD). Friction 0.89pt RT.

**RESULTS (163 days, DATA/ATLAS_NT8/1m):**
- v1 (with σ-scaled scratch): −$32.28/day CI[−61.5,−3.8] **SIGNIFICANT LOSS**, 2376 trades. Decomp: target +$41/t (703) vs scratch −$23/t (1371). **Doc 107 re-confirmed**: the scratch IS a cut policy and cuts lose.
- v1.1 (scratch removed): +$2.02/day CI[−36.5,+39.3] — dead coin. Target +$47/t (877) EXACTLY offset by theme-death −$47/t (866).
- v1.2 (hold through gray, exit opposite-flip only): −$16.16/day n.s.; 1139 targets +$51/t but 162 opposite-flips −$307/t (full-reversal tail) + EOD −$128/t. **Doc 098 re-confirmed**: exit-policy shuffling is a wash.
- Smoke-sample warning validated: 12-day run showed +$36/day; full run killed it. Never tune on the smoke window.

**CONCLUSION: the mechanizable skeleton of the owner's process is a COIN at scale.** The edge, if real, lives in the judgment layer the skeleton doesn't capture: theme READ quality (the "music" ≠ rolling slope), 5s entry timing, selectivity (~5/day vs 15). Joins the program's finding: every mechanical thread lands at coin; oscillator-vs-runaway is the wall.

**THE DECISIVE NEXT EXPERIMENT (reason-not-feelings, purest form):** same-days owner-vs-machine. Owner trades replay days BLIND in chart_replay_recorder; score P&L vs v1.1 on identical days. Owner >> v1.1 ⇒ the delta IS the judgment layer → THAT's what the dojo/distillation should chase. Owner ≈ v1.1 ⇒ the process is coin and we say so with receipts.

**Files:** owner_process_v1.py + reports/human_dojo/owner_process_v1{,_1_nocut,_2}.{csv,json,md}, frame_stability.{md,json}, level_coordinate_system.py, level_frame_stability.py, chart_replay_recorder.py.

## POCKET DOJO (owner counter-proposal, late PM)
Owner rejected desk-bound replay task + my Android-app pushback → counter-proposal accepted: casual phone dojo where the Telegram BOT serves images and the SESSION (Claude in the TG thread) is the companion. Key upgrade over blind sit-down: narration+chart-state pairs = the distillation corpus (judgment layer captured as text), scored vs v1.1 passively on the same days.
**New:** `research/dojo_forge/tools/pocket_dojo.py` — stateful fogged replay engine (state gate_state/pocket_dojo_state.json, corpus reports/human_dojo/pocket_<day>.jsonl). Commands: new/step/chart/sigma/call/exit/note/score. Causal next-bar-open fills, target/stop/EOD, friction 0.89pt. Render: candles+cubic+σ(sweepable W)+telescope frame+fog. Smoke-tested end-to-end (call→fill→exit→P&L→log). First session launched to owner's phone.
**Workflow:** owner texts plain words in TG → session translates to pocket_dojo commands → bot sends frame → session replies as companion → everything logged. NO Android app (rejected: 2nd platform, charting rebuild, data plumbing, double maintenance forever).

## QWEN DOJO PLAYER (owner proposal before sleep: "qwen tries the same approach, taught by you")
The teacher-student north star in miniature: Claude teaches qwen3:14b the owner's process via a CURRICULUM system prompt (frame-as-coordinates, theme cascade, level-bounce entry, other-cusp target, NO scratches per doc-107, ≥10pt room, ~5 trades/day selectivity). Qwen plays fogged days under THE SAME engine semantics as owner_process_v1 (next-bar-open fills, target touch, EOD, friction) → apples-to-apples three-way: OWNER vs QWEN vs v1.1-machine on identical days.
**New:** `research/dojo_forge/tools/qwen_dojo_player.py` — llama_cpp GPU (blob sha256-a8cc..., 37s load, ~2.5s/decision), decisions every 15 bars, <think> stripped, JSON actions, all reasoning logged (corpus). Pilot day 2026_07_16: qwen held with process-grounded reasons ("theme conflicting"), 1 selective trade citing curriculum verbatim, −$25 vs v1.1 +$369 (N=1, meaningless).
**Running overnight:** 10-day eval seed 3 (task b7ruwn39g) → reports/human_dojo/qwen_player_summary.json with v1.1 same-day baseline comparison.
**Infra note:** no ollama daemon on this box — dojo runs llama_cpp directly on the ollama blob store at /media/moi/WindowsCode/ollama/models/blobs; CUDA libs via the nvidia pip wheels LD_LIBRARY_PATH (launch_dojo.sh pattern, moi-env variant).

## Qwen player run 1 (seed 3) — INVALIDATED by target-side bug, but informative
- Result as-run: qwen −$186/10d vs v1.1 −$541 on the SAME days. But 4 of qwen's 6 losing closes were "target" fills at NEGATIVE pts: qwen set its LONG target AT the support it entered from (not the next level above), and the engine's guard only checked |Δ|≥2pt, not the SIDE → instant "target" fill at a loss (−16.5/−30.6/−52.6pt).
- Fixes: (1) engine rejects targets on the wrong side (long: ≥px+5; short: ≤px−5; logged as reject_bad_target); (2) curriculum now states target must be ABOVE for long / BELOW for short, never the entry level.
- Valid observations that survive: qwen is SELECTIVE (595 hold / 7 long / 0 short — note: never shorted, long-only bias worth watching), holds cite the process correctly ("theme conflicting"), 4 of 10 days zero trades. v1.1 lost −$541 on these 10 days (it churns in chop; qwen sat out the −$200/−$247/−$246 days entirely — selectivity IS the machine's missing piece).
- Re-run with fixes: seed 4, task biizb8rhc.

## Qwen player run 2 (seed 4, target-side fix) — CLEAN RESULT
- qwen(taught): +$21.66 total, +$2.17/day CI[−8.4,+12.7] — coin, N=10 days / only 3 trades (2 target winners +15.9/+13.3pt, 1 exit loser −18.4pt). Guard rejected 5 more bad-side targets (qwen still sometimes aims at its own entry level).
- v1.1 same 10 days: −$337 (incl. a −$594 and a −$235 day — qwen took ZERO trades both days, holds citing theme conflict). NOTE: v1.1 full-run is +$2/day coin, so its −$337 on this slice is noise — do NOT read "qwen beat machine" as significant.
- Persistent qwen bias: LONG-ONLY (15 long calls across both seeds, 0 short) despite a symmetric curriculum. Watch/probe later.
- **Three-way scoreboard status: MACHINE = coin (N=160, powered). QWEN(taught) = coin-to-slightly-positive, unpowered (3 trades). OWNER = unmeasured — pocket dojo live, awaiting play.** The curriculum's transferable piece is SELECTIVITY (sit out chop); entry edge unproven at this N. Next lever if pursued: more days per run (power), short-side probe, and teacher feedback loop (memo) on rejected/bad targets.
