
## ADX->label overlap (077): first stable directional signal
ADX(SMA 84/25) agrees with active AI label direction 0.57/0.58/0.61 across 2024/2025/2026
(baseline 0.50, day-block CIs exclude it, NO tuning after 2024Q1) — weak (+7-11pp,
conditional band) but replicated 3 years and fires EARLY (most signals in first half of
label). Detects only 3% of labels -> a stage-0 combiner FEATURE, not a standalone.

## ADX at label transitions (078)
ADX = turn-confirmer: 0.69-0.78 agreement within 2min of a turn, decaying to 0.60 by 10min,
INVERTING past 10min (0.45 current / 0.55 NEXT label, median 16min before the next turn).
Pooled 0.58 hid this. Caveat: oracle clock; needs causal zigzag pivot-age to be live.

## ADX logistic P(right) OOS (079)
AUC 0.660 OOS (2025+26, trained 2024 only); tercile agreement 0.39/0.62/0.74 (35pp spread,
day-block CIs non-overlapping); low tercile <0.5 => INVERT = ~61% right. Pre-registered
check passed: age-x-alignment interaction carries the inversion (coef +0.553 vs align -1.24).
First stage-0 signal calibrated. Next: same pipeline on zigzag + remaining dossiers.

## AUTONOMOUS NIGHT (080-081): 12-dossier signal league + pooled combiner
Built `research/nt8_catalog/tools/dossier_signal_pipeline.py` (12 article-faithful causal
generators, specs cited to verified Batch A/B detectors + audited legacy; continuous
windows; skip-list honored). All 576 label days, train 2024 / test 2025+26, day-block CIs.
- LEAGUE (OOS AUC): OHLC-01 **0.841** (terciles 0.07->0.77, BOTH tails actionable, ~1/day);
  PIVOT-16 0.939 on base 0.05 (pure inverter — "bounce" premise backwards); VWMA-10 0.714;
  ROUND-05 0.623 (high tercile 0.75 on N=9.7k); SEASON/CROSS/DOW/VWAP/TUNNEL 0.60-0.62;
  ZIGZAG 0.556 on 0.96 base; ORB-02/ATR-09 dead (tautology-flat).
- ZIGZAG phase split: MODE of confirm = label's last minutes (rear-view), but 43% of
  confirms land phase 0.1-0.5 with 21-40 min left at 0.94-0.97 agreement = the turn-clock.
- COMBINER (pooled logistic + consensus feature, N=154,760 fires): **OOS AUC 0.687**,
  decile calibration MONOTONE-HONEST (pred 0.28->obs 0.25 ... pred 0.83->obs 0.80);
  tails = 75% right inverted / 80% right acted, ~17.3k OOS fires. P(label-right), NOT P($).
- INCIDENTS: C: hit 0 bytes free (crash mid-run; freed 3.6GB via npm/pip cache cleans —
  DISK NEEDS A REAL PLAN); VWAP multi-fire bug fixed pre-run (prime-on-crossing).
- Files: tools/{dossier_signal_pipeline,combiner_preview,zigzag_phase_in_label,
  league_merge_from_rows}.py; reports/{dossier_signal_league,combiner_preview,
  zigzag_phase_in_label}.md; comms/081. Next: economic conversion, overfit-decay
  shelf-life, Mamba state-vector handoff (proposals in 081 §7 — nothing executed).
