# 2026-07-24 — Overnight autonomous session: policy consultant cycle CLOSED; speed chain recovered from a 6-hour stall

## Headline
The Mamba policy architecture completed a full **two-consultant adversarial review
cycle** (3 amendment rounds) and is now FROZEN pending owner ratifications + a
gate-passing teacher. The speed chain hit a shell bug (bare `wait`) that stalled it
6 hours; recovered, fixed, restarted. Gen-1 gate run still queued behind it — will
complete during the day, not by morning (expectation reset).

## 1. Policy proposal — consultant cycle complete
File: `research/mamba_zigzag_baseline/POLICY_PROPOSAL_v1.md` (v1 + amendments v1.1/v1.2/v1.3)

- **v1.1** (consultant-1, approve-conditional): γ=1 pinned (real math bug caught —
  F=γΦ(s′)−Φ(s) invariance), entry-head freeze recommended, soft-CE + exit-event
  reweighting (margin weighting REMOVED), fidelity metrics pre-registered, hard KL
  constraint, gate margin above CI>0.
- **v1.2** (consultant-2 part 1): NEW attack accepted — **absorbing-state asymmetry**
  (EXIT ends episode; symmetric leash blocks duration exploration exactly when anchor
  exits early). Resolution: **ASYMMETRIC LEASH** — wide ε for HOLD-extension
  deviations, tight ε for early-EXIT deviations; protection points the same direction
  as measured physics (never-bail N=23,378). Ratio pre-registered, not tuned.
- **v1.3** (consultant-2 part 2 — 5-step sequence triaged):
  - ADOPTED: teacher temperature-calibration before Layer-1 dataset (leakage guard:
    fit on training-side days only, temperature frozen+hashed).
  - ADOPTED: leash upgraded to **ε(direction, duration)** — v1.2 asymmetry + wider
    late in rides (anchor off-distribution there; holding cheap per physics).
  - TEST-GATED: DAgger 50/50 (consultants disagreed — c1 "once if needed", c2
    "always 50/50"; resolution: cheap on/off-trajectory gap test first, 50/50
    activates only above pre-registered threshold).
  - Already done: γ=1 (concordant).
  - PUSHBACK: Beta-skewed fill rejected — owner's uniform-in-next-1s-bar +
    adverse-stress brackets reality with ZERO free parameters; Beta shape = tunable
    knob on the cost model. Recorded as fallback.
- Commits: 80612998 (v1.1), 36220770 (v1.2), af9ba0ca (v1.3).
- **Owner ratifications open**: entry-head freeze, asymmetric+gated leash,
  calibration step.

## 2. Speed chain — stall, root cause, recovery
- **E1c (ngram-mod speculation)**: bench completed 00:54 but chain HUNG 6h — root
  cause: `bench()` used bare `wait`, which waits on ALL background children incl.
  the llama-server (never exits). Fixed: `wait $CPIDS` (curl pids only).
- **E1c recovered verdict** (manual, from server timings):
  - Real reasoning text (task 1): **41.28 tok/s @ 44.5% draft acceptance** vs ~34.2
    baseline ≈ **+20%** — promising.
  - Degenerate-repetition arms (tasks 3/4): 186–193 tok/s @ 99–100% acceptance —
    ARTIFACT (temperature=0 repetitive prompt), excluded.
  - **E1c-real (actual frame prompts) still required before adoption.**
- Chain restarted 07:10 from E3 (skipping recovered E1c): E3 (-np 4, KV q8_0, full
  offload) → E1b (GPU draft, acceptance-rate decision ≥65%) → "chain done" marker
  releases the armed gen-1 gate (pid alive, ctx 14336, 156 eps).
- **Lesson (standing)**: in a script that backgrounds a server, never bare-`wait`;
  and gate stages on GPU memory, not pgrep (heredoc procs unmatchable).

## 3. What to look for next
1. `e3_e1b.log`: E3 aggregate tok/s (concurrency win) and E1b draft acceptance
   (≥65% = adopt GPU draft; else ngram path pending E1c-real).
2. `gen1_gate.log` → `acceptance_results_gen1.jsonl`: gen-1 (GENOME v1) vs
   never-bail, day-block bootstrap CI, vs 1.07% noise floor. THE number of the day.
3. Owner ratifications (policy v1.1–v1.3 items) + standing reviews (genome fork
   defaults, docs 151/152, frontier-stratification vs memory pilot).

## Files changed
- `research/mamba_zigzag_baseline/POLICY_PROPOSAL_v1.md` — amendments v1.2, v1.3
- `/home/moi/e3_e1b_chain.sh` — wait-bug fix, E1c excised, log append mode
- `/home/moi/e3_e1b.log` — E1C_NGRAM manual recovery entry

## Expected impact
No production-code changes. Policy plan de-risked by external red-team; speed
verdicts + gen-1 gate result determine whether the teacher loop is fast enough and
whether GENOME v1 clears (or approaches) the never-bail frontier.

## 4. Midday: E2 port into the gate harness (owner-approved kill+restart)
- Gen-1 one-shot run stopped at 19/156 (archived to
  `acceptance_results_gen1_oneshot_partial.jsonl`); pace had degraded to
  13.6 min/ep. CORRECTION to earlier claim: census evaluates ALL frames
  regardless of decisions — the slowdown was 6 CPU-spilled layers + desktop
  contention, NOT gen-1 holding longer.
- Ported E2 anchor-KV two-phase prefill into `eval_native_tiered.py`
  (`--prefill anchor2p`; oneshot default unchanged). Validation drill caught:
  (1) BPE boundary merge → 19/20 fallbacks on one episode → fixed via
  common-token-prefix derivation against a real frame tokenization;
  (2) cross-process bitwise A≠B (Δlogit ≤0.8, 0 decision flips) → this is the
  stack's known run-to-run drift (the 1.07% noise floor's cause), not the
  port; verdict bar recalibrated to decision-level, the level the gate uses.
- Layout delta anchor2p vs oneshot: 0/44 decision flips.

## 5. Evening: the self-repair drill saga (owner-directed) — PASS on round 2
Owner mindset ratified: layered self-heal = the architecture for everything
("the daemon self heal thats the mind set we should follow").
- **Round 1 (FAIL, 3 dead rescue agents, ~40 min bridge downtime):** drill
  caught (a) invalid `Write(path)` allow-rules — only `Edit(path)` covers
  file-editing tools (2 agents died at launch); (b) **systemd cgroup murder**:
  a oneshot unit's exit kills its "detached" Popen children —
  `start_new_session` is NOT cgroup escape. Every timer-spawned rescue agent
  had this flaw. Fix: `systemd-run --user --collect` transient units; fixed
  unit name doubles as a single-instance lock.
- **Round 2 (PASS, fully autonomous, sabotage→recovery ≈15.5 min):**
  strike 1 restart 17:54 → strike 2 escalation 17:58 → repair Sonnet as own
  unit → read journal traceback → removed EXACTLY the sabotage line (git diff
  clean) → restarted daemon → verified polling → reported to owner's phone.
- **Standing lessons:** a chain is only drilled when every link fired for
  real; spawn syntax and spawn *survival* are links. /cli deal (owner):
  invoking /cli = incident + pre-authorized infra breakage (science layer
  excluded). Excursion-strike ladder doctrine + salvage-zone floor placement
  (owner: 50pt salvage → +$5k/20d backtest — needs CI receipt, re-measure).
- Bridge stack now: Restart=always → tg-verify (2min, strike counter →
  code-repair Sonnet) → tg-watchdog (5min → fallback chain claude→agy) →
  ag-sentinel (2h, independent-vendor judge) → /health /fix /cli /agy → WoL.
