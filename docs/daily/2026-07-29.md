# 2026-07-29

## "Police reminders" mystery solved (AM)
Owner's "remove the police reminders" = the tg-nudge systemd timer nagging about STALE policy-ratification items (entry-head freeze / eps leash / teacher temp-calibration — RL era, Jul 24) still sitting in tools/telegram_bridge/state/pending_for_owner.json. The nudge system itself is healthy; the session failed its contract to clear items. Cleared + replaced with current asks (CLAUDE.md prune question, pocket dojo frame). Offered quieter/off options for the nudger. CLAUDE.md prune awaiting owner's A–E pick.

## Pocket dojo bug: stray debug command reversed an owner exit (self-caught, fixed)
While executing an owner "exit, close and watch" I left a leftover placeholder `call long --now` guard-check in the command sequence that actually fired (the new `--now` flag made it a live no-op-turned-real call). It reversed the flat exit into an unintended long. Caught and voided within the same turn before the owner saw wrong state; corrected P&L (short closed +9.9pt as owner called) sent with full disclosure. Lesson: never leave "placeholder"/"no-op guard" commands in a real execution sequence — pocket_dojo has no dry-run mode; every command is live.

## Pocket dojo gap found: 4h/day theme can't compute (no prior-day context)
Owner asked about the theme/trend before bar 50. Answer: bars 0-49 rose ~40.5pt (prior uptrend, off-screen), then 1h rolled to DOWN (-26.9 slope, past deadband) — theme flipped. But 4h/day scales show "warming" — pocket_dojo only loads the SINGLE fogged day (no prev-day concat), so 4h(240) and day(480) windows can never fill on an early-session bar. owner_process_v1's backtest concatenates prev+current day precisely to avoid this. FIX NEEDED: pocket_dojo._bars / state should load prev day too for theme continuity, same as owner_process_v1.run_day. Not urgent tonight (owner is exploring, not scoring), but note before drawing conclusions from pocket-dojo theme readouts.

## Pushed back on running build_dataset.py solo (owner asked, autonomous mode)
Owner asked me to backfill F-space via core_v2/build_dataset.py. CLAUDE.md flags this entry point "(user-runs)" deliberately — heavy CUDA job writing DATA/ATLAS feature parquets in place. Declined to fire it off solo in autonomous mode; sent a scoped ready-to-go command (--start 2026-03-21 --end 2026-07-28, incremental) and asked for explicit "go". Holding per project convention until confirmed.

## build_dataset.py: auto-mode classifier blocked the run even after owner go-ahead
Owner said "daytime, proceed" clearing the user-runs hold. Attempted the scoped backfill command; blocked by the session's auto-mode permission classifier (heavy GPU/data-write job), independent of the CLAUDE.md convention. Did not attempt to route around it. Gave owner the exact command to run themselves when convenient. Continuing other work.

## Pocket dojo switched off ATLAS_NT8 -> DATA/ATLAS (owner directive)
Owner: "moving forward don't use NT8 days for dojo." Changed pocket_dojo.py DATA constant to DATA/ATLAS/1m (schema-compatible: timestamp/open/high/low/close/volume, verified). Note: this is also the atlas_backtest/F-space source range (through 2026-03-20) -> future dojo days can carry combiner F-space once that's backfilled. Scope: only pocket_dojo.py changed; level_coordinate_system.py/owner_process_v1.py/qwen_dojo_player.py DATA constant left on ATLAS_NT8 to preserve the already-reported N=163 backtest's reproducibility -- flag for owner if they want the backtest+qwen-eval moved too (would need re-running, invalidates the reported -$32/day v1 and +$2/day v1.1 numbers' data source).

## Level-selection insight: round numbers can be dense-but-invisible to the pivot-density telescope
Owner flagged a missing region at 25000. Checked: 19/45 view bars touched it (dense chop), but zigzag-pivot density fit missed it -- the level was a consolidation ZONE (many touches, few confirmed zigzag PIVOTS since price didn't swing far enough between touches to register R). Implication for level_coordinate_system.py: round-number levels may need a separate always-candidate layer (touch-density on raw bars, not just zigzag pivots) alongside the telescope. Not yet built -- flagging for the level tool's next iteration.

## Pocket dojo design gap: fills anchor to chat-message-arrival bar, not signal bar
Owner pushback: a long call referencing "the really long red bar" filled 5 bars later than the signal (message typed after continued observation). Corrected entry retroactively (24987.50@bar96 -> 24986.00@bar91, right after the red bar) per owner's stated intent -- 1s-reaction fill, not typing-latency fill. Real gap: pocket_dojo has no way to anchor a call to a PAST bar the owner is referencing; it always fills relative to `cur` (now). FIX NEEDED: support something like `call long --after-bar N` or reference-by-description, so causal fidelity survives chat latency. Not fixed yet -- logged for next iteration.

## Pocket dojo: immediate-fill made the DEFAULT (owner rule, not opt-in)
Owner generalized the prior fix: "it will always be the next 1s bar you show" -- for both entries AND exits. Rewrote pocket_dojo.py: call/exit now fill immediately at the current bar's close (no next-1m-open queue, no pending/exit_next machinery, --now flag removed since it's the only behavior now). Reverse-while-in-position also simplified to an immediate close+flip at the same price. _advance() during 'step' now only handles target/stop hits on an already-open position -- entries/exits are no longer deferred. This is the correct default given the engine has no 1s data loaded: "next 1s bar" in practice = right now.

## Owner exit rule: level-touch, not fixed target -- applied retroactively
Owner: "if I was trading I would have exited on the 1s bar nearest to the level." Applied to the open long: closed @ 25000.00 (owner's own reference line, first touched bar97) -> +13.1pt (+$26.22), day trades now 2 (short +9.9pt, long +13.1pt), all positive. Reinforces the level-coordinate-system's centrality: exits are level-triggered in real-time, not a pre-set target distance. Corpus now has two clean owner round-trips on this fogged day.

## Retcon: flip anchored to the ref-level touch, not the lazy-advance bar
Owner confirmed the flip was intended at the 25010.5 touch (bars 98-99), only advanced 5 bars generically ("lazy"), not because they meant to wait past it. Retconned short entry 25001.75@bar101 -> 25010.50@bar98. Reinforces the standing engine gap noted earlier: calls should anchor to the REFERENCED bar/level, not the bar the message happens to land on. Corpus now clean: long 24986->25000 (+13.1pt), short 25010.5->(open).

## Corrected: L5 ldist feature layer vs L5 decision engine (my earlier answer conflated them)
Owner corrected me: there IS an intrabar distribution feature family separate from B7/B9/B10 -- `L5_<tf>_ldist_*` in core_v2/features.py (n/quantiles/mean/std/skew/kurtosis/outlier_pct/level of within-bar 1s closes, formula in statistical_field_engine.py `_ldist_group_stats`). This does NOT need the trained models or full build_dataset run -- just raw 1s data, which DOES exist for this fogged day. Computed it ad hoc (exact formula ported) for bars 99-103: only 1 tick per 1m bar (matches the earlier-flagged thin overnight liquidity), so skew/kurtosis forced to 0 (n<4 guard). Feature is real and computable; this specific day/hour has nothing to show because of session sparsity, not a data gap. Lesson: don't conflate feature LAYERS with decision MODELS when answering -- check core_v2/features.py before claiming something needs the heavy pipeline.

## Retcon: flat applied to the decision bar, not the bar the message landed on (same pattern as the entry-anchor gap)
Owner: "retcon we flatted previous bar" -- the wick/deceleration flat-and-watch call was made at bar108, but I hadn't executed it (waited for explicit confirmation) and the next message just said "next bar", advancing to bar109 where price moved back up 24992.75->25001.25 against the still-open short. Owner clarified the flat should apply AT bar108. Retconned: short closed 25010.50->24992.75 = +16.86pt (+$33.72). Day total now +29.97pt (+$59.94), 2/2 winning trades.
This is the SAME underlying gap flagged twice before (long-entry anchor, short-flip anchor): decisions need to bind to the bar they're ABOUT, not the bar where a subsequent message happens to land. Now surfaced a 3rd time on an EXIT. Escalating priority: pocket_dojo needs a real "anchor to bar N" mechanism, not more manual retcons.

## Fixed: pocket_dojo telescope was single-day-only, missing real multi-day levels
Owner: "missing reference lines in our close up view" -- root cause confirmed same as the earlier theme gap: pocket_dojo's telescope call only ever received TODAY's ~112 bars, so a real prior-day swing (25150, visible in the 1h zoom-out) could never enter the fit. FIXED: new `_bars_with_history()` loads LEVEL_TRAIL_DAYS=5 trailing sessions + today, cached per-day (`_hist_cache`), telescope now fit on the extended frame sliced causally to the cursor. Verified: day 25154(14t)/25105(14t) now appear in the fit (matching the zoom-out), correctly withheld from the close-up RENDER only by the existing ±15pt visible-range filter (by design, not a bug) -- they'll surface as price approaches. This closes both flagged gaps (theme + levels) from the same root cause: single-day data loading.

## Session moral (owner, end of night): "trust the process, and the references"
Closing reflection on tonight's pocket-dojo session. Ties directly to the program's core finding: the mechanized process skeleton (owner_process_v1) is coin at N=160-163, but every live judgment call tonight that leaned on the reference-level frame worked (25000 exit +13.1pt, 25010 flip +16.86pt after retcon, wick-deceleration read). The references + process are real; the gap is that the machine doesn't yet read them with the owner's judgment. This is the explicit target for the human-capture/distillation program going forward.

## Claude self-test: 3 legs, using the owner's captured method live
Owner: "let's see if you learned my method, do the next 3 legs, see how you fare." Took over decision-making in pocket_dojo for 3 legs using the captured process (reference-frame room check, cubic slope/curvature momentum, wick/deceleration entries, confirmed-reversal scratch discipline learned from tonight's own corrections):
- LEG1: waited (no chase, no room), entered short on a real deceleration/rejection near day 25047, scratched -5.1pt after a 2-bar CONFIRMED reversal (not panic on 1 bar) -- reversal then immediately re-reversed (this day is genuine chop, no theme).
- LEG2: entered long on a support bounce (25011), banked +10.4pt 1.5pt short of target rather than risk giveback after a reversal candle -- asymmetric-risk exit.
- LEG3: entered short on momentum continuation off leg2's exit, small scratch -1.6pt on the same confirmed-reversal criterion as leg1 (consistency over rationalizing a hold).
NET: +3.6pt (+$7.16), 1W-2L, no blowup. Self-assessed: survived without disaster, mildly positive, but NOT clearly better than coin over N=3 -- same qualitative conclusion as the N=160-163 owner_process_v1 backtest, now felt live rather than just measured. Honest gap named: "confirmed reversal" here = 1-2 bars of cubic sign flip, cruder than the owner's actual read (wick shape + level confluence + "music"/theme feel).

## SQL corpus + conversation transcript built (owner: "are we capturing all of this in a journal and SQL database?" / "we need also a transcript of the conversation")
Gap was real: journal (docs/daily/) was current, but the pocket-dojo action log was JSONL-only (no SQL), and NO conversation transcript existed at all -- outbound (assistant) messages were sent via ad-hoc inline `requests.post` calls all night with zero logging.
**Built:**
1. `research/dojo_forge/tools/pocket_dojo_db.py` -- SQLite mirror of the pocket-dojo JSONL corpus (`gate_state/pocket_dojo.db`, table `events`). Ingested 75 existing lines; `pocket_dojo.py._log()` now write-throughs to SQL live on every action (call/fill/close/note/retcon/owner_line/...), JSONL stays source of truth.
2. `tools/telegram_bridge/tg_send.py` -- shared send helper (send_text/send_photo) logging BOTH directions to `state/transcript.jsonl` + SQLite (`gate_state/session_transcript.db`, table `transcript`). Backfilled 294 historical INBOUND messages from inbox.jsonl (recoverable). Outbound history before tonight is NOT recoverable -- Telegram Bot API has no send-history endpoint, and no logging existed before this fix. All future sends should route through this module.
**Standing task**: use `tg_send.send_text`/`send_photo` for all future Telegram replies (not inline requests.post) so the transcript stays complete going forward.

## Dojo fine-tuning per owner constraints (AuDHD-casual is the design spec)
Owner pushbacks accepted: (1) retcons were HONEST intent-fidelity corrections (also played against himself when it cut that way) -- the lesson is they were manual compensation for chat-latency misbinding, so protocol v2 = each frame is the decision point, advancing binds intent automatically; (2) qwen grading loop PARKED until a few more casual sessions fine-tune the instrument; (3) casual-blind is the METHODOLOGY -- owner is AuDHD, cannot and should not hold long sessions; his natural forgetting IS the re-blinding mechanism; state persistence means sessions resume across days with zero memory burden.
**Code changes (pocket_dojo.py):**
- `_pick_start()`: session starts at ANY hour (owner trades whenever free -- dropped my RTH-weighting idea) but liquidity-gated: trailing-30 median 1m volume >= LIQ_FLOOR=400 (empirical overall median; dead zone 03-07h UTC ~150/bar produced tonight's 1-tick bars). Fallback to liveliest spot on dead days. Verified: 5 random days -> starts 13:17-19:24 UTC, vol 653-3016.
- `_bars()` now loads volume.
- `step` events now LOGGED with passed=true when flat -- frames the owner saw and passed on are negative examples, as valuable as entries for the corpus.

## Corpus target set: 60 owner-narrated legs (owner proposal)
Owner: ~60 legs should be enough narration. Math: typical casual sitting = 2-5 legs -> 60 legs ~= 12-20 sittings, a few weeks at casual pace. 60 legs = CORPUS milestone (rubric/distillation); distinct from ~35 full sessions for P&L significance -- chasing the first mostly delivers the second. Added to pocket_dojo: `who` attribution (owner/claude, state-carried, DB column + backfill of tonight's mixed session + reattributed my smoke-test days), and `progress` command (owner-legs bar toward 60 + narration count + lifetime pt). Current: 3/60 legs, 27 narrations, real start.

## Staged distillation protocol adopted (owner proposal)
Owner: "I will request the Sonnet to trade when I feel it should have learned; when we agree with its decisions we can then pass to qwen." Pipeline formalized: owner narrates legs -> periodically Claude trades (who=claude, narrating in the owner's terms) -> owner verdicts each decision (new `verdict agree|partial|disagree [why]` command, structured in DB) -> agreement rate over claude-legs = DISTILLATION-READINESS gate (threshold to pre-register, ~>=80% over 15+ legs) -> only then write the qwen curriculum from the validated corpus. CAVEAT pinned: agreement != edge (shared blind spots agree on coin; v1 skeleton IS coin) -- agreement gates transfer, the P&L ledger gates edge, the frozen RIDE_EDGE_GATE still owns final go/no-go. Note: when Claude trades, the session model varies (fable/sonnet/haiku per owner's /model switching) -- worth tagging model in claude-leg notes for later analysis.

## Grading = quality-of-read, not P&L (owner refinement, adopted)
Verdicts grade the READ (5-dimension rubric added to OWNER_PROCESS.md: leg position, theme transition, reference use, timing tell, restraint) -- P&L stays a separate edge ledger, never the teaching signal. Owner's concrete example doubles as tonight's confirmed miss: after banking the big down leg, the theme flipped up and Claude shorted into it twice (self-test legs 1&3) despite the captured "legs alternate" rule. Losing-trade-correct-read = agree; winning-trade-wrong-read = disagree. Offered future tooling: post-hoc auto quality metrics per leg (leg-capture %, exit-vs-pivot lag, theme-flip detection lag) once fog lifts.

## "Look left" rule captured (owner post-mortem of the bar-108 miss)
Owner identified tonight's cleanest quality-miss: at bar 108 we flatted at the LOW of a visible ~25pt/~20-bar oscillation (24980->25011->24990) and failed to look left -- cycle base-case was the up-swing to 25010 (bar 110 delivered). The same "previous oscillations" logic was the owner's own bar-96 entry reason, dropped 10 bars later. Added to OWNER_PROCESS.md as rubric dimension 6 (Cycle context / look left). Honest caveat recorded: cycle read earns the opposite band, not the expansion beyond it.

## Slice numbering added (owner: "number the slices so we can backtrack")
Global monotonic decision-point counter (gate_state/pocket_slice_counter.txt): increments on `new` and each `step`; all events logged in between (notes/calls/verdicts/closes) inherit the current slice. Stamped on the frame TITLE (S<n>, visible in every Telegram frame), in JSONL records, and as a `slice` column in pocket_dojo.db. "S38" now resolves to exact day+bar+full context in one query. Pre-slice history (tonight) remains addressable by day+bar.

## Thesis-before-reveal protocol adopted
Owner: when asked for a read, Claude should consult memory/priors and commit a thesis BEFORE the bar reveal; discuss; then step. Formalized in OWNER_PROCESS.md: priors-cited -> thesis+horizon -> falsifier, both sides commit pre-reveal, (thesis,outcome) pairs accumulate into a two-sided calibration ledger. _log() now allows per-event who= override so Claude theses are attributed correctly mid-owner-session. Ties to north star: this is the glass-cockpit interpreter role, rehearsed live.

## Two-way questioning adopted (owner)
Claude questions the owner pre-reveal too — Socratic elicitation, question types tied to rubric dimensions (falsifier/reconciliation/selection/unmentioned-signal/look-left), capped ~1 sharp question per slice to keep the casual (= methodological) register. Captured in OWNER_PROCESS.md.

## "Sounds right" = protected answer (owner)
Tacit calls logged verbatim, never pressed (confabulation risk — fake rules worse than none). Light probes only: falsifier + attention. Corpus split by design: articulable->rules, tacit->(state,action) imitation pairs (= why 60 LEGS is the target). Hypothesis registered: entries tacit, vetoes articulable, vetoes carry the transferable edge.

## Third answer class: spitballed = hypothesis (owner)
Owner's self-observation: fabricated on-the-spot reasons sometimes later prove true (same pattern engine doing the fabricating -> noisy-but-correlated with the real computation). Protocol: tag as HYPOTHESIS, deferred verdict, testable against corpus + 600d historical data; verified->rule, refuted->discarded, never trusted raw into curriculum. Pressure cap unchanged.

## F-space attached to every slice (owner intuition: "the answer is in F-space")
pocket_dojo now joins the causal atlas_backtest combiner snapshot to each step/slice silently (P_topk, gov, fires, zz state, top-3 streams) + `fspace` command for conscious viewing. Coverage: RTH 14:30-21:15 UTC only, 64/584 days empty (incl. current day 2025_12_19 — unlucky); liquidity-gated starts naturally land in coverage. The access mechanism for the owner's intuition = triangulation at corpus scale: correlate his call-slices vs pass-slices in F-space to identify which features the tacit pattern engine reads. Verified on 2025_11_14: snapshot returns P_topk 0.778, gov TMPL0(-1), 5 fires, top streams TMPL0/ROUND05/RENKO24.

## F-space "peppering" on phone frames (owner request)
F-space metrics now ride the frame CAPTIONS (compact FS: line — P_topk, gov stream+arrow, fires, zz leg/age, top-3 streams) whenever coverage exists; chart itself stays clean. Rationale: ambient exposure lets the owner's background pattern engine graze on F-space and maybe latch onto something (how they learned charts originally). Tradeoff flagged + accepted: owner's calls are no longer combiner-independent — corpus records that metrics were shown, analysis can condition on it. `fsline` command added; step/chart captions auto-append.

## Corrected: ACTUAL F-space (V2 layers) + dual-panel telescope frames
Owner corrections: (1) "F-space" = the V2 statistical layer families (z, vr, lambda, ldist skew/kurt, velocity/accel/sigma) — NOT the combiner streams ("gibberish"); (2) frames should show 1m PLUS a telescopic 5s of the last few bars.
Built: `_v2_fspace()` (cached multi-layer loader from DATA/ATLAS/FEATURES_5s_v2: L4_1m z_21/vr_exact/lambda_t_21, L5_1m + L5_5m ldist skew/kurt/outlier, L2_1m velocity/accel/sigma; causal last-row<=ts), caption `_fs_line` now in owner language; step logs carry BOTH snapshots with clean names (fspace=V2, combiner=22-stream). `_render` now two-panel: 1m (unchanged toolkit) + TELESCOPE 5s last ~6min (72 bars, causal cutoff at cur-bar close). Verified on 2025_11_14 RTH: dual-panel renders, F-line "z-0.47 vr0.60 λt+1.21 | 1m sk-0.46 ku-0.79 | ...". FEATURES coverage: 2024_01_02..2026_03_20 (matches dojo day pool).

## Telescope 5s panel now overlays reference lines (owner request)
Owner: needs the 1m reference lines overlaid on the 5s telescope panel to correlate structure across TFs. Wired: tele_lines collected once in the 1m panel, drawn on ax5 too (owner_lines included), y-range-filtered. Verified visually on S2 (2025_08_24): 23577 line correctly bleeds onto the 5s panel.
Owner also flagged a risk read (sudden climb + compressed bars vs surrounding = possible hard drop) referencing the 08-22 1h gap-up candle. Checked honestly: that surge is in MULTI-DAY history behind the cursor, not in the current session's own 1m bars -- local 1m range (last30 vs last200) is roughly flat (3.5 vs 3.75), no compression signature YET on this slice. Offered to re-check compression specifically on bars following the actual 08-22 surge if wanted. Flagged the scale mismatch rather than force-confirming.

## Fixed: off-view line was squishing the 1m panel (axis autoscale bug)
Owner: "the 1m looks smooshed." Root cause: axhline() participates in matplotlib autoscale by default -- the 23400 owner line (far below the ~23570 cluster) was forcing ylim to stretch, squishing the actual candles. FIXED: explicit ylim computed from candle+sigma-envelope data only (view window), ignoring off-range hlines. Verified: full 20pt oscillation now visible, 23400 correctly off-view without distorting anything.

## Sigma-band compression: root cause found, fix choice pending owner
Owner: sigma horizontals "too compressed together," wants a sweet-spot window to cover the oscillation. Tested W=10..60 on cubic-residual sigma: ALL give 2sigma<1.5pt against a 20pt observed swing -- NOT a window-tuning problem. Root cause: CUBIC_W=8 is fast enough to track/absorb the whole 44-bar oscillation into the trend line itself, so the residual sigma measures stays near zero regardless of window. Bypass test: raw-close sigma (no cubic detrend) at W=60 gives 2sigma=15.6pt, much closer to the swing. Proposed 2 fixes: (a) raw-price sigma for bands (risk: drifts on trending windows per earlier finding, less risk here since currently ranging) or (b) second slower cubic just for the envelope. Offered to default to (a) given current range regime; awaiting owner confirm/redirect.

## 5s telescope: minute-boundary gridlines added (owner request)
Owner asked for vertical lines marking where each 1-minute boundary falls on the 5s panel, for better time-sense. Added: dotted gridlines at each 5s-bar whose timestamp % 60 == 0, with real HH:MM tick labels. Verified visually -- clean, readable time reference alongside the intrabar candles.

## Correction: "EOD" was a data-file boundary artifact, not a real market halt
Owner asked if the earlier EOD-flat was a genuine maintenance break. Verified properly: files are split by UTC calendar day; 2025_08_24.parquet ends 23:59 UTC, and 2025_08_25.parquet starts exactly 00:00 UTC continuing the same price action (no gap/halt -- real continuous trading). So pocket_dojo's EOD-flat rule fired on a FILE boundary, not a genuine session end. Real scope limitation: a session starting late in a UTC day can hit this artificial cutoff well before the actual session/week ends. Flagged to owner, offered to extend sessions across the file boundary if wanted -- not yet built, awaiting owner preference.

## FULL SESSION AUTONOMOUS RUN (Claude, overnight, owner asleep) — comprehensive report
Owner: "I'm really interested in you doing a full day, log everything, we'll talk of your experience." Ran ~185 bars (~3hrs market time, 2025_06_05 14:35-17:39 UTC) autonomously as claude, applying every rule captured tonight (room-to-target, wait-for-release+buyers-struggling tell, confirmed-2-bar-reversal exit discipline, look-left/cycle context, theme cascade, volume-divergence confirmation) plus originating new judgment live.

**Trades (3, all losing, 0% hit rate):**
1. SHORT 21908.75->21913.75 (-5.89pt): multi-confluence entry (2-bar reversal + volume divergence + room), exited on confirmed 2-bar reversal against. Correct process, wrong outcome.
2. SHORT 21912.25->21917.25 (-5.89pt): same validated pattern, re-fired deliberately (one loss doesn't invalidate a process). This time NO sharp reversal fired -- caught via a slow-grind-against read instead (new nuance: reversal isn't always a clean sharp event).
3. LONG 21909.75->21890.50 (-20.14pt, biggest loss): with-trend entry after a REGIME ADJUSTMENT following the 2 short losses ("stop fighting the 1h uptrend"). Reversed hard against with BUILDING volume. Self-critique: the regime-adjustment heuristic was folk wisdom ("don't fight the trend") applied WITHOUT checking it against this project's own already-established findings (memoryless leg reversal, N=160-163 owner_process_v1 = coin at scale). Same class of error as the detrend-breakthrough retraction earlier in the program.

**Key findings promoted to OWNER_PROCESS.md:**
- Regime-dependence: the deceleration+volume-divergence short tell (validated on owner's range-bound session) failed twice on a trend day -- needs theme-gating, doesn't generalize blindly across regimes.
- Structural gap: level-anchored room-to-target CANNOT fire on trend/breakout days (no forward levels exist by construction) -- range-bound-day toolkit != trend-day toolkit. Proposed fix (measured-move target) computed but correctly NOT traded (untested + marginal room).
- Self-critique on regime-alignment as unvalidated folk wisdom -- explicit tie to project history (the earlier detrend-breakthrough false-positive retraction) as the same failure pattern: intuitive appeal != validated edge.
- After 3 losses, tightened selectivity live (restraint) rather than revenge-trading or mechanically continuing -- demonstrated recovering discipline, not just initial discipline.
- Extensive, well-reasoned RESTRAINT throughout: passed a genuine news-event-volatility open (~15 bars, 5-12k volume, no forced trade), passed multiple ambiguous/indecisive stretches, passed a with-trend setup specifically because room-to-target failed (didn't force it despite theme+momentum agreeing).

**Corpus impact:** 63 logged events this session (notes/calls/fills/closes), all who=claude tagged (does not pollute the owner's 60-leg target). Full quality-gradeable trail for the owner's verdict pass per the staged distillation protocol (owner grades agreement, not P&L).

**Self-assessed quality (not P&L):** process discipline was consistently good (waited for confirmation, exited on confirmed reversals not panic, didn't force trades without room, recovered selectivity after losses). The one clear FAILURE was epistemic, not mechanical: trading an unvalidated heuristic (trend-alignment) with real conviction instead of treating it as an untested hypothesis first. That's the single most important lesson from the session.
