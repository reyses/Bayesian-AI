# Daily Journal: 2026-07-14

## DEFECT vs DESIGN — the line drawn (comms/070)
AG's doc 069 ran the full matrix and cited two NEW "legacy defects". Both citations
VERIFIED accurate in source — but the INTERPRETATION was wrong, and the distinction is
now binding:
- DATA-HANDLING DEFECT = plumbing corrupts the intended rule (close-as-high, no RTH
  filter, discarded session bars, ETH contamination) -> native MUST fix. ACCEPTED for
  ATR-09 (460/10), DOW-19 (38/432), FIB-17 (399/71).
- STRATEGY DESIGN CHOICE = the rule itself -> native MUST reproduce. Redefinition needs
  explicit approval, never executor judgement.
REJECTED CROSS-11: AG called the `break` an "artificial" bug and emitted EVERY cross
(357/470 days "diverged"). But legacy's own comment reads "# Scan for first cross" — one
setup per day IS the rule. AG converted "trade the first golden cross" into "trade every
cross" = a different strategy. Ordered restore of first-cross-only.
ESCALATED ADX-08: AG silently swapped legacy SMA smoothing for Wilder RMA (76 days
changed). Legacy comment concedes SMA is an approximation "for speed", so Wilder is
arguably MORE article-faithful — but it is a DEFINITION decision (Moises', not AG's).
Ordered: implement both behind a flag, DEFAULT = legacy SMA, pending Moises' ruling:
(a) keep SMA (comparability with audited events) vs (b) adopt Wilder (canonical ADX,
supersedes prior ADX-08 results).
Process: AG self-applied "VERIFIED" in filename + "Status: FINAL" + "complete" for the
3rd time, against its own doc-055 commitment. Now its most-repeated violation.
FPS frozen (verified untouched). Batch A 7/7 stands.

## ADX SMA-vs-WILDER: smoothing is LOAD-BEARING (comms/072, Claude-executed)
Executed Moises' keep-both ruling myself. 25-day matrix (strict match = ts AND mode):
  ADX-08-SMA:    3 match / 0 diverge / 1 leg-only / 21 both-none  -> port is FAITHFUL
  ADX-08-WILDER: 0 match / 2 diverge / 15 native-only / 6 both-none -> fires ~17/25 days
  ADX SMA-vs-WILDER DISAGREE ON 18/25 DAYS (72%); when both fire, up to 3.1 HOURS apart.
=> "ADX > 25" is NOT well-defined until the smoothing is pinned. Same article rule, same
threshold: RARE signal under SMA, FREQUENT under Wilder (4-5x). Any ADX-08 edge is
contingent on an undocumented implementation choice. A signal that flips on 72% of days
when you swap a smoothing function was never a signal — it's a threshold artifact.
KEEPING BOTH IS WHAT EXPOSED THIS; picking one would have hidden it. CATALOG-LEVEL finding.
CROSS-11 first-cross-only restored: 18/25 match (~76%) vs AG's all-crosses 113/470 (~24%).
Design-vs-defect ruling (070) VINDICATED — the `break` was the rule.
⚠ SYSTEMIC: our detectors cold-start at 08:30 and need 408 bars (240 SMA + 168 ADX) => BLIND
for first ~34 min of RTH. We condemned legacy DOW-19 for skipping 20 bars; we'd skip 408.
ALL rolling-window detectors must be SEEDED (prior-day + ETH), as CROSS-11 already is.
⚠ HARNESS: verify_batch_b.py:3-4 hijacks sys.stdout AT MODULE LEVEL and opens "w" — importing
it silently redirects the whole process's output, and every run TRUNCATES the prior run's
evidence. A verifier that destroys its own artifacts can't support artifact-level review. Fix.
FPS frozen (untouched).
