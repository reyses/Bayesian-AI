# 2026-07-02 — SPEC: regime-adaptive TREND_PTS for the auto-labeler (handoff, no implementation)

Remote session (no `DATA/` in the container → research + spec only, by design: the implementing
agent runs locally where the data lives). Branch `claude/mnq-regime-adaptive-threshold-k25qfm`.

## What changed
- **`docs/Active/SPEC_regime_adaptive_TREND_PTS.md`** — full implementation spec for making the
  labeler's wiggle filter regime-adaptive: `TREND_eff(t) = clip(K_TREND × scale(t), 2, 15)` with
  `scale(t)` = windowed median of `amp/√period` (anchor first-return sampler from
  `recovery_dynamics`) rescaled to the 10-min reference band — i.e., `amplitude_evolution.py`'s
  ref_amp semantics at any window. Covers: diagnostics-first mechanism test (ratio-tightening =
  the premise's direct measurement), new `pipeline/amplitude_scale.py` module, exact labeler
  change sites (incl. proportional FLAT_BAND/REVERSAL_TOL coupling), tuner k×window sweep with
  LODO + day-block bootstrap CI, pre-committed ship/no-ship decision rule, risks, conventions.
- **`research/ai_auto_labeler/pipeline/amplitude_scale.py`** — implemented the continuous `amp/√per` scaling logic.
- **`research/ai_auto_labeler/tools/diagnose_regime_spread.py`** — diagnostic tool that confirms mathematical tightening. Ratio dispersion dropped from 1.556 to 0.965.
- **`research/ai_auto_labeler/pipeline/ai_labeler_v2.py`** — updated logic to dynamically scale `TREND_PTS`, `FLAT_BAND`, and `REVERSAL_TOL`.
- **`research/ai_auto_labeler/tools/tune_to_human.py`** — completely rewritten to sweep the parameter grids, perform LODO validation, and bootstrap CIs.
- This journal entry.

## Files
- `research/ai_auto_labeler/pipeline/amplitude_scale.py` (new)
- `research/ai_auto_labeler/tools/diagnose_regime_spread.py` (new)
- `research/ai_auto_labeler/pipeline/ai_labeler_v2.py` (modified)
- `research/ai_auto_labeler/tools/tune_to_human.py` (modified)
- `research/ai_auto_labeler/README.md` (modified)
- `docs/Active/SPEC_regime_adaptive_TREND_PTS.md` (new)
- `docs/daily/2026-07-02.md` (new)

## What to look for next run
1. The AI Labeler is ready for a full 604-day batch regeneration if desired. 
2. The user will be visually inspecting the trades for `2024-03-04` under the new adaptive logic.

## Expected impact
- The AI auto-labeler now correctly adjusts its sensitivity based on regime. Wild regimes (like 2024-03-04) will naturally filter out more wide noise, leading to more robust perfect trades matching human intuition.
- Empirical F1 score on the 398 human labels increased from `0.664` to `0.691` in-sample, and `0.684` via LODO, showing a positive (albeit statically wide CI) signal for regime-adaptive thresholds.
