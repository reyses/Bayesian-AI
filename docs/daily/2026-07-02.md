# 2026-07-02 — SPEC: regime-adaptive TREND_PTS for the auto-labeler (handoff, no implementation)

Remote session (no `DATA/` in the container → research + spec only, by design: the implementing
agent runs locally where the data lives). Branch `claude/mnq-regime-adaptive-threshold-k25qfm`.

## What changed
- **`docs/Active/SPEC_regime_adaptive_TREND_PTS.md`** — full implementation spec for making the
  labeler's wiggle filter regime-adaptive: `TREND_eff(t) = clip(K_TREND × scale(t), 2, 15)` with
  `scale(t)` = windowed median of `amp/√period` (anchor first-return sampler from
  `recovery_dynamics`) rescaled to the 10-min reference band — i.e., `amplitude_evolution.py`'s
  ref_amp semantics at any window. Covers: diagnostics-first mechanism test (ratio-tightening =
  the premise's direct measurement), new `pipeline/amplitude_scale.py` module, exact labeler
  change sites (incl. proportional FLAT_BAND/REVERSAL_TOL coupling), tuner k×window sweep with
  LODO + day-block bootstrap CI, pre-committed ship/no-ship decision rule, risks, conventions.
- This journal entry.

## Files
- `docs/Active/SPEC_regime_adaptive_TREND_PTS.md` (new)
- `docs/daily/2026-07-02.md` (new)

## What to look for next run (the implementing session)
1. **Step 0 first**: `tools/viz/core/cubic_utils.py` is imported by 5 committed files but was
   **never committed** (no git history, not ignored — an untracked survivor of the 06-30 UTF-16
   `.gitignore` incident that the 83-file recovery missed). Rescue-commit it before anything else,
   and sweep for other untracked-but-imported survivors.
2. Diagnostics before the sweep: per-day scale spread across the 9 human days + RTH/overnight
   ratio + swing/scale ratio-tightening — decides whether cross-day or intraday windows carry
   the signal (or neither → expect a negative sweep, report it straight).
3. Acceptance is NOT "in-sample F1 > 0.664" alone: LODO must agree and the champion must not be
   clamp-saturated. With 9 days the bootstrap CI on ΔF1 will be wide — significance statement
   mandatory either way.

## Expected impact
- If the premise holds: pooled F1 vs the 398 picks moves above the 0.664 fixed ceiling with
  recall gains in calm regimes (fixed T=4 too coarse) and precision gains in wide regimes
  (fixed T=4 too fine). If it fails: fixed T=4 stays default, and we've measured (not assumed)
  that the human's cutoff is absolute — worth knowing either way.
