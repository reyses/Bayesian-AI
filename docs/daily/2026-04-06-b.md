# 2026-04-06b — Blended Engine + Tree Exhausted + CNN Next

## Blended Engine Results
```
IS:  $35/day, 52% winning days, 12,453 trades
OOS: $29/day, 62% winning days

By tier:
  CASCADE:    54 trades, 94% WR, $16.10/trade
  KILL_SHOT: 355 trades, 93% WR, $15.60/trade
  BASE_NMP: 12,044 trades, 58% WR, $0.30/trade
```

## Pipeline with Blended Engine
```
H0 IS:  $295/day, 71% winning, 216 trades/day, $1.40/trade
H0 OOS: $386/day, 71% winning, 210 trades/day, $1.80/trade
```
Tree-based AI generates 216 trades/day — mode hold = 1 bar (5s). 46% of trades held <1 min and lose money. Only 5-15min holds ($8.50) and 15m-1h ($18.20) are profitable.

## Tree Exhausted — All Questions Tried

### Direction prediction (same vs counter): 55.5%
- Entry 79D only → 55.5% (barely above 50% coin flip)
- Wick_ratio is the only feature that helps
- BASE_NMP is genuinely 50/50 without wick

### Duration prediction: 64.4%
- time_of_day explains 64% of importance
- 79D features add almost nothing beyond the clock

### 3-point path (entry + mid + exit): 56.0%
- Path features, deltas, PnL trajectory → no improvement

### 5-point curve (entry + 25% + 50% + 75% + exit): 53.8%
- 128 features, 5 time points, 12 key metrics → WORSE than simpler models
- More features = more noise for the tree

### Conclusion
The decision tree cannot predict direction for BASE_NMP trades. The 79D trajectory does not contain enough information. The wick rejection is the ONLY reliable direction signal. Everything else is noise for the tree.

## Regime Flip Experiments
- regime_flip exit (vr > 1.0): 14% WR, -$19.90/trade — POISON
- regime_shift_early (DMI + VR 0.8): 23% WR, -$14.90/trade — still bad
- Flip on regime shift: regime_flip_center 38% WR, -$0.10 — breakeven
- Removed all regime logic — simplest is best

## Regret Analysis (all tiers)
```
CASCADE:    $871 actual, $4,624 optimal → 19% capture
KILL_SHOT:  $5,538 actual, $29,778 optimal → 19% capture
BASE_NMP:   $3,362 actual, $1,221,988 optimal → 0.3% capture
```
All tiers capture only 19%. Exit is the bottleneck uniformly.
BASE_NMP: 48% same, 47% counter — pure coin flip on direction.

## Next Session: CNN
The tree is exhausted. CNN can detect multi-feature spatial patterns across the 6×13 TF grid that the tree treats as independent features. The CNN sees cross-TF relationships (resonance cascade pattern) that the tree cannot.

Goal: CNN similarity embedding on corrected trades → find patterns the tree missed.

## Files Added
- `nn_v2/nightmare_blended.py` — unified tiered engine
- `nn_v2/nightmare_cascade.py` — resonance cascade engine
- `nn_v2/nightmare_wick_overshoot.py` — wick entry + overshoot exit
- `nn_v2/tree_flip.py` — flip predictor (55.5% — exhausted)
- `tools/blended_test.py`, `tools/exnmp_trio_test.py`, `tools/wick_overshoot_test.py`
