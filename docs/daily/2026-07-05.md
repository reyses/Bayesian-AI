# 2026-07-05 — Mamba RL runtime review (RTX 3060 / WSL2)

## What happened
Speed-only review of `research/mamba_zigzag_baseline/pipeline/` per task spec
(profile first, verify 4 hypotheses, fix in order, loss-identity gate).
Full report: `research/mamba_zigzag_baseline/reports/perf/PERF_REVIEW_2026-07-05.md`.

## Headline findings
1. **The shipped torch.compile config crashed on WSL** (reduce-overhead CUDA
   graphs vs carried hidden state → output-aliasing error at step ~2). Prior
   smoke tests passed because Windows skips compile. De-facto baseline was
   eager: **53.15 bars/s**.
2. **Two of the four task hypotheses were stale** — the L=1 scan fallback (a)
   and the pinned single-H2D pack (b) were already in HEAD. The real costs:
   TBPTT backward 32%, double forward 38%, env CPU 16%, syncs 10%.
3. **mamba-ssm fused step() is an autograd trap**: runs under WSL fine
   (wheels: mamba_ssm 2.2.4 + causal_conv1d 1.5.0.post8 cu12torch2.6abiFALSE),
   but backward silently produces None input grads — would train wrong, not
   crash. PureMambaBlock stays for training. Parallel fused scan measured
   **250×** at L=500 → the door for a sequence-restructure later.
4. CUPTI is dead under this WSL driver — no CUDA-time profiler tables at all;
   used host tables + sync-bracketed wall attribution instead.

## What changed (commits)
- `0175b4f1` perf instrumentation (opt-in flags + 3 tools; inert by default)
- `e3d8f3a5` fix[3]: per-step syncs/H2D scalars removed (entropy deferral,
  torch.full, pos_weight hoist) — parity **bitwise** PASS
- `32b10810` env hot path: `assemble_v2_grid` name→index map cached (416
  linear scans/call → one gather; also fixes the standing TODO(perf) in
  research_A eval), per-bar grid-row cache, per-day tz caches, lazy
  `BarState.v2` dict — parity **bitwise** PASS
- (docs) fix[1] verdict + probe evidence
- fix[4]: compile reduce-overhead → default mode (crash fix), then flipped to
  **opt-in `--compile`** per the "any divergence = revert" acceptance rule:
  measured loss drift 1.5e-3 vs eager (bf16 refusion; actions still identical
  over 1300 steps), speed +~20% same-sweep (49-53 → 63 bars/s). Eager is the
  default; the flag exists for anyone accepting the drift.

## What to look for next run
- Same loss trajectories (parity gates were bitwise for the applied fixes).
- bars/sec in the high-50s/low-60s eager; run-to-run ±10% on this box.
- `--seed/--max-steps/--no-checkpoint/--loss-dump` now available on
  `train_mamba_rl.py` for reproducible spot checks.

## Expected impact / open decisions
- **<12h 200-epoch target NOT met within "runtime only"** (~29–34h at
  current rate; needs ~7×).
- **[2026-07-06 follow-up] next_value-reuse TESTED AND REJECTED**: built a
  bit-exact deferred-bootstrap variant (parity BITWISE over 1299 steps —
  provably runtime-only, no approval needed), but interleaved ABAB (n=4
  each) measured NO wall gain (two-forward 47.18 vs deferred 45.33 bars/s,
  Δ −1.85, CI includes 0). The no_grad forward's GPU work overlaps the
  env.step CPU phase behind the per-bar action.item() sync — it was already
  free. Reverted per one-change-at-a-time; variant + A/B data archived in
  reports/perf/. LESSON: sync-bracketed breakdown = work attribution, NOT
  critical path; don't project wall speedups from it. Also gated
  compile_threads=1 to Windows-only (was serializing WSL --compile warmup).
- Remaining lever to <12h: **sequence-window training on the fused parallel
  scan only** (>10×, real math change, needs explicit approval).
- Pre-existing semantics flag (NOT touched): the L=1 refactor dropped the
  conv1d's 3-bar receptive field (no carried conv state). mamba-ssm's step
  carries conv state properly — if the user ever wants it back, that's a
  math change to decide deliberately.
- `torch._inductor.config.compile_threads = 1` header setting serializes
  inductor warmup under WSL (minutes); safe to lift when next touching the file.
