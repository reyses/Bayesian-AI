# 2026-05-04 — Morning briefing (overnight work)

## What you'll find when you wake up

User went to sleep at ~midnight PDT having kicked off `python training/run.py blended --fresh` in a separate terminal. Training will produce updated baseline numbers in ~2-4h. While that ran, Claude did Phase 0 + Phase 0b of the V1→V2 migration audit.

## TL;DR overnight

1. **Training**: started by user, in progress when this was written. Check `training/output/blended/physics_oos_daily.csv` and `reports/oos_report.txt` for results.

2. **V2 audit (Phase 0)**: V2 is lookahead-clean (28/28 tests pass), covers all V1 dates, and V2 deliberately dropped 6 V1 concepts per `research/feature_spec_v2.md` Principle 7 ("L3+ compositions are the model's job, not the feature builder's"). The V2 design is the right move; V1's tier engine just doesn't know yet.

3. **V1 compat shim (Phase 0b)**: Built `core_v2/v1_compat.py` — a runtime shim that derives V1 concepts from V2 features without modifying the V2 cache. Honors Principle 7. Includes `wick_ratio`, `p_at_center`, `vol_rel`, `dir_vol`, `variance_ratio`, and a DMI substitute.

4. **Parity test (`tools/v1_v2_parity_test.py`)**: ran against 4 days × 4 TFs. Results:
   - **wick_ratio, vol_rel, variance_ratio (1m/5m/15m)**: machine precision (Pearson 1.0000)
   - **variance_ratio at 1h**: tight (Pearson 0.97), one day with 0.32 max abs err
   - **p_at_center**: formula correct, but V1's z_se ≠ V2's z_se_w (Pearson 0.87, median 0.47σ shift). Drifts because of underlying SFE implementation differences (V1 cuda vs V2 numpy), not because the shim is wrong.
   - **dir_vol**: ~19% sign disagreement because V1 uses `MarketState.velocity` and V2 uses `L2_price_velocity_w`. Same root cause as p_at_center.

5. **Critical migration finding**: V1 vs V2 `z_se` Pearson 0.87, sign agreement 81%. Tier-engine thresholds (ROCHE=2.0, VR_ENTRY=1.0, etc.) will fire on slightly different bars between V1 and V2 caches. **The −$164/day baseline will shift** when migrating; expected drift is small but not zero. Threshold re-tuning will be needed.

6. **Migration design doc** at `docs/JULES_v1_to_v2_migration.md` — full 6-phase plan. Phase 0 + 0b done. Phases 1-5 await user approval.

## Three open questions — first one answered by overnight calibration

1. ~~**Threshold re-tuning policy**~~: **ANSWERED**. I ran a distribution comparison on 5 days × 4 TFs (41k bars). **Keeping V1 literal `ROCHE = 2.0` on V2 features drops 5m NMP fire rate by 88%** (6.83% → 0.81%). V2 z_se has narrower distribution. Must re-tune to match V1 bar-firing fraction. Calibrated values: 1m→1.87, 5m→1.57, 15m→1.72, 1h→1.76. Same procedure for all z_se / velocity thresholds in `nightmare_blended.py` (~10 of them). variance_ratio, wick_ratio, vol_rel are EXACT — no retune.

2. **CNN strategy**: Option A (79D shim-fed, smaller change) or Option B (184D V2-native, V2 spec-aligned)? A first, B as follow-up experiment.

3. **Migration commit policy**: end-to-end branch (clean revert, slow review) or phase-by-phase (intermediate checkpoints, complex revert)?

## How to read the training results when they finish

Per MEMORY.md the current honest baseline is **−$164/day IS on 348 days** post-lookahead-fix. After your `--fresh` run, check:

| File | What it shows | Compare to |
|---|---|---|
| `reports/is_report.txt` | IS headline metrics | -$164/day baseline |
| `reports/oos_report.txt` | OOS headline metrics | Whatever was previous OOS |
| `training/output/blended/physics_oos_daily.csv` | Per-day OOS PnL | bootstrap CI to check stability |
| `training/output/isolated/<TIER>.pkl` | Per-tier trades | my regime-gate analysis ran on these |
| `reports/findings/baseline_*.md` | Auto-generated baseline summary | — |

After verification, **re-run my regime-gate analysis** to see if RIDE_AGAINST gate (+$21/day OOS) still holds with the freshly-rebuilt tier outputs:

```bash
python tools/v2_tier_regime_analysis.py
```

If gate still survives all 3 splits positive, that's a robustness signal worth deploying.

## Tools created overnight

| Path | Purpose |
|---|---|
| `core_v2/v1_compat.py` | V1 concept derivation from V2 features (140 LOC) |
| `tools/v1_v2_parity_test.py` | Parity test framework (250 LOC) |
| `docs/JULES_v1_to_v2_migration.md` | Full 6-phase migration plan |

## What I did NOT do

Per project rule, I didn't run `training/run.py` myself — you did. I also didn't:
- Touch the live engine (`live/engine_v2.py`) — Phase 2 deferred (not trading live)
- Modify V1 cache files (`DATA/ATLAS/FEATURES_5s/`) — read-only audit
- Modify V2 cache files — they're already clean
- Modify the tier engine (`training/nightmare_blended.py`) — Phase 1 awaits approval

## Decisions waiting on you

1. Approve / modify the migration plan in `docs/JULES_v1_to_v2_migration.md`
2. Pick the three open questions above
3. Decide whether to deploy the RIDE_AGAINST regime gate on V1 NOW (as a safety net before migration) — gate is +$21/day OOS, no migration risk

## Commits tonight

- `0efa7ae4` — Phase 0b: V1 compat shim + parity test
- `ac26e335` — Phase 0b deliverable: migration design doc
