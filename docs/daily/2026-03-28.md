# 2026-03-28 — DualHeadPredictor: 1h PASS, 1m PASS, Oscillation Insight

## Summary
Built DualHeadPredictor (shared backbone, state head + direction head). Trained per TF independently. 1h passed gate at 95.4% (t+1, epoch-based). 1m failed at t+10 (83%), passed at t+4 (96.3%, walk-forward). Key insight: t+4 works because it predicts TO the oscillation peak, not past it. Forward horizon = half the oscillation period, not a tuning parameter.

## Key Insights
- **Oscillation half-cycle is the forward horizon**: t+4 at 1m works because the natural oscillation period is ~8 bars. Predicting t+4 = predicting to the peak. t+10 = predicting past the peak into the next cycle = noise.
- **1-bar lookahead is sufficient**: Instead of predicting N bars ahead, just predict "will the next bar continue this direction?" The oscillation emerges from the sequence of 1-bar predictions: up, up, up, up, DOWN → hold 4 bars then exit.
- **Epoch-based overfits 1m**: Best epoch was 5 on 350K bars. Walk-forward (daily carry-forward) is correct for 1m (1,380 bars/day = enough data).
- **Epoch-based works for 1h**: Only 5.8K train bars, can't overfit. Epoch-based + held-out 2026 validation.
- **Dual head reinforces**: State head (MSE) forces physics understanding. Direction head (BCE) forces binary correctness. At 1h both heads agree 98% and accuracy rises. DIR_LOSS_WEIGHT=0.1 best for 1m (physics dominates).
- **1s needs sharded processing**: 15M bars crashes single-pass. Monthly shards (~1M each) through SFE, cached to disk, walk-forward across months.

## Models Saved
| TF | Forward | Dir Acc | Top 5% Acc | Gate | Checkpoint |
|----|---------|---------|------------|------|------------|
| 1h | t+1 | 78.7% | **95.4%** | PASS | `checkpoints/direction_1h/best_model.pt` |
| 1m | t+4 | 75.1% | **96.3%** | PASS | `checkpoints/direction_1m/best_model.pt` |
| 1s | t+10 | pending | pending | pending | training next |

## What Changed
- `core/direction_cnn.py` — NEW: DualHeadPredictor (shared backbone, state + direction heads)
- `training/train_direction.py` — NEW: per-TF training pipeline (epoch + walk-forward + sharded 1s)
- `training/train_trade_cnn.py` — unified ATLAS, --oos-start flag, oos3 phase
- `live/cnn3_layer.py` — hold clamp 5-10 bars, playback warmup, fill confirmation
- `live/launcher.py` — --cnn3, --playback with date, zero warmup
- `live/live_engine.py` — CNN3 integration, Playback101, NT8 message logging
- `live/bar_aggregator.py` — preload_from_atlas for zero-warmup playback

## Architecture Decision
Three independent models, macro to micro:
1. **1h** (structural direction) — PASSED, saved
2. **1m** (oscillation half-cycle) — PASSED, saved
3. **1s** (micro-timing) — training next

Each must pass 95% accuracy at top 5% confidence independently before merging.
The merge will combine latent vectors from passing models.

## Insight: Forward Horizon = Half Oscillation Period
```
t+4 at 1m = predict to oscillation peak (PASS 96.3%)
t+10 at 1m = predict past the peak into next cycle (FAIL 83.6%)

The optimal prediction horizon is a PHYSICAL MEASUREMENT:
  forward_bars = oscillation_period / 2

Not a hyperparameter to tune — a property of the market at that TF.
```

## Future: Probabilistic Conviction Decay Curve
Multi-horizon P(long) per bar — the model predicts n+1 through n+K simultaneously.
The decay shape IS the trade signal:

```
Bar 100: n+1=95% n+2=88% n+3=72% n+4=55%  → "3 bars of runway"
Bar 101: n+1=92% n+2=85% n+3=68% n+4=51%  → "still 3 bars"
Bar 102: n+1=87% n+2=60% n+3=45%           → "only 1 bar left"
Bar 103: n+1=48%                             → RETREAT
```

Key principles:
- **Decision is ALWAYS n+1** — "will the next bar continue?" Act only on this.
- **Curve is CONTEXT** — n+2 through n+K measure runway remaining.
- **Recalculated every bar** — not a fixed prediction, a rolling assessment.
- **Steep decay** = near oscillation peak, exit soon.
- **Flat decay** = strong trend, hold through oscillation dips.
- **n+1 flips below 50%** = next bar goes against you = RETREAT signal.
- When n+1 through n+4 all agree = high conviction. When n+1 says long but n+2 says short = last bar of the move.
- No fixed hold duration. No fixed SL. The physics decide.
- The "lookahead" misses (peaks/troughs) are where the curve is steepest — the model knows it's uncertain there.

This replaces: L2 (duration), L3 (retreat), fixed exits. One model, one output (decay curve), all decisions derived.

## Late Session: Trajectory Navigation Architecture
- **All three models pass**: 1h (95.4%), 1m (96.3%), 1s (96.6%) at top 5% confidence
- **Statistical significance**: P-values effectively zero, CI never includes 50%
- **Binomial logistic regression** calibrates P(D) to actual outcomes
- **1h chop zone**: 50-62%, **1m chop zone**: ~42-50%
- **Three regimes**: trending (P(D) consistently above chop), inflection (P(D) rapidly crossing 50%), chop (P(D) stuck near 50%)
- **Key distinction**: P(D)=0.48 after being 0.92 = INFLECTION (exit). P(D)=0.48 after being 0.51 = CHOP (no trade)
- **Architecture**: 1h=WHERE we're going, 1m=WAVES along the way, 1s=WHEN to step in
- **Full spec**: `docs/Active/SPEC_TRAJECTORY_NAVIGATION.md`

## What to Look For
- 1s model: does t+10 (10 seconds) hit the micro-oscillation half-cycle?
- Merged model: do 1h + 1m + 1s latents improve over individual models?
- Live playback: does CNN3 work with the new per-TF models?

## March 29 Continuation (late session)

### Oscillation Research
- Built `tools/measure_oscillations.py` — measures oscillation per TF per day
- March 25 measurements: 1h=5.2 bar period (310min), 15m=10.6 (159min), 5m=9.1 (46min), 1m=12.8 (13min), 15s=12.3 (3.1min), 5s=12.3 (1.0min), 1s=15.2 (0.3min)
- Nesting ratios: 1h/15m=1.9x, 15m/1m=12.4x, 1m/15s=4.2x
- **1m half-cycle = 7 bars** — confirms the ~8 minute hypothesis
- 5s/15s are denoised versions of 1s (bandpass filters, not independent oscillators)
- Overlay chart shows all TFs nested on one plot — real data version of the kids chart

### Calibration Results
- 1h chop zone: [0.250, 0.770] — very wide, only trust when very confident
- 1m chop zone: [0.430, 0.510] — tight, well calibrated
- 1s chop zone: [0.000, 1.000] — USELESS for direction (entire range is chop)
- 15m needs conf >= 0.63 for 80% calibrated accuracy
- Dropped 1s from direction decisions, kept for future price-movement analysis

### Advance Engine with Structural Bias
- March 25: -$72 with structural bias (vs -$5,558 old CNN3)
- Calibrated thresholds made it worse (-$2,808) — 15s exits not firing
- Root cause: top 5% metric was a vanity metric, not per-bar usable

### Level Drawing Tool
- Built `tools/draw_levels.py` — interactive candlestick chart with click+drag levels
- Z cycles TFs (4h -> 1h -> 15m -> 5m -> 1m), D deletes, CME session times
- Drew 6 levels on week of March 8: R=25169, 24937, 24849, 24648, S=24570, 24298
- **Validation**: levels held for the ENTIRE month of March
  - 25169 = month ceiling, 24298 = month floor, 24648 = key pivot
  - Levels from one week predict structure for 3+ weeks
- This is what the CNN needs to learn: institutional order flow zones

### Data Quality
- Fixed corrupted 1m bar (fake high 24628 → actual 24429 from 1s ground truth)
- Upsampled 5s and 15s from 1s to March 27 coverage
- Unified ATLAS now has all 7 TFs through March 27

### Key Insight: Levels > Oscillation > Direction
The hierarchy of importance for trading:
1. **Levels** (support/resistance) define WHERE to trade — proven to persist weeks
2. **Oscillation** (per-TF rhythm) defines WHEN within those levels
3. **Direction** (CNN prediction) confirms which way — but only matters within levels
Previous approach was backwards: predicted direction first, added levels later.
Correct approach: establish levels first, trade the oscillation within them.

## Open Items
- Fine-tune drawn levels with full month context (merge close levels)
- Draw more weeks for CNN training set
- Build automated level detection from human examples
- Wire levels into trajectory navigation system
