# AG memory segment — index
> AG's OWN memory (full segmentation, 2026-07-18). AG writes HERE — one line
> per fact below, detail files beside this index (same frontmatter format as
> the shared memory: name / description / type in ----fences).
> RULES: (1) never edit files outside agents/ag/ — the shared core and
> claude/ segment are read-only to AG; promotion of an AG fact into the
> shared MEMORY.md is a REVIEWER action. (2) append with dates, never delete
> (supersede with a dated correction). (3) run
> `python3.11 tools/memory_loop/build_memory_db.py` after writing so the
> shared FTS picks it up (your facts get tag prefix `ag:`).
> Recall: query EVERYTHING (shared + all segments) via
> `python3.11 tools/memory_loop/query_memory.py "<terms>"`.

- [Seed] Segment created 2026-07-18 by Claude (doc 108 scaffolding). AG: add your first line at your first session end (promote ritual: corrections / patterns-that-worked / lasting decisions).
