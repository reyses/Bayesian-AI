# Daily Journal — 2026-03-02

## Sessions Today

### Session 1: NT8 Setup + Connection Verified
**Completed (from previous context)**:
- Installed NinjaTrader 8, compiled BayesianBridge indicator
- Fixed 3 rounds of compile errors: removed Newtonsoft.Json dependency,
  fixed ExecutionEventArgs API, fixed CreateOrder signature, added FindPosition helper
- TCP connection verified: Python client received CONNECTED message from NT8
- Account: DEMO6872628 on Sim101

### Session 2: Multi-TF Bridge + DOM + History Warmup Bypass

**NT8 Bridge updates** (`docs/NT8_BayesianBridge.cs`):
- **Multi-TF OnBarUpdate**: Uses `BarsInProgress` index to send bars from all 12
  data series (1s, 15s, 30s, 1m, 2m, 3m, 5m, 15m, 30m, 1h, 4h, 1D).
  Each BAR message now includes `tf` label and `bar_period_s`.
- **History buffer**: Every bar buffered in `_allBars`. On Python client connect,
  dumps entire history → sends `HISTORY_DONE` message. Bypasses warmup completely.
- **MaximumBarsLookBack.Infinite**: NT8 retains all loaded bars. Set chart
  "Days to Load" high to maximize history.
- **Calculate.OnBarClose**: Only fires on completed bars (no partial).
- **DOM streaming**: `OnMarketDepth` handler sends top-of-book bid/ask/size/spread/
  imbalance, throttled to 250ms. Configurable `DomLevels` property (default 5).

**bar_aggregator.py fixes**:
- Removed duplicate `reset()` method (kept version with `_sub_bars.clear()`)

**Brain checkpoint warning**: `No brain checkpoint found — using empty brain` is
expected — no `pattern_XXXX_brain.pkl` exists. Needs training run to produce one.

## Key Design Decisions
- Base computation TF is 15 seconds (not 15 minutes). 15m is one of 12 worker TFs.
- NT8 chart should be 1s primary with 11 AddDataSeries for higher TFs.
- DOM data streamed alongside bars for order flow analysis (imbalance metric).
- History dump on connect means Python engine starts pre-warmed from NT8's loaded history.

## Python-Side TODO (not yet done)
- Handle `HISTORY_DONE` message in live_engine.py message loop
- Handle `DOM` messages (store latest bid/ask/imbalance for signal enrichment)
- Route bars by `tf` field to per-TF buffers (multi-TF worker architecture)
- bar_aggregator 1s→15s aggregation still works but redundant (NT8 sends 15s natively)

## Key Files Modified
- `docs/NT8_BayesianBridge.cs` — multi-TF, DOM, history buffer
- `live/bar_aggregator.py` — duplicate reset fix
