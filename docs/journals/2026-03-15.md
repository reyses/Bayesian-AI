# 2026-03-15 — TrailingStop Rewrite + SL TF Scaling + Tooling

## Session Summary
Major session: discovered BreakevenLock (old trailing stop) was the primary exit (56% of trades)
disguised as "stop_loss" — activating after just 4 ticks ($1) and ratcheting SL above entry.
Rewrote as TrailingStop with MFE-based activation (80% of template expected profit).
Also fixed SL TF scaling (sqrt diffusion model), added report traceability, shard_reader tool,
I-MR date targeting, and division-by-zero guards.

## Root Cause Analysis: Early Exits
- **SL floor too tight**: `sl_min_ticks=4` ($1.00) — noise on 15s bars. Raised to 12 ($3.00).
- **TF mismatch in bar stats**: Template `avg_mfe_bar`, `p75_mfe_bar`, `shape_delay_bars` are in
  discovery TF units (1m=60s, 5m=300s) but compared against `pos.bars_held` in 15s execution bars.
  A 1m template's "4 bars to MFE" = 4 minutes, but exit engine sees 4×15s = 1 minute → 4x early.

## Changes Made

### TF Scaling Fix
- **`core/fractal_clustering.py`**: Added `discovery_tf_seconds: float` to `PatternTemplate` dataclass.
  Populated during `create_templates()` from `TIMEFRAME_SECONDS[tf]`.
- **`training/trainer.py`**: Added `discovery_tf_seconds` to lib_entry dict passed to exit engine.
- **`core/exit_engine.py`**: Compute `_tf_ratio = discovery_tf_seconds / 15.0` at position open.
  Apply to: `anchor_mfe_bars`, `max_hold_bars` (from p75_mfe_bar), `shape_delay_bars`.
- **`core/trading_config.py`**: `sl_min_ticks` changed from 4.0 to 12.0.

### Macro Trend Observation (Non-Actionable)
- **`core/timeframe_belief_network.py`**: Added `get_macro_trend()` method — reads DMI + band
  position from 30m/1h/4h workers, combines with 60/40 weighting, dead zone ±0.15.
- **`training/trainer.py`**: Added `_macro_obs()` helper that calls `get_macro_trend()` and returns
  6 observation columns: `macro_direction`, `macro_strength`, `macro_z`, `macro_band_pos`,
  `macro_alignment` (WITH/COUNTER/NEUTRAL), `macro_detail`.
- Columns injected into `pending_oracle` dict (flows to oracle_trade_log.csv via `**pending_oracle`)
  AND into `decision_matrix_records` (signal_log.csv).
- **NOT actionable** — SL/TP/trail remain unchanged. Columns are for research analysis only.

### Bug Fix
- Fixed `_stream_log_file` UnboundLocalError in `_write_forward_pass_reports()` — moved file close
  to before the report method call in `run_forward_pass()`.

## Exit C&E Matrix + PFMEA
Generated `reports/findings/2026-03-14_exit_CE_matrix_PFMEA.md` — key findings:
1. **Giveback IS→OOS flip (RPN 336)**: $2.90 IS → -$11.57 OOS — threshold may be overfit
2. **Regime decay premature fires (RPN 252)**: ADX oscillating near 20 → 18.5% wrong exits
3. **SL volume = noise trading (39%)**: 3,140 trades at $0.39/trade — barely breakeven
4. **Duration is master variable**: C&E score 10/10 — everything downstream of "did trade develop?"

## Direction-Weighted SL/TP Plan (Approved, Deferred)
Plan at `C:\Users\reyse\.claude\plans\buzzing-finding-puzzle.md`:
- With-trend: wider SL/TP (more room, macro pulls price)
- Counter-trend: tighter SL/TP (smaller target, limited upside)
- Fractal band TP: when macro z favors entry direction, boost TP proportionally
- **Deferred**: user wants to see observation data before making actionable.

## Fractal Band Insight
User's key insight: "If 1h is at -1σ (lower band, starting to bounce) and 1m is at -3σ
(extreme lower), the 1m will likely revert strongly because the 1h structure is pulling it back."
Higher TF band position determines WHERE lower TF will revert TO — not just "to the middle."

## Session 2 — TrailingStop Rewrite + SL Investigation

### Root Cause: SL as Primary Exit (56% of trades)
- **BreakevenLock** activated after 4 ticks ($1 MFE) → ratcheted SL above entry
- Any 1-tick reversal triggered "stop_loss" at $0.50 profit → 100% WR on SL exits
- 5,174/9,253 IS trades exited this way — NOT a real stop loss, just breakeven scalping
- Dashboard showed 80% WR (real), shard reader showed 97% (wrong — signal_log unreliable)

### TrailingStop Rewrite (`core/exits/breakeven.py`)
- Renamed `BreakevenLock` → `TrailingStop` (honest name, backward-compat alias kept)
- **Activation**: 80% of template p75_mfe (TF-scaled), floor $10, ceiling $100
- Example: template with $50 expected MFE → trail activates at $40 MFE reached
- Trades now must reach statistical potential before protection kicks in
- SL remains as true last resort (200 tick cap = $100)

### SL TF Scaling (`core/execution_engine.py`)
- MAE/MFE stats scaled by sqrt(discovery_tf / 15s) — diffusion model
- `sl_max_ticks = 200` ($100) hard cap
- anchor_mfe also TF-scaled in `open_position()` for trail activation

### Report Traceability
- IS/OOS reports now show commit hash + message + diff summary in header
- Every report self-documents which code version produced it

### New Tools
- `tools/shard_reader.py` — read partial IS/OOS signal_log shards during a run
- `tools/imr_golden_path.py` — added `--date` param for targeting specific dates
- `tools/imr_regime_segments.py` — fixed date windowing (all prior data = context)

### Division-by-zero Fixes (`training/trainer.py`)
- `_total_opps` guarded with `max(..., 1)` in expected profit analysis
- Pre-loop initialization of `_pp_enabled`, `_pp_flip_count`, `_pp_all_trades`

### Thin Market Analysis (I-MR)
- Jan 1 (NYD): 1 regime, R:R 1.0, 60 bars — untradeable
- Jan 5 (Mon after NYD): no regimes detected (insufficient context)
- Jan 13 (normal Tue): 14 regimes, avg MFE $89, R:R 2.6 — clean structure
- Month-boundary analysis: no systematic warmup bug (Feb IS -7.8% delta is outlier)
- Brain carries IS learning into OOS, TBN gets IS tail context — warmup is adequate
- Thin market skip proposed then reverted — user correctly pushed back: workers should decide

### What to Look For Next Run
1. SL exit % should drop dramatically (from 56% to <10%)
2. Giveback/envelope should become primary exits
3. Hold time should increase naturally (trail doesn't force early exit)
4. WR might drop initially (trades held longer = more exposure to reversal)
5. But avg PnL/trade should increase significantly (capturing more of the move)

## Session 3 — Exit Audit + Gate PFMEA + Funnel Research

### Exit Cascade Audit (all 12 exit modules)
- Full code-level review of every exit module
- 7 new PFMEA findings added (RPN 48-243)
- Fixed: ExitAction enum reuse (BELIEF_FLIP, TIDAL_WAVE, V_REVERSAL added)
- Fixed: Watchdog wrong reference field (anchor_mfe instead of trail_activation)
- Fixed: Band urgent loss gate (now fires on thesis invalidation regardless of PnL)
- Fixed: 5 magic numbers extracted to TradingConfig
- Fixed: Giveback constructor default mismatch (0.70 → 0.10)

### Gate Cascade PFMEA (15 failure modes)
- **RPN 648**: Score competition ignores conviction — biggest single issue
- **RPN 392**: Conviction gate after competition — wrong cascade position
- **RPN 320**: 70% of signals unaccounted — FIXED (competition_loser counter)
- **RPN 300**: Brain reject is dead code (0 signals blocked ever)
- Full gate funnel now tracked in reports (competition losers explicit)

### Forward Pass Results (commit 8996cac — TrailingStop rewrite)
- IS: 6,541 trades, 66% WR, $52,177 PnL, $7.98/trade
- OOS: 1,814 trades, 53.5% WR, $2,538 PnL, $1.40/trade
- SL exits down from 56% to 1.3% — TrailingStop rewrite confirmed working
- 2-5min trades: 1,986 at 84% WR, $19.70/trade — the sweet spot
- Value destroyers: giveback (-$12.69), watchdog (-$18.83)
- Parity FAILED: OOS3 68 trades vs OOS2 251

### MFE-Proportional SL Cap
- SL capped at 1× expected MFE (risk = reward, not risk > reward)
- Linear scaling for sub-15s templates (1s→15s: 15× scale down)
- Effect: 1s template SL drops from $100 to $17.50

### Decision Funnel Research
- Spec: `docs/Active/SPEC_DECISION_FUNNEL.md` — temporal candidate narrowing
- First attempt (template matching) blocked by ancestry feature gap
- Decision tree approach: train on I-MR auto seeds as ground truth
- **June result: 85% accuracy, price_range (65%) + sigma_mean (17%) dominate**
- Rule learned: tight range + high sigma = energy coiling → breakout imminent
- High-MFE seeds get higher tree confidence (+0.053 gap)

### Architectural Insight — Probabilistic System
User insight: price behaves like electron position (Heisenberg). The system was designed
probabilistic from the start (z-score = wave function, halflife = probability decay,
Bayesian brain = prior updating). The gate cascade breaks this model by imposing binary
decisions on continuous probabilities. The scoring function should output P(success),
not distance + tier. This is the fundamental redesign direction.

## Next Steps (next session)
1. **Fresh run**: `python training/trainer.py --fresh --lookback` (brain stale from old breakeven)
2. **Probabilistic scoring spec**: replace gate cascade with P(success) estimation
3. **Integrate funnel tree**: bar-level P(setup developing) as Phase 0 pre-filter
4. **Investigate giveback/watchdog**: both destroying value (-$12.69, -$18.83 avg)
5. **Signal_log schema**: add loser conviction for real competition analysis
