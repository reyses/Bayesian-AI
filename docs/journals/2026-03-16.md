# 2026-03-16 — Quantum Reconnection + Exit Cascade Overhaul + Live Restructure

## Session Summary (FULL DAY — longest session to date)
Massive session. Started with debugging _Tee hang + SL fixes, ended with
discovering that 9 quantum state variables are computed on GPU every bar
and thrown away. The original probabilistic scoring system exists in the
codebase — it was just disconnected when the gate cascade was added.

## Key Discoveries

### 1. The SL Was Never Really a Stop Loss
- `stop_loss` exit was 56% of all IS trades at 100% WR, avg $0.50/trade
- Old BreakevenLock (4-tick activation) was scalping 1-tick profit then locking
- Not real trades — noise captures disguised as wins
- Fixed: TrailingStop now activates at 80% of expected MFE (from template stats)
- Fresh run pending to validate

### 2. Nine Orphaned Quantum Variables
Research agents traced every variable from original quantum_field_engine.py:
- **P_at_center, P_near_upper, P_near_lower** — wave function probabilities (ORPHANED)
- **entropy** — three-body chaos measure (ORPHANED)
- **breakout_probability, reversion_potential** — O-U outcomes (ORPHANED)
- **lyapunov_exponent** — hardcoded to 0.0 (INACTIVE)
- **market_regime** — hardcoded to 'STABLE' (INACTIVE)
- All computed on GPU every bar, results discarded

### 3. Original Scoring Was Probabilistic (No Gates)
From the original design docs (recovered from PDFs):
```
P_i > 0.75 AND tunnel_prob > 0.60 AND entropy_low → TRADE
```
No boolean gates. Continuous spectrum of expected value. The gate cascade
replaced this with `distance < 3.0 AND conviction > 0.48` — binary decisions
on continuous probabilities.

### 4. Market Hierarchy = Three-Body Social Model
- Gods (institutions) = gravity (μ, fair value)
- Demi-Gods (HFT algos) = PID control (Kp/Ki/Kd)
- Avatars (prop firms) = momentum amplification
- Mortals (retail) = noise (Brownian)
- 1σ "PID trance" = HFT control zone, don't trade
- 4σ = PID saturated, erratic price, stay flat
- Resonance cascade = all three bodies align, disable TP

### 5. Funnel Decision Tree Validated
- Trained decision tree on I-MR auto seeds (ground truth regime entries)
- 85% accuracy, 73% precision, F1=0.67 on June 2025
- Key features: price_range (65%), sigma_mean (17%), dmi_slope (3%)
- Rule: tight range + high sigma = energy coiling → breakout imminent

### 6. No Month-Boundary Warmup Bug
- Compared first 3 days vs rest of month across all 12 IS months
- Only Feb IS showed -7.8% delta (market conditions, not data bridge)
- OOS showed NO pattern (+0.1%, +2.7%, +1.3%)
- IS→OOS bridge works correctly (112K bars of December context)

## Files Changed
- `core/exit_engine.py` — TrailingStop rewired to MFE-based activation
- `core/exits/breakeven.py` — BreakevenLock rewired for MFE
- `core/trading_config.py` — sl_max_ticks, trail_activation_floor_ticks
- `training/trainer.py` — _Tee deadlock fix, report traceability, PP var init
- `tools/funnel_tree_research.py` — NEW: decision tree on I-MR seeds
- `tools/shard_reader.py` — NEW: signal log shard analysis
- `docs/reference/NIGHTMARE_PROTOCOL.md` — NEW: master equation
- `docs/reference/QUANTUM_DESIGN_INTENT.md` — NEW: pre-gate scoring + unimplemented theories
- `docs/reference/QUANTUM_STATE_REFERENCE.md` — NEW: unified physics theories
- `docs/reference/MARKET_HIERARCHY.md` — NEW: three-body social model
- `docs/reference/quantum_field_engine_original.py` — recovered from git (919 lines)
- `reports/findings/quantum_state_mapping.txt` — variable tracing results

## Commits
- `215682b` — report traceability + tqdm timestamp fix + tools index
- `b2b5343` — SL TF scaling + max cap + OOS3 standalone + _Tee deadlock
- `f4e33f1` — funnel decision tree research
- `dc11edf` — Nightmare Protocol reference doc
- `f4ec7f8` — original scoring logic + unimplemented theories

## Next Session — Priority Queue
1. **Read fresh run results** — compare SL exit % (target: <10% vs old 56%)
2. **Phase 1: Instrumentation** — verify all 9 orphaned quantum variables are live
3. **Phase 2: Soft thresholds** — convert hard gates to probability multipliers
4. **Phase 3: Probabilistic scoring** — P(success) from wave function + tunnel + entropy
5. **Cascade mode** — is_resonance_cascade flag + suppress TP during cascade
6. **Liquidation level anchoring** — read BAYESIAN_AI_MASTER.md section 8 for level-anchored mode

## What to Watch in Fresh Run
- SL exit % (was 56%, should drop with MFE-based trail activation)
- WR (may drop from 97% illusion to ~80% real)
- $/trade (should increase — real profits, not $0.50 noise)
- Hold times (should naturally increase without 4-tick breakeven chop)
- Macro observation columns (WITH/COUNTER/NEUTRAL alignment data)

## Afternoon Session — Exit Cascade Overhaul

### Run Results (post-SL fix, pre-TF-aware exits)
- IS: 6,518 trades, 66.4% WR, $53,034, $8.14/trade, PF 2.58
- OOS: 1,816 trades, 54.6% WR, $4,716, $2.60/trade
- Capture rate: 81% of trades in Reversed+Q1 (entries are bad, exits save it)

### Exit Fixes Implemented
1. **15s execution-TF flip exit** — halves giveback threshold when 15s worker flips
   against trade. OOS: 66% of losses had 15s flip vs 44% wins (+22% edge).
2. **Watchdog repurposed** — no longer kills trades at 75s. Session-ending only.
3. **Giveback patience fix** — never suppress when trade is losing (current_ticks < 0).
   177/219 giveback exits were held through reversal by broken patience window.
4. **Giveback min peak** — requires 20% of anchor MFE before activating.
5. **DMI 3-bar confirmation gates giveback** — direction must be confirmed (15 min
   at 5m) before giveback can fire. Research: MAE drops 40% at 3 bars.
6. **TF-aware regime_decay** — reads discovery TF DMI instead of hardcoded 5m.
   Adjacent higher TF provides hold override (ride the chop if macro agrees).
   Research: depth 8 regime_decay was 380 trades at -$5.22/trade OOS.

### Infrastructure
- Default run reuses templates (skip Phase 1-3 if checkpoints exist)
- 22D lookback always on (--lookback flag deprecated)
- Pre-IS checkpoint backup to `checkpoints/pre_is_backup/`
- Dashboard: price chart fed from trainer, capture rate quartiles, entry/exit triangles
- Decision record: `docs/decisions/2026-03-16_22D_lookback_default.md`

### Key Research
- DMI crossover confirmation: 3 bars (15 min) is sweet spot for 5m DMI
  (MAE drops 40%, MFE +10%, accuracy 98.8%). `tools/dmi_cross_confirmation.py`
- Depth→TF mapping: depths 5-7 (2m-5m) are sweet spot ($13-17/trade, 72-78% WR)
- Live pipeline audit: 10 KEEP, 2 MERGE, 3 REDUNDANT files in live/
- Research audit: all 9 prior findings map to quantum framework

### Key Insight
- 81% of entries are Reversed or Q1 — system profits purely from exit quality
- Entry selection is the bottleneck, not exits
- Order of improvement: exits (almost done) → entries (quantum scoring) → scoring model
- Each depth should check its OWN TF's DMI, not hardcoded 5m
- Adjacent higher TF = hold override (ride the chop, macro trend alive)

## Evening Session — Late Exits + Live Restructure + Brownian Motion

### OOS Progression (all 4 runs today)
| Run | PnL | WR | $/trade | Key Change |
|-----|-----|-----|---------|------------|
| 1 | $2,518 | 53.4% | $1.24 | Baseline (broken exits) |
| 2 | $4,716 | 54.6% | $2.60 | Giveback patience + 15s flip |
| 3 | $5,563 | 58.7% | $3.26 | TF-aware regime_decay |
| 4 | $8,501 | 57.7% | $6.16 | DMI floor + tidal_wave TF-aware |

### Additional Exit Fixes (evening)
7. **Survival flatline flip-imminent gate** — only exits flat trades when higher TF DMI fading
8. **Survival stop/flatline split** — separate tracking for ePnL vs structural flatline
9. **DMI crossover disabled below 3m TF** — research: 1s/5s/15s DMI is pure noise (MFE/MAE=1.0x)
10. **Tidal_wave TF-aware** — same treatment as regime_decay
11. **Giveback pre-DMI Brownian motion** — adaptive σ√N reversal detection replaces fixed 50%
12. **Giveback moved to #1 in cascade** — catches peaked trades before regime_decay panic-kills them

### Live Pipeline Restructure (Phase B-C)
- `live_engine.py`: 1,954 → 1,617 lines (-337 / -17%)
- Entry decisions unified through BarProcessor
- Exit decisions unified through BarProcessor (exit_only=True for 1s sub-bar)
- Deleted: `_check_exit` (89 lines), `_check_entry` (50 lines), `_gate_label_to_pct` (13 lines)
- Deleted: `_write_replay_parity_report` (264 lines) — OOS3 handles this
- Phase A audit: `docs/specs/LIVE_PHASE_A_AUDIT.md`

### Quantum Score Logging
- q_score computed on every trade (observation only): 8-component weighted formula
- Components: q_wave, q_tunnel, q_entropy, q_coherence
- Next run will have these fields for post-run correlation analysis

### Research
- DMI sample size: 1s/5s/15s = noise, 3m+ = reliable. `tools/dmi_sample_size_research.py`
- Regret analysis embedded in reports. `tools/regret_analysis.py`
- Oracle coverage: 91% of bars have a move, only 5.9% trigger patterns, 2.2% traded
- Peak analysis: trades peaking 80t+ capture 50%+, trades <20t lose money

### Dashboard Improvements
- Price chart with live bars + trade markers (entry/exit/skip)
- Yellow diamonds for skipped signals (visual decision funnel)
- Up triangles = entry, down triangles = exit
- Horizontal entry line while in trade
- Capture rate quartiles (6 buckets)
- Chart data saved to CSV at end of each phase

### Specs Written
- `docs/specs/QUANTUM_RECONNECTION_SPEC.md` — Phase 1 engineering spec
- `docs/specs/LIVE_PIPELINE_RESTRUCTURE.md` — thin shell + BarProcessor
- `docs/specs/LIVE_PHASE_A_AUDIT.md` — coverage verification
- `docs/decisions/2026-03-16_22D_lookback_default.md` — 16D deprecated

## Open Research
- Quantum reconnection plan: `reports/findings/quantum_reconnection_plan.txt`
- Funnel tree results: `reports/findings/funnel_tree_20260315_232846.txt`
- Variable mapping: `reports/findings/quantum_state_mapping.txt`
- DMI confirmation: `reports/findings/dmi_confirmation_20260316_094508.txt`
- DMI sample size: `reports/findings/dmi_sample_size_20260316_154645.txt`
- Regret analysis: `reports/findings/regret_analysis_20260316_171527.txt`
- Weekend recalibration: `memory/project_weekend_recalibration.md` (future)

## Next Session Priorities
1. **Read run results** — check brownian_giveback + cascade reorder + q_score
2. **Quantum orphan research on new trade log** (has q_score columns now)
3. **Phase 1 quantum scoring** — wire coherence as soft multiplier into competition
4. **Belief_flip investigation** — 98 trades at -$9.47 (worst per-trade capture)
5. **Depth 10 filter** — consistently negative across all runs
