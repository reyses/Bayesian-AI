# 2026-03-21 — Cat Brain + Rolling PID + Sensor Gate Calibration

## Session Summary
Overnight session: implemented cat brain, rolling PID, proportional sensor gate,
counterfactual engine spec, resonance cascade hypothesis. Three full pipeline runs
for comparison.

## Results Comparison

| Run | Gate Type | IS Trades | IS PnL | IS WR | OOS Trades | OOS PnL | OOS WR |
|-----|-----------|----------|--------|-------|-----------|---------|--------|
| **No gate** (1m inactive) | None | 34,446 | **$209,734** | 70.4% | 4,636 | $21,631 | 67.1% |
| **Absolute gate** (vol<-0.5, fm<-1.0) | Strict | 21,779 | $114,328 | 66.6% | 3,957 | $16,409 | 65.4% |
| **Proportional gate** (z-score) | Not yet run | — | — | — | — | — | — |

### Key Finding: 1m Gate Costs ~$95K in IS
- No gate: $209K on 34K trades (70.4% WR, $6.09/trade)
- With gate: $114K on 21K trades (66.6% WR, $5.25/trade)
- Gate blocked 13K profitable entries
- BUT: no gate means Feb 9 style crashes are unprotected

### Previous Baselines
- Lizard brain (no cat, cumsum PID): IS $1,943 / OOS $22,812
- Cat brain (rolling PID, no 1m gate): IS $209,734 / OOS $21,631
- Cat brain (rolling PID, absolute 1m gate): IS $114,328 / OOS $16,409

### The Rolling PID Was the Game Changer
- Old cumsum PID: IS = $1,943 (poisoned by 13 months of accumulated drift)
- New rolling window PID (200 bars): IS = $209,734 (100x improvement)
- The ONLY cumulative field was term_pid = ki * cumsum(z_scores)
- Replacing cumsum with rolling mean fixed IS permanently

## Changes Made

### Code (committed + pushed)
1. **Rolling window PID** — `statistical_field_engine.py` line 368: cumsum -> rolling mean(200)
2. **Cat brain** — `core/cat_brain.py`: 8-channel rolling delta regime classifier
3. **--cat CLI flag** — benchmarking toggle for cat brain
4. **Volume-relative giveback** — adaptive threshold from recent trade outcomes
5. **Giveback record_exit hook** — connected adaptive learning loop
6. **Dashboard daily redraw** — TICK_UPDATE once per simulated day (was per bar, froze GUI)
7. **1m worker loading** — was inactive, now loads 1m parquet for sensor gate
8. **1s/5s daily filtering** — was loading full month (1.9M bars), now filters to day
9. **TF load order** — macro to micro (4h -> 1m -> 5s -> 1s)
10. **Proportional sensor gate** — z-score thresholds from cat's rolling window
11. **Skip log timestamp fix** — fallback to _last_bar_ts when bar_ts is 0
12. **Dashboard PIN button** — toggle always-on-top
13. **Counterfactual engine spec** — `docs/specs/COUNTERFACTUAL_ENGINE.md`

### Architecture Decisions
1. **Only ONE cumulative field existed**: term_pid (cumsum of z_scores). Everything else
   (F_momentum, DMI, ADX, velocity, volume_delta) was already per-bar or rolling window.
2. **Sensor gate should be proportional**: absolute thresholds don't scale across regimes.
   Z-score relative to rolling stats auto-calibrates.
3. **Fake peak filter belongs on EXIT side, not ENTRY**: high vol+fm at entry = good entry.
   High vol+fm DURING trade = the move hasn't peaked yet, hold.
4. **The counterfactual engine is the goat brain**: every trade and skip spawns phantom
   trades with alternative parameters. Optimal settings emerge from real-time what-if.

### Brain Evolution (established)
```
Lizard:  counter (dead — replaced by cat)
Cat:     rolling delta regime classifier (deployed, --cat flag)
Crow:    k-NN seed matching (31K seeds, 8-12h build)
Monkey:  CNN three-head model (12-18h build)
Goat:    counterfactual engine + adaptive tuning (spec written)
```

### Resonance Cascade Hypothesis (NEW)
Every TF has a parent that validates its peaks:
- 15s peak validated by 1m (real or fake?)
- 1m peak validated by 5m
- 5m peak validated by 15m
- 15m peak validated by 1h
- 1h peak validated by 4h

When ALL pairs agree on direction = resonance cascade = crash/rally.
When <3 pairs agree = chop. Feb 9 was a full 5/5 cascade.

**Research needed**: run peak detection on all TFs simultaneously, count pair
agreement, correlate with next N bars' direction. If 5/5 predicts 90%+
accuracy, we have the crash/rally detector.

A "trend" is NOT a separate concept — it's the DECAY of peaks in one
direction over time. LONG peaks getting weaker, SHORT peaks getting stronger.
The rolling MFE per direction IS the trend signal.

## Next Steps
1. Run with proportional gate (z-score) — compare against both baselines
2. Research: multi-TF peak detection (resonance cascade validation)
3. Fake peak -> exit override (giveback conversation)
4. Capture rate 6 buckets on dashboard
5. Crow brain: seed enrichment with rolling delta features
6. Live parity test with ATLAS warmup + proportional gate

## INFLECTION POINT #2 (commit 09cb6171)

**Best results ever:** IS $243K PF=2.56 / OOS $22.4K PF=2.30

**The parity wall:** IS uses 2,000-line inline path duplicating BarProcessor.
Crow spawned 0 phantoms because IS bypasses _build_candidates().
Decision: delete IS inline path, route through BarProcessor.process_bar().

### Full session output (all-nighter Mar 20-21):
- Rolling PID: IS $1.9K -> $243K (100x, the game changer)
- Cat brain deployed (default, --no-cat to disable)
- Crow brain built (--crow flag, blocked by IS parity wall)
- 1m entry gate REMOVED (research: cost $95K, parent agreement is contrarian)
- Cascade fade exit (4+ TFs against = exit, research PF=2.44)
- Resonance cascade: CONTRARIAN (fading 4-TF agreement = PF 2.44)
- TF pair validation: parent agrees = worse entry, unvalidated = fresher
- Unified IS->OOS pipeline (brain freeze at month boundary)
- Exit fixes: giveback override, tidal_wave suppress, peak_state tighten
- Scorecard report (80 lines, actionable)
- 900MB+ codebase cleanup
- Brain evolution: Lizard->Cat->Crow->Monkey->Goat

### Next session priorities:
1. IS inline path -> BarProcessor (the parity fix, ~1 session)
2. Re-run crow (verify phantoms)
3. Live parity test Monday
