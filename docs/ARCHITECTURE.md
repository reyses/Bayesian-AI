# Bayesian-AI — Architecture Reference
> Auto-generated by Jules on 2026-03-05. Do not edit manually.

## System Overview
Bayesian-AI is a multi-timeframe trading system driven by a "Nightmare Protocol" unified physics engine, applying quantum mechanics, three-body gravitational fields, and Bayesian learning to market microstructure. It translates raw price and volume into 16D feature vectors composed of z-scores, momentum, Lagrange zones, entropy, and PID algorithmic market-maker control forces.

The system uses a fractal tree structure across timeframes (from 1D down to 1s) to discover, cluster, and match repeating price trajectory shapes. The clustering engine dynamically segments geometry via I-MR charts into Hypervolume Trees, classifying templates and assessing historical win rates, bias, and expectancy via a Bayesian HashMap.

Execution is governed by an Orchestrator and a Timeframe Belief Network that aggregates predictions across multiple active timeframe workers to calculate a final "path conviction". Exits are dynamically managed by a Wave Rider system incorporating Coherent Structure Tethers and statistical regret analysis.

## Phase Pipeline
| Phase | Name | File | Description |
|-------|------|------|-------------|
| 1 | Data Prep | `training/databento_loader.py` | Ingests Databento `.dbn` and converts to parquet, generating multiple timeframes. |
| 2 | Pattern Discovery | `training/fractal_discovery_agent.py` | Extracts price trajectory shapes into 16D vectors and applies geometric matching. |
| 3 | Template Optimization | `training/fractal_clustering.py` | Segments patterns using I-MR charts and groups into Hypervolume Trees (templates). |
| 4 | Forward Pass | `training/orchestrator.py` | Simulates trading over an out-of-sample period using learned templates and the Bayesian Brain. |
| 5 | Strategy Selection | `training/trade_analytics.py` | Analyzes Phase 4 results comparing winners/losers across all metrics to find insights. |

## Core Files
| File | Class / Function | Role |
|------|-----------------|------|
| core/quantum_field_engine.py | QuantumFieldEngine | Computes physics metrics (z-score, PID force, coherence) using CUDA/Numba. |
| core/bayesian_brain.py | BayesianBrain | O(1) HashMap learning system storing template historical win rates and PnL expectations. |
| core/three_body_state.py | ThreeBodyQuantumState | Dataclass representing the complete quantum state of the market at a single bar. |
| core/dynamic_binner.py | DynamicBinner | Computes optimal Freedman-Diaconis bin edges to hash continuous state variables. |
| training/orchestrator.py | Orchestrator Worker | Main entry point integrating discovery, clustering, dashboard, and forward pass simulation. |
| training/fractal_clustering.py | FractalClusteringEngine | Builds the Hypervolume Tree of patterns for lookups and clusters patterns by physics. |
| training/fractal_dna_tree.py | FractalDNATree | Maintains the hierarchical path of clusters from highest (1h) to lowest (15s) timeframes. |
| training/timeframe_belief_network.py | TimeframeBeliefNetwork | Aggregates P(LONG) models from 8 workers (1h to 15s) into a unified "path conviction". |
| training/wave_rider.py | WaveRider | Exit logic manager with regret analysis, runner modes, and CST boundary tracking. |

## Key Constants & Parameters
| Constant | Default | Description |
|----------|---------|-------------|
| `VELOCITY_CASCADE_THRESHOLD` | 1.0 | Minimum points per second to trigger a velocity cascade. |
| `DEFAULT_PID_KP` / `KI` / `KD` | 0.5, 0.1, 0.2 | Default PID control force multipliers in the physics engine. |
| `IMR_Z_THRESHOLD` | 1.0 | Composite signal threshold for shape segmentation in fractal clustering. |
| `R2_STOP_THRESHOLD` | 0.90 | Adj-R2 target for DOE convergence in template optimization. |
| `DIRECTION_CONFIDENCE_THRESHOLD` | 0.05 | Conviction threshold required to lock in a direction. |
| `CONVICTION_SL_MULTIPLIER` | 0.5 | Scales stop-loss distance dynamically based on entry conviction. |
| `CONSENSUS_CONFIDENCE_THRESHOLD` | 0.60 | Multi-Timeframe consensus threshold for entry validation. |

## CLI Reference
| Flag | Default | Description |
|------|---------|-------------|
| `--data` | `DATA/ATLAS` | Path to ATLAS root, single TF directory, or parquet file |
| `--iterations` | 1000 | Iterations per pattern |
| `--checkpoint-dir` | `checkpoints` | Checkpoint directory |
| `--no-dashboard` | false | Disable all UI (popup and dashboard) |
| `--dashboard` | false | Show full live dashboard instead of default lightweight popup |
| `--skip-deps` | false | Skip dependency check |
| `--exploration-mode` | false | Enable unconstrained exploration mode |
| `--fresh` | false | Clear all pipeline checkpoints and start fresh |
| `--forward-pass` | false | Run Phase 4 forward pass using existing playbook |
| `--train-only` | false | Run Phases 2-3 only, skips forward pass and strategy report. |
| `--oos` | false | Standalone OOS rerun (Phase 6 only) |
| `--skip-oos` | false | Skip the auto-chained OOS forward pass (Phase 6) |
| `--account-size` | 0.0 | Starting account equity in USD |
| `--train-end` | None | Cap training data at this date (e.g. 20251231) |
| `--forward-data` | None | Separate ATLAS root for Phase 4 forward pass |
| `--forward-start` | None | First day to include in forward pass |
| `--forward-end` | None | Last day to include in forward pass |
| `--telemetry` | false | Print per-trade decision telemetry |
| `--min-tier` | None | Only activate templates of this tier or better (1-4) |
| `--bias-threshold` | None | Oracle bias threshold for direction lock |
| `--dmi-threshold` | None | Min |dmi_diff| required to use DMI signal |
| `--r2-target` | 0.90 | Adj-R2 target for DOE convergence |
| `--sweep-params` | false | Sweep filter combinations on oracle_trade_log.csv |
| `--strategy-report` | false | Run Phase 5 strategy selection report |
| `--mc` | false | Enable Monte Carlo sweep after Bayesian Phase 3 |
| `--mc-iters` | 2000 | Monte Carlo iterations per (template, timeframe) combo |
| `--mc-only` | false | Skip discovery, just run Monte Carlo from existing templates |
| `--anova-only` | false | Skip MC sweep, just run ANOVA on existing results |
| `--refine-only` | false | Skip MC+ANOVA, just run Thompson refinement |

## Data Flow
```text
Raw Parquet (1D to 1s) -> QuantumFieldEngine -> ThreeBodyQuantumState
  -> FractalDiscoveryAgent -> FractalClusteringEngine (HypervolumeTree)
  -> TimeframeBeliefNetwork -> Orchestrator (Forward Pass)
  -> BayesianBrain (Expectancy/Win Rate) -> WaveRider (Exits)
  -> trade_analytics.py (Analysis & Reports)
```

## Output Files
| File | Location | Description |
|------|----------|-------------|
| oracle_trade_log.csv | checkpoints/ | Per-trade oracle log containing feature vectors and outcomes |
| phase4_report.txt | checkpoints/ | High-level forward pass summary report |
| trade_analytics.txt | reports/ | Statistical suite comparing winners vs losers |
| pattern_library.pkl | checkpoints/ | Optimized cluster templates and Hypervolume Tree |
| CURRENT_STATUS.md | docs/ | Auto-generated system status via script |
