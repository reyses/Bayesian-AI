# DOJO — COMPLETE (2026-07-26)

The teacher-dojo portion is closed. It achieved its ACTUAL valuable outcome (a
working meaning-engine + the instruments), even though its INTENDED product
(distillable exit judgment) proved edgeless.

## What the dojo delivered
- **Meaning loop, proven end-to-end.** Educated teacher → hypothesis memos →
  full-tape educated-curator reflection → BECAUSE-warranted, data-bearing bank
  → day-carry retrieval. Gen-2: 66 memos, 100% data-bearing, 100% warranted.
  0.7% → 100% info-rate across 7 sprints + 2 gates (meaning_velocity report).
- **The instruments.** The entire leg-health dossier (gauge, conviction,
  anomaly detectors, wrong-direction score, composite) came out of dojo data +
  analysis. These are the dojo's real yield.
- **The hard negatives.** Exit-timing has no causal edge (never-bail optimal,
  proven across all policy classes); raw 1m return is efficient. Both killed
  cheaply, on CPU — the gate spec working as designed.

## Final fix (retrieval read-path)
Gen-2 day-carry silently returned 0: reflection-admitted memos were stored
WITHOUT state tags (now_frame_text passed as ''), so retrieval-by-state never
matched. FIXED — reflection now passes the originating frame's text; memos
carry `(state: …)` tags; retrieval verified to grant on state overlap. A LIVE
2-day confirmation run is queued for after the GPU is restored (driver
mismatch, unrelated).

## Handoff to the blackboard
The dojo's machinery IS the blackboard's substrate:
- teacher_memory SQL bank → the blackboard's shared bus
- interrogation tools (ask-why / interview / opus) → qwen-interpreter half
- reflection + guards + ledger → the discipline layer
- retrieval (now fixed) → the read path the loop depends on
The dojo is not discarded; it is REPOINTED — from "teach a teacher to exit" to
"the memory+reasoning half of a discovery loop." See BLACKBOARD_ARCH.md.

## Status: COMPLETE. Next generational teacher work happens INSIDE the
blackboard (qwen interpreting mamba findings), not as standalone exit-dojo.
